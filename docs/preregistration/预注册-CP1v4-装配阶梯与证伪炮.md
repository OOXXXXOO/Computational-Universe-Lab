# 预注册:CP1-v4 装配阶梯、门、证伪炮(装配开工前冻结)

*车道A,2026-07-20。本文档在 CP1-v4 开工前冻结;此后只许收紧不许放松。任何"达不到就改门"的提议 = 失败信号。P5 查新已过(无阻塞先例、无 no-go),放行。*

---

## 一、装配阶梯(逐级加件,任何一级失败停在该级写归因矩阵)

### L1 核 + 约束(R14 核 + R19 交错存储 + R17 语义)
| 门 | 数值 |
|---|---|
| N_prop(无投影 Riemann-SVD,≥4 个 k 含斜向)| = 2 |
| 同时 c_gw/c_matter | = 1 ± 1e-2 |
| \|K@TT\|(实空间算子,离壳)| < 1e-12 |
| SV 硬门 | 第 3 大 SV < 0.05 且 tt_match > 0.95 |

### L2 + 无滞后阻尼(Z4c 型,R22;γ ∈ [0.05, 0.066] 或 Chebyshev 整循环)
| 门 | 数值 |
|---|---|
| 约束 C | 降到机器底(fp64 < 1e-12),无 6e-4 型地板 |
| TT 保持 | = 1(1e-10 内) |
| 规范模衰减 | **属预期**(R18 H3:阻尼不动点=TT,不算 DOF 丢失)|
| R20 三 bug 回归测试 | h̄₀₀ 时间槽 / hprev 滞后 / 折返写全复量,各一断言 |
| enforce≡measure | 同一函数,断言差=0 |

### L3 + 源(R13 J̄,de Donder 相容注入)
| 门 | 数值 |
|---|---|
| 规范反常泄漏率(∝G)| 较 on-site 源 ≥100× 降 |
| 源处 de Donder 残差 | < 1e-10 |
| 闭环 T 行守恒(live)| < 1e-12(体区)|

### L4 + sponge(B4 参数起步)
| 门 | 数值 |
|---|---|
| 反射系数 | < 1e-3 |
| 体区能量记账 | 差 < 1e-6 |
| 全 L1-L3 门重放 | 不回退 |

L4 全绿 ⟹ 进 M3 全表(北极星计划 M3 节判据 + endurance T=2000)。

## 二、证伪炮(九件,每次全绿必须全放,全部应 FAIL)

1. tr_sign=0(无迹反转)
2. 无阻尼
3. 滞后阻尼(一步 lag)
4. on-site 双线性源(替代 J̄)
5. θ_g ≠ θ_matter(双锥 → J5 必 FAIL)
6. 关交错存储/关放置簿记(→ dim 5-6,对照值 0.316)
7. slave 缺 0ν 相位(R21 硬要求的反面)
8. Z_μ 辅助场静止载波(R22 消融的反面)
9. TT 投影仪用 k 方向替代 κ 方向(大 k 判据失准)
10. **【正控,2026-07-20 收紧新增】自旋-1 光子对照**:同一套机器(walk 核 + 交错放置 + 约束阻尼 + 涌现判据)指向 U(1) 场 A_μ(4 分量、1 条 Lorenz 约束、放置=经典 Yee 格),**必须**给出 N_prop = 2(光子两个横向极化)+ 规范模零场强 + 约束闭合。这不是证伪炮(应 PASS 而非 FAIL),是机器的阳性对照:评审必问"你们的涌现判据在已知答案的自旋 1 上工作吗"。分工:符号层证书=车道A(R23,即刻);格点实测=车道B,排 L2 过线后、不阻塞主线。机器若过不了麦克斯韦,M3 全表无效。

## 二b、L1 冻结记录与三项申报裁定(车道A,2026-07-20)

**L1 判定:PASS,构造冻结**(hash 4dad03be…8035;车道A逐位复算见 l1_recheck_laneA.log)。五门全过、四炮全崩、enforce≡measure 差=0、炮组含"无 slave 自由 walk N_prop=6"的牙口对照。M3 式合体互毁未出现——这是全程第一次零件合体即过门。

**申报裁定三条:**

1. **门口径(placed-κ 计数器):批准。** 依据 R15 §三/R19 §五.5:TT 判定必须用构造自身微积分的 κ 方向;stock 全角口径在无阻尼 walk 态上属微积分失配测量,5–6 是 R15 负控签名、非失败。**但他们的"免费预言"升格为 L2 正式门:阻尼后两口径必须同时收敛到 2**(收紧,允许)。此门永久堵死"挑了会过的计数器"这类指控。
2. **ω=0 规范 Jordan 模(h₀₀∝t):接受为真发现,入册。** 物理内核:约束面≠渐近稳定(无阻尼时),与 R16/R18 的"阻尼不动点=TT"叙事一致;数值相对论中谐和规范的长期模+Z4c 的存在理由正是同一件事——我们在格点上独立重现了这条谱系。裁定:线性去趋势**仅限 L1(无阻尼级)作为已申报预处理合法**;**L2 起禁止去趋势**,Jordan 模必须被 Z4c 动力学杀灭——它就是 L2 的第一靶,杀灭本身入 L2 门(h₀₀ 长期斜率 < 1e-10/步)。
3. **sv3 裕度 8%:PASS 有效,不重掷种子。** 预注册种子如实报告即为合规。记录一条**预测(非门)**:sv3 源于 (2,2,0) Trotter 残余=离约束面内容=可阻尼 ⟹ L2 后 sv3 应显著下降(oracle 值 0.035 → 更低)。若 L2 后 sv3 反升,视为装配异常信号停手。
4. **附:措辞规范一条**——"各向同性 spread=0.0" 是**共核比值**离散(J5 分方向恒 1,R14 共享伪影抵消的直接后果);锥的**绝对**各向异性非零且属预期,M3 报告必须另列,防止被读成"格点无各向异性"的超claim。

**L2 门表(在 §一 L2 基础上收紧新增):** 双口径 N_prop 收敛 =2;Jordan 模动力学杀灭(h₀₀ 斜率 <1e-10/步,禁去趋势);R20 三 bug 回归断言;sv3 走向记录。

**挂牌跟踪 W1(车道A复核时立据):体对角 (2,2,2) 在 16³ 无阻尼下 n_prop=6、sv3=0.249**(车道B已如实申报,R15 §五预留的 Trotter 壳修正极端点,L1 门的 4k 集合不含它、通过合法)。处置预注册:L2 阻尼后复测对角线;**M3 全表时对角线必须入 k 集**——要么过门,要么以物理归因明示豁免("非回文核的 Trotter 壳修正在严格体对角占主导;J5 比值不受影响"),豁免的话主张范围相应收窄并写进摘要。不许静默绕开。

## 二c、L2 冻结记录与三项裁定(车道A,2026-07-20)

**L2 判定:PASS,L1+Z4c 阻尼冻结,放行光子格点正控 + L3。** 七门全过四炮全崩;车道A抽查:率门逐位(0.9629170 vs 谱预言 0.9628835)、Jordan 0.546→1.7e-16、TT 漂移≤3.6e-13、sv3 全 k 机器零、对角=2、炮组 breaks 一致;逐位复算后台进行(l2_recheck_laneA.log)。一处措辞对齐:报告"relC 1.98→"应改用文件 relC_max=3.03(初值 vs 瞬态峰值,非门相关)。

**裁定三条:**

1. **迹行 T 块:批准(限真空级),附两个条件。** 物理依据:de Donder 后仍有调和剩余规范,真空中可用它到达完全 TT 规范,迹在壳上纯规范——T 行=用掉剩余规范,合法。条件一:预飞三元组(仅W/仅A/W+A)升格为**记录在案的炮 C5**(T 行自己的对照),数值入库。条件二(**关键,现在预注册**):**有源时迹不再纯规范**——牛顿井的 h̄₀₀ 是被源出来的,T 行有杀死井的风险。L3 金丝雀门:静源+T 行激活下 h₀₀/φ=2.00±0.02;若 T 杀井,T 行必须改为源感知(只固定剩余规范部分)——L3 若失败,头号嫌疑人已具名。
2. **G5 操作化口径:接受。** "把纯 TT 内容喂过 verbatim 读数器、逐点复现超额(≤1.5e-4)"正是读数伪影的标准证明法。M3 报告措辞固定:"stock 计数器物理帧全 k=2;verbatim 帧超额为已认证读数伪影"。
3. **体对角转正:批准,W1 结案升级。** (2,2,2) 阻尼下=2(16³+24³ 双确认)⟹ M3 正式 k 集**必须**含体对角且必须过门(豁免条款作废——只许收紧)。机制注记:L2 不动点 ⊊ ker K 与 R18 H3(阻尼不动点=TT)是同一件事——压规范的机制顺手杀了对角 Trotter 残余,理论环闭合。

**M4 先验/炮划分草案:未随包附上,不做未见先批。** 发全表来,L3/L4 期间复核,不阻塞。

## 二d、L3 停级三裁 + L3-v2 指令(车道A,2026-07-20)

**L3 判定:NOT PASS 维持,停级合规,归因质量为全程最高**(金丝雀按预注册逐字引爆 + 根因再深挖一层到核的 ω=0 通道 + 波/源病灶分离证明)。

**三裁:**

1. **金丝雀两分支表:终裁定格。T 行对有源扇区死刑**——预注册的雷逐字兑现(有源时 tr_η h̄=−h̄₀₀≠0,T 行强迫 tr=0 即杀井);T'' 救迹不救形(p=−1.62≠−2)。七变体归因矩阵已证明任何行子集到不了 (2.00, >0.99),**不再试 T 变体**,此路封档。
2. **T'' 伴随参考场:诊断合法、入门非法。** 作为"迹病 vs 形状病"的分离诊断工具是正确用法;作为门的组成部分不行(参考场=把 oracle 缝进被判对象)。定性入册。
3. **G1 改口径申请:拒绝放松,改为"悬置+重立"。** 预注册纪律不许放松门;但 on-site 炮哑火已证明 G1 瞄的靶在该核下死亡——门无靶=病态,非"过"。处置:G1 悬置,**L3-v2 新核下重推 oracle、重立同强度门(泄漏≥100× 或新核等价物)**,L3 记为未过直至 v2 重跑全表。

**L3-v2 指令(R24 已认证,r24_results.json):**

根因是几何核缺 ω=0 双极点(幺正一阶系统静响应 ~1/k 非 1/k²,repo 定理回旋镖)。**R14 诚实自纠(T12):"2 条带 ⟹ 必须幺正 split-step"过窄——本质性质是{恰 2 条带}+{共享半角壳},幺正是充分非必要。** 时间交错 (h,π) 辛对(Yee-FDTD 时间结构/velocity-Verlet):

```
π(t+½) = π(t−½) + [cos²θ·Lap_half h(t) + S]      (Lap_half=半角交错拉普拉斯)
h(t+1) = h(t) + π(t+½)
```

三证书:T10 恰 2 条带、|eig|=1、**壳与 walk 逐位同一(2.2e-16)⟹ J5/κ_placed/R17/R19/Z4c 全部原封不动**;T11 静响应 h*=S/(4cos²θΣsin²(k_i/2)) 精确格点 1/k²(实迭代 4.2e-15);T13 静井精确 ∈ ker K(ω=0 ⟹ κ₀=0)⟹ **Z4c 不与井打架,具名张力消解**。这就是"R13 桥放置侧版本"的本体:π 住半时间步=时间侧的交错放置。

**L3-v2 门表**:原 L3 五门(G1 重立)+ 牛顿金丝雀 h₀₀/φ=2.00±0.02(T 行已死,无豁免)+ Jordan 复检(新核自己的 k=0 零模=物理牛顿零模,与规范 Jordan 区分入册)+ 波扇区门全重放(N_prop 含对角、J5——壳同一,预期无恙,但要实测)。**除几何演化核一件外,其余零件全部冻结不动**(κ_placed 约束、Z4c、R19 存储、源链——你们已证源链架构无关,正资产直接继承)。

## 三、复核协议

每级车道B贴:门数值表 + 证伪炮结果 + 种子/参数/代码 hash。车道A独立复核(oracle:R16 逐k在壳、R15/R17 符号层、R18 谱界),24h 内回执 PASS/追因。连续两轮同级不过 → 停手会商。

## 四、成文口径(P5 后冻结,防止报告期漂移)

- 新颖性主张按 P5 三条分级:(c) 共核共光锥⟹J5 按构造【最强】;(a) 交错实现+半角壳匹配【待 divdiv 等价性补检,可能再收窄】;(b)【证据最薄,谨慎】。全部加"据我们查新所及(2026-07)"。
- **必引**:Rocek–Williams 1981、Hamber–Williams 2004(格点 De Donde 引力子传播子+弱场只传 TT——注意这是**支持性**先例:独立确认了我们判据链的物理内核)、Christiansen 2011/2024、Hanot–Hu 2025(同病灶不同药)。
- Christiansen 不稳定性猜想的回应措辞**限定**:我们的稳定性在立方交错格+walk核,其猜想在单纯形网格——回应的是"格点线性引力可稳定"的精神,不是对其猜想本身的反例。
- Hanot–Hu 差异化:他们保约束结构,我们做涌现判定+证伪工程+物质共锥耦合;互补表述,不贬低。
- M3 未过线前对外表述:"线性层全部零件各自过证书,装配未过"。

## 五、卡点② no-go 裁定 + R26(车道A,2026-07-24)

裁定 **A(重构)**。详见 `docs/reports/小报告-R25R26-nogo确认与双曲化裁定.md`,证书 `experiments/r25_nogo_r26_feasibility.py`。

- **对抗复核确认 no-go 为真**:σ_min(K)∝k^1.00 ⟹ 投影 gap∝k²;就地阻尼=热扩散,长波违反保留 1.000(清不掉,非仅慢);有界局域 P gap∝k²。register 保留 no-go 结论。
- **R26 可行且重构**:约束扇区双曲化 ⟹ 违反以群速 0.480≈c 传播,配 L4 sponge 清除时标 ~L/c(k-无关,对比就地 1/γk² 发散);物理 (h,π) 块本征逐位不变(dev=0,牛顿零模/TT/J5 受保护)。**R26 = 约束扇区复用 L3→(h,π) 双曲升级 + L4 边界,非冻结 R25 加质量。**
- **与 L3-v3 正交但须合流**:R26(违反清除机制)与 L3-v3(K 随实核共变形)是两条轴;R26 的 B3 须在共变形后 K 上重认证,卡点④联验合流,不各自宣告关闭。
- **第二波前置**:卡点③ 的运动源镜像 sector(保 h̄0i)先补。

## 六、卡点⑦ 根因裁定(车道A,2026-07-24)

裁定 **C 优先(便宜可证伪),不开新构造线**。详见 `docs/reports/小报告-R27-卡点7根因-判据壳失配.md`,证书 `experiments/r27_emergence_shell_audit.py`。

- **卡点⑦ FAIL 真实**(M3 涌现A 门未过),但"R25 核心 2-DOF 主张崩塌"读法过悲观。
- **硬证据 E1**:placed-κ 规范模在**壳一致 placed Riemann** 下曲率 = 7.3e-31(机器零,全 k)⟹ 壳一致判据看规范模为零曲率,不计入 DOF。
- **单调链**:朴素理想判据(动量 k)6 → R25 Wilson 混合 4-5 → 壳一致 placed 判据 **2**。4-5 = R25 已部分共变形、判据/演化壳未完全一致的中间态。
- **诚实边界**:E1 证壳一致对**静态**规范模零曲率(必要条件);未证 lane B 的 U **保持** placed 子空间(演化是否也需 Wilson 共变形,需真-U 上验)。
- **三分便宜检验(压 A 之前)**:judge Riemann 微分换 placed κ,在真实 U 输出上重判 —— →2 纯判据修(最便宜)/ →中间 walk 核也需共变形(中等)/ →不变 深障碍(裁 B)。
- **卡点④ 按住**:N_prop=2 未解前联验无意义。register:卡点⑦ 从"崩塌"降为"壳一致性工程未完成,可行性有硬下界"。

## 七、R28 裁定接受 + DOF 轴固化为具名开放阻塞(车道A,2026-07-24)

裁定 **B(固化开放问题)**,并认账 R27 乐观读法被 R28 证伪。

- **R27 证伪认账**:判据壳失配假说错。E1(placed 规范模零曲率)对但不相关——R28 正控证明 walk 的携曲率传播模不是规范模。动态 placed(真实 U)= [4,4,4,4],placed 化只削 5→4,救不回 2。单调链补全:naive 6 → R25/ideal 4-5 → 壳一致静态 4-5 / 动态 **4**。C 否,B 立。
- **开放问题精确陈述(良定义,有验收判据)**:
  - ker K = placed 规范(4)⊕TT(2)= **6 维**(R15 T9 已证);正交补 = 约束行空间 4 维。
  - N_prop=2 ⟺ walk U 的传播不变子空间 ⊆ ker K(即 U 保持约束满足面,且面上只有 TT 携曲率)。
  - R28 实测:U 的 12 单位模有 **34–49% 能量在 ker K 之外**(约束行空间),6–7/12 模残差>0.3。⟹ U **不保持** ker K。
  - **验收判据(R28 给定)**:单位模能量在 placed 规范⊕TT 内的残差 → 0 ⟺ N_prop = 2。
  - 定位:43f 结构定理("演化/约束/规范共享同一离散微分")在**动力学层未闭合**——约束与规范已共享(R15/R17),演化核 walk 尚未共享到"保持 ker K"。
- **封存**:第二波三 PASS(R26 违反清除、Eddington 张量签名、镜像 sector)固化为"R25 静态 + 违反-清除扇区已通"阶段结论。DOF 轴 = 具名开放阻塞,留作下一战役(A:构造层让单位模压进 ker K,碰 R25 核心复形,PI 单独起范围)。
- **卡点④ 按住**;M3 涌现A 门未过,对外表述不变。
