# 外部深研评估与下阶段计划

*车道A,2026-07-23。用户提交一轮外部深度研究(综述式,覆盖纲领定位、标量天花板、
宽盒vs光锥、交错构造、约束阻尼、算力基建)。本文是逐条核对后的客观评估 + 下阶段
工作的明确排序。纪律:不迎合,与总账/负控逐条对拍。*

---

## 一、评估总评

约六成内容是本仓库文档(README/HANDOFF/纲领)的转述,不构成新信息。真正新增
且核对有效的借鉴点 3 条,已否证或接反的主张 2 条,细节错误若干。**报告不知道
我们的负控记录(字面 Yee → N_prop=5),其最核心处方恰是已踩过的坑的朴素版本。**

### 可借鉴(3 条)

| # | 内容 | 价值 | 注意事项 |
|---|---|---|---|
| 借1 | **Z4c/CCZ4 约束阻尼**:把约束破坏提升为动力学场 Z_μ,传播+衰减(κ₁/κ₂),替代/补充逐点反应式阻尼 | 阻尼扇区(第二阻塞)的成熟设计参照,与 M1 "sponge+无滞后阻尼" 同构 | Z4 加 4 个额外场,**N_prop 计数必须显式扣除**,否则涌现判据自污染;先 in-vitro 对比 R18 Chebyshev 循环,赢了才进装配 |
| 借2 | **"5 模 = 有质量引力子"归因假设**:3+1D 有质量自旋2恰 5 极化 | 若查实,"紧致盒/walk核为何是5"从现象升级为具名机制(Fierz-Pauli) | 两个廉价签名:(a) 多余3模色散有质量隙;(b) vDVZ ⟹ 偏折 = GR 的 3/4(Eddington ~1.5 非 2.0)。现有 green_one 数据 FFT 可查 |
| 借3 | **Weinberg-Witten 定位段**:格点无精确洛伦兹协变流 ⟹ 定理前提不成立 | 成文时的审稿防御,一段即可 | 标准 lore,无实验含义,不占关键路径 |

### 不可采信(点名,防止回坑)

- **字面 Yee 交错处方**(对角放节点/h_0i 放边/h_ij 放面):`staggered_geometry.py`
  已否证(N_prop=5)。有效版本必须从 **walk 转移映射**导出、实现 κ_i=2sin(k_i/2),
  硬门 |K@TT|≈0 且 |K@gauge|≈0,**不许只数维数**(R17 v2 门盲点教训)。
- **Nordström 类比在 EP 轴接反**:Nordström 耦合迹 T(光子脱耦、慢物质全耦);
  我们是 F∝T⁰⁰·(v_g/c)²(慢物质脱耦、超相对论全耦),且 c=cosθ 下光会偏折。
  成文引用时写"天花板同类、机制不同",不许照抄类比。
- 细节:宽盒限速是 von Neumann 稳定界(cg2≤1/3)非"数值色散";"CDT/因果集/自旋
  网络共同瓶颈"说法过松,不作证据引用。

---

## 二、下阶段工作(排序,回 M1/M2 纪律不变)

### P1(唯一主攻):staggered-h 放置构造 —— 车道A

从 R13 转移映射把约束侧扩到半格:从 χ-walk 子步态导出 h_μν@(x+½(e_μ+e_ν)) 的
放置算子,使实空间实现 κ_placed(吸收 e^{±ik/2})。
**硬门(预注册,进门先查):|K@TT|≈0 且 |K@gauge|≈0(单 k、在壳),字面 Yee 与
任何整数格模板作为负控必须 FAIL。** 三轮撞墙教训:整数格上别再试。

### P2(并行,车道B):R18 五条处方 in-vitro 执行

单 k、在壳初条、逐项对齐,**不做全装配**:
γ∈[0.05, 0.066](或三级 Chebyshev 整循环);enforce 与 measure 同一函数、断言同 K;
次序焊死 walk→测C→同步阻尼;SV 谱第3大 <0.05 且 tt_match>0.95 两条进硬门。
逐项过而装配仍败 = 新信息,停手会商。

### P3(廉价诊断,一轮内):5 模扇区质量谱检验(借2)

现有 green_one 数据:多余 3 模色散拟合质量隙;若有隙,补 vDVZ 偏折签名
(预期 GR 的 3/4)。查实则写"紧致盒=有质量引力子离散化"具名小报告。

### P4(择机):Z4c 式阻尼 in-vitro 对比(借1)

R16 oracle 上对比:逐点 γ 阻尼 vs Chebyshev vs Z4c 型(Z_μ 传播+衰减)。
判据:约束衰减率、TT 保真、N_prop(扣除 Z 场)。赢了才有资格进 M1 装配。

### P5(文献,一次性):离散对称张量复形定向检索

staggered-h 的数学成熟对应物:线性化 Regge 单元(Christiansen)、
Arnold–Winther 弹性复形、mimetic/FEEC 对称张量离散——它们解决的正是
"对称张量分量放哪里才能让约束算子有正确的核"。一次定向检索,输入给 P1,
避免重新发明或漏掉已知不可行定理。

### 成文线(不占关键路径)

C1 标量引力成文时加:Nordström 对照段(按"机制不同"口径)+ WW 定位段(借3)。

---

*口径不变:J5 = c_gw=c_matter 单一光锥,不许放松成 c_gw≤c;诚实定位 =
光锥半边已赢 + DOF 半边具名开放,不许暗示整体已解。*
