# 小报告:R25 运动源符号层——moving exact-current source 接入 14 分量/共轭双 sector

*车道B,2026-07-24。R25 pre-M3 卡点③的符号层前半:把匀速运动物质波包的 R10 精确键流,
提升为 R25 双 sector 的几何源项,并给出四证书 + 卡点① 实空间 step 的源接入规范。
本报告只认领本文件声明的四条符号层证书,不代表 M3 进展;实空间集成归卡点①。*

- 脚本:`experiments/r25_moving_source_symbol.py`(sha256 见 JSON `source_sha256`)
- 结果:`data/results/r25_moving_source_results.json`
- 冻结继承(只读 import):`cp1_v4_L2.walk_symbol`、`r25_detour_complex`、`r25_auxiliary_wilson_complex`
  (`static_newton_lift`/`base_kappa`/`augmented_maps_at_z`)、`r25_dynamic_symbol.damped_map`
  (μ = 1.9953e-3,12 单位模工作点)、`r10_current_generator`(运动 4 分量 Dirac 对)
- fp64 numpy,确定性种子,全程 ~8 秒。**status: PASS(四证书 + reality pairing)**

---

## 一、结论先行

1. **核心构造——walk 协变运动源(唯一 z 依赖在时间分量振幅 A(z))。** 由冻结 walk 符号
   `U(k)=a I − i b·σ`(`a=trU/2`,`b_j=(i/2)tr(σ_j U)`,精确 SU(2) 恒等 `a²+b·b=1`)与时移符号 z:
   ```
   A(z) = (b·b)/(z − a)          # z=1 处 = 1+a(可去极点),运动时携带源的推进相位
   h̄00 = A²/(2c) ,  h̄0i = −A·i b_i/(2c) ,  h̄ij = (b·b δij − 3 b_i b_j)/(4c)[与 z 无关]
   ```
   代数恒等式 `(z−a)A = b·b` ⟹ **`κ(z)·η·h̄(z) ≡ 0` 对一切 z**(逐项相消,nu=0 与 nu=i 两行均消)。
   z=1 时 A=1+a,逐位退化为 `r25_static_newton.static_newton_lift`。空间块与 z 无关 = 与静态源逐位相同;
   源的全部"运动"只进时间-时间/时间-空间振幅 A(z)。这保证运动源天然 walk 相容(不带 L3 病B 的离壳失配)。

2. **四证书全绿(机器精度)。** S1 守恒 1.1e-15(血统锚 R10 运动走子 1.6e-18);S2 de Donder 1.4e-15
   且静态极限逐位对拍 3.9e-14;S3 源构造非奇异(max|A|=1.98,无极点)且无 secular(gap>0 ⟹ 有界);
   S4 半时间步槽位机器零,冻结/整数槽误放 → O(1)(5.9,复现 R13/L3"时间槽错=主犯"教训)。

3. **reality pairing 给出一条结构性归因(如实交付)。** 星对合保持双 sector de Donder 暗性(8.9e-16),
   增广 gauge/约束秩不加倍(4/8)。**但静态源的 `physical = Re(h̄₊)` 收拢配方对运动源失效**——它会抹掉
   物理动量流 h̄0i(单 sector 内纯虚)。运动源的实性必须用显式共轭/镜像(反轴序)sector +
   位置空间厄米性 `h̄(−k)=conj(h̄(k))`,darkness 与 4-gauge/2-偏振计数不变(不加倍)。这是一次**位置空间装配**,
   写进接口规范交卡点①,不是符号层障碍。

## 二、四证书数值表

| 证书 | 门线 | 实测 | 判定 |
|---|---|---|---|
| **S1** 符号连续性 `κ(z)·η·S=0`(全 BZ,v∈{0.1,0.3,0.5}) | <1e-12 | **1.11e-15** | PASS |
| **S1** R10 血统锚:运动 4 分量 Dirac 对键流连续性 | ~1e-15 量级 | **1.63e-18** | PASS |
| **S2** 运动 de Donder 残差 `C₊(z_v)·S`(全 BZ 16³×3v) | <1e-12 | **1.40e-15** | PASS |
| **S2** 静态极限 z=1 逐位对拍 `static_newton_lift` | <1e-11 | **3.87e-14** | PASS |
| **S2** 静态 z=1 全 8 行 K₊ 暗性(物理+辅助) | <1e-11 | **1.22e-15** | PASS |
| **S3** 源构造有界 max|A|(无源侧极点;min|z_v−a|=0.019) | <1e3 | **1.981** | PASS |
| **S3** 无精确共振 `min_k|z_v−spec(M)|`(全 v) | >1e-6 | **4.16e-06** | PASS |
| **S3** 无 secular:最坏 k 时域 ‖x_t‖ 终/中比(T=6000) | <2.5 | **1.998** | PASS |
| **S4** 半时间步(T3 居中)槽位残差 | <1e-12 | **8.95e-16** | PASS |
| **S4** 冻结/整数槽位误放残差(反面,应 O(1)) | >1e-2 | **5.92** | PASS |
| **reality** 星对合双 sector 暗性 | <1e-12 | **8.95e-16** | PASS |
| **reality** 增广 gauge/约束秩(不加倍) | [4]/[8] | **[4]/[8]** | PASS |

### S2 辅助 sector 申报(诊断,非门)
物理 de Donder `C₊`(4 行)对一切 z 暗;只有辅助 Wilson selector `D₊`(H 行)在 z≠1 看到源的运动
(实测 `H·h_lift(z_v)` 非零)——与静态 lift 只在 z=1 辅助暗完全同构。**这是 Wilson 正则 sector 承载源运动的申报特征**,
非缺陷;卡点① 的 14 分量 drive 辅助 4 行按此路由(见接口规范)。

### S3 诚实边界(near-resonance 归因)
`min_gap>0` ⟹ 分解算子 `(z_v I − M)⁻¹` 有限 ⟹ 响应**数学有界、无 secular(线性 t)增长**。
但近共振振幅在 v=0.3 最坏 k 达 resolvent 界 1.4e5(gap=4.2e-6);T=6000 步内时域范数仅 ~4.2e3(仍缓升,
饱和需 ~1/gap≈2.4e5 步)。**此近共振幅度受制于冻结的极弱阻尼 μ(收缩率 0.9999993)——即阶段复盘 §2.4
已在册的 pre-M3 rate-quality 开放项,不是运动源新病理。** 任意有限 endurance 窗内运动源不发散(数千量级);
渐近界大是弱 μ 问题,归 `r25_precond`(他 agent)。逐 v 明细见 JSON `S3_moving_limit.per_velocity`。

## 三、实空间接口规范(交卡点① 的 `step(h, π, drive=D)`;只写规范,不改其文件)

| 项 | 约定 |
|---|---|
| **drive 定义** | `D` 14 分量场:物理 10 行 = `a_W · h_lift`(h̄→normal 打包,用源本地推进振幅 A(z_v));辅助 4 行 = 0。阻尼+源闭环平衡态 = `h_lift`,静态极限精确复现 `r25_static_newton`。 |
| **进哪个 sector** | 注入 plus(有序 xyz)sector;共轭/镜像 sector 由 reality `h̄(−k)=conj(h̄(k))` 生成。**禁止用 `Re()` 收拢**(会抹掉运动动量流 h̄0i)。物理计数保持 4 gauge / 2 偏振,不加倍。 |
| **进哪个时间槽** | S00(密度)整数步;S0i(动量)半整数槽 = R9 T3 居中 `J=½(F_t+F_{t−1})` = R19 `OFFSET[(0,i)]`。若 step 只在整数槽注入,S0i 走 **R13 放置转移映射**(R19 §六预留项)。冻结/整数槽误放 → de Donder O(1) 崩(S4)。 |
| **三处注入** | 14 向量 D 同时进 (i) π 更新、(ii) h 更新、(iii) K_state proposal `z·h_t → (1−a_W)h + π + D`——与协调给定 step 语义及静态不动点一致。 |
| **归一** | D 携带 `a_W = 2 − tr(U_walk) + r²`,物理 Green 响应是唯一标量 `1/a_W`(无其他求逆);源电流振幅用与 `base_kappa` 相同的 `/c` 归一(C=0.5)。 |
| **守恒契约** | 注入 D 满足 `κ(z)·η·D_phys = 0` 机器零(S1/S2);step 用居中时间槽即保持之。de Donder 残差 = 在线健康监视器。 |
| **本文件不做** | 运动 lift 的实空间有限 stencil、R13 半步转移映射实现、镜像 sector 实装配、动态 Newton 不动点/endurance——全归卡点①,符号层不越界。 |

## 四、诚实边界

- 证书限 fp64 符号层;实空间集成、endurance、Eddington 重放不在本文件,不得据此宣告 M3。
- S3 弱 μ 近共振幅度大(见 §二 S3 边界),归在册 rate-quality 项;运动源本身构造非奇异、无 secular。
- reality 的运动实装配(镜像 sector)是**指认的位置空间待做件**,已写入接口规范;符号层只证 darkness/秩不变。
- μ、walk 符号、static lift、R10 生成器均只读继承,运行时 import;未改任何现有脚本,未碰
  `r25_realspace_step`/`r25_precond`/`r25_family`,未碰 git。

## 五、复现

`RULESPACE_BACKEND=numpy .venv/bin/python experiments/r25_moving_source_symbol.py`,~8 秒,
确定性种子;JSON 含四证书全数值、S3 逐 v 明细、reality 归因、接口规范与 sha256。

## 一句话结论

**运动精确电流源已在符号层接入 R25 的 14 分量/共轭双 sector:唯一 z 依赖是时间振幅 A(z)=(b·b)/(z−a),
四证书(守恒 1e-15、de Donder 1e-15 且静态逐位退化、v∈{0.1,0.3,0.5} 源非奇异无 secular、半时间步槽位机器零而误放 O(1))全绿,
reality pairing 不加倍且指认出"运动源不能用静态 Re 收拢"这一位置空间装配待做件——实空间集成待卡点① step 落地。**
