# R30:离散线性化-Bianchi 张量复形的动力学半 —— 构造成功,存在性证得

*车道A / B 理论攻坚,2026-07-24。承接 R29 钉死的方向(投影路线证伪,走 Yee 型非 Regge,剩余命题=局域酉张量复形存在性)。证书:`experiments/r30_tensor_complex_dynamical.py` → `data/results/r30_results.json`。*

---

## 结论先行(三选一裁定:构造成功)

**裁定 = 构造成功。** 存在一个**局域、酉、离散线性化-Bianchi 的张量复形**,其演化 `U` 是局域算子之积、离散 Bianchi(张量版 `div curl = 0`)作为**算子恒等式**机器零成立、约束传播是其**推论**(非投影、非 Noether)、运动学半 `R∘D=0` 保持、且无投影动力学 **N_prop = 2** 在全部 k(含体对角 (2,2,2))断崖式成立。

这**决定性地正面回答了 R29 留下的存在性命题**:三方评审提出的 Nielsen–Ninomiya 型顾虑**没有杀死自旋2复形**——张量版 `div curl = 0` 和 Maxwell 一样是**关于 kappa 的多项式恒等式**,因此局域酉的张量复形存在。

**红线(必须与验收同页):** 这是**存在性构造**,`U` 是**装配的**(不是涌现的 Dirac 行走)。构造成功 = 张量复形**存在**(可宣告),但**不等于 M3、不等于涌现**。"涌现行走能否被形变到此复形上"(= M3)仍开放、本轮未碰。R28 走行 `U` 判 4 的负结果依旧成立且被此构造精确定位为"行走不在复形内",而非"复形不存在"。

---

## 一、与 Maxwell 模板逐项对照(血统对拍)

| 结构 | Maxwell(自旋1,R23) | 本构造(自旋2,R30) |
|---|---|---|
| 场 | `A_mu`(4) | `h_mu_nu`(10) |
| 局域 curl | `F = kap∧A`(单 curl) | `inc(h) = 线性化黎曼`(双 curl / incompatibility) |
| **div curl = 0** | `\|k·(k×v)\| = 1.0e-15` | 收缩 Bianchi `\|kap^m G_mn\| = 5.6e-15`;全二阶 Bianchi `4.4e-15` |
| 约束 | 高斯 `k·E=0`(1) | 去唐纳 `kap^m hbar_mn=0`(4) |
| 规范 | `A+kap ξ`(1 参) | `hbar+kap ξ+ξ kap-η(kap·ξ)`(4 参) |
| 物理 | 2 光子极化 | 2 TT 引力子极化 |
| 演化 | Yee leapfrog `E,B` | placed 波 leapfrog `(h,π)` |
| 约束机制 | `div curl=0` 恒等式(非投影) | **同左**:`kap^m G_mn=0` 恒等式(非投影) |

对拍成立:张量层的 `div curl=0` 与 Maxwell 同为 `1e-15` 量级机器零。**张量层未出现障碍**——这正是 R29 悬置的真命题的答案。

---

## 二、Bianchi 恒等证书(张量版 div curl = 0,全 BZ)

placed 半角演算 `kappa_i = 2 sin(k_i/2)`、`kappa_0 = 2 sin(ω/2)/c`(R15 T8,= 整数 roll 后向差分,支撑半径 1)。全 BZ 342 个在壳 k × 随机 h:

- **`inc∘D = 0`(运动学半,R∘D=0)**:规范模的黎曼 = **2.66e-15**(复验 R15/R16 E1)。
- **`div∘inc = 0`(收缩 Bianchi `kap^m G_mn`)**:**5.62e-15**。
- **`div∘inc = 0`(全二阶 Bianchi `kap_[l R_ma]nb`)**:**4.38e-15**。
- **实空间算子恒等式**(整数 roll 中心差分,非逐 k):`max|D^m G_mn| = 5.57e-16` —— 因所有差分算子对易,多项式恒等式**逐字提升为算子恒等式**(对拍 R23-P6 的 Maxwell 手法)。
- **Maxwell 基线** `|k·(k×v)| = 1.03e-15`(7e-16 血统)。

机制:`inc` 是双 curl(`R ∼ kap kap h`),`div` 再取一次与反对称 ε 结构缩并的导数,`kap_i kap_j` 对称 × ε 反对称 → 0,与 Maxwell 完全同理。**恒等式对任意 h、任意 kappa 成立,不经过投影**。

---

## 三、Yee 演化证书(局域 + 辛 + 严格酉)

`U` = placed 波算子 `d_tt h = -L(kappa) h` 的 Störmer–Verlet(leapfrog)宏步,`L = Σ_i kappa_i^2`(3 点 stencil,整数 roll ±1,支撑半径 1),`dt = c = cos(π/3)`。

- **局域**:Laplacian 3 点/轴、curl 后向差分,`U` = 两个局域剪切之积,支撑半径 1(入册)。
- **严格酉**:`max ||eig|-1| = 2.22e-16`(CFL 带内 `dt^2 L < 4`,全样本满足)。
- **辛**:`max|det-1| = 2.22e-16`;`max|M^T J M - J| = 2.22e-16`。
- **色散对齐行走**:`dt=c` 使模频率 = 行走壳,`max|freq - shell_omega| = 2.22e-16`。

这正是 R29 独立裁定的核心:**走 Yee(算子级离散 Bianchi 恒等式)而非 Regge(作用量变分)**,兼容酉性——本构造实现了它。

---

## 四、约束传播成为推论(非投影)+ 决定性 N_prop 验收

从**干净 IC**(ker K = placed 规范(4) ⊕ TT(2),声明前提,同 卡点⑦/R28)出发,`U` 演化 `T=320` 步:

| k | 标签 | N_prop | 去唐纳 `\|C\|/rms`(全 T 上确界) | ker K 外残差(均值) | sv(前 4) |
|---|---|---|---|---|---|
| (2,0,0) | axial | **2** | 7.7e-15 | 3.6e-16 | 1, 0.8, 1e-15, 4e-16 |
| (0,3,0) | axial | **2** | 5.7e-15 | 5.1e-16 | 1, 0.5, 9e-16, 5e-16 |
| (2,2,0) | face-diag | **2** | 1.3e-14 | 1.4e-15 | 1, 0.3, 1e-15, 1e-15 |
| (2,2,2) | **body-diag** | **2** | 1.2e-14 | 1.3e-15 | 1, 0.6, 2e-15, 1e-15 |

- **约束传播 = 推论**:去唐纳 `C_ν = kap^m hbar_mn` 全程停在机器零。**机制不是投影**:`C` 是所有分量共享同一标量 leapfrog 的固定线性组合,其保持 0 的根因**正是收缩 Bianchi 恒等式 `kap^m G_mn = 0`**(`□C = □(kap·hbar) = -2 kap^m G_mn = 0`)——`U` **由构造**保持 ker K。
- **N_prop = 2 断崖**:sv 第 3 起即 `~1e-15`,与前两个 `O(1)` 之间断崖(第三方 Fierz–Pauli 类比)。全部 k 含体对角。
- **ker K 保持(击败走行的关键)**:单位模残差 `~1e-15`,**对比 R28 走行的 0.34–0.49**。走行 `U` 泄漏出 ker K → N_prop 卡在 4;本 `U` 不泄漏 → N_prop = 2。

### 双正控(判据有齿、非被钉成 2)

- **teeth(全 10 分量 generic IC)→ [6,6,6,6]**:判据能看满曲率,没被做成只输出 2。
- **合成 placed-TT → [2,2,2,2]**:真 2-DOF 经同判据 → 2 带断崖(placed Fierz–Pauli 对照)。
- **纯规范 IC → [0,0,0,0]**:规范无传播曲率、无峰(`inc∘D=0` 的动力学显证)。

正控全绿 ⇒ 判据未被 kappa/壳失配蒙蔽,`[2,2,2,2]` 是真读数。

---

## 五、单调链(把 R28 的负结果精确定位)

```
naive 全规范误读 6  →  R25/ideal 壳失配 4–5  →  R28 placed 走行动力学 4
                                              →  R30 placed 复形 U 动力学 2   ← 本轮
```

R28 的 `[4,4,4,4]` 与 0.34–0.49 残差是**真实的**:它精确地说"走行 `U` 不在张量复形内"。本轮证明**复形本身存在且给出 2**——所以 R28 的障碍是"走行未落到复形上",**不是**"复形不存在"。这把封存文档里"唯一开放阻塞"从"是否存在"降格为"涌现走行能否被形变到已证存在的复形上"(= M3 的内容)。

---

## 六、纲领定位与去向

- **R29 剩余命题(真 N-N 判定的第一步)= 正面证毕**:局域酉离散线性化-Bianchi 张量复形**存在**。自旋2版**未**被 N-N 杀死。
- **43f 结构定理的动力学层缺口**:此前"演化核 walk 未共享到保持 ker K"。本轮给出一个**确实共享**(保持 ker K)的局域酉演化核——但它是**装配核**,不是涌现走行核。
- **仍开放(= M3,本轮红线外)**:能否把**涌现的 Dirac 行走**(其度规藏在硬币角)形变/联接到此复形上,使卡点④单次联验全门+九炮+endurance 一次通过。这是下一战役由 PI 定范围的对象;本轮为它提供了**精确的靶**(把走行 `U` 拉进 ker K = 拉进本复形)。

---

## 附:复现与证书

```bash
RULESPACE_BACKEND=numpy .venv/bin/python experiments/r30_tensor_complex_dynamical.py
```
- 冻结件只读复用:`r15_walk_dedonder`(placed κ / gauge / TT / shell)、`r16_cp1_invitro`(riemann_energy)、`emergence_judge`(packed_to_44)、`r28_placed_judge_reverdict`(判据口径)。
- `source sha256 = eae6034d8403a93977988d0a56e80c8899b9ffe80fcacc4084fc0244b2d519a4`
- `results sha256 = d15785a20242072e36cb7fafb3ac08b34c7ecd2f74eca7a161f52fae3aba3e26`
- 后端 numpy(fp64),运行 ~1.4s。

**一句话**:张量版 `div curl = 0` 是关于 kappa 的多项式恒等式,和 Maxwell 一样机器零——所以局域酉的离散线性化-Bianchi 张量复形**存在**,N_prop=2 断崖证得;这决定性关闭了 R29 的存在性命题,但**不是** M3,涌现走行落到此复形上仍是下一战役。
