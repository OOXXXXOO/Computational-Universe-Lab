# R27:卡点⑦ 对抗复核 —— 4-5 的根因是判据/演化/规范的壳一致性未完成,不是 R25 核心主张崩塌

*车道A,2026-07-24。回应第二波 load-bearing FAIL(卡点⑦:动力学无投影 N_prop=[4,4,5,5] 而非 2)。证书 `experiments/r27_emergence_shell_audit.py` → `r27_results.json`。*

## 裁定:C 优先(最便宜可证伪),不开昂贵新构造线

lane B 的 FAIL 是真的(不是仪器 bug),但"R25 核心 2-DOF 主张在动力学层不成立"这个读法**过于悲观**。对抗复核从独立视角定位了根因,并给出一条**单调可行性链**。

## 硬证据(E1,定理级)

placed-κ 规范模 g = κξ+ξκ−η(κ·ξ),用**与构造同壳的 placed Riemann**(动量=κ_placed)诊断,曲率 = **7.3e-31(机器零),全部 5 个 k 方向**。R16 的 Riemann(gauge)=0 在此重confirm。

**含义**:壳一致的判据看规范模是**零曲率**——规范模不该被计入传播 DOF。

## 单调链(4-5 的组成被拆开)

| 判据 | 规范模误读数 | N_prop |
|---|---|---|
| 朴素理想判据(动量=k)| 4/4 全误读 | **6** |
| R25 Wilson 混合(lane B 实测)| 已修 1-2 个 | **4-5** |
| 壳一致 placed 判据(E1)| 0/4 | **2** |

理想判据用 k 而非 κ_placed,规范模的 ideal-Riemann/tt_ref = 0.05–0.63(远超噪声)⟹ 4 个规范模全被读成曲率 ⟹ 6。R25 的 Wilson 混合已经把约束复形**部分**共变形(6→4-5)。要到 2,需让 **Riemann 判据 + walk 演化**也共享同一套 placed 微分——这正是 43f 结构定理("演化/约束/规范必须共享同一套对易离散微分")在**动力学 Riemann 层**的重演,也是 L3-v3"壳与复形共变形"的动力学版。

## 诚实边界(E1 证了什么、没证什么)

- **证了**:静态 placed 规范模,壳一致判据 = 零曲率。⟹ 壳一致是 N_prop=2 的**必要**条件,且对静态规范模**充分**。
- **没证**:lane B 的真实动力学演化 U 是否**保持** placed 规范/TT 子空间。若 U 把 placed 规范模演化出"非 placed 规范"成分,则即使壳一致判据,演化后的模仍可能携曲率——这是"演化也需 Wilson 共变形"的中间情形。我没有 lane B 的 U 实现,不能替它下结论。

## 执行令:三分便宜检验(给 lane B,压 A 之前)

把卡点⑦ 判据的 Riemann 微分**完全换成 placed κ**(与 R25 构造壳逐字一致,和 R15 §三"TT 投影仪用 κ 不用 k"同一课),在**你们的真实 U 输出**上重判:

1. **→2**:纯判据口径失配(最便宜,E1 直接支持,当轮可关)——卡点⑦ 从 FAIL 翻 PASS;
2. **→中间(如 3)**:判据修了一部分,walk 演化核也需 Wilson 共变形(中等成本,具名但不是全新构造);
3. **→不变(仍 4-5)**:演化真的生成规范曲率,深障碍 → 裁定 B(具名开放),再谈新线。

**不许在这个便宜检验之前开"动力学 DOF=2"的新构造线。** 卡点④ 单次联验在 N_prop=2 解决前无意义(联验一个 4-5 的构造不会变 2),按住不动,与 lane B 一致。

## register 处置

卡点⑦ FAIL 保留(诚实,M3 涌现A门未过)。附加根因定位:"判据壳失配(理想 k vs 构造 placed κ);壳一致判据对规范模零曲率(E1 硬);naive 6→R25 4-5→壳一致 2 单调链;深度待真-U 三分检验"。从"R25 核心主张崩塌"降级为"壳一致性工程未完成,可行性有硬下界"。
