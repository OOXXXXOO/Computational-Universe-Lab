# R30 独立复核:自旋2 局域酉张量复形存在性 —— 证得,N-N 不杀它

*车道A,2026-07-24。回应 lane B 的 R30 构造成功声明。独立证书 `experiments/r30_complex_verify.py` → `r30_audit_results.json`。*

## 裁定:R30 机制独立确认,存在性成立,但边界严格保持

lane B 的 R30 是这条线迄今最大的正面结果。我没有重跑他们装配的 U(在他们手里),而是独立从头验证整个构造赖以成立的**代数骨架**——复形的两个定义恒等式:

| 恒等式 | 含义 | 实测(连续/sin/半角三族) |
|---|---|---|
| 收缩 Bianchi κ^μ G_μν=0(off-shell)| 约束传播的来源:□C∝κ^μG=0 | 7.1e-15(机器零)|
| 规范零曲率 G∘D=0 | 规范模不携曲率 | 4.7e-15(机器零)|

**关键确认:两者都是 4 矢量 κ 的纯代数恒等式,形式无关**——对连续 k、格点 sin k、半角 2sin(k/2) 逐字成立。这正是 R30 的核心洞见(和 Maxwell 的 div curl=0 同血统):机制不是连续极限近似,是代数恒等式,所以有限模板整数-roll 实现能机器零。lane B 报的实空间 5.6e-16 与此一致。

**到 N_prop=2 的逻辑链,每一环独立在手:** 恒等式2 ⟹ 规范模在曲率核(此处+R16);恒等式1 ⟹ 由 G 构造的演化保持 ker C(此处);ker C=规范⊕TT 6维(R15 T9);只 TT 携曲率(R16)⟹ 携曲率传播模=dim TT=2(R30 断崖)。所以 R30 的 N_prop=[2,2,2,2] 断崖是这套骨架的推论,可信。

**未独立重现**(诚实):lane B 具体装配的 U(两局域剪切之积)及其 N_prop 断崖流水线是他们的工程;我验证的是它站立其上的数学。给定骨架确认,断崖可信。

## 三点裁定(回应 lane B 请求)

1. **R30 复核:通过。** 核心恒等式独立机器零确认,存在性成立。
2. **背景 N-N 定理正式记为:自旋2 局域酉张量复形存在性 = 证得(R30)。** R29 悬置的存在性命题决定性关闭在"存在"一侧。后续是构造/可达性,不是"识别放松哪一项"。
3. **下一战役 = 把涌现走行形变到 R30 复形上**,PI 定范围;卡点④ 仍按住(走行落到复形、N_prop=2 之前联验无意义)。

## 边界严格保持(存在 ≠ 涌现 ≠ M3)

lane B 自己诚实标注了,我强化不放大:**R30 是存在性构造,U 是手搭的局域剪切之积,不是涌现的 Dirac 走行。** 这是"手搭正确离散 vs 涌现"这条老区分在张量层的重演——数学对象造出来了,还没从 QCA 规则涌现出来。不能让"复形存在"被读成"引力涌现了"。

## 前沿移位(这才是 R30 的真正意义)

- **之前**:唯一开放阻塞 = "R25 复形同调=2 但动力学 N_prop=4-5",隐含两个未知:复形是否存在 + 走行能否达到它;
- **现在**:R30 把第一个未知答成 **YES**。R28 的 [4,4,4,4] 被精确重定位为"涌现走行不在这个已证存在的复形内",而非"复形不存在";
- **新前沿**:涌现的物质走行能否被形变到这个已证存在、有完整证书的目标上(= M3)。**从"在黑暗里找一个可能不存在的东西"变成"把走行形变到一个已知存在、有验收判据的目标上"——目标具体了,风险从"存在性"降到"可达性"。**

这是自 R25 复盘以来第一个把开放阻塞往前**移位**(而非又一次确认卡住)的结果。它没解决 M3,但它把 M3 的最大不确定性(那个复形到底存不存在)消掉了。
