# R15 定理笔记:walk 自身的交错去唐纳——DOF 半边的理论关闭

*车道A,2026-07-20。回应 #43x(M2'-CP0:光锥半边全绿、DOF 半边 N_prop=[5,6,5,5],追因定位到约束扇区微积分)。证书:`r15_walk_dedonder.py` → `r15_results.json`,203 个 3D 波矢全过。**这是 M2'-CP1 的装配配方。***

---

## 一、你们的追因是对的,且病根可以说得更尖:是相位,不是口味

walk 壳是半角的:$\sin^2(\omega/2)=c^2\sum_i\sin^2(k_i/2)$。约束 $C_\nu=\kappa^\mu\bar h_{\mu\nu}$ 要让 {C=0}=TT⊕规范,充要条件是**符号矢量 κ 在壳上 η_c-零**(规范模代入得 $C=(\kappa\!\cdot\!\kappa)\,\xi$,零性即规范模落进 ker C)。而:
- 无错排前向差分:符号带 $e^{\pm ik/2}$ 相位,壳上 $|\kappa\!\cdot\!\kappa|=0.518$(O(1) 破坏);
- 中心口味:壳上 $|\kappa\!\cdot\!\kappa|=0.035$(小但非零——**这个 0.035 就是你们看到"慢渗出到 5-6"而非干脆 6 的泄漏尺度**)。

两个负控的 dim(ker C/gauge)=6:约束面根本不商掉规范。所以任何 slave 行数、任何差分口味的组合都到不了 2——与你们两轮否证一致,现在是定理不是经验。

## 二、T8:Yee 错排 ⟹ 符号变纯 ⟹ 壳上精确零性

**把 $h_{\mu\nu}$ 放在 $x+\tfrac12(e_\mu+e_\nu)$**(指标 0 = 时间半步;引力的 Yee 格)。前向差分在自然交错点读出,符号变纯:
$$\kappa_0=\frac{2\sin(\omega/2)}{c},\qquad \kappa_i=2\sin(k_i/2)$$
(半相位被放置吸收)。于是 walk 壳恰是 $\kappa_0^2=\sum\kappa_i^2$,即 **κ·κ=0 精确**。证书:203 个 k(含随机 3D)max 7.7e-16。

⚠ 与 M1"字面 Yee → N_prop=5"不冲突:那是把 stride-1 **流**Yee 重建后喂**宽盒**(微积分错配);这里演化本身是 walk(半角原生),Yee 放置正是它自己的微积分。同一个词,相反的角色。

## 三、T9:约束面 = TT⊕规范,连续统代数逐字照搬

κ 纯实且零 ⟹ 全部连续统 Fierz-Pauli 线性代数以 κ 代 k 逐字成立。证书(203 k):
| 量 | 实测 |
|---|---|
| ‖C·gauge‖(规范模精确在 ker C)| 7.7e-16 |
| rank C | =4(全部 k)|
| dim(ker C / gauge) | =2(全部 k)|
| dist(ker/gauge, TT(κ))(商空间比)| 3.3e-8 |

**关键操作细节**:TT 的横向方向是**半角矢量** $n\propto(2\sin(k_i/2))$,不是 $k_i$——涌现判据的 TT 投影仪必须换成 κ 方向(小 k 时同向,大 k 偏离;判据不换会在大 k 误判)。

## 四、M2'-CP1 装配配方(车道B)

1. **放置**:10 分量按 $x+\tfrac12(e_\mu+e_\nu)$ 错排(实现=数组不动、差分模板按放置选取读出点;时间半步=χ-walk 的子步中点,walk 核天然提供);
2. **约束**:$C_\nu=\kappa^\mu\bar h_{\mu\nu}$ 用前向差分于放置读出点(全部 4 行 slave,不再只 0ν);
3. **阻尼**:无滞后,驱向 {C=0}(现在它精确=TT⊕规范,阻尼不再伤物理);
4. **判据**:emergence_judge 的 TT 投影仪换 κ 方向;
5. **预注册预期**:N_prop=[2,2,2,2] **且** c_gw/c_matter=1±1e-2 **同时**成立(#43x 障碍关闭的判定);
6. **证伪炮新增**:去掉错排(同模板)→ 按 T8 必回 5-6;TT 投影仪用 k 方向不用 κ → 大 k 判据失准(定量:两方向夹角 ~k³/24)。

## 五、诚实边界

- 证书是符号层(每个 (ω,k) 的线性代数,203 点机器零)——**动力学装配**(阻尼驱动、源接 R13 J̄、3D 回文序)是 M2'-CP1 的实验内容,未预支;
- 3D 回文 walk 的壳在非轴向 k 有 Trotter 修正(壳形≠严格 $\sum\sin^2(k_i/2)$ 的部分):κ 零性在轴向精确、非轴向到 O(Trotter);若 CP1 在斜 k 出现残余,先查壳形实测再动结构(预记:该修正对 c_gw/c_matter 比值不可见,因两扇区共享);
- 全绿一号 = 本配方 + R14 核 + R13 源,三件理论此后无欠账——若 CP1 仍不过,新障碍将是真发现而非已知遗漏。
