# R13 定理笔记:步幅桥——平均化 R10 流精确生活在 wide-box 微积分上

*车道A,2026-07-20。回应 M1 挖出的结构障碍("R10 stride-1 半格流 与 emergence 所需 stride-2 wide box 不相容,字面半格注入 → N_prop=5")。证书:`r13_stride_bridge.py` → `r13_results.json`,全部机器零。**M2 解冻条件已满足。***

---

## 诊断改判:障碍不是"stride-1 vs stride-2",是"生流 vs 平均流"

## 定理(全部已认证,fp64,回文3D走,力项激活)

**T5(时间桥,telescoping)** 两个 R10 前向平衡相加:
$$b(t{+}1)-b(t{-}1)=-\nabla^-\!\cdot[F_t+F_{t-1}]+(\Phi_t+\Phi_{t-1})$$
证书 A:1.22e-18(2分量θ场走)。

**T6(空间桥=T3 的 10 分量/3D 推广)** 定义对平均 $A_c f=\tfrac12(f+S_c f)$($S_c$=沿 c 平移一格)与胞平均 $\bar A=A_0A_1A_2$。核心算子恒等式:
$$A_b\circ\nabla^-_b=\nabla^{c2}_b,\qquad \nabla^{c2}_b F=\tfrac12(F-S_b^2F)$$
(对平均把前向差分**精确**变成 stride-2 中心差分)。对 T5 全式作用 $\bar A$:
$$\bar A\,[b_a(t{+}1)-b_a(t{-}1)]=-\sum_b \nabla^{c2}_b\,\bar J_b+\bar A(\Phi_t+\Phi_{t-1})$$
$$\boxed{\ \bar J_b=\Big(\textstyle\prod_{c\neq b}A_c\Big)(F_{b,t}+F_{b,t-1})\ }$$
证书 B(2分量θ场):4.88e-19;证书 C(4分量Dirac对+手性混合质量币):3.52e-19。**含剪切(b≠a 通量)、含力项,全分量。**

**T7(Nyquist 湮灭=为什么生流注入必然 N_prop=5)** 符号对偶:中心-1 差分符号 $i\sin k=2i\sin\frac k2\cos\frac k2$,盲区 $k=\pi$;对平均核符号 $\cos\frac k2\,e^{-ik/2}$,零点**恰在** $k=\pi$。生 stride-1 流带 Nyquist 分量 → 直接注入即向 box 的盲扇区泵能量(那个多出来的传播模);$\bar A$ **恒等地**杀掉盲扇区。证书 D:纯 Nyquist 模 $A_b f\equiv 0$(严格 0.00e+00);盲集=零集扫描 True。

## 给 M2 的消费接口(一句话)

**喂 wide box 的源永远是 $\bar J_b$(横向对平均 + 相邻两步时间和的 R10 键流),绝不是生 F。** 密度侧对应地用 $\bar A b_a$;力项(de Donder 源的 Φ 部分)用 $\bar A(\Phi_t+\Phi_{t-1})$。精确性由 T5/T6 定理保证(不是近似——中心连续性残差在 1e-19 量级),步幅由构造匹配,Nyquist 由 T7 干净。

## 预测(M2 的可证伪预期,现在记下)

1. $\bar J$ 喂源后,M1 那个 N_prop=5 的额外模消失(它是 Nyquist 泵出来的);
2. 规范反常泄漏率(∝G)显著降(M2 预注册的 ≥100× 判据照旧);
3. 若泄漏仍不降 → 残余来源是 Trotter 轴序倾斜(∝sinθ|k|),按计划 M2 FAIL 分支的 θ→0 归因扫描走。

## 边界(诚实)

平均是**精确恒等式**不是近似,但它是低通:源的 Nyquist 物理内容(若有)被丢弃——对我们无害(物质波包живет在小 k,Nyquist 内容本来就是格点伪影),但要写明。时间平均引入的半步相位在 J5 速度测量里是 O(k) 小量,M3 各向同性判据覆盖它。
