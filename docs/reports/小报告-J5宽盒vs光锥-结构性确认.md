# 小报告:J5"宽盒 vs 光锥"障碍——车道B 独立实验确认为结构性,两条自然逃逸均失败

*车道B,2026-07-20。承接 M3 全绿一号 FAIL(#43v)。用户拍板"攻宽盒vs光锥障碍"。
本报告是攻坚第一轮的追因结果 + 提请两车道会商的证据。探针:`green_one_subcycle_probe.py`
(纯车道B新建,只读 import go/ej,未改任何被 import 模块)。*

---

## 一、追因动机(先怀疑约定失配,再谈物理)

M3 的 J5 用 cg2=0.25 跑 3D 闭环几何,c_gw=0.5,远够不到 c_matter=0.993。第一嫌疑不是
"物理不可能",而是 **green_one 的 3D `_geom_step` 未子循环**(单次 leapfrog,CFL cg2≤1/3),
而 tier2_gw 的金标准恰是 cg2=1(子循环后 cg2_sim=0.25 稳定,c_gw≈1,ratio=1.004 PASS,已复跑)。
即:1D gw 门想要 cg2≈1,3D 闭环却被 CFL 锁在 cg2≤1/3。**假设:给 3D 几何嵌套 n 子步/外步
(每子步 cg2_sub=cg2_phys/n²,CFL 稳定),几何每外步多走 n 倍 ⟹ c_gw≈√cg2_phys≈1,而空间
宽盒 stencil 不变 ⟹ N_prop 应仍=2。** 若成立则 J5 是可标定,不是结构。

## 二、实验结果(判据:judge_dof 纯几何,G_m=0,feedback=False,N=16 T=384)

| 配置 | 稳定 | N_prop | v_meas(cells/外步) |
|---|---|---|---|
| cg2=1.0, n=1(未子循环) | ✗(CFL 炸) | [0,0,0,0] | — |
| **cg2=1.0, n=2**(子循环) | ✓ | **[5,6,5,5]** | **~0.96(快!)** |
| cg2=1.0, n=3 | ✓ | [5,5,5,5] | ~0.93 |
| cg2=0.985, n=2(瞄 c_gw=0.992) | ✓ | [5,6,5,5] | ~0.96 |
| **cg2=0.25, n=1**(green_one 实际) | ✓ | **[2,2,2,2]** | ~0.50(慢) |

**假设被实验否证,结构性墙被证实并锐化:** 子循环**确实**把引力子提速到 c_gw≈0.96,但代价是
**N_prop 跳到 5–6**。**快引力子 ⟺ 错 DOF;正确 DOF(=2)⟺ 慢引力子(0.5)。** 这正是宽盒 vs 光锥
障碍,子循环不逃逸而是**复现**它。

## 三、机制诊断:stride-2 模加倍在提速时被唤醒

宽盒 `_lap_wide` 是 **stride-2** 空间 stencil(色散 sin²(ω/2)=cg2·Σ sin²kⱼ,盲区在 k=π,
即 R13/T7 的 Nyquist 盲扇区)。慢跑(green_one cg2=0.25)时,那些 stride-2 doubler 模不被激发、
不传播 ⟹ N_prop=2 干净。一旦子循环提速,doubler 醒来开始传播 ⟹ N_prop=5–6。这与 M1
("字面半格 Yee → N_prop=5")、R13/T7("生 stride-1 流的 Nyquist 分量正是泵出 N_prop=5 的")
是**同一个 doubler 病**,只是这次由"提速"而非"生流注入"触发。

## 四、最自然的逃逸(R13 Ā 投影)也失败

T7 定理:胞平均 Ā=A₀A₁A₂ **恒等地**杀 Nyquist 盲扇区。故试:每子步对几何场施 Ā(空间轴
-4,-3,-2),压住被唤醒的 doubler,期望 N_prop 回 2、探针模(k≈π/4,远离 Nyquist)几乎不受影响。

**结果:每子步 Ā ⟹ 不稳定(N_prop=[0,0,0,0],stable=F)** 于 cg2∈{1.0,0.985} n∈{2,3} 全部。
反复低通 + de-Donder 约束解相互作用把场压死/驱散。**这条自然逃逸不通。**

## 五、诚实定级与提请会商

- **级别**:实证负结果(给了反例),非定理。两条 Lane-B 逃逸(补子循环 / R13 Ā 投影)均被
  实验否证。不排除存在更深的几何核解法,但那是**设计/理论问题,非参数探针能碰**。
- **不是**:不是 green_one 的 bug,不是约定失配(子循环约定照 Lane A 的 SUB/SUB2 复现),
  不是收敛性(N=16 T=384 稳定跑出,doubler 是真传播模非数值噪声)。
- **按主线计划**:checkpoint 一轮不过 + 追因确认物理障碍 ⟹ 停 Lane-B 硬攻,**两车道会商**,
  不"疯狂跑"。**这正是计划零节预注册的风险**:"剩余风险全部集中在统一到同一构造时各扇区
  是否互相破坏"——现在确认 J5 扇区(要光锥引力子)与涌现扇区(要宽盒 N_prop=2)硬冲突。

### 给车道A的具体问题(理论攻坚方向,供会商)
1. 是否存在一个空间 stencil,其色散在整个布里渊区**只有 2 支传播模**(无 stride-2 doubler)
   **且**群速可稳定达 ~1?候选思路:(a) 非 stride-2 的紧致高阶核 + 迎风/耗散把 doubler 支
   变为衰减而非传播(需证不碰物理 TT 支);(b) 在色散层直接对 doubler 支加"质量"使其非传播;
   (c) 承认 c_gw<c_matter 但重新论证 J5 是否必须比 0.993(GW170817 只要 c_gw≤c——**慢引力子
   其实满足 GW170817 的因果侧**!是否 J5 判据写反了方向?这条值得车道A核判据方向)。
2. 特别是第 1(c),但**先泼冷水防止误当逃逸**:GW170817 的观测侧只要 c_gw≤c(不超光速),
   慢引力子不违反因果。但这**不等于**慢引力子对我们无害——**c_gw≠c_matter 意味着引力与物质
   有两个不同的光锥,即不存在单一涌现光锥**,这恰恰是涌现 Lorentz 不变性/等效原理在 O(1) 量级
   被破坏的签名(本纲领的命门判据 J4/J5 的物理内核)。所以 c_gw=0.5c_matter **很可能是真实物理
   缺陷,不是判据方向写反**。请车道A判定:J5"要相等"是否就是"要单一光锥"的正确表述?若是(我倾向
   是),则**不许把门放松成 c_gw≤c 凑过**——那是自欺;障碍就是障碍。属"只许收紧不许放松",我不
   擅自动,但明确不建议往松了改。

**处置建议**:M3 维持 FAIL/回 M1/M2;J5 障碍升为具名开放问题;**真正的攻坚是找一个让引力子与
物质共享单一光锥(c_gw=c_matter)且只有 2 支传播模的几何核**(上面第 1(a)/(b)),这是理论设计
问题。等车道A回执后再定 Lane-B 下一轮,期间不"疯狂跑"参数探针。
