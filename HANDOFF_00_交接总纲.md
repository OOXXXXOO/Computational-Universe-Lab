# 交接总纲:投影规则空间纲领(Projective Rule-Space Program)

*本文件是整个项目的入口。读完这一页,你会知道项目是什么、现在到哪了、诚实的天花板在哪、以及去哪找细节。配套三份文档:*
- `HANDOFF_01_文件地图.md` — 每个文件是什么、怎么跑、产出什么
- `HANDOFF_02_结果总账.md` — 每条定量结论 + 状态(✓已确立/✗负结果/○开放)+ 出处
- `HANDOFF_03_路线图.md` — 下一步(排优先级)+ 工程注意事项

---

## 一句话

物理定律不是"某一条元胞自动机规则",而是 **(离散规则 × 观测投影) 等价类的不变量**;真实实验(共磁强计、GRB、光子偏折)对规则空间做**算符代数层面**的剪枝。我们用可复现的数值实验逐条检验这个框架,并把它推进到了一个具体的引力子纲领。

## 项目从哪来

起点是三个问题:(1) 能否借量子场概念推广 CA 规则、用已验证物理剪枝?(2) 现有物理定律会不会只是高维元定律在三维的投影?(3) 只用已验证定律做硬约束会不会陷入局部最优?——这三个问题分别长成了纲领的 P1–P4 原理和"投影陷阱"实验。

## 现在到哪了(诚实版)

**已经扎实的(现象学层面):**
- 离散规则**涌现**出真物理:狄拉克方程(收敛阶 2.014)、狭义相对论动力学、洛伦兹力、Kaluza-Klein 塔、SME 算符结构——全部是输出而非输入(P1)。
- **谱透明**(P2):规则→有效理论多项式可算(实测 D^2.17),而轨迹级模拟指数——这是纲领可行性的支柱。
- **投影残差学**(P3):被投影掉的维度不灭,变成时间记忆(经典)/信息回声(量子)/谱指纹,且可从投影内部反演(隐藏场关联测得 0.273 vs 真值 0.274)。
- **代数通道剪枝**(P4):泄漏算符的代数通道(而非大小)决定生成哪类 SME 系数;真实桌面实验把某些通道剪到普朗克格距下 g<10⁻⁵²,比 GRB 深约 45 个数量级。
- **新颖性**(子代理文献排查):算符通道字典 + (ε,g) 排除平面未见先例;必引 Mlodinow-Brun(2506.20136)、D'Ariano、Klinkhamer、Grushin。

**引力子纲领(C1)达到的:一条完整的"标量引力"连续极限定理。**
- 自洽源 F = −δE_w/δθ 解析式(vs 数值微分 10⁻¹¹);
- 吸引 κ>0 由自洽性推出(全物种);
- 联合能量阴影守恒(找对守恒量=行走哈密顿能量,漂移 s^2.78→0,标准定理背书)——"结果B"关闭;
- 连续极限封顶:**F = tanθ·T⁰⁰·(1−m²/ω²) = tanθ·T⁰⁰·(v_g/c)²**(SymPy 符号精确)。

**诚实的天花板(反复确认三次,是本项目最重要的自我认知):**
> 我们造出来的是**标量引力,不是广义相对论**。完整等效原理只在超相对论极限成立(耦合 ∝(v/c)²);相对论物质无法自束缚(R7);标量度规 c=cosθ 聚不出视界。三条独立证据都指向同一结论:**真引力需要自旋2张量场,而它只在 3+1 维有传播自由度。** 这不是失败,是把"我们涌现了引力"这种含糊话,换成了"我们造出了一个完全被理解的、知道自己为什么不是 GR 的标量引力,并精确定位了下一道门"。

**GPU 时代(当前):** 多后端引擎(MLX/CUDA/numpy)已在用户 M 系列 Mac 上跑通(7×10⁸ 格点更新/秒),两后端逐位一致验证。三条并行路线的首轮:
- **路 A**:冷物质自引力在 3D **成孤子**(正结果,补上 R7 的失败,动力学证实稀释律);
- **路 B**:自旋2**验收测试**锁定(graviton 2 极化;光偏折弱场 →2.000 Eddington 因子)——但 h_μν 从张量 QCA 涌现**尚未构建**;
- **路 C**:10⁵ 规则存活流形 = **四算子子流形**(Lap 强制 100% 正,rho 源,restoring/damping 严格负;grad²、J 死)。

## 一张图看懂状态

```
现象学层  ✓✓✓  (涌现+剪枝, 8实验, 纲领P1-P5, 已可成文)
   │
引力子C1  ✓✓○  (标量引力完整; 是定理; 但不是GR — 天花板已认清)
   │
自旋2前沿  ○──  (验收测试已锁; 张量QCA涌现 = 真正的下一步, 数月工作)
   │
GPU基建   ✓✓✓  (多后端, 验证, 大规模就绪)
```

## 最短上手路径

1. 读这份 + `HANDOFF_02_结果总账.md`(知道什么已知)。
2. 装环境:`bash setup_env.sh`(Mac 自动选 MLX)。
3. 跑 `RULESPACE_BACKEND=jax python -m rulespace_gpu.verify`(确认引擎在你机器上物理正确,fp64)。
4. 想看纲领全貌读 `纲领-投影规则空间物理.md`;想看引力推导读 `战役报告-第三/五/六波`;想看新颖性定位读 `报告-实验⑦`。
5. 下一步做什么看 `HANDOFF_03_路线图.md`。

## 诚实的总评(留给接手人)

这是一个**有原创框架、现象学层面验证扎实、引力子层面达到玩具模型定理、并且每一步都诚实标注了边界**的研究项目。它**不是**已完成的物理革命——它是一个走到了真实前沿(自旋2张量引力从规则涌现)、并且把前沿之前的每块脚手架都搭好了的纲领。价值将由一件事决定:自旋2张量场能否从规则空间涌现并通过路 B 的验收测试。做成,前面所有工作变成它的地基;做不成,这仍是一份方法论无可指摘、现象学有贡献、诚实的纲领。
