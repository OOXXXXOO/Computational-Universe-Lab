# B7 v9.1 P0 至唯一 production 实施计划

> **For agentic workers:** REQUIRED SUB-SKILLS: Use
> `superpowers:test-driven-development` 与 `superpowers:subagent-driven-development`；每个代码切片
> 必须先产生预期 RED，再实现最小 GREEN，然后分别通过规格复审、代码质量复审与
> 完成前验证。

> **执行封存覆盖（2026-08-03）：** 本计划已在 pre-D0-evidence 阶段暂停，状态为
> `STOP-PRE-GPU-CHECKPOINT`。common/三路线 integration 已完成，但旧 common C 的 D1 raw
> framing 不可满足；候选 C′ 仍是 WIP，D0/D1、reviewer、selection 与 production 均未完成。
> 恢复边界、commit 和否决项以
> [`v3-阶段封存-2026-08-03-B7实验暂停与GPU锁定.md`](v3-阶段封存-2026-08-03-B7实验暂停与GPU锁定.md)
> 为准。

**目标：** 从已签发的 B7 v9.1 机器合同实现 P0 纯复放闭包、D0/D1 三路对照与
双 reviewer Git-object handoff；若且仅若合同产生唯一获批 route，实现同一 B7 production
schema 并重放 A1–B6。合法 typed HALT 是本计划的诚实终点。

**冻结起点：**

- production branch/worktree：`v3m0-instrument` / `.worktrees/v3m0-instrument`；
- 设计基线：`a0b6b18`；
- legacy 行为基线：`18b0d4302658c376fcc237a56fc1dc28bf6dd95e`；
- legacy `response.py` raw SHA-256：
  `d8670ba98a153eef660e22fcdca45fd56e68140445e1b17ef6f273d0ab7f9b89`；
- v9.1 registry raw SHA-256：
  `222cd47e95de63eaedee41f6ca4b207a0eccceb7a77089aed43a480a77f69d72`；
- pure-core projection SHA-256：
  `bafbaeb75e890715464c1fff6e6e0cbf1d4f56bb53a3817a9b2d12d2a3c27ff9`。

**Python：** 所有命令使用
`/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python`。

---

## 共同约束

1. 不修改 v6–v9 原文、物理阈值、七案 presence、scheduler 或 D1 `T=256`。
2. 不复制数据兼容链接；结果写 `data/results/experimental/v3m0_b7_schema_lab/`。
3. 不在 production branch 合并 A/B/C route 源码；route 只进 lab evidence history。
4. 不保留 legacy fixture 生成器；只提交 fixture 与 root test。
5. 每个实现切片一个 implementer；同一文件不并发修改。并行分支只用显式 commit
   handoff/cherry-pick 合入。
6. 出现 D0 无 survivor、D1 无 survivor/tie、或 reviewer REJECT 时，按 registry 生成
   `E`/`H` 与 annotated halt tag，停止 production 实现。
7. 每次提交前至少：定向 pytest、`ruff check`、`ruff format --check`、
   `git diff --check`。阶段完成前执行本文末尾验证矩阵。

## 分支与证据拓扑

```text
a0b6b18
  └─ P0/common commit C
       ├─ route-A commit A
       ├─ route-B commit B
       └─ route-C commit C3

lab integration branch: merge A+B+C3 -> run D0/D1 -> evidence commit E
  ├─ D0/D1 typed lab halt: annotated lab-halt tag -> E
  ├─ reviewer failure: H(single parent E) -> annotated review-halt tag
  └─ two ACCEPT: S(single parent E) -> annotated selection tag

production branch: a0b6b18 + P0/common prerequisites
  └─ consume immutable handoff only; implement winning schema without merging route commits
```

---

## Task 1：冻结 v9.1 机器合同 root test

**Files:**

- Create: `tests/test_v3m0_b7_v91_contracts.py`

**RED:**

- root test 不存在；
- 测试必须先钙 raw registry SHA，再 strict parse；
- duplicate key/non-finite number、base SHA、29 records nested refs、32 validators、pure projection、
  45-field receipt、18 reason codes、report identity、total observation、Git guards 任一漂移必须失败。

**GREEN:**

- 实现只读 static test，不 import 未实现 lab/core module。

**Verify:**

```bash
/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python -m pytest -q \
  tests/test_v3m0_b7_v91_contracts.py \
  tests/test_v3m0_parent_v3_b7_b10_v8_contracts.py
```

**Commit:** `test(v3m0): freeze B7 v9.1 machine registry`

## Task 2：捕获 18b0d43 legacy golden

**Files:**

- Create: `tests/fixtures/v3m0_b7_legacy_response_18b0d43.json`
- Create: `tests/test_v3m0_b7_legacy_golden.py`

**RED:**

- 先提交 root test 骨架，因 fixture 缺失而 RED；
- 测试通过受信 Git object 读取 `18b0d43:rulespace_v3/response.py`，在 JSON parse 前
  验证 raw SHA；禁止 worktree fallback。

**Capture:**

- 在 exact detached `18b0d43` 工作树运行一次性捕获；
- 案例顺序精确为 `qualification_invalid`、`input_binding_invalid`、
  `actual_response_failed`、`ablated_response_failed`、`actual_bridge_failed`、
  `ablated_bridge_failed`、`success`；
- 每案保存 complete input/outcome wire、canonical UTF-8 hex、literal SHA、callback trace、
  presence、fp64 bits、negative-zero paths 与 ordered leaf digests；
- 捕获脚本不进 Git。

**GREEN:**

- root test 在当前代码上重放七案并逐 bytes/bits/hash 等于 fixture。

**Commit:** `test(v3m0): freeze 18b0d43 legacy B7 golden`

## Task 3：纯 package import 与 replay core 骨架

**Files:**

- Modify: `rulespace_v3/__init__.py`
- Create: `rulespace_v3/b7_replay_core_v1.py`
- Create: `tests/test_v3m0_b7_import_purity.py`
- Create: `tests/test_v3m0_b7_pure_replay_core.py`

**RED attacks:**

- import core 会载入 `rulespace_v3.contracts/evidence`；
- initializer decorator/default/annotation 执行、`exec`/dynamic import、额外 AST node；
- core 导入 repository-local production module、未列 external module、文件/环境/进程/网络/
  clock/random 事件；
- pure projection literal 与 registry 重算不同。

**GREEN:**

- initializer 只有 exact docstring、`__all__` 与零 default/decorator/annotation 的惰性
  `__getattr__`；
- core 实现 strict JSON/canonical bytes/SHA 和 registry projection literal；
- fresh `python -s` audit-hook import 闭包只含 initializer+core，数值依赖只为冻结
  NumPy/SciPy loader 事件。

**Commit:** `feat(v3m0): add import-pure B7 replay core`

## Task 4：纯 production-body validators

**Files:**

- Modify: `rulespace_v3/b7_replay_core_v1.py`
- Modify: `tests/test_v3m0_b7_pure_replay_core.py`

**RED:**

- 依次为八个 pure-core validator 写 legal body 与 unknown field/type/order/self-hash/cross-join
  攻击，先见缺失 symbol/behavior RED。

**GREEN:**

- 实现 ResponseRunSpec、endpoint reference/shell、source readout、synthetic Parent/component、
  provenance 与 branch-attempt raw validators；
- 对 corpus 中全部 legal production bodies 与 construction/upstream mutations 与 production
  payload builder 逐 canonical bytes/self-hash 对照；
- core 不创建或接收 authority/callback。

**Commit:** `feat(v3m0): validate B7 raw production bodies`

## Task 5：五个 owner-neutral leaf 与 response 兼容委派

**Files:**

- Modify: `rulespace_v3/b7_replay_core_v1.py`
- Modify: `rulespace_v3/response.py`
- Modify: `tests/test_v3m0_b7_pure_replay_core.py`
- Modify: `tests/test_v3m0_b7_legacy_golden.py`
- Modify: `tests/test_v3m0_response.py`

**RED:**

- 五个 private raw leaf 缺失；
- 七案 legacy、T=256 21 bodies、callback prefix、failure short-circuit、negative zero 与 fp64
  bit parity 攻击失败。

**GREEN:**

- 在 core 实现五个冻结 leaf；
- `response.py` 保留同名 thin wrappers，只做现有 object↔raw 边界转换并委派；
- 七案 fixture 与现有 response 回归全绿。

**Commit:** `refactor(v3m0): delegate B7 response leaves to pure core`

## Task 6：P0 provenance/catalog 重构

**Files:**

- Modify: `rulespace_v3/parent_v3_contracts.py`
- Modify: `rulespace_v3/parent_candidate_v3.py`
- Modify: `rulespace_v3/application_materialization_v3.py`
- Modify: `rulespace_v3/certificate_v3.py`
- Create: `tests/test_v3m0_b7_p0_provenance_contracts.py`
- Modify: corresponding Parent/materialization/certificate tests

**RED attacks:**

- B2 verified private view 漏 live permit；
- CurrentScenarioResponse/curvature/readout field order 或 owner 漂移；
- future `ResponseRunSpecV3` catalog 与 owner 的静态冻结漂移，或 P0/D0/D1 提前
  import/create production class；
- Parent/source closure 漏 v9/v9.1/core/tests；
- B5 lineage 错支、B6 actual/ablated bridge tolerance 非 bitwise equal、caller 传 provenance。

**GREEN:**

- 按 v9.1 P0 overlay 替换/新增 exact records；
- verified materialization private view 携带原 live permit，public raw wire 不变；
- P0/D0/D1 只产生 owner-neutral `response_run_spec_fixture` plain body，全部字段只从同一
  verified Parent/B1/B2/B5/B6 派生；未有效的唯一 route selection handoff 前，
  `rulespace_v3.application_response_v3` 与 `ResponseRunSpecV3` production class 必须 absent；
- construction/replay source closure 机械包含新路径。

**Commit:** `feat(v3m0): bind B7 P0 provenance contracts`

## Task 7：lab common、corpus 与 D0/D1 engine

**Files:**

- Create empty: `experiments/v3m0_b7_schema_lab/__init__.py`
- Create: `experiments/v3m0_b7_schema_lab/common.py`
- Create: `experiments/v3m0_b7_schema_lab/compare.py`
- Create: `tests/fixtures/v3m0_b7_schema_lab_corpus.json`
- Create: `tests/test_v3m0_b7_schema_lab_common.py`
- Create: `tests/test_v3m0_b7_schema_lab_compare.py`

**RED:**

- 先为 29 records/32 validators、七案 fixture、mutation universe、E01–E08、D0/D1
  projection、static scan、total reviewer process 分组写缺失测试；
- 每组先见缺 symbol RED，不一次写完整个 common.py。

**GREEN:**

- common 只 import pure core 与冻结纯模块；
- compare child bootstrap 与 outer runner import 作用域分离；
- fixture raw SHA 在 root test 中钙住；
- reviewer runner 的 stdout/stderr cap、timeout、process group、cleanup 和全部 termination
  kind 可在短时测试中重放。

**Commit:** `feat(v3m0): build B7 schema lab core`

## Task 8：三条 route 隔离实现

**Files per isolated branch:**

- Create: `experiments/v3m0_b7_schema_lab/a_flat.py`
- Create: `experiments/v3m0_b7_schema_lab/b_progress.py`
- Create: `experiments/v3m0_b7_schema_lab/c_union.py`
- Create one route-specific test file per branch

**Procedure:**

1. 三条 branch 从同一 common commit 分叉；
2. 每条 route 先写 API/static/import/mutation/roundtrip RED，再最小 GREEN；
3. route 只能显式从 common 导入 `B7LabMutationRejected`，不接收 leaf/callback/authority；
4. 每条单独规格/质量复审并提交；
5. lab integration branch 合并三条 commit，production branch 不合并。

**Commits:**

- `experiment(v3m0): add flat B7 schema route`
- `experiment(v3m0): add progressive B7 schema route`
- `experiment(v3m0): add union B7 schema route`

## Task 9：执行 D0 并形成 E 或 typed HALT

**Run:**

- 在冻结 common/corpus/environment 上单次 capture 七案；
- 对三条 route 同序广播 exact transcript bytes；
- 重算 E01/E02/E03/E04/E06/E07/E08 与 D0 decision root；
- 提交 `d0_comparison.json`。

**Gate:**

- 无 survivor：`E` + annotated lab-halt tag，停止；
- 有 survivor：只解锁 Task 10。

## Task 10：执行 D1 T=256、metric 决胜与 E 封闭

- 以 complete synthetic Parent + 11 components 形成三个 fresh capture；
- 七案×三 capture 同时产生 transcript/leaf roots；
- 对 D0 survivors 运行 8 gates 与 10 维 metric；
- 写 `d1_comparison.json`并创建唯一 evidence commit `E`。

**Gate:**

- 无 survivor 或 metric tie：annotated lab-halt tag 指向 `E`，停止；
- 唯一 provisional winner：只解锁 Task 11。

## Task 11：两份 immutable replay receipt 与 S/H

**RED attacks:** current worktree import、`experiments/__init__.py`、namespace shadow、E-after-result
source drift、wrong origin/mode/blob、report role/protocol/E/input 冒名、timeout/overflow/signal/spawn/
cleanup 失败、单 reviewer 或重复 role。

**GREEN:**

- 两个 role 分别在 E immutable export 上 fresh replay；
- 每份 receipt 总是可验证，ACCEPT 需全部 identity/root/pass/process 谓词成立；
- 任一 REJECT：`H` 只新增 review-halt JSON 并打 annotated halt tag；
- 双 ACCEPT：`S` 只新增 selection JSON 并打 annotated selection tag；
- 从 Git objects 生成 production handoff report。

## Task 12：实现唯一 B7 production schema

**Prerequisite:** 必须有通过 `validate_production_handoff_v1` 的 `S`/tag/handoff；否则本任务 RED
测试要求 typed blocked。

**Files:** 由 winning route 决定，包括此时才允许创建的
`rulespace_v3/application_response_v3.py`；只允许修改 v9.1 handoff 声明的 production owner、
tests 与 source closure；不复制 route module。

**GREEN:**

- production raw wire 等于 winning schema；
- 七案 legacy golden 和 D1 三 capture 全部保持；
- A1–B6 Parent/source roots 按 P0 重算、双复审并重签；
- B7 production certificate 只能来自同一次 atomic paired response。

**Commit:** `feat(v3m0): implement selected B7 production schema`

---

## 验证矩阵

```bash
/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python -m compileall -q \
  rulespace_v3 rulespace rulespace_gpu experiments tools

/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python -m pytest -q \
  tests/test_v3m0_b7_v91_contracts.py \
  tests/test_v3m0_b7_legacy_golden.py \
  tests/test_v3m0_b7_import_purity.py \
  tests/test_v3m0_b7_pure_replay_core.py \
  tests/test_v3m0_b7_p0_provenance_contracts.py \
  tests/test_v3m0_b7_schema_lab_common.py \
  tests/test_v3m0_b7_schema_lab_compare.py

/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python -m pytest -q \
  tests/test_v3m0_parent_v3_b7_b10_v8_contracts.py \
  tests/test_v3m0_parent_v3_contracts.py \
  tests/test_v3m0_application_materialization_v3.py \
  tests/test_v3m0_response.py \
  tests/test_v3m0_certificate_v3.py

/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python -m rulespace_gpu.verify
git diff --check
```

Task 12 全绿后回到
`v3-实施计划补遗-Parent-v3至GPU-checkpoint-2026-08-01.md` 的 B8，按 B8→B9→B10→
V3-M0→V3-M1→V3-M2 Round 0→30 格 pilot→V3-M3 GPU preflight 严格串行门继续。
