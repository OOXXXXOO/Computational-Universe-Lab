# V3-M0 双轴仪器迁移实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development`
> task-by-task；每个任务先做规格复核，再做代码质量复核。任何红灯不得跳过。
>
> **2026-07-31 生效勘误：** Task 12–17 的 application scenario、typed termination、
> C20 analysis lane、permit→construction authority 与 reference shell 以
> `v3-勘误-application-scenario与typed-termination-2026-07-31.md` 为准；本计划下文
> “C04–C20 均产出 VerifiedResponseBlock”的旧措辞不再有效。

**目标：** 在不运行 R30/R23/R25 物理重测、不设计新 family、不启动 pilot 或 GPU 长跑的
前提下，交付可重放的 V3-M0 仪器：形式化 no-go、机械 construction trace、matched
ablation、共同响应块、`ε_causal`、`δ_geom`、`σ=(α,A)` 与完整 synthetic controls。

**架构：** 依赖链冻结为

```text
Lean toolchain → formal no-go
schema/evidence → exact verifier
schema → ConstructionTrace → MatchedAblation
factory + ClosedControlRegistry + pre-structure → MeasuredTransition
MeasuredTransition → full-state bridge + Laurent residuals + full-64 spectral margins
上述完整证据 → VerifiedDynamicsCertificate
VerifiedDynamicsCertificate → per-control/per-T endpoint shell + paired response
全部 control 的候选证据 → WindowCalibrationOutcome
WindowCalibrationOutcome → VerifiedWindowThresholdCalibration
C01–C03 selected evidence / C04–C20 application permits → VerifiedResponseBlock
VerifiedResponseBlock + independent threshold capabilities → CausalSurvival
VerifiedResponseBlock + independent threshold capabilities → GeometricDistance → SigmaFit
all blocks → controls orchestrator → V3-M0 state
```

`CausalSurvival` 与 `GeometricDistance` 禁止各自重建 source selector；
`SigmaFit` 只接受 Geometry 输出的同一 manifest `g(k)`。科学数值显式使用 NumPy
`float64/complex128`，不调用会自动选择 MLX fp32 的 backend。

**技术栈：** Python 3.9+、NumPy、SciPy、SymPy、`unittest`、Lean 4.32.1、
mathlib 4.32.1、canonical JSON/SHA-256。

**签发边界：** 本计划只可到
`READY-V3-M1-ANCHOR-CERTIFICATION`。任何 R30/R23/R25 actual/ablated 物理运行、新
family、Round 0、30 格 pilot、正式扫描和 GPU 长跑均越权。

跨阶段北极星状态机仍须写死且不可跳转：

```text
同一 family 的 V3-M2 Round 0 PASS
  → 才可签发该 family 的 30 格 pilot permit
  → 30 格 pilot 可分辨性复核 PASS
  → 才可另行冻结并签发 10^2–10^3 正式扫描 manifest
  → GPU preflight PASS
  → 才可启动正式长跑
```

`READY-V3-M1-ANCHOR-CERTIFICATION` 不等于上述任一 PASS；换 family 会把链重置到该
family 的 Round 0，任何旧 family/pilot/证书不得跨 family 复用。

---

## 共同执行规则

1. 使用隔离分支 `v3m0-instrument` 和工作树 `.worktrees/v3m0-instrument`。
2. Python 命令每次都使用绝对入口
   `/Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python`；不得假设前一条
   shell 的变量会跨 exec 保留。

3. Lean 命令显式使用：

   ```bash
   PATH=/Users/prismer/.elan/bin:$PATH
   ```

4. 每个 Python 任务严格执行 RED → GREEN → 定向回归；先看到因缺失行为而失败的测试，
   再写最小实现。
5. 每个 Lean theorem 先在聚合入口增加 import/声明并确认 build 失败，再补无
   `sorry/admit` 的证明。
6. 每段完成后运行 `git diff --check`，只暂存该段文件并提交。
7. JSON verdict 只由原始量重算，验证器不得信任摘要布尔值。

## Task 1：Lean 工程与可复现工具链

**文件：**

- Modify: `.gitignore`
- Create: `formal/v3m0/lean-toolchain`
- Create: `formal/v3m0/lakefile.toml`
- Create: `formal/v3m0/V3M0.lean`
- Create: `formal/v3m0/V3M0/Common.lean`
- Create: `formal/v3m0/lake-manifest.json`

**冻结接口：**

```lean
namespace V3M0

def sq (x : ℝ) : ℝ := x * x

end V3M0
```

- [ ] 在 `V3M0.lean` 先写 `import V3M0.Common`，但暂不创建模块。
- [ ] 运行：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ```

  预期 RED：`unknown module prefix 'V3M0.Common'`。
- [ ] 创建 `Common.lean`；固定 `leanprover/lean4:v4.32.1`，`lakefile.toml` 的 mathlib
  rev 固定 `v4.32.1`，`.gitignore` 定向忽略 `formal/v3m0/.lake/`。
- [ ] 分别运行以下命令；提交生成的
  `lake-manifest.json`。

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake update
  PATH=/Users/prismer/.elan/bin:$PATH lake exe cache get
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  PATH=/Users/prismer/.elan/bin:$PATH lean --version
  ```
- [ ] 断言 manifest 中 mathlib rev 是
  `520045ab14e26149ee970e2e617ca04b09bde5d6`，Lean commit 是
  `f054605aea4b840552cca2e725580bffd1e1b704`。
- [ ] 提交：`build(v3m0): pin Lean formal toolchain`

## Task 2：旧 `ε` 互补与 extensional provenance no-go

**文件：**

- Create: `formal/v3m0/V3M0/EpsilonComplement.lean`
- Create: `formal/v3m0/V3M0/ExtensionalNoGo.lean`
- Modify: `formal/v3m0/V3M0.lean`

**冻结定理轮廓：**

```lean
theorem epsilon_complement
    (N j : ℚ) (hN : N ≠ 0) :
    (1 - j / N) + j / N = 1 := by
  field_simp

theorem epsilon_complement_real
    (N j : ℝ) (hN : N ≠ 0) :
    (1 - j / N) + j / N = 1 := by
  field_simp

structure History where
  operator : Operator
  provenance : Provenance

theorem extensional_observer_equal
    (h₁ h₂ : History)
    (hop : h₁.operator = h₂.operator)
    (obs : Operator → α) :
    obs h₁.operator = obs h₂.operator := by
  simpa [hop]

theorem extensional_observer_no_go
    (h₁ h₂ : History)
    (hop : compile h₁ = compile h₂)
    (hlabel : label h₁ ≠ label h₂)
    (obs : Operator → Provenance)
    (recovers₁ : obs (compile h₁) = label h₁)
    (recovers₂ : obs (compile h₂) = label h₂) :
    False := by
  apply hlabel
  calc
    label h₁ = obs (compile h₁) := recovers₁.symm
    _ = obs (compile h₂) := congrArg obs hop
    _ = label h₂ := recovers₂
```

- [ ] 先加入两个 import，运行以下命令，确认 RED 为模块缺失：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ```
- [ ] 在 `ℚ` 与 `ℝ` 分别实现互补定理，并同时保留 `0≤j≤N` 的语义前提版本。
- [ ] 定义 `History`、`compile`、provenance label；先证等 operator 的 extensional
  observer 输出相同，再证若它声称同时恢复两个不同 label 则推出 `False`，确保
  `hlabel` 真正参与证明。
- [ ] 运行：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ! rg -n '\\b(sorry|admit)\\b' V3M0 V3M0.lean
  ```

- [ ] 提交：`formal(v3m0): prove legacy epsilon and provenance no-go`

## Task 3：局域支撑与辛性定理

**文件：**

- Create: `formal/v3m0/V3M0/LocalSymplectic.lean`
- Modify: `formal/v3m0/V3M0.lean`

**冻结定理：**

- `Z³` Chebyshev 半径模型中，有限支撑卷积半径不超过 `r+s`；
- 周期格 exact bridge 的前提显式为 `L>2(r+s)`；
- 对实对称 `V`，kick、drift 和 kick-drift-kick Verlet 满足 `MᵀJM=J`；
- complex Hermitian symbol 先 realify 为实对称矩阵；
- half-step `τ` 是独立标量，不假设 `2` 可逆。

- [ ] 先加入 import，运行以下命令并确认 RED 为缺失模块：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ```
- [ ] 先证明 support radius，再证明 block-matrix 辛恒等。
- [ ] GREEN：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake env lean V3M0/LocalSymplectic.lean
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ! rg -n '\\b(sorry|admit)\\b' V3M0 V3M0.lean
  ```
- [ ] 提交：`formal(v3m0): prove locality and symplectic primitives`

## Task 4：高风险子空间存活定理

**文件：**

- Create: `formal/v3m0/V3M0/SubspaceSurvival.lean`
- Modify: `formal/v3m0/V3M0.lean`

**必须早停的定理组：**

```text
K_surv = U₁† U₀ U₀† U₁ = (U₀†U₁)†(U₀†U₁)
0 ≼ K_surv ≼ I
spec(K_surv) ⊂ [0,1]
```

以及零/full anchor、正交直和 DOF 加权、非零复标量/相位、共同左酉换基下的谱不变。
还必须证明

```text
Re tr(K_surv)/rank(P₁)
  = 补零后的 squared singular values 的算术均值
```

其中 rank/维数非零与奇异值列表长度均显式建模。不在本切片形式化 `arccos`。

- [ ] 先加入 import，运行以下命令，确认 RED 为缺失模块：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ```
- [ ] 将矩阵维数、`U†U=I`、projector 和 rank 非零作为显式前提，禁止把数值 SVD
  事实偷渡成定理。
- [ ] GREEN：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake env lean V3M0/SubspaceSurvival.lean
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ! rg -n '\\b(sorry|admit)\\b' V3M0 V3M0.lean
  ```
- [ ] 若 PSD/上界主定理无法在冻结定义下成立，写
  `HALT-V3M0-FORMAL`，不得先做 Python 主坐标。
- [ ] 提交：`formal(v3m0): bound causal survival spectrum`

## Task 5：observer-collapse 与 gauge quotient

**文件：**

- Create: `formal/v3m0/V3M0/ObserverCollapse.lean`
- Modify: `formal/v3m0/V3M0.lean`

**冻结定理：**

- `B_h` 满射蕴含 `range(inc ∘ B_h)=range(inc)`；
- `A=inc∘B_h` 的非零 Gram 支撑为 `range(A†A)=range(A†)`，经 `B_h` 映回一般为
  `range(B_hB_h†inc†)`；不得由满射直接跳到 `range(inc†)`；
- `S_curv=range(inc†)` 只在完整非零谱选择、冻结白化 metric 下
  `B_hB_h†=I_h`（或认证其保持 `range(inc†)`）、`rank inc=6`、
  `Gauge≤ker inc`、`dim Gauge=4`、`TT₂⊕Gauge₄⊕Row₄` 两两正交完备分解和
  `ker C=TT⊕Gauge` 等前提下成立；条件边界以
  `v3-勘误-observer-collapse-coisometry-2026-07-30.md` 为准；
- 在上述前提和冻结 projector 定义下，证明 principal spectrum 的条件性固定形状：
  `sin²θ` multiset 为 `0` 两重、`1` 四重（等价 `sinθ=(0,0,1,1,1,1)`）；若当前
  mathlib 表示只便于平方谱，正文和桥接中明确不经 `sqrt` 外推；
- 若 `Π_phys g=0`，则 `Π_phys(t+a g)=Π_phys(t)`。

- [ ] 先加入 import，运行以下命令，确认 RED 为缺失模块：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ```
- [ ] 实现最小 range 与 quotient 定理，不把 Floquet 稳定或数值秩写入 Lean。
- [ ] GREEN：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake env lean V3M0/ObserverCollapse.lean
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ! rg -n '\\b(sorry|admit)\\b' V3M0 V3M0.lean
  ```
- [ ] 提交：`formal(v3m0): prove observer collapse and gauge invariance`

## Task 6：Python schema、证据与 undefined 传播

**文件：**

- Create: `rulespace_v3/__init__.py`
- Create: `rulespace_v3/contracts.py`
- Create: `rulespace_v3/evidence.py`
- Create: `tests/test_v3m0_contracts.py`

**冻结接口：**

```python
class UndefinedReason(str, Enum):
    TRACE_UNCLASSIFIED = "trace_unclassified"
    ABLATION_NOT_REVERSIBLE = "ablation_not_reversible"
    STATE_SCHEMA_MISMATCH = "state_schema_mismatch"
    RESPONSE_NULL = "response_null"
    RESPONSE_GREY = "response_grey"
    RANK_GAP = "rank_gap"
    ENDPOINT_SHELL_AMBIGUOUS = "endpoint_shell_ambiguous"
    PAIRED_RESPONSE_FAILED = "paired_response_failed"
    RESPONSE_BRIDGE_FAILED = "response_bridge_failed"
    PRESTRUCTURE_INVALID = "prestructure_invalid"
    REALITY_VIOLATION = "reality_violation"
    LAURENT_RESOURCE_EXCEEDED = "laurent_resource_exceeded"
    DYNAMICS_BRIDGE_FAILED = "dynamics_bridge_failed"
    WINDOW_UNRESOLVED = "window_unresolved"
    STRUCTURE_UNRESOLVED = "structure_unresolved"
    STABILITY_UNRESOLVED = "stability_unresolved"
    UNSTABLE = "unstable"
    MANIFEST_MISMATCH = "manifest_mismatch"

@dataclass(frozen=True)
class BlockStatus:
    defined: bool
    reason: UndefinedReason | None

@dataclass(frozen=True)
class RequiredBlockReport:
    status: BlockStatus
    undefined: tuple[tuple[str, UndefinedReason], ...]

def evaluate_required_blocks(
    blocks: Mapping[str, BlockStatus],
    required_ids: Sequence[str],
) -> RequiredBlockReport: ...

def canonical_sha(payload: Mapping[str, object]) -> str: ...

def validate_evidence_fields(
    payload: Mapping[str, object],
    *,
    required_fields: Sequence[str],
) -> None: ...
```

- [ ] 测试先覆盖：NaN/Inf 拒绝、漏必填 ID 拒绝、字典顺序不改 SHA、required block
  undefined 向总状态传播。
- [ ] canonical JSON 只接受字符串键 mapping、list/tuple 与 JSON scalar；显式拒绝
  任意其他 `Sequence` 和循环容器。最终 response/control profile 使用任务书 §8 的
  16 字段；中间 artifact 只验证各自冻结 profile，禁止伪造尚不存在的 ID。
- [ ] `toolchain_manifest` 验证为非空 canonical-JSON mapping；required block 按传入的
  预注册顺序保留全部 `(block_id, reason)`，缺 ID/重复 ID 是 schema error，optional
  undefined 不传播。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_contracts -v
  ```

  预期为 `ModuleNotFoundError: rulespace_v3`。
- [ ] 写最小实现；所有 dataclass 冻结，canonical JSON 使用排序键和有限数检查。
- [ ] 运行：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_contracts -v
  ```

- [ ] 提交：`feat(v3m0): add immutable evidence contracts`

## Task 7：SymPy exact verifier 与 Phase 0 no-go JSON

**文件：**

- Create: `rulespace_v3/exact.py`
- Create: `experiments/v3m0_formal_nogo.py`
- Create: `tests/test_v3m0_exact.py`
- Generate: `data/results/v3m0_formal_nogo.json`

**冻结接口：**

```python
def verify_exact_certificate(cert: Mapping[str, object]) -> None: ...
def build_phase0_certificate(parent_v2_path: Path) -> dict[str, object]: ...
```

- [ ] 先测篡改 variable order、pivot/minor、support、residual 或 SHA 必须失败；语义篡改
  测试须在篡改后重签 certificate SHA，证明失败来自独立重算而不只是外层 digest。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_exact -v
  ```

  预期为缺失 `rulespace_v3.exact` 或缺失 verifier 行为。
- [ ] 实现 Laurent support、exact adjoint/rank/minor、symplectic residual，并逐项重算：

  ```text
  Π_phys · Gauge = 0
  quotient representative equivalence
  dim(T_phys)
  ν_inc(k) exact analytic scale
  first-BZ `0<ρ≤π` punctured domain
  frozen direction list
  ```

  `ν_inc(ρd)=4Σᵢsin²(ρdᵢ/2)` 本身是二阶量；证书重算
  `lim_{ρ→0}ν_inc(ρd)/ρ²=||d||²`，只声明第一 Brillouin 基本域
  `0<ρ≤π` 的逐点局部化，不声称 Laurent 环逐项整除。禁止把字段命名为
  `nu_reference_squared` 或将其误写成一阶尺度。
- [ ] physical-`h` packing 固定为
  `(h00,h01,h02,h03,h11,h12,h13,h22,h23,h33)`；curvature row 固定为
  `R[m,a,n,b]` C-order flatten；Gauge 使用 R15 gauge block 后的 R32 trace reversal，
  TT 使用冻结 R32 basis。以 Gram projector 重算 `Π_phys·Gauge=0`、
  quotient representative、`dim(T_phys)=2`、`rank(inc)=6` 和条件性
  `sin²={0,0,1,1,1,1}`。两个 projector 固定为标准 Hermitian/Euclidean metric 下的
  `P_B` 与 `P_K`，其中 `B=range(inc†)`、`K=[Gauge,TT]`。
- [ ] 冻结 Phase 0 intermediate evidence profile：parent/manifest/evaluator/local-family
  source/local-family certificate SHA，30 格与三个 direction manifest，90 条 reference
  record，exact geometry，static bridge，formal source/toolchain digests，gate、claim ceiling
  与 certificate SHA。不得填 response/unary 占位 ID。
- [ ] 父 JSON 没有 raw kernel；证书固定
  `evidence_kind=source_sha_static_symbol_reconstruction`、
  `raw_kernel_available=false`、`raw_kernel_recomputed=false`、
  `production_symbol_equivalence=round0_plane_wave_spotcheck`，主张上限为
  `conditional_full_positive_observer_collapse`。只允许：
  1) 精确重算 frozen geometry；
  2) 从父 JSON 重算已保存谱的 `N_curv/j/ε/no-go`；
  3) 用锁定 `symbol_of_potential` 独立 static 重构 90 个参考点，并逐点记录
  `B_h=orth(raw_h)` 的 `||B_hB_h†-I₁₀||₂≤10⁻¹²` whitened-coisometry 门；
  4) 引用带 SHA 的既有两段 plane-wave spotcheck lineage。
  禁止声称验证了 30 个 kernel digest 对应的 raw 内容。
- [ ] 在首次调用 `symbol_of_potential/FROZEN.mod` 前，以 Python AST 从
  `LOCAL/frozen/invariants/epsilon/family/r15/r32/r25_aux` 根机械发现保守本地 import
  闭包并识别 literal `importlib.import_module`；本签发版本闭包必须恰为 40 文件。每项保存
  authority/expected/actual/match，
  authority 只能来自 frozen registry、父 protocol code/record-only、既有 source 证书或
  本次明确签发的 closure freeze。闭包逐项全匹配后才允许 static 执行；全部 SHA 同时进入
  cache identity、lineage、closure SHA 与独立 gate，闭包新增/遗漏或任一漂移均 HALT。
- [ ] 另以 fresh subprocess 执行
  `import runner → SHA preflight → first static reference`，审计 `sys.modules` 的本地源码。
  当前实际加载 41 文件，必须全部属于 40 文件冻结闭包或已在 v3 live-source lineage 签名的
  runner/exact；`unexpected=[]`。诚实边界为：preflight 位于 Python package bootstrap/
  runner top-level import **之后**、首个 LOCAL/FROZEN static 求值**之前**，不得声称它在
  Python import 执行前。
- [ ] 保存严格环境归因 manifest：Python/NumPy、NumPy build/config SHA、BLAS、LAPACK、
  threadpool、platform/machine/system/release；另运行并保存 `lake env lean --version`
  实际输出与版本匹配门。环境字段明确
  `attribution_only=true,cross_platform_bit_identical_required=false`，不进入科学 gate；
  它用于解释跨环境 fp64 字节差异，不能冒充新通过条件。
- [ ] `build_phase0_certificate` 自行运行增量 `lake build` 并记录所需 theorem
  source/toolchain digests；verifier 不信任 `formal.all_passed` 摘要。历史 runner 的
  original/finalizer SHA 分列记录，不要求当前 runner SHA 冒充旧 protocol SHA。
- [ ] `v3m0_formal_nogo.py --generate` 只读复核 v2 pilot 的 30×3 摘要、执行上述
  static/exact/formal bridge，并以临时文件 + fsync + atomic replace 只写
  `data/results/v3m0_formal_nogo.json`；`--check` 不创建或修改任何科学数据、版本化文件
  或 runtime artifact，只重算并与已生成 v3 JSON 的 canonical object/bytes 比较。
  两种模式运行 `lake build` 时都允许更新未版本化的 `formal/v3m0/.lake/build` 编译缓存；
  该缓存不进入证书，测试对其单独豁免。JSON 不保存运行时生成时间，拒绝 NaN/Inf，
  保证确定性；两种模式都不得写回 v2。
- [ ] 在运行前后比较 `data/results/v2m3_pilot.json` SHA 完全相同。
- [ ] 生成独立原子 JSON；只有 Lean、exact、manifest/lineage、saved-scalar reduction、
  90 点 whitened-coisometry 与 static bridge 全绿才写 `READY-V3M0-CONTROLS`。该状态仍须保存
  `raw_kernel_independent_recomputation=false`，且不解锁物理锚。
- [ ] GREEN：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_exact -v
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    experiments/v3m0_formal_nogo.py --generate
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    experiments/v3m0_formal_nogo.py --check
  ```
- [ ] 提交：`feat(v3m0): certify formal and exact no-go bridge`

## Task 8：ConstructionTrace 与 pre-lowering taint

**文件：**

- Create: `rulespace_v3/trace.py`
- Create: `tests/test_v3m0_trace.py`

**冻结接口：**

```python
class MechanismKind(str, Enum):
    TARGET_BLIND = "target_blind"
    TARGET_CONDITIONED = "target_conditioned"
    UNCLASSIFIED = "unclassified"

class ProvenanceOperation(str, Enum):
    GRAMMAR_PRIMITIVE = "grammar_primitive"
    GRAMMAR_CONSTANT = "grammar_constant"
    TARGET_SPEC_READ = "target_spec_read"
    TARGET_EQUATION_READ = "target_equation_read"
    TARGET_PROJECTOR_READ = "target_projector_read"
    TARGET_AWARE_OBJECTIVE = "target_aware_objective"
    DERIVE = "derive"
    CACHE = "cache"
    COPY = "copy"
    RENAME = "rename"
    SEARCH = "search"
    SELECTION = "selection"
    MANUAL_SELECTION = "manual_selection"
    OPAQUE_LITERAL = "opaque_literal"
    UNCLASSIFIED = "unclassified"

@dataclass(frozen=True)
class ProvenanceNode:
    provenance_id: str
    operation: ProvenanceOperation
    depends_on: tuple[str, ...]
    target_refs: tuple[str, ...]
    objective_tags: tuple[str, ...]
    search_run_id: Optional[str]
    source_sha: str

@dataclass(frozen=True)
class CoefficientRecord:
    mechanism_id: str
    expression_schema: str
    variable_order: tuple[str, ...]
    expression_ast: tuple[object, ...]
    provenance_root_id: str
    coefficient_digest: str

@dataclass(frozen=True)
class PrimitiveTrace:
    grammar_id: str
    target_spec_id: str
    mechanism_id: str
    production_id: str
    kind: MechanismKind
    depends_on: tuple[str, ...]
    support_offsets: tuple[tuple[int, ...], ...]
    state_channels: tuple[str, ...]
    coefficient_digest: str
    symbolic_origin_tags: tuple[str, ...]
    neutral_ablation: Optional[str]
    design_objective_tags: tuple[str, ...]
    search_run_id: Optional[str]
    source_sha: str
    design_provenance: str

@dataclass(frozen=True)
class ConstructionTrace:
    schema_version: str
    grammar_id: str
    target_spec_id: str
    provenance_nodes: tuple[ProvenanceNode, ...]
    coefficient_records: tuple[CoefficientRecord, ...]
    primitives: tuple[PrimitiveTrace, ...]
    trace_sha: str

def build_construction_trace(...) -> ConstructionTrace: ...
def verify_construction_trace(...) -> ConstructionTrace: ...
def construction_trace_payload(...) -> dict[str, object]: ...
def evaluate_closed_coefficient(record: CoefficientRecord) -> complex: ...
```

- [ ] 冻结 grammar ID 为 `v3m0.local-linear-primitive.v1`。blind productions 仅为
  `integer_translation/lattice_symmetry_mix/local_canonical_shear/frozen_matter_walk/
  universal_mass_term/universal_dispersion_term`；target productions 仅为
  `target_operator/target_projector/target_basis/placed_target_coefficient/
  target_repair_operator`；未知 production 为 unclassified。taint join 固定
  `target_conditioned > unclassified > target_blind`。
- [ ] 先测 primitive/provenance ID 唯一、两层 DAG 无环且无悬空依赖、taint 传递、
  opaque literal 为 unclassified、runner API 根本不能覆盖 kind。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_trace -v
  ```

  预期为模块缺失或 schema/taint 断言失败。
- [ ] `design_provenance` 必须解析为 typed provenance DAG 根 ID。增加 target-aware
  objective/search/selection/人工择优 taint；`derive/cache/copy/rename` 只能继承祖先；
  target-derived literal 不洗白，standalone opaque literal 为 unclassified；缺少可追踪
  objective/search 祖先的 search/selection/manual selection 不得成为 blind。SEARCH 必须
  继承真实 objective/search 祖先，dependencyless SEARCH 不得自授权；SELECTION/
  MANUAL_SELECTION 的 `search_run_id` 必须来自实际 SEARCH 祖先，不能自填新 run ID。
- [ ] `CoefficientRecord` 由 exact SymPy expression 生成受限 canonical AST，只允许
  `Integer/Rational/Symbol/I/Add/Mul/Pow` exact 子集；拒绝 Float、NaN/Inf 与未知函数，
  verifier 不对外部字符串调用 `sympify`。variable order、AST、provenance root 与
  digest 任一被篡改，即使重签 trace SHA 也必须失败。
- [ ] coefficient Symbol 只允许标准 commutative、无 assumptions 的 `sp.Symbol(name)`；
  expression 的 symbol 集必须与 `variable_order` 精确相等。非交换 symbol、任何 assumptions
  或同名异义 symbol 一律拒绝，不得在 canonicalization 中静默合并。
- [ ] `evaluate_closed_coefficient` 只接受已验证且 `variable_order=()` 的 closed AST，
  递归得到 finite complex 值；有自由 symbol、非法 AST 或 nonfinite 结果一律拒绝。
  Task 9 不得接收 caller 另报的 numeric coefficient。
- [ ] 测 canonical trace SHA；完整 body 包含 provenance、coefficient 与 primitive DAG。
  set-like 字段排序，primitive 顺序保留；split/merge、恒等插入和可交换层重排只允许响应
  不变量，不要求 trace SHA 相同。
- [ ] adversarial Mapping 以唯一一次递归 `items()` 快照为权威，显式拒绝 items 流重复键、
  循环、非字符串键与非 JSON 值；快照后不得再调用 `__getitem__` 或二次遍历，因此
  items/getitem 分裂及后续变值不能影响验证结果。同一容器在多处分支 alias 时只读取一次，
  memo 必须强持有原对象并以 identity 对照，复用第一次 immutable snapshot；Mapping 须先
  完整物化一次 `items()` 流、sequence 先物化 tuple，再递归 child，防 child 改写父尾部；
  cycle 仍拒绝。篡改 computed kind 后重签 SHA 仍由独立 taint 重算拒绝。
- [ ] 冻结资源界：snapshot depth `128`、nodes `100_000`；DAG nodes `4_096`、edges
  `16_384`、depth `4_096`；coefficient AST depth `64`、nodes `4_096`、integer digits
  `256`、Pow exact exponent numerator/denominator magnitude `≤256`，并在 eager numeric
  Pow 前检查结果位数上界。DAG 分析使用有界迭代拓扑序，不递归。越界统一抛
  `ValueError`，不得泄漏 `RecursionError` 或先构造巨整数。builder 返回前必须走同一完整
  verifier/resource gate，且 builder/verifier 均拒绝三集合任一为空的 trace。
- [ ] Task 8 不导入 v2/GPU/legacy experiments，不读写文件、不运行 subprocess/factory，
  不生成 JSON、不推进状态。coefficient sidecar 只证明内部一致性；历史源码等价留给 Task 16。
- [ ] GREEN：重复上面的定向测试，预期全部 `ok`。
- [ ] 提交：`feat(v3m0): add mechanical construction traces`

## Task 9：Matched ablation 与合成 primitive factory

**文件：**

- Create: `rulespace_v3/factory.py`
- Create: `rulespace_v3/ablation.py`
- Create: `rulespace_v3/controls.py`
- Create: `tests/test_v3m0_ablation.py`

**冻结合同：**

```python
ComplexWire = tuple[float, float]

@dataclass(frozen=True)
class BasisManifest:
    basis_schema_version: str
    role: Literal["source", "holdout_source", "readout"]
    state_schema_id: str
    channel_order: tuple[str, ...]
    vectors_wire: tuple[tuple[ComplexWire, ...], ...]
    manifest_id: str

@dataclass(frozen=True)
class FrozenComplexTensor:
    tensor_schema_version: str
    shape: tuple[int, ...]
    values_wire: tuple[ComplexWire, ...]
    tensor_sha: str

@dataclass(frozen=True)
class PrimitiveInterface:
    interface_id: str
    state_schema_id: str
    spatial_ndim: int
    channel_order: tuple[str, ...]
    dtype: Literal["complex128"]
    backend: Literal["numpy"]

@dataclass(frozen=True)
class Primitive:
    primitive_schema_version: str
    mechanism_id: str
    production_id: str
    layer_slot_id: str
    operation_id: Literal["local_canonical_shear", "neutral_identity"]
    interface_id: str
    source_channel: str
    destination_channel: str
    offset: tuple[int, ...]
    support_offsets: tuple[tuple[int, ...], ...]
    coefficient_wire: ComplexWire
    coefficient_digest: str
    neutral_identity_id: Optional[str]

@dataclass(frozen=True)
class LinearRealspaceFactory:
    factory_schema_version: str
    factory_role: Literal["actual", "matched_ablated"]
    factory_id: str
    construction_trace_sha: str
    grammar_id: str
    target_spec_id: str
    target_spec_sha: str
    runtime_operator_sha: str
    interface: PrimitiveInterface
    state_schema_id: str
    spatial_ndim: int
    channel_order: tuple[str, ...]
    state_shape: tuple[int, ...]
    dtype: str
    backend: str
    dt: float
    target_blind_parameters: tuple[tuple[str, float], ...]
    layer_slot_ids: tuple[str, ...]
    source_manifest_id: str
    readout_basis: BasisManifest
    boundary_manifest_id: str
    run_length: int
    primitives: tuple[Primitive, ...]
    factory_sha: str

@dataclass(frozen=True)
class PrimitiveOperatorWire:
    mechanism_id: str
    production_id: str
    layer_slot_id: str
    operation_id: Literal["local_canonical_shear"]
    interface_id: str
    source_channel: str
    destination_channel: str
    offset: tuple[int, ...]
    coefficient_wire: ComplexWire

@dataclass(frozen=True)
class CalibrationSeed:
    calibration_schema_version: str
    calibration_protocol_id: str
    interface: PrimitiveInterface
    state_shape: tuple[int, ...]
    dt: float
    target_blind_parameters: tuple[tuple[str, float], ...]
    source_basis: BasisManifest
    holdout_source_basis: BasisManifest
    readout_basis: BasisManifest
    boundary_manifest_id: Literal["periodic-v1"]
    runtime_operator_payload: tuple[PrimitiveOperatorWire, ...]
    runtime_operator_sha: str
    seed_sha: str

@dataclass(frozen=True)
class CalibrationObservation:
    observation_schema_version: str
    seed_sha: str
    runtime_operator_sha: str
    holdout_source_manifest_id: str
    readout_manifest_id: str
    response_tensor: FrozenComplexTensor
    observation_sha: str

@dataclass(frozen=True)
class FrozenSyntheticTarget:
    target_schema_version: str
    target_spec_id: str
    target_spec_sha: str
    calibration_protocol_id: str
    seed_sha: str
    observation: CalibrationObservation
    target_response: FrozenComplexTensor
    runtime_operator_sha: str

class VerifiedFactory:
    """Opaque module-issued wrapper; public construction is forbidden."""

    @property
    def factory(self) -> LinearRealspaceFactory: ...

    @property
    def role(self) -> Literal["actual", "matched_ablated"]: ...

@dataclass(frozen=True)
class AblationReplacement:
    layer_slot_id: str
    mechanism_id: str
    actual_primitive_sha: str
    neutral_identity_id: str
    ablated_primitive_sha: str

@dataclass(frozen=True)
class AblationManifest:
    manifest_schema_version: str
    construction_trace_sha: str
    actual_factory_sha: str
    ablated_factory_sha: str
    replacements: tuple[AblationReplacement, ...]
    manifest_sha: str

@dataclass(frozen=True)
class AblationPair:
    actual: VerifiedFactory
    ablated: VerifiedFactory
    manifest: AblationManifest

@dataclass(frozen=True)
class AblationDynamicsReport:
    report_schema_version: str
    factory_sha: str
    state_schema_matches: bool
    reversible: bool
    stable: bool
    certificate_sha: str
    report_sha: str

class VerifiedDynamicsReport:
    """Task 9 冻结的 fail-closed opaque type；Task 9 无 success issuer。"""

    @property
    def report(self) -> AblationDynamicsReport: ...

    @property
    def certificate_sha(self) -> str: ...

@dataclass(frozen=True)
class AblationConstructionOutcome:
    status: BlockStatus
    pair: Optional[AblationPair]

class QualifiedAblationOutcome:
    """Opaque、tamper-evident qualification；public construction forbidden。"""

    @property
    def status(self) -> BlockStatus: ...

    @property
    def pair(self) -> Optional[AblationPair]: ...

    @property
    def dynamics_report(self) -> Optional[VerifiedDynamicsReport]: ...

    @property
    def qualification_sha(self) -> str: ...

    @property
    def certificate_sha(self) -> Optional[str]: ...

@dataclass(frozen=True)
class SyntheticControlBundle:
    control_schema_version: str
    control_id: Literal["full", "zero", "direct_sum"]
    mode_count: int
    target: FrozenSyntheticTarget
    construction_trace: ConstructionTrace
    factory: VerifiedFactory
    source_basis: BasisManifest
    expected_actual_rank: int
    expected_ablated_rank: int

def basis_manifest_payload(basis) -> dict[str, object]: ...
def verify_basis_manifest(basis) -> BasisManifest: ...
def build_basis_manifest(
    *, role, state_schema_id, channel_order, vectors
) -> BasisManifest: ...
def basis_manifest_array(basis) -> np.ndarray: ...
def frozen_tensor_payload(tensor) -> dict[str, object]: ...
def verify_frozen_tensor(tensor) -> FrozenComplexTensor: ...
def freeze_complex_tensor(values) -> FrozenComplexTensor: ...
def frozen_tensor_array(tensor) -> np.ndarray: ...
def calibration_seed_payload(seed) -> dict[str, object]: ...
def calibration_observation_payload(observation) -> dict[str, object]: ...
def synthetic_target_payload(target) -> dict[str, object]: ...
def verify_synthetic_target(target) -> FrozenSyntheticTarget: ...
def primitive_operator_payload(operators) -> tuple[dict[str, object], ...]: ...
def runtime_operator_payload(primitives) -> tuple[dict[str, object], ...]: ...
def runtime_operator_sha(primitives) -> str: ...
def primitive_payload(primitive) -> dict[str, object]: ...
def primitive_sha(primitive) -> str: ...
def factory_payload(factory) -> dict[str, object]: ...
def factory_sha(factory) -> str: ...
def build_calibration_seed(
    *,
    calibration_protocol_id,
    interface,
    state_shape,
    dt,
    target_blind_parameters,
    source_basis,
    holdout_source_basis,
    readout_basis,
    boundary_manifest_id,
    operator_payload,
) -> CalibrationSeed: ...
def measure_calibration_holdout(seed) -> CalibrationObservation: ...
def freeze_synthetic_target(
    seed, observation, target_spec_id
) -> FrozenSyntheticTarget: ...
def build_factory_from_trace(
    trace,
    target,
    *,
    factory_id,
    interface,
    state_shape,
    dt,
    target_blind_parameters,
    layer_slot_ids,
    operator_payload,
    source_manifest_id,
    readout_basis,
    boundary_manifest_id,
) -> VerifiedFactory: ...
def build_full_factory_from_seed(
    trace,
    target,
    seed,
    *,
    factory_id,
    layer_slot_ids,
) -> VerifiedFactory: ...
def verify_factory(
    factory: LinearRealspaceFactory,
    trace: ConstructionTrace,
    target: FrozenSyntheticTarget,
) -> VerifiedFactory: ...
def _verify_matched_ablated_factory(
    factory, trace, target
) -> VerifiedFactory: ...
def _apply_primitive(
    primitive, interface, state
) -> np.ndarray: ...
def apply_factory_step(
    factory: VerifiedFactory, state
) -> np.ndarray: ...
def factory_support_offsets(
    factory: VerifiedFactory, macro_steps
) -> tuple[tuple[int, ...], ...]: ...
def matched_ablation(
    factory: VerifiedFactory
) -> AblationConstructionOutcome: ...
def ablation_manifest_payload(manifest) -> dict[str, object]: ...
def dynamics_report_payload(report) -> dict[str, object]: ...
def build_dynamics_report(
    *,
    factory_sha_value,
    state_schema_matches,
    reversible,
    stable,
    certificate_sha,
) -> AblationDynamicsReport: ...
def verify_verified_dynamics_report(
    verified: VerifiedDynamicsReport,
) -> VerifiedDynamicsReport: ...
def verify_ablation_pair(pair) -> AblationPair: ...
def qualify_ablation(
    outcome, dynamics_report: Optional[VerifiedDynamicsReport]
) -> QualifiedAblationOutcome: ...
def verify_qualified_ablation(
    outcome: QualifiedAblationOutcome,
) -> QualifiedAblationOutcome: ...

def build_full_control(
    seed, observation, target
) -> SyntheticControlBundle: ...
def build_zero_control(
    mode_count, target, spatial_shape, dt
) -> SyntheticControlBundle: ...
def build_direct_sum_control(
    mode_count, target, spatial_shape, dt
) -> SyntheticControlBundle: ...
```

- [ ] `BasisManifest` 与 `FrozenComplexTensor` 是跨 Task 9/10 的唯一复数证据 wire。
  `values_wire` 为 row-major flat `(re,im)`，`prod(shape)==len(values_wire)`；
  所有 fp64 finite。`manifest_id/tensor_sha` 分别为排除自身字段后的 strict canonical
  payload SHA。转成 ndarray 时每次返回 fresh `complex128`，不得暴露 owning evidence。
  commit `39d1c14` 的 public helper 集逐名冻结为
  `basis_manifest_payload/verify_basis_manifest/build_basis_manifest/basis_manifest_array`
  与
  `frozen_tensor_payload/verify_frozen_tensor/freeze_complex_tensor/frozen_tensor_array`；
  Task 10 不得删除、改名或缩窄这些入口。
- [ ] 所有 basis 的 `channel_order` 非空且唯一，wire shape 统一为
  `(n_vector,n_channel)` 且 `n_vector>0`。source/holdout source 的行向量 `v_a` 形成
  injection `J[c,a]=v_a[c]`（不共轭）；readout 行向量 `w_a` 形成
  `P[a,c]=conj(w_a[c])`。禁止 role-dependent 的其他隐式转置或共轭。
- [ ] state layout 固定 `(channel,*spatial)`，dtype/backend 只允许 NumPy `complex128`，
  boundary registry 在 V3-M0 仅有 `periodic-v1`。factory 持有完整 interface，重复的
  schema/spatial/channel/dtype/backend 字段必须逐位相等；`state_shape` 必须精确等于
  `(len(channel_order),*spatial_shape)`，每个空间长度为正整数。
- [ ] runtime 只接受 module-issued `VerifiedFactory`。其构造器要求模块私有 token，
  wrapper 内保存 factory、已验证 trace 与 frozen target，并由模块侧签发注册表保存不可由
  wrapper 属性改写的完整 seal；每次进入 executor 前重算 `factory_sha`、trace/target/role
  fingerprint 并与注册表核验。必须测试 `object.__setattr__` 改写 private slot 后 fail-closed，
  防止 role 替换和 qualification 后 TOCTOU。公开 `LinearRealspaceFactory` 只能作为待验证 payload，
  不能直接传给 `apply_factory_step/measure_transition`。单 primitive kernel 必须是模块私有
  `_apply_primitive`，不得留下接受任意 public `Primitive` 的执行入口。
- [ ] issuance role 是 factory strict payload 与 wrapper 的一部分。公开
  `build_factory_from_trace/verify_factory` 只接受全为 `local_canonical_shear` 的 payload，
  且只签发 `role="actual"`；caller-supplied neutral primitive 一律拒绝。只有
  `matched_ablation` 内部可调用 `_verify_matched_ablated_factory` 签发
  `role="matched_ablated"`，而 `matched_ablation` 本身只接受 actual role。
- [ ] synthetic primitive 只允许从 Task 8 `variable_order=()` 的 closed AST 经
  `evaluate_closed_coefficient` lower。对 `local_canonical_shear`，存储的 coefficient
  wire 必须逐位等于该 lowering，
  改 numeric 而沿用 digest 必须失败；actual coefficient 的 real/imag 不得同时为任一符号位
  的 fp64 zero，唯一 no-op 只能是闭 registry 的 neutral encoding。每个 primitive 的
  mechanism/production/support/
  channel/digest 与 trace 逐项绑定，factory 另绑定 trace SHA、grammar、target ID/SHA 与
  runtime operator SHA。`build_factory_from_trace` 按 primitive 顺序匹配 operator wire，
  但 coefficient 重新由 trace 计算并只把逐位相等作为接受条件。
- [ ] `runtime_operator_sha` 必须由 factory primitives 投影回同一有序、可表达
  actual/neutral 两种 opcode 的 `runtime_operator_payload` 后重算，不能信任 caller
  字符串；public `PrimitiveOperatorWire` 本身只允许 actual shear；
  `AblationReplacement` 的 actual/ablated primitive SHA 由完整 primitive payload 重算。
  ablated factory 在 replacement 后重算自己的 runtime SHA；只有 wire `full` actual factory
  要求与 calibration seed/target runtime SHA 相等。
- [ ] actual shear 要求 source/destination 是不同合法通道；trace
  `state_channels==canonical({source,destination})` 且
  `support_offsets==canonical({zero,offset})`。约定执行
  `destination(x)+=coefficient·source(x+offset)`，NumPy periodic 实现等价于对 source
  逐轴 `roll(shift=-offset)`；禁止 evaluator-only symbol。
- [ ] neutral 的唯一规范编码为：保留 original
  mechanism/production/layer/interface/source/destination/coefficient_digest，设置
  `operation_id="neutral_identity"`、`offset=zero`、`support_offsets=(zero,)`、
  `coefficient_wire=(0.0,0.0)`、`neutral_identity_id="neutral-identity-v1"`。actual
  primitive 的 `neutral_identity_id=None`。neutral 的 digest/provenance 仍绑定 original
  trace，但其 wire 只校验规范零值，不与 original sidecar lowering 数值比较。除此之外的
  no-op 编码均拒绝。
- [ ] 先测试 actual/ablated 保持 state schema、`complex128`、`dt`、blind 参数、层槽位、
  source/readout 与运行长度。
- [ ] 同时逐位保持 backend 和 boundary manifest；测试失败消息必须指出首个不匹配字段。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_ablation -v
  ```

  预期为 factory/ablation 模块缺失。
- [ ] neutral registry 是模块内闭集、无 public register/callable 注入，只生成
  `neutral-identity-v1` no-op placeholder 并保留 slot/interface。API 不接收替代 ablated
  factory，因此 q=0/另 candidate/inverse/删层/重调参均不可表达；pair verifier 独立重放。
- [ ] 所有含自 SHA 的 schema 统一以“排除该 SHA 字段后的完整 body”计算：
  `seed_sha/observation_sha/target_spec_sha/factory_sha/manifest_sha/report_sha`；
  helper/verifier 必须严格 schema、拒绝未知字段，并有篡改后重签回归。
- [ ] construction 与 qualification 使用不同返回类型；presence invariant 为：
  construction defined 当且仅当 pair 非空；qualified 的 construction 失败沿用原状态且
  pair/report 均空，construction 成功则 pair/report 均非空。后续 evaluator 只接受
  并在入口调用 `verify_qualified_ablation` 重验 opaque `QualifiedAblationOutcome`。
  raw/self-hashed `AblationDynamicsReport` 必须 fail-closed；Task 10 的 full-state
  `DynamicsCertificate` verifier 未签发 `VerifiedDynamicsReport` 前，不得产生 defined
  qualification，也不得保留可导入 capability/测试 issuer。opaque outcome 的
  `qualification_sha` 覆盖 status、construction/pair 与 verified report/certificate，
  construction 失败传播也有稳定 SHA。状态首错顺序冻结为：
  trace binding (`MANIFEST_MISMATCH`) → classification
  (`TRACE_UNCLASSIFIED`) → neutral construction (`ABLATION_NOT_REVERSIBLE`) →
  state schema/shape/dtype/backend/dt/blind params/slot/source/readout/boundary/run length
  (`STATE_SCHEMA_MISMATCH`) → report SHA/factory binding (`MANIFEST_MISMATCH`) →
  report `state_schema_matches` (`STATE_SCHEMA_MISMATCH`) → `reversible`
  (`ABLATION_NOT_REVERSIBLE`) → `stable` (`UNSTABLE`)。Task 9 不凭 metadata 猜稳定。
  `VerifiedDynamicsReport.certificate_sha` 与
  `QualifiedAblationOutcome.certificate_sha` 是 commit `39d1c14` 的 public property，后者
  在无 report 时为 `None`；`ablation_manifest_payload/dynamics_report_payload/
  build_dynamics_report/verify_verified_dynamics_report` 同样保留原签名。Task 9 自身的
  `verify_verified_dynamics_report` 仍无条件 fail-closed，Task 10 只能增加独立 broker，
  不能回写或替换这个 public 语义。
- [ ] 冻结 strict `factory_payload/factory_sha`；`factory_id` 不代替 digest。
  `AblationReplacement/Manifest/Pair/DynamicsReport` 全部严格 schema、深不可变；
  `Replacement` 只为每个 target-conditioned primitive 生成，按原 layer 顺序排列，blind
  slot 不生成 replacement。Replacement 与 Pair 由唯一 `manifest_sha` 递归绑定，自身不另设
  SHA 字段；dynamics report 另有 `report_sha` 并必须绑定 ablated factory SHA 与
  `DynamicsCertificate.certificate_sha`，不能只保存任意文本 ID。
- [ ] 以同一 synthetic primitive API 构造 full/zero/direct-sum controls，禁止在
  evaluator 中写 `if control_name`。每 mode 固定 `y+=x; x-=y; y+=x` 三 shear
  quarter-turn，避免 Jordan 增长。wire `full` 三层全 blind、actual/ablated 响应秩均 N；
  `zero` 三层全 conditioned、actual N/ablated 0；`direct_sum` 为 blind⊕conditioned，actual
  2N/ablated N。控制标签不进入通用执行/秩算法。
- [ ] wire `full` 使用两阶段
  `CalibrationSeed → hold-out response → FrozenSyntheticTarget → final trace/factory`
  协议；seed 的 runtime payload 只含与 target 无关、operation 类型固定为
  `local_canonical_shear` 的 `PrimitiveOperatorWire`，且
  `runtime_operator_sha=canonical_sha(runtime_operator_payload)`。唯一 calibration executor
  复用与 factory executor 相同的 primitive 内核，输入 seed 内冻结的 hold-out source/readout，
  对 holdout basis 的每个 vector 在空间原点注入单位 delta，运行一次有序 seed operator，
  读取 full periodic 输出；response tensor shape 固定
  `(n_readout,n_holdout,*spatial_shape)`，原点、signed impulse support 与 no-wrap 规则和
  Task 10 完全相同。它输出 immutable `CalibrationObservation`。target 内嵌完整
  observation 并同时
  绑定 calibration protocol、seed SHA 与 runtime SHA；
  `target.seed_sha==observation.seed_sha==seed.seed_sha`、
  `target.observation==observation` 且 `target_response==observation.response_tensor`；
  `target_spec_sha` 排除自身后覆盖全部字段。`build_full_factory_from_seed` 必须证明最终
  factory、seed、target 三者 runtime SHA 相等，并逐 primitive 证明 final trace 全部
  `target_blind`；任何 target-conditioned provenance 即使 operator wire 相同也拒绝。
  API 不接受 raw projector/response ndarray。
- [ ] 三个 control builder 冻结 `mode_count=N>0`。每个 mode 的 channel 顺序为
  `(x.000,y.000,x.001,y.001,...)`；wire `direct_sum` 先用全局编号 `0..N-1` 的
  `(x.b.NNN,y.b.NNN)`，再用全局编号 `N..2N-1` 的 `(x.c.NNN,y.c.NNN)`，不得复用 ID。
  canonical source basis 逐列注入每个 `x`，readout basis 逐行读取对应 `y`。每对依次
  三个零偏移 shear `y+=x;x-=y;y+=x`。`full` 全 blind，`zero` 全 conditioned，
  `direct_sum` 前半 blind/后半 conditioned；预言秩为 `N/N`、`N/0`、`2N/N`。
  builder 可按 control kind 组装，通用 executor、Fourier、SVD/rank 函数的参数和 payload
  中不得出现 control label。
- [ ] factory 是一次宏步且 `run_length==1`。canonical full-state impulse 只测一步得到
  square `M_full(k)`；full-state 随机多步属于 `FullStateBridgeSpec`，Fejér/source-readout
  多步属于 `ResponseRunSpec`，均不写入 factory。stencil support 含零；一宏步按有序层取
  Minkowski 和，多宏步按对应 spec 重复
  Minkowski，每次去重后按整数 tuple lexicographic 排序，neutral=`{0}`。逐轴
  `r_i=max(abs(s_i) for s in support)`，impulse no-wrap 必须 `L_i>2r_i`。冻结
  macro steps/cardinality/pair-product 上限为 `16_384/100_000/1_000_000`，乘积上限在
  分配前检查；Fejér 不显式展开 T 步 support。
- [ ] Task 10 交接 full-state transition kernel shape 固定
  `(n_state,n_state,*spatial)`；canonical state basis 由 `channel_order` 唯一定义，
  source/readout 只在后续 paired Fejér response 投影。公开 executor 是唯一运行路径。
- [ ] GREEN：重复定向测试，预期全部 `ok`。
- [ ] 提交：`feat(v3m0): construct matched realspace ablations`

## Task 10：全状态实空间 transition、动力学证书与 symbol bridge

**文件：**

- Modify: `rulespace_v3/contracts.py`
- Modify: `rulespace_v3/ablation.py`
- Modify: `tests/test_v3m0_contracts.py`
- Modify: `tests/test_v3m0_ablation.py`
- Create: `rulespace_v3/parent_freeze.py`
- Create: `rulespace_v3/registry.py`
- Create: `rulespace_v3/prestructure.py`
- Create: `rulespace_v3/dynamics.py`
- Create: `rulespace_v3/qualification.py`
- Create: `tests/test_v3m0_dynamics.py`
- Create: `tests/test_v3m0_prestructure.py`
- Create: `tests/test_v3m0_import_dag.py`

**冻结接口：**

```python
@dataclass(frozen=True)
class TaggedScalarWire:
    value_kind: Literal["integer", "fp64-bits", "text", "complex128-bits"]
    integer_value: Optional[int]
    fp64_bits_value: Optional[int]
    text_value: Optional[str]
    complex128_bits_value: Optional[tuple[int, int]]

@dataclass(frozen=True)
class SyntheticApplicationOperation:
    operation_schema_version: str
    operation_instance_id: str
    operation_kind: Literal[
        "identity-v1",
        "canonical-shear-v1",
        "phase-rotation-v1",
        "amplitude-rescale-v1",
        "source-linear-mix-v1",
        "direct-sum-v1",
        "geometry-subspace-v1",
        "coverage-subspace-v1",
        "deterministic-series-v1",
    ]
    input_operation_instance_ids: tuple[str, ...]
    parameters: tuple[tuple[str, TaggedScalarWire], ...]
    operation_sha: str

@dataclass(frozen=True)
class SyntheticApplicationBasisProtocol:
    protocol_schema_version: str
    source_basis: BasisManifest
    readout_basis: BasisManifest
    protocol_sha: str

@dataclass(frozen=True)
class SyntheticApplicationGridProtocol:
    protocol_schema_version: str
    spatial_ndim: int
    spatial_shape: tuple[int, ...]
    response_torus_denominators: tuple[int, ...]
    response_reciprocal_indices: tuple[tuple[int, ...], ...]
    direction_ids: tuple[str, ...]
    primitive_directions: tuple[tuple[int, ...], ...]
    path_ids: tuple[str, ...]
    ordered_paths: tuple[tuple[tuple[int, ...], ...], ...]
    closure_path_pairs: tuple["DirectionPathClosure", ...]
    bridge_reciprocal_indices: tuple[tuple[int, ...], ...]
    bridge_steps: tuple[int, ...]
    reference_reciprocal_index: tuple[int, ...]
    preregistered_phase_bands: tuple[tuple[float, float], ...]
    expected_shell_rank: int
    expected_shell_rank_source_id: Literal[
        "parent-freeze-control-application-spec-v1"
    ]
    protocol_sha: str

@dataclass(frozen=True)
class SyntheticApplicationReadoutProtocol:
    protocol_schema_version: str
    source_metric_whitener: FrozenComplexTensor
    h_metric_whitener: FrozenComplexTensor
    curvature_incidence_operator: FrozenComplexTensor
    curvature_metric_whitener: FrozenComplexTensor
    curvature_normalizer_id: str
    protocol_sha: str

@dataclass(frozen=True)
class SyntheticApplicationProtocolConstants:
    constants_schema_version: str
    tagged_constants: tuple[tuple[str, TaggedScalarWire], ...]
    max_operation_count: int
    max_dependency_edge_count: int
    max_parameter_count: int
    max_serialized_bytes: int
    constants_sha: str

@dataclass(frozen=True)
class V3M0SyntheticControlApplicationSpec:
    application_schema_version: str
    control_case_id: str  # closed C01..C20 Literal registry
    application_instance_id: str
    builder_id: str
    basis_protocol: SyntheticApplicationBasisProtocol
    grid_protocol: SyntheticApplicationGridProtocol
    readout_protocol: SyntheticApplicationReadoutProtocol
    operations: tuple[SyntheticApplicationOperation, ...]
    output_operation_instance_ids: tuple[str, ...]
    required_pipeline_stages: tuple[str, ...]
    expected_prediction_profile_id: str
    expected_control_evidence_schema: str
    application_spec_sha: str

@dataclass(frozen=True)
class ParentFreezeManifest:
    parent_freeze_schema_version: str
    program_id: Literal["projective-rule-space-v3m0-v1"]
    parent_v2_sha: str
    task9_commit_sha: Literal[
        "39d1c1427aefa38cd46e1272affb9a10dd46a073"
    ]
    taskbook_source_sha: str
    implementation_plan_source_sha: str
    synthetic_control_application_specs: tuple[
        V3M0SyntheticControlApplicationSpec, ...
    ]
    protocol_constant_payload: SyntheticApplicationProtocolConstants
    source_closure: tuple[tuple[str, str], ...]
    parent_freeze_sha: str

class VerifiedParentFreeze:
    """Opaque；证明闭合 freeze body 相等，不把 SHA 冒充墙钟先后。"""

    @property
    def manifest(self) -> ParentFreezeManifest: ...

def issue_v3m0_parent_freeze() -> VerifiedParentFreeze: ...
def verify_parent_freeze(
    manifest: ParentFreezeManifest,
) -> VerifiedParentFreeze: ...

@dataclass(frozen=True)
class ControlReadoutCalibrationSpec:
    spec_schema_version: str
    source_metric_whitener: FrozenComplexTensor
    h_metric_whitener: FrozenComplexTensor
    curvature_incidence_operator: FrozenComplexTensor
    curvature_metric_whitener: FrozenComplexTensor
    curvature_normalizer_id: Literal["synthetic-identity-v1"]
    spec_sha: str

@dataclass(frozen=True)
class ControlRegistryEntry:
    entry_schema_version: str
    control_id: Literal["full", "zero", "direct_sum"]
    builder_id: str
    factory_sha: str
    source_basis: BasisManifest
    readout_basis: BasisManifest
    readout_calibration_spec: ControlReadoutCalibrationSpec
    mode_count: int
    expected_h_actual_rank: int
    expected_h_ablated_rank: int
    expected_curv_actual_rank: int
    expected_curv_ablated_rank: int
    parent_freeze_sha: str
    entry_sha: str

@dataclass(frozen=True)
class ClosedControlRegistry:
    registry_schema_version: str
    entries: tuple[ControlRegistryEntry, ...]
    parent_freeze_sha: str
    registry_sha: str

class VerifiedControlRegistry:
    """Opaque；私有 seal 绑定 raw registry 与三个 verified bundle/factory snapshots。"""

    @property
    def registry(self) -> ClosedControlRegistry: ...

def build_closed_control_registry(
    controls: tuple[SyntheticControlBundle, ...],
    parent: VerifiedParentFreeze,
) -> VerifiedControlRegistry: ...
def verify_closed_control_registry(
    registry: ClosedControlRegistry,
    controls: tuple[SyntheticControlBundle, ...],
    parent: VerifiedParentFreeze,
) -> VerifiedControlRegistry: ...

@dataclass(frozen=True)
class MeasuredTransition:
    transition_schema_version: str
    parent_freeze_sha: str
    prestructure_authority_sha: str
    factory_sha: str
    factory_role: Literal["actual", "matched_ablated"]
    state_schema_id: str
    channel_order: tuple[str, ...]
    spatial_shape: tuple[int, ...]
    dt: float
    boundary_manifest_id: Literal["periodic-v1"]
    state_basis_convention_id: Literal["channel-identity-v1"]
    kernel: FrozenComplexTensor
    support_offsets: tuple[tuple[int, ...], ...]
    support_sha: str
    macro_steps: Literal[1]
    transition_sha: str

class VerifiedTransition:
    """Opaque；绑定 factory，并由 realspace impulse 重测签发。"""

    @property
    def transition(self) -> MeasuredTransition: ...

@dataclass(frozen=True)
class DirectionPathClosure:
    closure_id: str
    first_path_id: str
    first_path_position: int
    second_path_id: str
    second_path_position: int
    reciprocal_index: tuple[int, ...]

@dataclass(frozen=True)
class DirectionManifest:
    direction_schema_version: str
    direction_ids: tuple[str, ...]
    primitive_directions: tuple[tuple[int, ...], ...]
    path_ids: tuple[str, ...]
    ordered_paths: tuple[tuple[tuple[int, ...], ...], ...]
    closure_path_pairs: tuple[DirectionPathClosure, ...]
    direction_manifest_sha: str

@dataclass(frozen=True)
class DynamicsKGridManifest:
    grid_schema_version: str
    qualification_profile: Literal[
        "exact-offset-zero-v1",
        "cartesian-full-64-v1",
    ]
    spatial_ndim: int
    torus_denominators: tuple[int, ...]
    reciprocal_indices: tuple[tuple[int, ...], ...]
    dynamics_grid_sha: str

@dataclass(frozen=True)
class ResponseKGridManifest:
    grid_schema_version: str
    qualification_profile: Literal["directional-momentum-shell-path-v1"]
    spatial_ndim: int
    torus_denominators: tuple[int, ...]
    reciprocal_indices: tuple[tuple[int, ...], ...]
    direction_manifest: DirectionManifest
    response_grid_sha: str

@dataclass(frozen=True)
class BridgeKGridManifest:
    grid_schema_version: str
    spatial_shape: tuple[int, ...]
    torus_denominators: tuple[int, ...]
    reciprocal_indices: tuple[tuple[int, ...], ...]
    bridge_grid_sha: str

@dataclass(frozen=True)
class FullStateBridgeSpec:
    bridge_spec_schema_version: str
    factory_sha: str
    parent_freeze_sha: str
    prestructure_authority_sha: str
    state_schema_id: str
    channel_order: tuple[str, ...]
    spatial_shape: tuple[int, ...]
    bridge_grid: BridgeKGridManifest
    macro_steps: tuple[int, ...]
    state_trial_vectors: FrozenComplexTensor
    trial_generation_id: Literal["canonical-or-sha256-dense-v1"]
    trial_seed_sha: str
    trial_domain_separator: Literal["v3m0-full-state-bridge-trials-v1"]
    trial_gram_frobenius_upper: float
    trial_gram_gate_method_id: Literal[
        "outward-frobenius-dominates-spectral-v1"
    ]
    bridge_tolerance: float
    bridge_spec_sha: str

@dataclass(frozen=True)
class RuntimeEvidenceManifest:
    runtime_schema_version: str
    evaluator_id: str
    source_closure: tuple[tuple[str, str], ...]
    python_version: str
    numpy_version: str
    scipy_version: str
    blas_config_sha: str
    lapack_config_sha: str
    platform_id: str
    runtime_manifest_sha: str

@dataclass(frozen=True)
class SyntheticStructurePreregistration:
    preregistration_schema_version: str
    evidence_lane: Literal["synthetic-classical"]
    control_registry_sha: str
    control_registry_entry_sha: str
    factory_sha: str
    factory_role: Literal["actual", "matched_ablated"]
    ablation_manifest_sha: str
    ablation_construction_sha: str
    target_spec_sha: str
    state_schema_id: str
    channel_order: tuple[str, ...]
    canonical_channel_pairs: tuple[tuple[str, str], ...]
    fourier_adjoint_convention_id: Literal["minus-k-transpose-v1"]
    structure_form: FrozenComplexTensor
    reality_convention_id: Literal["real-kernel-positive-zero-v1"]
    parent_freeze_sha: str
    preregistration_sha: str

@dataclass(frozen=True)
class AdapterCommitmentReceipt:
    receipt_schema_version: str
    repository_identity: str
    commit_sha: str
    committed_blob_shas: tuple[tuple[str, str], ...]
    clean_source_tree_sha: str
    required_ancestor_commit_sha: str
    ancestry_path: tuple[str, ...]
    external_anchor_id: str
    receipt_sha: str

@dataclass(frozen=True)
class AdapterStructurePreregistration:
    preregistration_schema_version: str
    evidence_lane: Literal["classical-adapter", "quantum"]
    factory_sha: str
    target_spec_sha: str
    state_schema_id: str
    channel_order: tuple[str, ...]
    canonical_channel_pairs: tuple[tuple[str, str], ...]
    fourier_adjoint_convention_id: Literal[
        "same-k-dagger-v1",
        "minus-k-transpose-v1",
    ]
    structure_form: FrozenComplexTensor
    reality_convention_id: Optional[
        Literal["real-kernel-positive-zero-v1"]
    ]
    parent_freeze_sha: str
    commitment_receipt: AdapterCommitmentReceipt
    preregistration_sha: str

@dataclass(frozen=True)
class AblationPairSnapshot:
    snapshot_schema_version: str
    construction_status: BlockStatus
    actual_factory: LinearRealspaceFactory
    ablated_factory: LinearRealspaceFactory
    ablation_manifest: AblationManifest
    ablation_construction_sha: str
    snapshot_sha: str

@dataclass(frozen=True)
class PrestructureAuthority:
    authority_schema_version: str
    authority_kind: Literal[
        "synthetic-registry-v1",
        "synthetic-application-v1",
        "adapter-preregistration-v1",
    ]
    parent_freeze: ParentFreezeManifest
    factory_sha: str
    factory_role: Literal["actual", "matched_ablated"]
    ablation_pair_snapshot: AblationPairSnapshot
    ablation_manifest_sha: str
    ablation_construction_sha: str
    synthetic_registry: Optional[ClosedControlRegistry]
    synthetic_registry_entry_sha: Optional[str]
    synthetic_preregistration: Optional[SyntheticStructurePreregistration]
    synthetic_application_spec: Optional[V3M0SyntheticControlApplicationSpec]
    synthetic_application_permit_sha: Optional[str]
    adapter_preregistration: Optional[AdapterStructurePreregistration]
    authority_sha: str

class VerifiedPrestructureAuthority:
    """Opaque；raw 仅含 snapshot；私有 seal 另绑定 live verified pair/role/factory。"""

    @property
    def authority(self) -> PrestructureAuthority: ...

    @property
    def parent(self) -> VerifiedParentFreeze: ...

    @property
    def factory_sha(self) -> str: ...

    @property
    def factory_role(self) -> Literal["actual", "matched_ablated"]: ...

@dataclass(frozen=True)
class StructureManifest:
    structure_schema_version: str
    evidence_lane: Literal[
        "synthetic-classical",
        "classical-adapter",
        "quantum",
    ]
    structure_kind: Literal["unitary", "symplectic"]
    target_spec_sha: str
    state_schema_id: str
    channel_order: tuple[str, ...]
    canonical_channel_pairs: tuple[tuple[str, str], ...]
    fourier_adjoint_convention_id: Literal[
        "same-k-dagger-v1",
        "minus-k-transpose-v1",
    ]
    structure_form: FrozenComplexTensor
    reality_convention_id: Optional[
        Literal["real-kernel-positive-zero-v1"]
    ]
    prestructure_authority_sha: str
    structure_manifest_sha: str

@dataclass(frozen=True)
class RealityCertificate:
    reality_schema_version: str
    factory_sha: str
    transition_sha: str
    structure_manifest_sha: str
    factory_coefficient_count: int
    transition_entry_count: int
    imaginary_bit_pattern_id: Literal["all-positive-zero-f64-v1"]
    implied_fourier_identity_id: Literal["m-minus-k-equals-conj-m-k-v1"]
    reality_certificate_sha: str

@dataclass(frozen=True)
class MetricOriginManifest:
    origin_schema_version: str
    origin_kind: Literal[
        "synthetic-identity-v1",
        "adapter-preregistered-v1",
    ]
    parent_freeze_sha: str
    prestructure_authority_sha: str
    factory_sha: str
    structure_manifest_sha: str
    evidence_lane: Literal[
        "synthetic-classical",
        "classical-adapter",
        "quantum",
    ]
    derivation_or_preregistration_sha: str
    metric_kernel_sha: str
    metric_support_sha: str
    origin_sha: str

@dataclass(frozen=True)
class Fp64RootIntervalEntry:
    root_index: int
    dyadic_exponent: Literal[192]
    real_lower_numerator: int
    real_upper_numerator: int
    imag_lower_numerator: int
    imag_upper_numerator: int
    real_center_f64_bits: int
    imag_center_f64_bits: int
    center_distance_squared_upper_numerator: int
    center_distance_squared_upper_power_of_two: Literal[-384]
    center_distance_upper_f64_bits: int

@dataclass(frozen=True)
class Fp64RootOfUnityIntervalTable:
    table_schema_version: str
    table_id: Literal["root64-dyadic-machin-taylor-containment-v1"]
    denominator: Literal[64]
    dyadic_exponent: Literal[192]
    pi_lower_numerator: int
    pi_upper_numerator: int
    machin_identity_id: Literal["pi-equals-16atan1over5-minus4atan1over239-v1"]
    taylor_precision_bits: Literal[192]
    remainder_method_id: Literal["bigint-alternating-rational-remainder-v1"]
    entries: tuple[Fp64RootIntervalEntry, ...]
    table_sha: str

@dataclass(frozen=True)
class Fp64EnclosureProtocol:
    protocol_schema_version: str
    unit_roundoff_numerator: Literal[1]
    unit_roundoff_denominator: Literal[9007199254740992]  # 2**53
    minimum_subnormal_numerator: Literal[1]
    minimum_subnormal_power_of_two: Literal[-1074]
    gamma_bound_method_id: Literal[
        "integer-ratio-q-u-over-one-minus-q-u-v1"
    ]
    complex_add_real_add_count: Literal[2]
    complex_multiply_real_multiply_count: Literal[4]
    complex_multiply_real_add_count: Literal[2]
    matrix_dot_operation_count_id: Literal[
        "complex-dot-real-component-q-equals-4n-minus-1-v1"
    ]
    scalar_expression_dag_id: Literal[
        "ordered-four-real-products-and-additions-no-fma-v1"
    ]
    roundoff_bound_id: Literal[
        "gamma-q-times-absolute-product-sum-plus-minsub-v1"
    ]
    gradual_underflow_additive_id: Literal[
        "real-operation-count-times-minsub-v1"
    ]
    root_center_error_propagation_id: Literal[
        "coefficient-frobenius-sum-times-root-center-distance-v1"
    ]
    frobenius_sqrt_containment_id: Literal[
        "exact-integer-ratio-square-containment-v1"
    ]
    fma_policy: Literal["forbidden"]
    reassociation_policy: Literal["forbidden"]
    finite_value_policy: Literal[
        "finite-normal-or-signed-zero-reject-nonzero-subnormal-ftz-nan-inf-overflow-v1"
    ]
    outward_rounding_id: Literal["nextafter-after-every-scalar-op-v1"]
    root_interval_table: Fp64RootOfUnityIntervalTable
    protocol_sha: str

@dataclass(frozen=True)
class StabilityMetricWitness:
    witness_schema_version: str
    structure_manifest_sha: str
    metric_kind: Literal["constant-state-v1", "finite-support-laurent-v1"]
    metric_kernel: FrozenComplexTensor  # shape=(n_support,n_state,n_state)
    metric_support_offsets: tuple[tuple[int, ...], ...]
    metric_support_sha: str
    normalization_id: Literal["trace-at-zero-equals-state-dim-v1"]
    positive_eigenvalue_floor: float
    condition_number_max: float
    metric_origin: MetricOriginManifest
    witness_sha: str

@dataclass(frozen=True)
class LaurentResidualCertificate:
    residual_schema_version: str
    residual_kind: Literal["canonical-structure", "stability-metric"]
    operand_shas: tuple[str, ...]
    spatial_ndim: int
    n_state: int
    fp64_enclosure_protocol_sha: str
    fourier_convention_id: Literal["signed-displacement-exp-minus-i-k-dot-d-v1"]
    matrix_norm_id: Literal["spectral-2-v1"]
    momentum_supremum_method_id: Literal[
        "sum-of-directed-outward-frobenius-upper-v1"
    ]
    support_offsets: tuple[tuple[int, ...], ...]
    coefficients: FrozenComplexTensor  # shape=(n_support,n_state,n_state)
    convolution_pair_counts: tuple[int, ...]
    coefficient_roundoff_frobenius_uppers: tuple[float, ...]
    coefficient_frobenius_upper_sum: float
    raw_global_momentum_supremum_bound: float
    residual_sha: str

@dataclass(frozen=True)
class NormalizedMetricResidualAudit:
    audit_schema_version: str
    metric_residual_sha: str
    spectral_margin_coverage_sha: str
    raw_metric_residual_upper: float
    covered_g_lambda_min_lower: float
    division_method_id: Literal["fp64-nextafter-outward-division-v1"]
    normalized_metric_residual_upper: float
    audit_sha: str

@dataclass(frozen=True)
class SpectralPointEnclosureColumnarSidecar:
    sidecar_schema_version: str
    qualification_grid_sha: str
    point_count: int
    candidate_algorithm_id: Literal[
        "scalar-gauss-jordan-hermitian-cholesky-v1"
    ]
    encoding_id: Literal["strict-base64-big-endian-f64-columns-v1"]
    raw_diagnostic_status: Literal[
        "available-lapack-v1",
        "unavailable-v1",
    ]
    raw_diagnostic_unavailable_reason: Optional[str]
    raw_m_sigma_min_b64: Optional[str]
    raw_m_sigma_max_b64: Optional[str]
    raw_g_lambda_min_b64: Optional[str]
    raw_g_lambda_max_b64: Optional[str]
    transition_symbol_error_upper_b64: str
    metric_symbol_error_upper_b64: str
    transition_frobenius_upper_b64: str
    inverse_frobenius_upper_b64: str
    inverse_residual_frobenius_upper_b64: str
    m_sigma_min_lower_b64: str
    m_sigma_max_upper_b64: str
    metric_frobenius_upper_b64: str
    cholesky_factorization_residual_frobenius_upper_b64: str
    cholesky_inverse_frobenius_upper_b64: str
    cholesky_inverse_residual_frobenius_upper_b64: str
    ell_lower_b64: str
    g_lambda_min_lower_b64: str
    g_lambda_max_upper_b64: str
    raw_byte_count: int
    column_data_sha: str
    roundoff_enclosure_method_id: Literal[
        "fp64-operation-count-nextafter-columnar-v1"
    ]
    sidecar_sha: str

@dataclass(frozen=True)
class SpectralMarginCoverage:
    coverage_schema_version: str
    transition_sha: str
    stability_metric_witness_sha: str
    fp64_enclosure_protocol: Fp64EnclosureProtocol
    qualification_grid: DynamicsKGridManifest
    spectral_diagnostic_grid: DynamicsKGridManifest
    torus_domain_id: Literal["minus-pi-pi-periodic-v1"]
    distance_convention_id: Literal["principal-linf-torus-v1"]
    fill_distance: float
    raw_diagnostic_status: Literal[
        "available-lapack-v1",
        "unavailable-v1",
    ]
    raw_diagnostic_unavailable_reason: Optional[str]
    raw_m_sigma_min: Optional[tuple[float, ...]]
    raw_m_sigma_max: Optional[tuple[float, ...]]
    raw_g_lambda_min: Optional[tuple[float, ...]]
    raw_g_lambda_max: Optional[tuple[float, ...]]
    point_enclosures: SpectralPointEnclosureColumnarSidecar
    grid_m_sigma_min_lower: tuple[float, ...]
    grid_m_sigma_max_upper: tuple[float, ...]
    grid_g_lambda_min_lower: tuple[float, ...]
    grid_g_lambda_max_upper: tuple[float, ...]
    m_sigma_min_axis_derivative_bounds: tuple[float, ...]
    m_sigma_max_axis_derivative_bounds: tuple[float, ...]
    g_lambda_min_axis_derivative_bounds: tuple[float, ...]
    g_lambda_max_axis_derivative_bounds: tuple[float, ...]
    m_sigma_min_coverage_increment: float
    m_sigma_max_coverage_increment: float
    g_lambda_min_coverage_increment: float
    g_lambda_max_coverage_increment: float
    covered_m_sigma_min_lower: float
    covered_m_sigma_max_upper: float
    covered_g_lambda_min_lower: float
    covered_g_lambda_max_upper: float
    covered_m_condition_number_upper: float
    covered_g_condition_number_upper: float
    spectral_radius_drift_diagnostic: Optional[float]
    coverage_sha: str

@dataclass(frozen=True)
class PowerDriftAudit:
    audit_schema_version: str
    normalized_metric_residual_audit_sha: str
    macro_step: Literal[16384]
    nonzero_delta_squaring_count: Literal[14]
    executed_squaring_count: Literal[0, 14]
    identity_branch: bool
    method_id: Literal["t16384-directed-repeated-squaring-v1"]
    delta_upper: float
    one_minus_delta_lower: float
    one_plus_delta_upper: float
    growth_upper: float
    contraction_upper: float
    drift_upper: float
    audit_sha: str

@dataclass(frozen=True)
class BridgeCaseAudit:
    reciprocal_index: tuple[int, ...]
    macro_steps: int
    trial_index: int
    raw_abs_residual: float
    scale: float
    normalized_residual: float

@dataclass(frozen=True)
class BridgeAudit:
    bridge_schema_version: str
    bridge_kind: Literal["full-state"]
    factory_sha: str
    transition_sha: str
    bridge_spec_sha: str
    cases: tuple[BridgeCaseAudit, ...]
    raw_abs_max: float
    normalized_max: float
    bridge_sha: str

@dataclass(frozen=True)
class InstabilityGrowthCounterWitness:
    witness_schema_version: str
    witness_profile: Literal["exact-zero-offset-jordan-2x2-v1"]
    parent_freeze_sha: str
    prestructure_authority_sha: str
    transition: MeasuredTransition
    transition_matrix: FrozenComplexTensor
    support_offsets: tuple[tuple[int, ...], ...]
    macro_step: Literal[16384]
    initial_vector: FrozenComplexTensor
    exact_power_formula_id: Literal[
        "jordan-one-one-zero-one-power-v1"
    ]
    initial_norm_squared: Literal[1]
    final_norm_squared: Literal[268435457]  # 16384**2 + 1
    required_growth_squared_strict_upper: Literal[100000001]
    witness_sha: str

@dataclass(frozen=True)
class DynamicsCertificate:
    certificate_schema_version: str
    transition: MeasuredTransition
    prestructure_authority: PrestructureAuthority
    structure: StructureManifest
    reality: Optional[RealityCertificate]
    stability_metric: StabilityMetricWitness
    fp64_enclosure_protocol: Fp64EnclosureProtocol
    full_state_bridge_spec: FullStateBridgeSpec
    full_state_bridge_audit: BridgeAudit
    structure_residual: LaurentResidualCertificate
    metric_residual: LaurentResidualCertificate
    spectral_margins: SpectralMarginCoverage
    normalized_metric_residual: NormalizedMetricResidualAudit
    power_drift: PowerDriftAudit
    runtime: RuntimeEvidenceManifest
    certificate_sha: str

class VerifiedDynamicsCertificate:
    """Opaque；verifier 从真实 transition 与完整 manifests 重算全部字段。"""

    @property
    def certificate(self) -> DynamicsCertificate: ...

class DynamicsCertificationFailure(str, Enum):
    PRESTRUCTURE_INVALID = "prestructure_invalid"
    TRANSITION_INVALID = "transition_invalid"
    REALITY_INVALID = "reality_invalid"
    LAURENT_RESOURCE_EXCEEDED = "laurent_resource_exceeded"
    STRUCTURE_RAW_UNRESOLVED = "structure_raw_unresolved"
    METRIC_RAW_UNRESOLVED = "metric_raw_unresolved"
    SPECTRAL_COVERAGE_UNRESOLVED = "spectral_coverage_unresolved"
    NORMALIZED_METRIC_UNRESOLVED = "normalized_metric_unresolved"
    FULL_STATE_BRIDGE_FAILED = "full_state_bridge_failed"
    POWER_DRIFT_UNRESOLVED = "power_drift_unresolved"
    CERTIFIED_INSTABILITY_COUNTERWITNESS = "certified_instability_counterwitness"

@dataclass(frozen=True)
class DynamicsCertificationAttemptAudit:
    attempt_schema_version: str
    factory_sha: str
    transition_sha: Optional[str]
    prestructure_authority_sha: str
    first_failure: Optional[DynamicsCertificationFailure]
    reality: Optional[RealityCertificate]
    structure_residual: Optional[LaurentResidualCertificate]
    metric_residual: Optional[LaurentResidualCertificate]
    spectral_margins: Optional[SpectralMarginCoverage]
    normalized_metric_residual: Optional[NormalizedMetricResidualAudit]
    full_state_bridge_audit: Optional[BridgeAudit]
    power_drift: Optional[PowerDriftAudit]
    instability_counter_witness: Optional[InstabilityGrowthCounterWitness]
    attempt_sha: str

@dataclass(frozen=True)
class DynamicsCertificationOutcome:
    status: BlockStatus
    failure: Optional[DynamicsCertificationFailure]
    attempt_audit: DynamicsCertificationAttemptAudit
    certificate: Optional[DynamicsCertificate]
    outcome_sha: str

class VerifiedDynamicsCertificationOutcome:
    """Opaque；内部 seal 绑定 raw outcome 与可选 VerifiedDynamicsCertificate capability。"""

    @property
    def outcome(self) -> DynamicsCertificationOutcome: ...

    @property
    def certificate(self) -> Optional[VerifiedDynamicsCertificate]: ...

@dataclass(frozen=True)
class CertificateBackedQualificationEvidence:
    evidence_schema_version: str
    construction_status: BlockStatus
    pair_snapshot: AblationPairSnapshot
    dynamics_report: AblationDynamicsReport
    verified_certificate_sha: str
    evidence_sha: str

@dataclass(frozen=True)
class CertificateBackedQualificationOutcome:
    status: BlockStatus
    evidence: Optional[CertificateBackedQualificationEvidence]
    outcome_sha: str

class VerifiedCertificateBackedQualification:
    """Opaque success capability；私有 seal 绑定 live construction/certificate。"""

    @property
    def outcome(self) -> CertificateBackedQualificationOutcome: ...

def verify_dynamics_grid_manifest(grid) -> DynamicsKGridManifest: ...
def verify_response_grid_manifest(grid) -> ResponseKGridManifest: ...
def verify_bridge_grid_manifest(
    grid, spatial_shape, signed_support
) -> BridgeKGridManifest: ...
def verify_full_state_bridge_spec(
    spec, factory, authority: VerifiedPrestructureAuthority
) -> FullStateBridgeSpec: ...
def verify_runtime_evidence_manifest(manifest) -> RuntimeEvidenceManifest: ...
def issue_synthetic_prestructure_authority(
    parent: VerifiedParentFreeze,
    registry: VerifiedControlRegistry,
    control_id: Literal["full", "zero", "direct_sum"],
    construction: AblationConstructionOutcome,
    factory_role: Literal["actual", "matched_ablated"],
) -> VerifiedPrestructureAuthority: ...
def verify_structure_manifest(
    manifest,
    factory,
    authority: VerifiedPrestructureAuthority,
) -> StructureManifest: ...
def certify_reality(
    factory, transition, structure
) -> RealityCertificate: ...
def verify_stability_metric_witness(
    witness, structure, transition
) -> StabilityMetricWitness: ...
def verify_laurent_residual_certificate(
    residual, transition, structure, stability_metric
) -> LaurentResidualCertificate: ...
def verify_spectral_margin_coverage(
    coverage, transition, stability_metric
) -> SpectralMarginCoverage: ...
def verify_normalized_metric_residual_audit(
    audit, metric_residual, spectral_margins
) -> NormalizedMetricResidualAudit: ...
def verify_power_drift_audit(
    audit, normalized_metric_residual
) -> PowerDriftAudit: ...
def verify_instability_growth_counter_witness(
    witness,
    factory,
    authority: VerifiedPrestructureAuthority,
) -> InstabilityGrowthCounterWitness: ...
def verify_bridge_audit(
    audit, factory, transition, bridge_spec
) -> BridgeAudit: ...
def measured_transition_payload(transition) -> dict[str, object]: ...
def verify_measured_transition(
    transition, factory, authority: VerifiedPrestructureAuthority
) -> VerifiedTransition: ...
def transition_kernel_array(transition: VerifiedTransition) -> np.ndarray: ...
def measure_transition(
    factory: VerifiedFactory,
    authority: VerifiedPrestructureAuthority,
) -> VerifiedTransition: ...
def transition_symbol(
    transition: VerifiedTransition, k
) -> np.ndarray: ...
def certify_transition_dynamics(
    factory,
    transition,
    prestructure_authority: VerifiedPrestructureAuthority,
    structure_manifest,
    stability_metric,
    full_state_bridge_spec,
    dynamics_grid,
    runtime_manifest,
) -> VerifiedDynamicsCertificationOutcome: ...
def verify_dynamics_certification_outcome(
    outcome,
    factory,
    prestructure_authority: VerifiedPrestructureAuthority,
) -> VerifiedDynamicsCertificationOutcome: ...
def verify_dynamics_certificate(
    certificate,
    factory,
    prestructure_authority: VerifiedPrestructureAuthority,
) -> VerifiedDynamicsCertificate: ...
def qualify_ablation_from_certificate(
    construction: AblationConstructionOutcome,
    ablated_certificate: VerifiedDynamicsCertificate,
) -> VerifiedCertificateBackedQualification: ...
def verify_qualified_ablation_from_certificate(
    outcome: CertificateBackedQualificationOutcome,
    construction: AblationConstructionOutcome,
    ablated_certificate: VerifiedDynamicsCertificate,
) -> VerifiedCertificateBackedQualification: ...
```

- [ ] `ParentFreezeManifest` 是完整 raw body，按固定顺序递归内嵌 C01–C20
  惰性 `V3M0SyntheticControlApplicationSpec`、Task 9 完整 commit、父 v2、两份权威文档、
  protocol/constants 与 source closure；C01–C20 的 `control_case_id` 与
  `application_instance_id` 在任何 window/response 前唯一冻结。每份 application spec
  递归内嵌完整 basis、grid、readout protocol body 与完整 protocol/constants payload，
  保存完整 tagged scalar 参数和 closed operation DAG，而不是 protocol ID/SHA 摘要；
  required stages 与 prediction profile；`application_spec_sha` 只是该 body 的派生字段，
  不能用 digest/expected schema 替代 body。Task 10 只冻结惰性规格而不运行，Task 13–17
  的 builder 必须逐 body 对拍，因此不存在等待后续结果回灌 ParentFreeze 的依赖环。
  每个 `TaggedScalarWire` 四个 Optional value 字段恰有一个非空且匹配 `value_kind`；
  `operation_instance_id` 全局唯一，`operation_kind` 只取 closed Literal；dependency 只引用
  instance ID。`output_operation_instance_ids` 非空 canonical，所有节点都从 outputs 反向
  可达且 DAG 无环。operation/edge/parameter/serialized-byte caps 取完整 constants body，
  并在构造 adjacency/parameter arrays 前预检；dangling、dead node 或 cap 超限均拒绝。
  `issue_v3m0_parent_freeze`
  不接受 caller SHA/body，`verify_parent_freeze` 逐字段对拍模块内同一闭合常量并以
  live-identity weak registry 签发 opaque `VerifiedParentFreeze`；复制 slots、自重签 raw
  manifest、过期 weakref 都拒绝。该机制消除 V3-M0 synthetic post-hoc 选择自由度，但
  **不声称 SHA 证明真实墙钟先后**。V3-M1 adapter 只能接受在运行前锚定的 Git
  blob/commit receipt，验证 clean source tree、required ancestor 与 ancestry path；
  若外部锚不可验证，必须停在 external-anchor 边界，不得用 self-hash 冒充时间证明。
  Task 10 只冻结 raw `AdapterCommitmentReceipt/AdapterStructurePreregistration` schema，
  **不导出** adapter authority issuer；真正 issuer 必须等 V3-M1 的独立
  `VerifiedV3M1ParentFreeze`，V3-M0-only `VerifiedParentFreeze` 不能跨 scope 签 adapter。
- [ ] `measure_transition` 不接受 steps，只测一次宏步 square `M_full`，且必须先接收
  role-specific `VerifiedPrestructureAuthority`。authority 递归绑定 full
  `AblationPair`、机械计算的 construction payload SHA、ablation manifest SHA、
  `factory_role∈{actual,matched_ablated}`、该 role 的 exact factory 与唯一 structure
  preregistration；issuer 必须重放 `verify_ablation_pair` 和完整
  `AblationConstructionOutcome` presence/status，不能仅凭 registry entry 的 actual
  factory SHA 签发。C01–C03 的
  `issue_synthetic_prestructure_authority(parent,registry,control_id,construction,role)`
  因而可分别给同一 pair 两支签 authority；C04–C20 则只经 Task 12 的 closed
  `synthetic-application-v1` issuer。`MeasuredTransition` 递归绑定
  `parent_freeze_sha/prestructure_authority_sha/factory_sha`。它对
  `channel_order` 的每个单位状态通道注入 delta，逐次调用同一个
  `apply_factory_step`，并读取全部状态通道；不得使用 source/readout projector 拼造
  transition。Fejér `T` 与 source/readout 多步属于 Task 11 的 `ResponseRunSpec`，不写入
  factory 或 Task 10 证书。
- [ ] 上述所有 `*_sha` 都排除自身 SHA 后递归覆盖完整 body；每个类型都有 strict
  payload/verifier，拒绝未知字段、NaN/Inf、bool-as-int 与摘要替代 body。raw
  `MeasuredTransition` 只可序列化；consumer 接受的 `VerifiedTransition` 必须带 factory
  与同一 opaque authority，先重验 parent/factory/prereg binding，再重跑 canonical
  impulses 并逐位对拍 kernel/support/SHA。没有 authority 的旧签名不存在，单凭自哈希
  不能签发，因而不能先测 transition 再挑 structure。
- [ ] Task 10 的 `registry.py` 从 commit `39d1c14` 已验证的
  `SyntheticControlBundle/VerifiedFactory` 与 `VerifiedParentFreeze` 构造 closed registry，
  不回改 Task 9 wire，且不再接受裸 `parent_freeze_sha`。
  `entries` 顺序逐位固定为 `("full","zero","direct_sum")`，无 public register/append。
  builder/verifier 重验 bundle、factory 与完整 SHA，从 bundle 读取 source，从
  `VerifiedFactory` 内读取并冻结完整 readout，再绑定 builder/factory/mode count/parent
  freeze。V3-M0 synthetic 的 h/curv actual/ablated 四个 rank 必须由现有
  `expected_actual_rank/expected_ablated_rank` 机械复制到两种 readout，API 不接收 caller
  rank。大写 `FULL/ZERO/DIRECT_SUM` 只允许作 UI 展示标签，不能进入 wire 或 canonical SHA。
  三个 entry 的 structure evidence lane 固定为 `synthetic-classical`，不能升级 quantum。
- [ ] registry entry 递归内嵌 `ControlReadoutCalibrationSpec`。V3-M0 synthetic issuer 从
  verified factory 的 readout 维数机械构造 bit-exact identity
  h whitener、curvature incidence、curvature whitener 与
  `synthetic-identity-v1` normalizer，不接受 caller matrix；因此 h/curv 是两条独立命名但
  机械相等的冻结映射，Task 11 仍须各自重算并保存谱。
- [ ] raw `ClosedControlRegistry` 只可序列化，不是 capability。
  `build/verify_closed_control_registry` 必须重验三个 bundle、各自 `VerifiedFactory` seal、
  source/readout、rank/spec 与全部 SHA，再返回 opaque `VerifiedControlRegistry`；模块私有
  issuance seal 内绑定三个 bundle/factory snapshots。structure、rank issuer、calibrator
  每次入口重验该 seal 与 snapshots，caller 重签 raw registry 不能取得 authority。
- [ ] 三类 grid 是不可互换的 strict 类型，所有 verifier/API 均按类型拒绝错接：
  `DynamicsKGridManifest` 只允许 `exact-offset-zero-v1` 或逐轴 denominator 64、精确
  `64^d` 点的 `cartesian-full-64-v1`；`ResponseKGridManifest` 只表达 directional momentum
  shell/path；`BridgeKGridManifest.torus_denominators` 必须逐位等于
  `MeasuredTransition.spatial_shape`。三者都要求 index 非空、唯一、canonical 顺序与
  `0<=n_i<D_i`，但没有通用 grid wrapper、通用字段或裸 bridge index 参数。
  `exact-offset-zero-v1` 的唯一 wire 固定
  `torus_denominators=(1,)*d`、`reciprocal_indices=((0,)*d,)`，coverage
  的真实 principal-`l∞` torus `fill_distance=π`、全部四组
  derivative/increment=`+0.0`；仅 transition 与 metric
  support 都恰为 `{0}` 时可用。`cartesian-full-64-v1` 的唯一 wire 固定为
  `torus_denominators=(64,)*d`，indices 必须是 `range(64)^d` 的 tuple-lexicographic
  完整 Cartesian product，`fill_distance=π/64`，不得由 caller 改采样顺序或密度。
  bridge grid 也不是 caller 选择：相关 signed support 为 `{0}` 时唯一为 origin；否则
  从 origin 开始，对每个存在 `d_j!=0` 的 active axis 机械加入 `+e_j` 与 `-e_j mod L_j`，
  最后 tuple-lexicographic 去重排序。每个 active axis 必须出现非零且非 Nyquist 的点，
  因此 `L_j<3` 立即拒绝。full-state 与 source/readout bridge 都复用这一生成/验证规则。
- [ ] `FullStateBridgeSpec` 递归绑定 factory/state schema/channel order/spatial shape、
  authority 的 `parent_freeze_sha`、完整 `BridgeKGridManifest`、逐位固定为
  `macro_steps=(1,2,4)`、shape 为
  `(n_trial,n_state)` 的 frozen complex128 state trials、逐位等于 `1e-12` 的 tolerance 与
  self SHA。V3-M0 冻结 `n_trial=min(n_state,32)`：`n_state<=32` 时 tensor 必须 bit-exact
  等于 `channel_order` identity rows；`n_state>32` 时由固定
  `canonical-or-sha256-dense-v1` protocol 以 factory SHA、parent freeze 与字面量
  `v3m0-full-state-bridge-trials-v1` 派生 seed。SHA256 counter bytes 以
  `sha256-counter-u53-complex128-v1` 映射成
  `A∈C^(n_state×32)` 的 normal finite complex128 entries；固定 thin Householder
  `A=QR`，每列 phase 取其首个非零分量为正实，最后令 `V=Q†`。保存的 vectors 必须满足
  `outward||VV†-I_32||F<=1e-12`，从而 unit-norm、互异、满行秩且 conditioning 有硬界。
  spec 保存 generation ID/seed SHA/字面量 domain separator 并由 verifier 重生成；禁止
  caller 挑选、全零、重复、病态或秩不足 trial。`verify_full_state_bridge_spec` 必须接收
  opaque `VerifiedPrestructureAuthority`，重验其 parent freeze 与 spec 逐位一致。
  canonical impulse 已覆盖全基，多步 trials 只做确定性实现复核；issuer 不接受 caller
  steps/grid，并按固定 `max(t)=4` 做 work preflight。它不含
  source/readout 或 Fejér order，且 Task 10 不依赖 `ResponseRunSpec`。
- [ ] 对每个 reciprocal index `n`，固定
  `k_i=2πn_i/L_i`、`V=∏L_i`，plane-wave lift 为
  `ψ₀[c,x]=v_c exp(+i k·x)/sqrt(V)`。symbol recurrence 固定
  `v_0=v, v_{s+1}=M_full(k)v_s`，每个 requested `t` 与 executor 从同一 `ψ₀` 重跑 t 步；
  symbol 侧把 `v_t` 用同一公式 lift 回完整周期格。residual 比较两个完整
  `(channel,*spatial_shape)` 场的 C-order flatten complex128 `L2` norm，不只读原点；
  等价 demod/readback 固定为
  `v_t[c]=Σ_x exp(-ik·x)ψ_t[c,x]/sqrt(V)` 并须与 full-field 比较一致。
  `BridgeAudit.cases` 必须恰为 bridge grid × steps × trials 的 canonical 完整笛卡尔积，
  不得抽样、去重或漏掉失败 case。
- [ ] kernel 保存 full periodic tensor，shape 固定
  `(n_state,n_state,*spatial_shape)`，两端 `n_state==len(channel_order)`，原点为各空间轴
  index `0`。transition
  `support_offsets` 使用**impulse displacement** `d=x_output-x_source`，等于 factory
  stencil support 的逐项取负，取负后去重并重新按整数 tuple lexicographic 排序；数组位置
  为 `d_i mod L_i`。`support_sha` 覆盖有序 signed
  offsets、spatial shape、channel order、state-basis 与原点约定。支持集外必须为
  bit-exact complex zero。
- [ ] `transition_symbol(transition,k)` 不接受裸 kernel，按
  `M_full(k)=Σ_d K_full[d] exp(-i k·d)` 在显式 signed support 上有限求和；这与
  `source(x+offset)` 的 plane-wave 因子 `exp(+i k·offset)` 一致。`k.shape==(spatial_ndim,)`
  且 finite fp64。
- [ ] 明确禁止把矩形 `P M_full J` 命名为 `M` 或对其取幂。加入 quarter-turn 反例：
  `P M J=1`，但 `P M² J=0`，而 `(P M J)²=1`。Task 11 的 Fejér 只能对 square
  `M_full` 做多项式，最后才乘 `P/J`。
- [ ] `StructureManifest/StabilityMetricWitness/RuntimeEvidenceManifest` 都保存完整可重验
  body，不能只给 SHA。structure 在 transition 前由 opaque
  `VerifiedPrestructureAuthority` 绑定 target/state/channel；synthetic 分支的 raw
  `PrestructureAuthority` 内嵌完整 `ClosedControlRegistry` 并用 entry SHA 唯一定位 entry，
  opaque wrapper 同时持有并重验 `VerifiedControlRegistry` seal；adapter 分支内嵌完整
  strict `AdapterStructurePreregistration`；application 分支内嵌 ParentFreeze 的完整
  application spec 并绑定 pre-response permit 与 matched construction。registry/application/
  adapter 三种 authority kind 的 presence 必须三者恰一，API 不接收未定义 authority/tag。
  evidence lane 明分
  `synthetic-classical/classical-adapter/quantum`；synthetic control issuer 只能签发
  `synthetic-classical`。verifier 必须机械拒绝交叉组合：
  `synthetic-classical|classical-adapter` 当且仅当
  `structure_kind=symplectic`、`fourier_adjoint_convention=minus-k-transpose`；
  `quantum` 当且仅当 `structure_kind=unitary`、`same-k-dagger`、identity structure form、
  `G=I` 且无 `RealityCertificate`。
- [ ] classical 的 `channel_order` 长度必须为偶数；`canonical_channel_pairs` 覆盖每个
  channel 恰一次、无重复，`structure_form` 必须逐位等于这些 pair 按 channel order 导出的
  real canonical block-`J`。verifier 重算 `Ω^T=-Ω` 与非奇异性。classical factory 每个
  coefficient wire 的 imaginary IEEE-754 bits、以及实测 transition kernel 每个 imaginary
  bits 都必须是 `+0.0`；`RealityCertificate` 保存计数与完整 SHA，并机械推出
  `M(-k)=conj(M(k))`，再审计 `M(-k)^TΩM(k)=Ω`。`-0.0` 或任意非零 imaginary bit 均拒绝。
  quantum lane 固定 `canonical_channel_pairs=()`、`reality_convention_id=None`、
  `RealityCertificate=None`、`same-k-dagger-v1` 与 identity structure/metric；classical
  evidence 不能改标签升级 quantum。
- [ ] runtime manifest 对 response/certificate 实际 import closure 保存有序
  `(path,sha256)`、Python/NumPy/SciPy、BLAS/LAPACK 与 platform；fresh-process probe
  比较实际加载本地文件集合，首次调用 transition/certificate static symbol 前完成 SHA
  preflight。verifier 重算完整 closure，不接受 caller 的 evaluator/toolchain 标签。
- [ ] `StabilityMetricWitness` 只接受 constant 或显式 finite-support Laurent metric
  kernel；support/sign/Fourier 约定与 transition 相同，禁止只列任意 per-k fitted matrices。
  support 必须 canonical、中心对称；缺项按 exact zero 补齐后逐 coefficient 证明
  `G_{-d}=G_d†`。`metric_kernel.shape` 固定
  `(len(metric_support_offsets),n_state,n_state)` 且第一轴与 support 逐位对齐；这个 coefficient
  层恒等式才证明全 torus Hermitian，full-64 逐点 Hermitian 只作 diagnostic。
  verifier 在 covered torus 重算 `G(k)=G(k)†≻0`、`tr(G(0))=n`、
  `λ_min(G)>=1e-12`、`κ₂(G)<=1e8`。稳定硬门是覆盖全 torus 后
  `||G^{-1/2}(M†GM-G)G^{-1/2}||₂<=1e-12`；辛性只证明 canonical structure，不代替 power bounded。
  对 normalized `δ<1`，二次型逐步界固定为
  `(1-δ)^t G ≼ (M^t)†GM^t ≼ (1+δ)^t G`，故保存
  完整 `PowerDriftAudit`。其唯一算法
  `t16384-directed-repeated-squaring-v1` 对 `δ>0` 从 outward `1±δ` 开始各做恰好 14 次定向平方，
  得到 `growth_upper=(1+δ)^16384-1` 与
  `contraction_upper=1-(1-δ)^16384`，再取 `drift_upper=max(...)<=1e-8`。
  `δ=0` 走不执行任何 squaring 的 exact identity branch：
  `delta/growth/contraction/drift=+0.0`，`one_minus/one_plus=1.0`，
  `executed_squaring_count=0`；非零分支该字段为 14。禁止调用
  `pow/exp`。`δ<=1e-12` **不自动推出**该门通过，少/多一次平方或 round-nearest 临界
  假 PASS 都拒绝。
  谱半径漂移仅在 raw eig diagnostic available 时定义为 full-64 raw grid 上
  `max_k|ρ(M(k))-1|`，否则为 `None`；两者都不进入全环面硬门。
  必须加入精确 Jordan 负控 `[[1,1],[0,1]]`：虽辛、可逆且 `ρ=1`，仍因不存在合格正定
  invariant metric 而 FAIL。零偏移 quarter-turn controls 固定 `G=I`。
- [ ] structure 与 `M†GM-G` residual 必须在 signed finite-Laurent coefficient 层做
  卷积，分别签发两个 `LaurentResidualCertificate`。每证书至少保存 operand SHA、
  `spatial_ndim/n_state`、Fourier 约定、matrix norm=`spectral-2-v1`、momentum supremum
  方法、完整 residual coefficients/support、每次 convolution pair count；
  `coefficients.shape=(len(support_offsets),n_state,n_state)` 且第一轴逐位对齐。冻结
  operand SHA 顺序：structure 为
  `(transition_sha,structure_manifest_sha,structure_form.tensor_sha)`，metric 为
  `(transition_sha,stability_metric.witness_sha,metric_kernel.tensor_sha)`，不得排序成 set。
  两份 `LaurentResidualCertificate` 都只保存 raw coefficient enclosure 与 raw global
  bound；schema 中不存在 coverage-dependent denominator 或 normalized 字段。
  冻结
  `M(k)=Σ_dK_d e^{-ik·d}` 下的 adjoint coefficients：
  same-k dagger 为 `A_r=K_{-r}†`，minus-k transpose 为 `A_r=K_{-r}^T`。classical residual
  依次卷积 `A*Ω`、再与 `K` 卷积并减 offset-zero `Ω`；quantum 依次卷积 `A*I`、再与 `K`
  并减 `I`；metric residual 必须先 `A*G`、再与 `K`，最后逐 support 减 `G`。每阶段先
  canonical Minkowski support，再做 shape-aligned matrix product/sum，禁止利用 zero-offset
  control 简化代码路径。随后用
  每个最终 coefficient 另保存与 support 逐位对齐的
  `coefficient_roundoff_frobenius_uppers`：每个 complex dot 的 real/imag component 都按
  冻结 scalar expression DAG 逐操作重放，`q=4n-1`（或 schema 明示且证明更保守的 q），
  用 `γ_q·Σ|real products| + real_op_count·minsub`，绝不使用
  `γ_n·|最终 cancellation result|`。两阶段卷积及最终 subtraction 的全部
  roundoff 向外累计成 `ε_r`。硬界固定为
  `sup_k||R(k)||₂<=Σ_r(outward||R̂_r||F+ε_r)`；证书的
  `coefficient_frobenius_upper_sum` 与 raw global bound 必须逐位等于该 directed-outward
  sum。exact arithmetic 路径也必须保存 bit-exact `+0.0` 的 `ε_r`，不得默认为遗漏。
  不得用逐因子 derivative 三角界乘
  任何 `SpectralMarginCoverage` distance/increment 去冒充硬证明。
  metric raw bound 记 `B_R=Σ_r(outward||R̂_r||F+ε_r)`。只有
  `SpectralMarginCoverage` 已成功得到正的 `covered_g_lambda_min_lower` 后，才另签
  `NormalizedMetricResidualAudit`，用 frozen outward division 计算
  `δ=B_R/covered_g_lambda_min_lower`。structure raw upper 与 `δ` 均要求 `<=1e-12`；
  不得把 raw `B_R` 直接称 normalized residual，也不得让 Laurent 证书反向依赖 coverage。
- [ ] full-64 grid + finite-support derivative/fill-distance 只覆盖有大 margin 的谱量：
  transition singular min/max 与 metric eigenvalue min/max。`SpectralMarginCoverage`
  保存四份 Optional raw diagnostic arrays 及 typed available/unavailable state、
  四组逐轴 derivative bounds、
  `principal-linf-torus-v1` distance/fill distance、四个 increments、covered extrema、
  两个 condition upper bounds 与 self SHA；它不含 residual，也不得签发 residual verdict。
  `exact-offset-zero-v1` 只在相关 transition/metric support 均为 `{0}` 时允许，真实
  principal-`l∞` fill upper 是 root table 的 outward `π_hi`，point_count 恰为 1 且只含
  origin；因为所有 `d_j=0`，四组 derivative 与 coverage increment 仍须 bit-exact
  `+0.0`。强塞 full-64 sidecar、把 fill 写 `+0.0` 或非零 support 用 singleton 都拒绝。
  非零 support 才使用 full-64 与 outward `π_hi/64`。covered transition
  `σ_min>=1e-8, κ₂<=1e8`，covered metric
  `λ_min>=1e-12, κ₂(G)<=1e8` 才能签发。
- [ ] derivative/enclosure 公式冻结：对 transition 每轴
  `L^M_j=Σ_d |d_j|·||K_d||₂`，对 metric 每轴
  `L^G_j=Σ_d |d_j|·||G_d||₂`；full-64 principal-`l∞` fill distance
  coefficient norm 一律用 directed-outward Frobenius upper bound（它上界 spectral-2），
  raw LAPACK spectral norm 只作 diagnostic。full-64 principal-`l∞` fill distance
  `h∞=π/64`，对应 increment 为 `Δ_M=h∞Σ_jL^M_j`、
  `Δ_G=h∞Σ_jL^G_j`。机械 covered extrema 为
  `σmin_lower=min_grid hard_σmin_lower-Δ_M`、
  `σmax_upper=max_grid hard_σmax_upper+Δ_M`、
  `λmin_lower=min_grid hard_λmin_lower-Δ_G`、
  `λmax_upper=max_grid hard_λmax_upper+Δ_G`，
  condition upper 分别为 upper/lower；lower 非正立即 FAIL。所有 coefficient norms、
  sums、increments 与 extrema 使用 `nextafter` directed outward rounding 的保守 enclosure，
  raw nearest-rounding 值与 outward bound 同时保存。exact-offset-zero 的 `L/Δ` 必须
  bit-exact `+0.0`。
- [ ] 每个 grid point 的 scalar enclosure 按 grid 顺序保存到唯一
  `SpectralPointEnclosureColumnarSidecar`；`SpectralGridPointEnclosureWitness` 不是 wire，
  不创建 262144 个 Python object。所有 column 长度必须逐位等于 `point_count`，sidecar
  自哈希使用 streaming canonical encoder，并在证据 bytes cap 内逐 column 重算。
  candidate `B/L/B_L` 由固定
  `scalar-gauss-jordan-hermitian-cholesky-v1` 从该点 raw `M/G` 逐点生成：inverse 使用
  scalar Gauss–Jordan（partial-pivot 并列取最小 index），metric 经 Hermitian gate 后用
  fixed-order scalar Cholesky 与 triangular inverse；不常驻/序列化；
  sidecar 只按 grid 顺序保存 algorithm ID 与 scalar norms/residuals/hard bounds。verifier
  逐点重建 candidates、重算 scalars 后立即释放，证据为 `O(n_k)` scalars、临时内存
  `O(n_state²)`，禁止 `O(n_k*n_state²)` candidate tensor artifact。LAPACK/SVD/eigh 只作
  Optional raw diagnostic；LAPACK/SVD/eigh 不可用时四列/arrays 必须同时为 `None` 并保存
  typed unavailable reason，monkeypatch 抛错不得影响 hard sidecar。`Fp64EnclosureProtocol`
  固定 `u=2^-53`、`minsub=2^-1074`；complex add 恰计 2 个 real add，complex multiply
  恰计 4 个 real
  multiply+2 个 real add；长度 n complex dot 的每个 real component 绑定实际 scalar DAG，
  q 固定为 `4n-1`，并保存 absolute-product sum 与 minsub additive。
  `γ_q=q·u/(1-q·u)` 的 fp64 upper 必须用 `as_integer_ratio` 交叉乘验证，少一 ulp 拒绝；
  minsub 同样以整数 dyadic body验证。hard lane 允许 finite normal 或 signed zero；
  nonzero subnormal/FTZ、overflow、NaN/Inf 立即 unresolved。只有 semantic canonical
  zero 字段要求 `+0.0` bits，一般算术中间值允许 `±0.0`。
  禁止 FMA、reassociation、extended precision；每个 scalar op 后按方向执行
  `nextafter`，不能只在最终 norm 外扩。full-64 symbol phase
  只能取完整内嵌、自哈希的 `root64-dyadic-machin-taylor-containment-v1` table；
  verifier 不调用 libm，而用 P=192 bigint dyadic，
  `π=16atan(1/5)-4atan(1/239)` alternating remainder 得 π bracket；再把 index 降到
  八分圆 `r∈[0,8]`，对 `x=rπ/32` 以 sin/cos alternating Taylor 做 containment，并证明
  stored interval 落在 center 的 RN rounding cell。runtime `sin/cos/exp` 禁用，
  negative displacement root 必须按 mod-64 机械处理。每项还保存真 root interval 到
  fp64 center 的 complex-distance upper；该 upper 的平方与 fp64 upper 必须以 exact
  integer-ratio square containment 复核。
  finite-support complex multiply/add 的 operation-count enclosure，加上
  `Σ_d outward||K_d||F·ε_root(d)` 与
  `Σ_d outward||G_d||F·ε_root(d)` 的 phase representation input error，分别累计为每点
  `η_M/η_G` 并保存在 witness。exact-offset-zero 的 phase 与 sum 均为 exact，两个
  `η` 必须 bit-exact `+0.0`。对 `M` 用 candidate
  inverse `B` 与同一 scalar-DAG/`γ_q` absolute-product-sum
  roundoff enclosure重算时，`e` 必须包含 symbol 输入误差项
  `outward||B||F·η_M`；仅 `e<1` 时取
  `hard_σmin_lower=(1-e)/outward||B||F`，并取
  `hard_σmax_upper=outward||M_fl||F+η_M`。对 `G` 保存 candidate Cholesky `L` 与
  `B_L`，同样重算 `E_G=G_fl-LL†`、`E_L=I-B_LL` 的 outward Frobenius upper，其中
  `E_G` bound 必须再加 `η_G`；令
  `ell=(1-||E_L||F)/outward||B_L||F`，仅在 `||E_L||F<1` 且
  `hard_λmin_lower=ell²-||E_G||F>0` 时接受。`cholesky_inverse_frobenius_upper` 必须显式
  保存 `outward||B_L||F`；`hard_λmax_upper`
  上界固定为 `outward||G_fl||F+η_G`。所有 Frobenius hard upper 的 sqrt 都必须使用
  `exact-integer-ratio-square-containment-v1`，证明 stored fp64 upper 的平方覆盖被开方
  dyadic upper；仅最终 `sqrt` 后 `nextafter` 不足。symbol error、residual bounds、method ID 与
  sidecar SHA 全保存，point 数与 qualification grid 逐位一致。exact-zero support 的
  `spectral_diagnostic_grid` 也只能是同一 origin singleton，常值使它即覆盖全 torus；
  非零 support 时 qualification/diagnostic grid 都是同一 lexicographic full-64 grid。
  available 时 spectral-radius diagnostic 只从这个机械 active grid 重算；unavailable 时
  必须为 `None`。
- [ ] `DynamicsCertificate` 必须从 verified factory、重测 transition 和上述完整 manifests
  逐项重算，并递归内嵌 raw `PrestructureAuthority`、
  `FullStateBridgeSpec+BridgeAudit`、两个 Laurent residual 与
  `SpectralMarginCoverage`，输出 opaque `VerifiedDynamicsCertificate`。
  `certify_transition_dynamics/verify_dynamics_certificate` 每次都通过真实
  `apply_factory_step` 重测 canonical impulses，并按完整 full-state spec 重复 executor，
  同时要求 caller 提供匹配的 opaque `VerifiedPrestructureAuthority`，重验 registry
  seal/bundle/factory 或 adapter prereg、authority SHA 与 structure binding，再重算每个
  bridge case 与全部证书字段；`normalized_max<=1e-12` 才能签发。token 不是
  真值来源，Task 10 对任何 Fejér/source run spec 都无依赖。
  `qualification.py` 的 public success issuer
  `qualify_ablation_from_certificate` 只接受绑定 construction
  中 ablated factory 的该对象，生成完全 raw、可序列化的
  `CertificateBackedQualificationOutcome` 并返回另行 seal 的 opaque capability。跨进程
  verifier 的 `outcome` 参数必须是该 raw outcome，不得传旧 opaque object；它连同 raw
  construction 与 `VerifiedDynamicsCertificate` 逐 body 重建新 opaque capability，response
  consumer 只接受该 wrapper。
  Task 10 上线前 Task 9 对
  successful construction 必须完全 fail-closed；不存在可导入 capability 或 caller
  bool→defined 的路径。actual 与 ablated 各有独立 certificate。
- [ ] certification 返回 typed `DynamicsCertificationOutcome`，不以异常或缺失证书暗示
  科学失败。raw outcome 只含可 canonical-serialize 的 optional raw
  `DynamicsCertificate`，其 self SHA 不覆盖 opaque object；`certify/verify` 返回
  `VerifiedDynamicsCertificationOutcome`，内部 seal 才持有 optional
  `VerifiedDynamicsCertificate` capability。成功当且仅当 `status.defined=True`、failure
  为空、raw certificate 非空且 opaque capability 非空；失败时保留 self-hash attempt audit、
  typed failure，两个 certificate view 都为 `None`。首错顺序冻结为 prestructure →
  transition → reality → allocation/resource preflight → mechanical exact-Jordan profile
  attempt → structure raw construction/resource → structure raw upper →
  metric raw construction/resource → spectral coverage → normalized metric residual →
  full-state bridge → power drift。reason 映射为：prestructure→`PRESTRUCTURE_INVALID`，
  transition→`MANIFEST_MISMATCH`，reality→`REALITY_VIOLATION`，resource→
  `LAURENT_RESOURCE_EXCEEDED`；structure raw upper 超阈或 enclosure
  失败一律→`STRUCTURE_UNRESOLVED`，不存在无 lower/counter-witness 支持的“actual
  canonical violation”路径。full-state bridge 实测超差→`DYNAMICS_BRIDGE_FAILED`。
  metric raw enclosure、inverse/Cholesky candidate、spectral coverage、normalized
  metric 或 power upper 不充分都只→`STABILITY_UNRESOLVED`，不得反推
  真实不稳定。Jordan attempt 不接受 caller witness：issuer 在重测 transition 后机械检查
  唯一 exact profile；完整匹配立即返回
  `CERTIFIED_INSTABILITY_COUNTERWITNESS/UNSTABLE`，不先落入 metric unresolved；不匹配则
  attempt witness=`None` 并继续普通 pipeline。`UNSTABLE` 路径必须内嵌并
  重验完整 `InstabilityGrowthCounterWitness`，而不能由“证书未签发”推出。V3-M0 v1
  仅接受 exact-zero-offset、`n_state=2`、bit-exact
  `[[1,1],[0,1]]`、`t=16384`、`v=e2`；verifier 重测 transition，逐位验证
  `J=I+N,N²=0,N≠0`，先以整数二项式证明对所有整数 t 有 `J^t=I+tN`，再验证
  `M^t=[[1,t],[0,1]]`、`||M^t v||²=t²+1=268435457` 且严格大于
  `(1+1e-8)·1e8=100000001`。attempt 中 witness 非空当且仅当 failure 为
  `CERTIFIED_INSTABILITY_COUNTERWITNESS`，其他 failure 必须为 `None`。
  attempt audit 中未到达阶段必须为 `None`，已到达阶段保存 raw evidence；
  status/failure/reason/certificate presence 任一不一致都拒绝。
- [ ] 模块 DAG 固定为
  `parent_freeze→registry→prestructure→dynamics`：`prestructure.py` import
  registry+ablation/factory，`dynamics.py` 只 import prestructure 而不 import ablation，
  `qualification.py` 才同时 import ablation 与 dynamics，response/endpoint 再消费
  qualification；不存在 import-time callback registration。raw `PrestructureAuthority`
  不得嵌含 `VerifiedFactory` 的 `AblationPair`，只能递归内嵌完全可序列化
  `AblationPairSnapshot`（两份完整 raw factory、raw manifest、construction status/SHA）；
  opaque seal 私下另绑 live verified pair/role。所有 snapshot/authority SHA 覆盖完整 body。
  qualification issuer/hydrator 同时重验 exact construction 与 certificate 的 live opaque
  identity，不使用 import callback、registration、raw-field port、caller bool/SHA/token
  或测试 success hook。Task 9 public `build_dynamics_report` 仍只能构造 raw wire，
  `verify_verified_dynamics_report` 在缺少 certificate-backed live identity 时 fail-closed。
  fresh-interpreter 测试分别按所有 import
  顺序导入 ablation/dynamics/qualification/response，断言无 cycle、无先占注册、无
  public success issuer；反射复制、object-new、过期 weakref、raw 重签全部拒绝。
- [ ] full-state `BridgeAudit` 对每个 `(k,t,trial)` 固定
  `raw=||y_exec-y_symbol||₂`、
  `scale=max(1,||y_exec||₂,||y_symbol||₂)`、
  `normalized=raw/scale`，逐 case 与 maxima 全保存、自哈希并重算；
  `normalized_max<=1e-12` 才 PASS，raw max 仍作为诊断保留。audit 统一绑定
  `bridge_spec_sha` 与 `FullStateBridgeSpec`。Task 11 source/readout 使用独立的
  dimensional `SourceReadoutBridgeAudit`，不得复用该 normalized scalar schema。
- [ ] 先测单位 delta kernel、support SHA、逐轴无 wrap 条件 `L_i>2r_i`。结构支撑展开
  受冻结 steps/cardinality 上限保护，超限 fail-closed，禁止内存无界增长。Laurent
  resource preflight 在任何候选 support/coefficient tensor 分配前逐次执行：每次卷积
  `len(left_support)*len(right_support)<=1_000_000`、每个结果 support `<=100_000`、
  coefficient entries `<=16_777_216`、每阶段 arithmetic work
  `stage_pair_count*n_state^3<=2_000_000_000`、momentum evaluation work
  `n_k*n_support*n_state^2<=2_000_000_000`。`M†GM` 的两次卷积必须分别预检；禁止先构造
  第一或第二个大数组再检查下一阶段。另冻结并在
  分配/循环前检查：transition complex entries `<=16_777_216`、任一 k-grid points
  `<=262_144`、spectral cubic work
  `3*n_k*n_state^3<=2_000_000_000`、bridge k points `<=64`、
  bridge executor work
  `n_k*n_trial*max(macro_steps)*prod(spatial_shape)*n_primitive
  <=2_000_000_000`（禁止用 `len(steps)` 或漏掉 volume/primitive 低估）、shell projector
  complex entries `<=16_777_216`、Fejér/Schur 工作估计
  `n_k·(n_state^3+T·n_state)<=2_000_000_000`、columnar raw bytes `<=67_108_864`、
  完整 spectral canonical body `<=100_663_296` bytes、其他任一 canonical evidence body
  `<=268_435_456` bytes。sidecar 必须 streaming encode/hash，禁止为算 SHA 物化逐点
  dict/object list。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_dynamics tests.test_v3m0_import_dag -v
  ```

  预期为 `rulespace_v3.dynamics`/`rulespace_v3.qualification` 缺失。
- [ ] 数值 RED 必须逐项覆盖：coverage verifier 触碰 raw residual canary 即失败，证明
  无反向依赖；raw/coverage SHA、`λ_lo/δ` 任一篡改拒绝；zero-support 的 fill=`+0.0`、
  多点 sidecar 或非零 support singleton 拒绝；column 长度/顺序/byte count/strict base64/
  point count、`||B_L||F` 缺失或任一 `e/E_G/ell` inward 1 ulp 拒绝。root64 任一 dyadic
  endpoint/center-distance/phase-to-symbol propagation inward 1 ulp、center bits、相位符号
  或 mod-64 错误拒绝，且 monkeypatch `sin/cos/exp` 抛错仍通过；dot q/DAG/absolute-product
  sum/minsub additive、任一 Frobenius sqrt containment inward 1 ulp、FTZ/overflow/nonzero
  subnormal/bool-as-int 拒绝，signed zero 通过且 semantic zero 只收 `+0.0`。raw diagnostics
  unavailable 时 hard sidecar 仍通过且四列/arrays 同时为 `None`。
  `δ=0` 必须是 delta/growth/contraction/drift=`+0.0`、one-minus/plus=`1.0`、
  executed squaring=0；非零分支少/多一次平方拒绝。Jordan 缺 universal integer
  witness、`N=0/N²!=0`、float wire、错误 t/vector/norm 只能
  `STABILITY_UNRESOLVED`，完整 matching fixture 必须在 ordinary metric 前返回 `UNSTABLE`。
  dense trial 改 1 bit、重复/零/降秩、Frobenius Gram upper inward 1 ulp 拒绝，monkeypatch
  `np.linalg.qr/SVD/eigh` 抛错仍通过；bridge case
  漏项/重排/重复、只对 origin 而全场错误、错误 denominator 与按 `len(steps)` 低估均拒绝。
  所有 cap 测试用 mock allocation 证明在第一次 `np.empty` 前失败。
- [ ] 测一步 full-state plane-wave symbol bridge，以及冻结随机向量上的重复 executor
  与 `M_full(k)^t` 对拍，归一化残差均 `≤1e-12`。verified source/readout 的
  `P M_full(k)^t J` 对拍只属于 Task 11，不进入 dynamics certificate。
- [ ] 对 Task 9 接线做 authority 回归：raw/self-hashed report、伪 certificate、错误
  factory/transition/structure/reality/Laurent residual/spectral margins/runtime、篡改
  opaque certificate 均不能签发
  defined qualification；只有 `verify_dynamics_certificate` 完整重算后的 ablated
  certificate 可解锁。普通 metric/spectral/power 证书失败必须返回
  `STABILITY_UNRESOLVED`；精确 Jordan shear 只有在完整解析增长 counter-witness 被重验后
  才返回 `UNSTABLE`。
- [ ] 明确：`L>2r` 只约束 impulse kernel；多步 plane-wave 对拍不错误要求
  `L>2Tr`，但只允许上述周期相容 k。
- [ ] 所有数组入口强制 NumPy `complex128`，非 fp64 返回错误。
- [ ] GREEN：重复定向测试，预期 bridge 残差 `≤1e-12` 且全部 `ok`。
- [ ] 提交：`feat(v3m0): measure fp64 full-state transitions`

## Task 11：Fejér 窗与阈值联合冻结

**文件：**

- Modify: `rulespace_v3/contracts.py`
- Modify: `tests/test_v3m0_contracts.py`
- Create: `rulespace_v3/response.py`
- Create: `tests/test_v3m0_response.py`
- Create: `rulespace_v3/window.py`
- Create: `rulespace_v3/thresholds.py`
- Create: `tests/test_v3m0_window_thresholds.py`

**冻结规则：**

```python
T_CANDIDATES = (256, 512, 1024, 2048, 4096, 8192)
# 16384 only compares 2T for T=8192
def phase_grid_step(T: int) -> float:
    return 2 * np.pi / (16 * T)

def phase_separation_min(T: int) -> float:
    return 8 * np.pi / T

OVERLAP_MARGIN_MIN = 0.2
PROJECTOR_T2T_MAX = 1e-10
SHELL_PARTICIPATION_MIN = 0.25
SHELL_PROJECTOR_RESIDUAL_MAX = 1e-12
SHELL_LOOP_RESIDUAL_MAX = 1e-10
SIGNAL_NOISE_RATIO_MIN = 1e3
RAW_GAP_MIN = 1e3

@dataclass(frozen=True)
class ControlWindowProtocolEntry:
    control_id: Literal["full", "zero", "direct_sum"]
    control_registry_entry_sha: str
    response_grid: ResponseKGridManifest
    source_readout_bridge_grid: BridgeKGridManifest
    source_readout_bridge_steps: tuple[int, ...]
    reference_reciprocal_index: tuple[int, ...]
    expected_shell_rank: int
    expected_shell_rank_source_id: Literal[
        "parent-freeze-control-application-spec-v1"
    ]
    preregistered_phase_bands: tuple[tuple[float, float], ...]
    source_trial_generation_id: Literal["registry-source-identity-v1"]
    entry_sha: str

@dataclass(frozen=True)
class WindowCalibrationProtocol:
    protocol_schema_version: str
    control_registry_sha: str
    parent_freeze_sha: str
    t_candidates: tuple[int, ...]
    control_entries: tuple[ControlWindowProtocolEntry, ...]
    phase_grid_protocol_id: Literal["two-pi-over-16T-v1"]
    phase_separation_protocol_id: Literal["eight-pi-over-T-v1"]
    participation_min_required: float
    overlap_margin_required: float
    loop_residual_max: float
    projector_residual_max: float
    protocol_sha: str

class VerifiedWindowCalibrationProtocol:
    """Opaque；在任何 transition-derived shell 前绑定 registry seal 与唯一 protocol。"""

    @property
    def protocol(self) -> WindowCalibrationProtocol: ...

def build_window_calibration_protocol(
    registry: VerifiedControlRegistry,
    control_entries: tuple[ControlWindowProtocolEntry, ...],
) -> VerifiedWindowCalibrationProtocol: ...
def verify_window_calibration_protocol(
    protocol: WindowCalibrationProtocol,
    registry: VerifiedControlRegistry,
) -> VerifiedWindowCalibrationProtocol: ...

@dataclass(frozen=True)
class ResponseRunSpec:
    run_spec_schema_version: str
    run_spec_id: str
    window_protocol_sha: str
    control_registry_entry_sha: str
    fejer_order: int
    state_schema_id: str
    channel_order: tuple[str, ...]
    source_basis: BasisManifest
    readout_basis: BasisManifest
    spatial_shape: tuple[int, ...]
    response_grid: ResponseKGridManifest
    source_readout_bridge_grid: BridgeKGridManifest
    source_readout_bridge_steps: tuple[int, ...]
    source_trial_vectors: FrozenComplexTensor
    bridge_tolerance: float
    spec_sha: str

@dataclass(frozen=True)
class EndpointReferenceSpec:
    reference_spec_schema_version: str
    window_protocol_sha: str
    control_registry_entry: ControlRegistryEntry
    actual_factory_sha: str
    actual_transition_sha: str
    actual_dynamics_certificate_sha: str
    candidate_fejer_order: int
    reference_reciprocal_index: tuple[int, ...]
    preregistered_phase_bands: tuple[tuple[float, float], ...]
    expected_shell_rank: int
    expected_shell_rank_source_id: Literal[
        "parent-freeze-control-application-spec-v1"
    ]
    reference_spec_sha: str

@dataclass(frozen=True)
class EndpointReferenceProjector:
    reference_schema_version: str
    control_registry_entry_sha: str
    actual_transition_sha: str
    actual_dynamics_certificate_sha: str
    reference_reciprocal_index: tuple[int, ...]
    reference_phase: float
    projector_coordinate_convention_id: Literal["g-whitened-state-v1"]
    rank: int
    projector: FrozenComplexTensor
    reference_sha: str

class EndpointReferenceFailure(str, Enum):
    PHASE_BAND_EMPTY = "phase_band_empty"
    PHASE_BAND_NONUNIQUE = "phase_band_nonunique"
    RANK_MISMATCH = "rank_mismatch"
    PARTICIPATION_FAILED = "participation_failed"
    RUNNER_UP_MARGIN_FAILED = "runner_up_margin_failed"
    PROJECTOR_INVALID = "projector_invalid"

@dataclass(frozen=True)
class EndpointReferenceAttemptAudit:
    attempt_schema_version: str
    reference_spec: EndpointReferenceSpec
    candidate_phases: tuple[float, ...]
    candidate_ranks: tuple[int, ...]
    expected_shell_rank: int
    expected_shell_rank_source_id: Literal[
        "parent-freeze-control-application-spec-v1"
    ]
    candidate_participations: tuple[float, ...]
    runner_up_overlaps: tuple[Optional[float], ...]
    hermitian_residuals: tuple[float, ...]
    idempotent_residuals: tuple[float, ...]
    g_invariance_residuals: tuple[float, ...]
    eigenphase_residuals: tuple[float, ...]
    observed_competitor_gaps: tuple[Optional[float], ...]
    attempt_sha: str

@dataclass(frozen=True)
class EndpointReferenceOutcome:
    status: BlockStatus
    failure: Optional[EndpointReferenceFailure]
    reference_spec: EndpointReferenceSpec
    attempt_audit: EndpointReferenceAttemptAudit
    reference: Optional[EndpointReferenceProjector]
    outcome_sha: str

class VerifiedEndpointReferenceOutcome:
    """Opaque；seal 绑定 actual M/G 重算与 raw reference outcome。"""

    @property
    def outcome(self) -> EndpointReferenceOutcome: ...

    @property
    def reference(self) -> Optional[EndpointReferenceProjector]: ...

@dataclass(frozen=True)
class EndpointShellSpec:
    shell_spec_schema_version: str
    window_protocol_sha: str
    control_registry_entry: ControlRegistryEntry
    response_grid: ResponseKGridManifest
    preregistered_phase_bands: tuple[tuple[float, float], ...]
    candidate_fejer_order: int
    endpoint_reference_projector: EndpointReferenceProjector
    extraction_protocol_id: Literal[
        "endpoint-single-node-reference-v1"
    ]
    shell_spec_sha: str

@dataclass(frozen=True)
class ShellPointAudit:
    reciprocal_index: tuple[int, ...]
    momentum_path_id: str
    momentum_path_position: int
    shell_phase: float
    rank: int
    hermitian_residual: float
    idempotent_residual: float
    g_invariance_residual: float
    eigenphase_residual: float
    participation: float
    nearest_competitor_gap: Optional[float]
    reference_overlap: float
    runner_up_overlap: Optional[float]
    predecessor_overlap: Optional[float]
    loop_residual: Optional[float]

@dataclass(frozen=True)
class EndpointShellManifest:
    shell_schema_version: str
    actual_factory_sha: str
    actual_transition_sha: str
    actual_dynamics_certificate_sha: str
    shell_spec: EndpointShellSpec
    dt: float
    eigenphase_convention_id: Literal["lambda-exp-plus-i-theta-principal-v1"]
    shell_phases: tuple[float, ...]
    shell_projectors: FrozenComplexTensor
    point_audits: tuple[ShellPointAudit, ...]
    hermitian_residual_max: float
    idempotent_residual_max: float
    g_invariance_residual_max: float
    eigenphase_residual_max: float
    participation_min: float
    nearest_competitor_gap_min: Optional[float]
    reference_overlap_min: float
    runner_up_overlap_max: Optional[float]
    predecessor_overlap_min: Optional[float]
    overlap_margin_min: Optional[float]
    loop_residual_max: Optional[float]
    ambiguous: bool
    shell_manifest_sha: str

class EndpointShellFailure(str, Enum):
    PHASE_BAND_EMPTY = "phase_band_empty"
    PHASE_SEPARATION_FAILED = "phase_separation_failed"
    GAP_FAILED = "gap_failed"
    PARTICIPATION_FAILED = "participation_failed"
    REFERENCE_AMBIGUOUS = "reference_ambiguous"
    RUNNER_UP_MARGIN = "runner_up_margin"
    LOOP_INCONSISTENT = "loop_inconsistent"
    PROJECTOR_INVALID = "projector_invalid"

@dataclass(frozen=True)
class ShellCandidatePointAttempt:
    reciprocal_index: tuple[int, ...]
    momentum_path_id: str
    momentum_path_position: int
    candidate_phases: tuple[float, ...]
    candidate_ranks: tuple[int, ...]
    candidate_participations: tuple[float, ...]
    candidate_reference_overlaps: tuple[float, ...]
    candidate_predecessor_overlaps: tuple[Optional[float], ...]

@dataclass(frozen=True)
class EndpointShellAttemptAudit:
    attempt_schema_version: str
    shell_spec: EndpointShellSpec
    point_attempts: tuple[ShellCandidatePointAttempt, ...]
    projector_residual_max: float
    loop_residual_max_observed: Optional[float]
    attempt_sha: str

@dataclass(frozen=True)
class EndpointShellOutcome:
    status: BlockStatus
    failure: Optional[EndpointShellFailure]
    reference_outcome: EndpointReferenceOutcome
    attempt_audit: EndpointShellAttemptAudit
    shell: Optional[EndpointShellManifest]
    outcome_sha: str

class VerifiedEndpointShellOutcome:
    """Opaque；递归绑定 verified reference 与 shell executor 重算。"""

    @property
    def outcome(self) -> EndpointShellOutcome: ...

    @property
    def shell(self) -> Optional[EndpointShellManifest]: ...

@dataclass(frozen=True)
class SourceFrameCoverageCertificate:
    certificate_schema_version: str
    source_basis_sha: str
    trial_matrix: FrozenComplexTensor
    frame_operator_lower: float
    canonical_dual_residual_upper: float
    certificate_sha: str

@dataclass(frozen=True)
class SourceReadoutBridgeMatrixAudit:
    reciprocal_index: tuple[int, ...]
    macro_steps: int
    raw_difference_matrix: FrozenComplexTensor
    frame_coverage: Optional[SourceFrameCoverageCertificate]
    raw_frobenius_upper: float
    raw_operator_norm_upper: float
    h_whitened_operator_error_upper: float
    curv_whitened_operator_error_upper: float

@dataclass(frozen=True)
class SourceReadoutBridgeAudit:
    bridge_schema_version: str
    branch: Literal["actual", "matched_ablated"]
    factory_sha: str
    transition_sha: str
    dynamics_certificate_sha: str
    run_spec_sha: str
    source_metric_whitener_sha: str
    readout_calibration_spec_sha: str
    matrix_audits: tuple[SourceReadoutBridgeMatrixAudit, ...]
    h_operator_error_max: float
    curv_operator_error_max: float
    bridge_sha: str

@dataclass(frozen=True)
class SourceReadoutResponse:
    response_schema_version: str
    branch: Literal["actual", "matched_ablated"]
    factory_sha: str
    transition_sha: str
    dynamics_certificate_sha: str
    source_basis: BasisManifest
    readout_basis: BasisManifest
    run_spec_sha: str
    shell_manifest_sha: str
    bridge_audit: SourceReadoutBridgeAudit
    values: FrozenComplexTensor
    response_sha: str

@dataclass(frozen=True)
class PairedFilteredResponse:
    pair_schema_version: str
    ablation_manifest_sha: str
    qualification_sha: str
    actual_dynamics_certificate: DynamicsCertificate
    ablated_dynamics_certificate: DynamicsCertificate
    actual: SourceReadoutResponse
    ablated: SourceReadoutResponse
    run_spec: ResponseRunSpec
    shell_manifest: EndpointShellManifest
    pair_sha: str

class PairedResponseFailure(str, Enum):
    QUALIFICATION_INVALID = "qualification_invalid"
    INPUT_BINDING_INVALID = "input_binding_invalid"
    ACTUAL_RESPONSE_FAILED = "actual_response_failed"
    ABLATED_RESPONSE_FAILED = "ablated_response_failed"
    ACTUAL_BRIDGE_FAILED = "actual_bridge_failed"
    ABLATED_BRIDGE_FAILED = "ablated_bridge_failed"

@dataclass(frozen=True)
class SourceReadoutBranchAttemptAudit:
    branch: Literal["actual", "matched_ablated"]
    response_values: Optional[FrozenComplexTensor]
    bridge_audit: Optional[SourceReadoutBridgeAudit]
    failure: Optional[PairedResponseFailure]
    attempt_sha: str

@dataclass(frozen=True)
class PairedResponseAttemptAudit:
    attempt_schema_version: str
    window_protocol: WindowCalibrationProtocol
    qualification_sha: str
    actual_factory_sha: str
    ablated_factory_sha: str
    actual_transition: MeasuredTransition
    ablated_transition: MeasuredTransition
    actual_dynamics_certificate: DynamicsCertificate
    ablated_dynamics_certificate: DynamicsCertificate
    run_spec: ResponseRunSpec
    shell_outcome: EndpointShellOutcome
    actual_branch_attempt: Optional[SourceReadoutBranchAttemptAudit]
    ablated_branch_attempt: Optional[SourceReadoutBranchAttemptAudit]
    first_failure: Optional[PairedResponseFailure]
    attempt_sha: str

@dataclass(frozen=True)
class PairedResponseOutcome:
    status: BlockStatus
    failure: Optional[PairedResponseFailure]
    attempt_audit: PairedResponseAttemptAudit
    paired_response: Optional[PairedFilteredResponse]
    outcome_sha: str

class VerifiedPairedResponseOutcome:
    """Opaque；失败也递归保留 protocol/spec/shell 与已到达 branch evidence。"""

    @property
    def outcome(self) -> PairedResponseOutcome: ...

    @property
    def paired_response(self) -> Optional[PairedFilteredResponse]: ...

@dataclass(frozen=True)
class ExpectedRankDeclaration:
    declaration_schema_version: str
    control_registry_sha: str
    control_registry_entry_sha: str
    control_id: Literal["full", "zero", "direct_sum"]
    expected_h_actual_rank: int
    expected_h_ablated_rank: int
    expected_curv_actual_rank: int
    expected_curv_ablated_rank: int
    parent_freeze_sha: str
    declaration_sha: str

@dataclass(frozen=True)
class CandidateAttemptAudit:
    attempt_schema_version: str
    control_registry_entry_sha: str
    fejer_order: int
    reference_outcome: EndpointReferenceOutcome
    shell_outcome: Optional[EndpointShellOutcome]
    paired_response_outcome: Optional[PairedResponseOutcome]
    attempt_sha: str

class ControlCandidateFailure(str, Enum):
    REFERENCE_FAILED = "reference_failed"
    SHELL_FAILED = "shell_failed"
    RESPONSE_FAILED = "response_failed"
    BRIDGE_FAILED = "bridge_failed"

def control_candidate_failure_reason(
    failure: ControlCandidateFailure,
) -> UndefinedReason: ...

@dataclass(frozen=True)
class ControlCandidateOutcome:
    status: BlockStatus
    failure: Optional[ControlCandidateFailure]
    run_spec: ResponseRunSpec
    attempt_audit: CandidateAttemptAudit
    outcome_sha: str

@dataclass(frozen=True)
class BranchSpectrumAudit:
    branch: Literal["actual", "matched_ablated"]
    declared_rank: int
    spectrum_shape: tuple[int, int]
    spectrum_order_id: Literal["k-major-singular-descending-v1"]
    raw_spectrum: tuple[float, ...]
    active_min: Optional[float]
    inactive_max: Optional[float]
    branch_sha: str

@dataclass(frozen=True)
class PerControlReadoutSpectrumAudit:
    readout_kind: Literal["h", "curv"]
    control_registry_entry_sha: str
    expected_rank_declaration_sha: str
    fejer_order: int
    run_spec_sha: str
    paired_response_sha: str
    actual: BranchSpectrumAudit
    ablated: BranchSpectrumAudit
    actual_bridge_operator_error_upper: float
    ablated_bridge_operator_error_upper: float
    audit_sha: str

@dataclass(frozen=True)
class ReadoutAggregateCalibrationAudit:
    readout_kind: Literal["h", "curv"]
    per_control: tuple[PerControlReadoutSpectrumAudit, ...]
    scale_ref: float
    null_max: Optional[float]
    bridge_operator_error_max: float
    noise_ref: float
    signal_min: float
    tau_sig: float
    signal_noise_ratio: float
    raw_relative_gap: float
    absolute_signal_gate_passed: bool
    relative_gap_gate_passed: bool
    aggregate_sha: str

@dataclass(frozen=True)
class ControlCandidateAudit:
    control_registry_entry: ControlRegistryEntry
    expected_rank_declaration: ExpectedRankDeclaration
    candidate_t: ControlCandidateOutcome
    comparison_2t: ControlCandidateOutcome
    readout_spectrum_audits: tuple[PerControlReadoutSpectrumAudit, ...]
    comparison_2t_readout_spectrum_audits: tuple[
        PerControlReadoutSpectrumAudit, ...
    ]
    phase_separation: Optional[float]
    overlap_margin: Optional[float]
    projector_t2t_distance: Optional[float]
    passed: bool
    audit_sha: str

@dataclass(frozen=True)
class WindowCandidateAudit:
    fejer_order: int
    control_audits: tuple[ControlCandidateAudit, ...]
    readout_aggregate_audits: tuple[ReadoutAggregateCalibrationAudit, ...]
    passed: bool
    audit_sha: str

@dataclass(frozen=True)
class WindowThresholdCalibrationManifest:
    calibration_schema_version: str
    control_registry: ClosedControlRegistry
    window_protocol: WindowCalibrationProtocol
    candidate_audits: tuple[WindowCandidateAudit, ...]
    calibration_manifest_sha: str

@dataclass(frozen=True)
class SelectedControlEvidenceRef:
    control_id: Literal["full", "zero", "direct_sum"]
    control_registry_entry_sha: str
    expected_rank_declaration_sha: str
    run_spec_sha: str
    paired_response_sha: str
    shell_manifest_sha: str
    comparison_2t_run_spec_sha: str
    comparison_2t_response_sha: str
    comparison_2t_shell_manifest_sha: str

@dataclass(frozen=True)
class WindowThresholdSelection:
    selected_fejer_order: int
    h_scale_ref: float
    h_noise_ref: float
    h_signal_min: float
    h_tau_sig: float
    curv_scale_ref: float
    curv_noise_ref: float
    curv_signal_min: float
    curv_tau_sig: float
    selected_evidence_refs: tuple[SelectedControlEvidenceRef, ...]
    selection_sha: str

@dataclass(frozen=True)
class WindowCalibrationOutcome:
    status: BlockStatus
    manifest: WindowThresholdCalibrationManifest
    selection: Optional[WindowThresholdSelection]
    outcome_sha: str

class VerifiedWindowThresholdCalibration:
    """Opaque；仅 successful outcome 经全量重验后由模块私有 issuer 签发。"""

    @property
    def outcome(self) -> WindowCalibrationOutcome: ...

    @property
    def manifest_sha(self) -> str: ...

    @property
    def selection(self) -> WindowThresholdSelection: ...

def response_run_spec_payload(spec) -> dict[str, object]: ...
def verify_response_run_spec(
    spec, protocol: VerifiedWindowCalibrationProtocol
) -> ResponseRunSpec: ...
def verify_endpoint_shell_spec(
    spec, protocol: VerifiedWindowCalibrationProtocol
) -> EndpointShellSpec: ...
def build_endpoint_reference(
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    actual_factory,
    actual_transition,
    actual_certificate,
    control_id: Literal["full", "zero", "direct_sum"],
) -> VerifiedEndpointReferenceOutcome: ...
def verify_endpoint_reference_outcome(
    outcome: EndpointReferenceOutcome,
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    actual_factory,
    actual_transition,
    actual_certificate,
) -> VerifiedEndpointReferenceOutcome: ...
def build_endpoint_shell(
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    reference: VerifiedEndpointReferenceOutcome,
    actual_factory,
    actual_transition,
    actual_certificate,
    shell_spec,
) -> VerifiedEndpointShellOutcome: ...
def verify_endpoint_shell_outcome(
    outcome,
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    actual_factory,
    actual_transition,
    actual_certificate,
) -> VerifiedEndpointShellOutcome: ...
def build_paired_filtered_response(
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    qualified_ablation: VerifiedCertificateBackedQualification,
    actual_transition,
    ablated_transition,
    actual_certificate,
    ablated_certificate,
    run_spec,
    shell: VerifiedEndpointShellOutcome,
) -> VerifiedPairedResponseOutcome: ...
def verify_paired_filtered_response(
    outcome: PairedResponseOutcome,
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    qualified_ablation: VerifiedCertificateBackedQualification,
) -> VerifiedPairedResponseOutcome: ...
def issue_expected_rank_declaration(
    registry: VerifiedControlRegistry,
    control_id: Literal["full", "zero", "direct_sum"],
) -> ExpectedRankDeclaration: ...
def compute_window_readout_calibration_audits(
    registry: VerifiedControlRegistry,
    control_audits: tuple[ControlCandidateAudit, ...],
) -> tuple[ReadoutAggregateCalibrationAudit, ...]: ...
def calibrate_window_and_thresholds(
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
    candidate_audits: tuple[WindowCandidateAudit, ...],
) -> WindowCalibrationOutcome: ...
def verify_window_threshold_calibration(
    outcome: WindowCalibrationOutcome,
    registry: VerifiedControlRegistry,
    protocol: VerifiedWindowCalibrationProtocol,
) -> VerifiedWindowThresholdCalibration: ...
```

- [ ] Fejér filter 本体固定为

  ```text
  λ                    = exp(iθ), θ∈(-π,π]
  F_T(M_full;θ)         = (1/Z_T) Σ_{t=0}^{T-1}(1-t/T)e^{-iθt}M_full^t
  Z_T                   = Σ_{t=0}^{T-1}(1-t/T)
  R_h                   = P_h F_T(M_full(k);θ_shell(k)) J
  ω_shell               = θ_shell/dt  # derived metadata only
  ```

  禁止对 `P M_full J` 取幂。actual/ablated 对必须共享同一 actual-derived
  `dt,T,θ_shell,source J,readout P_h`；禁止分别重寻中心，禁止从 ablated 或运行后
  family 坐标挑簇。
- [ ] `WindowCalibrationProtocol` 必须在任何 transition-derived shell 前冻结并自哈希，
  递归绑定 raw registry SHA/parent freeze、固定 T 表、按 registry wire 顺序的三个 control
  response/bridge grids、reference reciprocal node、phase bands、deterministic source trial
  protocol，以及 gap/participation/overlap/loop/projector hard constants。常数逐位固定为本
  Task 顶部 prereg 值/公式，builder 不接 caller per-candidate threshold。
  `build/verify_window_calibration_protocol` 重验 `VerifiedControlRegistry` 后返回 opaque
  capability；每个 `ResponseRunSpec/EndpointShellSpec` 必须绑定同一 protocol SHA，且 grid、
  band、entry、trial protocol、candidate T 必须等于其中对应项。calibrator manifest 递归保存
  raw protocol，所有入口重验 opaque seal；任何 candidate 单独重签 spec 都拒绝。
- [ ] V3-M0 shell 只实现 endpoint single node，不实现参数族 graph tracking。
  `EndpointShellSpec` 必须在运行前递归内嵌完整 `ControlRegistryEntry`、完整
  `ResponseKGridManifest`、预注册 phase bands、当前 candidate T，以及完整 frozen
  `EndpointReferenceProjector` tensor；reference 的 entry SHA 必须等于所嵌 entry，且只存
  reference SHA 不合格。reference 还必须绑定 protocol 的 reciprocal index、actual
  transition/dynamics certificate、`g-whitened-state-v1` coordinate convention 与正 rank；
  builder 从该 index 的 actual `M(k),G(k)` 在预注册 phase band 内重算 eigenspace/projector，
  再逐位对拍 frozen tensor，caller 自签 projector 不能通过。每个 `ShellPointAudit` 必须逐点保存 momentum path position、
  shell phase/rank、Hermitian/idempotent/G-invariance/eigenphase residual、participation、
  nearest competitor gap、reference/runner-up/predecessor overlap 与 loop residual。
  `nearest_competitor_gap/runner_up_overlap` 仅在存在竞争候选时非空；
  `predecessor_overlap` 仅在同一路径非首点时非空；`loop_residual` 仅在
  `DirectionManifest.closure_path_pairs` 中逐位声明的两条 path/position/同一 reciprocal
  index closure 点非空，其他情况必须为 `None`，禁止用
  `0/inf/NaN` 占位。manifest 的所有 residual maxima、participation/gap/overlap minima 或
  maxima、loop max 都必须从 `point_audits` 重算；`shell_phases` 也须逐位等于 point raw。
  失败 attempt 的 `point_attempts` 也必须逐项绑定 reciprocal index/path ID/path position，
  再保存该点全部 candidate phase/rank/participation/reference/predecessor overlaps；禁止
  把不同 k/path flatten 成失去关联的一维 tuple。
  `EndpointReferenceAttemptAudit.runner_up_overlaps` 长度必须逐位等于 candidates，
  每项仅在该 candidate 存在 competitor 时非空；`EndpointShellAttemptAudit` 必须递归
  内嵌完整 `EndpointShellSpec`，只存 `shell_spec_sha` 的失败证据拒绝。
  `path_ids` 与 `ordered_paths` 逐位对齐且唯一；closure pair 引用必须存在、两端 index
  逐位相等、每个声明 pair 恰产生一个 loop residual，未声明点出现 residual 或声明点缺失
  都拒绝。
  family 邻接、跨 cell overlap 与 holonomy graph 明确延期到 V3-M2，Task 11 不创建 graph。
- [ ] reference builder 返回 opaque `VerifiedEndpointReferenceOutcome`；raw outcome 无论成功
  失败都递归保存 `EndpointReferenceSpec`，绑定 protocol、factory、transition、
  dynamics certificate、candidate T 与 registry entry，并保存 candidate
  phases/ranks/participations/runner-up overlaps、Hermitian/idempotent/G-invariance/
  eigenphase residuals。`expected_shell_rank` 与来源逐位取自 ParentFreeze 对应
  application spec，不可由运行后 candidate 反推。phase/gap hard minimum 只由当前
  `T` 的 `phase_separation_min(T)=8π/T` 机械派生，schema 不另存可矛盾常量。成功当且仅当
  unique in-band、rank 与 prereg reference rank
  一致、participation/runner-up/projector 门全过且 reference 非空；否则 typed
  `PHASE_BAND_EMPTY/PHASE_BAND_NONUNIQUE/RANK_MISMATCH/PARTICIPATION_FAILED/
  RUNNER_UP_MARGIN_FAILED/PROJECTOR_INVALID`、reference=`None`，并机械映射
  `ENDPOINT_SHELL_AMBIGUOUS`。`EndpointShellSpec` 只能嵌 successful opaque outcome 所暴露的
  reference。
- [ ] projector 数学统一在
  `M_tilde=G^(1/2) M G^(-1/2)` 的 `g-whitened-state-v1` 坐标。逐点审计
  `||P-P†||₂`、`||P²-P||₂`、`||(I-P)M_tilde P||₂`、
  `||M_tilde P-exp(iθ)P||₂`；候选 rank 必须等于 reference rank `r>0`。等秩 overlap 固定
  `Re tr(PQ)/r`，nearest phase gap 固定 principal circle distance
  `|Arg exp(i(θ-φ))|` 的最小正值，loop residual 固定
  `||P_path1-P_path2||₂`。令
  `U_J=orth(G^(1/2)J)`、
  `U_P=orth((P_h G^(-1/2))†)`，dimensionless participation 固定为
  `min(Re tr(P U_JU_J†)/r, Re tr(P U_PU_P†)/r)`。所有 `orth/sqrt` 使用冻结 deterministic
  Hermitian routines 与 residual gate；这些公式同时用于 reference、runner-up 与 path
  continuation，禁止替代相似度。
- [ ] failure evidence graph 只能是
  `EndpointReferenceOutcome→EndpointShellOutcome→PairedResponseOutcome` 的递归 typed
  outcome，三个 successful verifier 分别返回
  `VerifiedEndpointReferenceOutcome/VerifiedEndpointShellOutcome/
  VerifiedPairedResponseOutcome`。每层成功当且仅当 `failure=None`、raw payload 非空且
  opaque capability 非空；失败则恰好相反。candidate 无条件保存完整 reference outcome；
  reference 成功后 shell outcome 才且必须存在；shell 成功后 paired outcome 才且必须
  存在。reference 失败时后两层为 `None`；shell 失败时 paired 为 `None`；response/bridge
  失败时必须保留 successful shell outcome 与 paired attempt 已通过的 branch evidence。
  不再有 `response_attempted` bool、`Optional[str] failure`、flat SHA 或失败即清空 shell
  的路径。`WindowThresholdCalibrationManifest` 递归保存整张 graph。
  `candidate_fejer_order` 是候选 T，不是 selected T；selected 只在联合 outcome 之后产生。
  endpoint 的全部 typed failure（含 phase separation、nearest gap 与 participation 硬门）
  对外 `BlockStatus.reason` 均为
  `ENDPOINT_SHELL_AMBIGUOUS`；attempt audit 保留具体原因，不引入 family tracking reason。
- [ ] `ControlCandidateFailure→BlockStatus.reason` 必须由
  `control_candidate_failure_reason` 完备机械映射：
  `REFERENCE_FAILED/SHELL_FAILED→ENDPOINT_SHELL_AMBIGUOUS`、
  `RESPONSE_FAILED→PAIRED_RESPONSE_FAILED`、
  `BRIDGE_FAILED→RESPONSE_BRIDGE_FAILED`；禁止借用 `RESPONSE_NULL` 或
  `MANIFEST_MISMATCH`。issuer 的首错顺序固定为 shell extraction → paired response
  construction/schema → repeated-executor bridge；命中首错后仍保存 attempt audit，但不得
  继续填后续成功字段。未知 enum、reason 不匹配或 status/failure presence 不一致都拒绝。
- [ ] paired builder 原子接收 verified qualification、actual/ablated transitions 与
  dynamics certificates、一个 `ResponseRunSpec` 和
  `VerifiedEndpointShellOutcome`；
  验证 source/readout 等于 registry entry 与 factory frozen manifests，dt/schema/channel、
  `response_grid` 和 `source_readout_bridge_grid` 各自同型同值，然后在一个 self-hash payload
  内递归绑定 qualification SHA、protocol、factory、两份完整 transition/certificate、
  run spec、完整 shell outcome、ablation manifest 与两支 response。失败 attempt 同样
  保存这些 body，而不是只在 success payload 出现。每支 response 再绑定
  自己的 certificate SHA。不存在两个独立单支 response 事后配对的 public API；
  verifier 必须重验 qualification 与两份 certificate。
- [ ] `ResponseRunSpec` 只保存 source/readout 响应证据：candidate Fejér T、完整
  `response_grid`、完整且 denominator 逐位等于 `spatial_shape` 的
  `source_readout_bridge_grid`、source bridge steps 与 source trial vectors；不得出现
  full-state trial/step。`fejer_order` 只允许
  `{256,512,1024,2048,4096,8192,16384}`，其中 16384 只作 8192 的 2T comparison。
  source bridge steps 必须逐位等于 protocol 对应项、非空严格升序、在 `1..16384` 且含
  至少一个 `t>1`；仅 `(1,)` 拒绝。trial shape
  `(n_source,n_source)`、complex128 finite，并须由 protocol 的
  `registry-source-identity-v1` 从 frozen source basis 机械生成完整 identity columns，
  禁止 caller 选择零、重复、抽样或降秩 trials；tolerance 逐位等于 `1e-12`。未来若使用
  非 identity frame，必须改用显式 protocol variant 且满足上述
  `SourceFrameCoverageCertificate`，不能沿用 identity ID。
  source bridge work 同样在执行前按 `n_k*n_trial*max(steps)` 预检，不得用 `len(steps)`
  低估。
- [ ] repeated-executor source/readout bridge 使用 `ResponseRunSpec` 的周期 k/steps，并
  对声明的**完整 source domain**逐列重建
  `ΔR=R_executor-PM_full^tJ`。每个 `(k,t,branch)` 保存完整
  `raw_difference_matrix`、directed-outward Frobenius/operator-norm upper，再以 entry 的
  source whitener 与 h/curv readout chain 重算同谱单位的
  `h_whitened_operator_error_upper/curv_whitened_operator_error_upper`。若 trial 不是完整
  source identity，必须递归内嵌正 `frame_operator_lower` 与 canonical-dual residual 的
  `SourceFrameCoverageCertificate` 并从 frame 机械重建完整 `ΔR`；不满秩 frame 拒绝。
  绝不允许 `scale_ref*normalized_residual` 冒充有量纲 bridge noise。
  `values.shape` 固定 `(n_k,n_readout,n_source)`，k 轴严格沿 shell grid canonical 顺序。
- [ ] `ExpectedRankDeclaration` 由 closed issuer 只从
  `VerifiedControlRegistry+ControlRegistryEntry` 重算，绑定 raw registry/entry/control/parent
  freeze 与 h/curv actual/ablated 四个秩，并在任何 candidate response 前签发；issuer API
  不接受 caller rank。
- [ ] `PerControlReadoutSpectrumAudit` 不能由 caller raw spectra/τ 签发。每个 control
  明确是某一 T 的 audit，必须绑定 `fejer_order/run_spec_sha/paired_response_sha`，并按
  `("h","curv")` 保存 actual/ablated `BranchSpectrumAudit`、declared rank partition
  与同 readout 单位的 branch bridge operator error，但不在单 control 内签发公共 τ/pass。
  `compute_window_readout_calibration_audits` 一次接收 registry 固定顺序的三个完整 control
  audits，重验 entry 的 `ControlReadoutCalibrationSpec`、declaration 与 T/2T paired
  outcomes；从 response
  values 依次施加 frozen h whitener，或 frozen curvature incidence → curvature normalizer
  → curvature whitener，再重算 actual/ablated raw singular spectra、noise/signal/gap 与全部
  spectra。随后在 `WindowCandidateAudit` 级分别为 h/curv 生成恰两项
  `ReadoutAggregateCalibrationAudit`，递归含三个 per-control audit，并联合计算
  scale/noise/signal/τ/gates。V3-M0 synthetic identity spec 必须机械推出两条 spectrum
  逐位相等，但仍各存一项；raw arrays 与 τ 只是可重放输出。
- [ ] 每个 raw spectrum wire 另存 `spectrum_shape=(n_k,n_singular)` 与
  `k-major-singular-descending-v1`；tuple 是 row-major flatten，每个 k/branch 奇异值降序。
  按 declaration 的 branch rank `r`，前 `r` 项为 active、其余为 inactive，`r=0` 时全部
  inactive。`active_min` 非空当且仅当 `r>0`，`inactive_max` 非空当且仅当
  `r<n_singular`；空集必须用 `None`，不得以 0/inf 占位。aggregate 的 `signal_min` 取三个
  controls 所有非空 active 的最小值，`null_max` 为所有非空 inactive 的最大值（若全空才
  `None`），`scale_ref` 取正响应 active 最大值，`bridge_operator_error_max` 直接取同
  readout 单位的 branch upper 最大值。
  `signal_min` 必须使用该声明，禁止 `np.linalg.matrix_rank` 或结果依赖阈值反推秩。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_window_thresholds -v
  ```

  预期为 window/threshold 模块缺失。
- [ ] typed graph 回归必须分别制造 reference、shell、actual response、ablated response、
  actual bridge、ablated bridge 首错，逐项证明已通过上游 body 保留、未到达下游为
  `None`，不存在 string failure/boolean attempted。bridge 回归篡改任一 `ΔR` column、
  source whitener、h/curv chain、frame lower/canonical dual、Frobenius/operator upper 或
  把 normalized residual 乘 scale 伪造量纲都必须拒绝。threshold 回归验证 active/inactive
  空集的 Optional presence、三个 control 错序/缺项、per-control 私签 τ 与跨 control
  聚合篡改全部 fail-closed。
- [ ] 每个 `WindowCandidateAudit` 必须按 registry 固定顺序含恰好三个
  `ControlCandidateAudit`。每个 control 递归绑定 declaration、T 与 2T 各自独立
  `ResponseRunSpec`、typed outcome、endpoint shell/paired response（若成功）和 attempt
  audit。只有 T 与 2T 两个 outcome 都成功且各有 paired response 时，
  T 的 `readout_spectrum_audits` 与 2T 的
  `comparison_2t_readout_spectrum_audits` 才各按 `("h","curv")` 恰含两项，且各自绑定不同
  run spec/paired response；禁止以 T 的 source SHA 代填 2T。三个 controls 都到达该阶段
  时，window candidate 的 `readout_aggregate_audits` 才恰含 h/curv 两项并计算公共 gate。
  任一 typed shell/response/bridge failure 时 aggregate audits 必须为空且
  `phase_separation=overlap_margin=projector_t2t_distance=None`，禁止伪造 `0/inf/NaN`
  占位；该 control 与公共 candidate 都必须 `passed=False`。公共候选 T 只有三个 control
  全部通过才 `passed=True`。
- [ ] 固定
  `noise_ref=max(null_max if present else 0,bridge_operator_error_max,
  2^-52·scale_ref)`、
  `τ_sig=sqrt(noise_ref·signal_min)`；两读出均须
  `signal_min/noise_ref≥10³`，每个 raw active singular value 必须
  `>raw_bridge_noise`，active/inactive raw gap 亦须 `≥10³`。
- [ ] eigenphase 网格固定 `2π/(16T)`，竞争峰间隔 `≥8π/T`，overlap 分差 `≥0.2`，
  `||P_T-P_2T||₂≤1e-10`。
- [ ] 选择最小公共合格 T；无合格项不得扩表。calibrator 返回
  `WindowCalibrationOutcome(status=BlockStatus(False, UndefinedReason.WINDOW_UNRESOLVED),
  selection=None)`；只有顶层 orchestrator 把该 typed reason 唯一映射为
  `HALT-V3M0-WINDOW`，底层 calibrator 不返回状态字符串。outcome presence invariant 为
  `status.defined ⇔ selection非空`；undefined 时 reason 必须恰为 `WINDOW_UNRESOLVED`。
- [ ] `WindowThresholdCalibrationManifest` 必须按精确升序表保存六个
  `WindowCandidateAudit` 和全部 per-control attempt；`T=8192` 的 2T outcome 使用
  `fejer_order=16384`，16384 不得成为第七候选。成功时另生成
  `WindowThresholdSelection`，保存唯一公共 T、h/curv 各自完整
  `scale_ref/noise_ref/signal_min/tau_sig` 和按 registry 顺序的
  `SelectedControlEvidenceRef`（T/2T run spec、paired response、shell、declaration）。
  verifier 从六项 raw audit 重算第一个公共 PASS，逐 SHA 对拍 selection refs 后才签发 opaque
  `VerifiedWindowThresholdCalibration`。省略失败项、错序、给失败项伪造 response ref、
  篡改声明或 selection 都拒绝。
  三个 `SelectedControlEvidenceRef` 只证明 C01–C03 calibration，不构成下游唯一 response
  白名单；C04–C20 必须走 Task 12 的 closed application permit，不能塞入 selection，也
  不能因不在这三支 refs 中而被非法拒绝。
- [ ] Task 11 只签发 window 与 h/curv signal lines；selection/schema/API 中不得出现
  `tau_surv/tau_geom/tau_cover`。survival、geometry 与 coverage threshold 分别由
  Task 13、14 的独立 verified calibration 和 control evidence 签发，Task 17 只组合这些
  capabilities。signal exact-null floor 仍为 `eps_fp64·scale_ref`。
- [ ] calibrator API 不接受 family candidate 数据。
- [ ] Fejér 函数用 G-whitened metric-unitary Schur functional calculus 计算同一个矩阵
  多项式；允许利用闭式标量 Fejér kernel 加速，但不得选择简并簇内向量，也不得把结果
  回灌为时间步。进入计算前执行 Task 10 的 projector/work caps。
- [ ] GREEN：重复定向测试；验证最小 T、无合格 HALT 和 16384 仅作 2T 对拍。
- [ ] 提交：`feat(v3m0): jointly freeze windows and thresholds`

## Task 12：V3-M0 control calibration-only `ResponseBlock`

**文件：**

- Create: `rulespace_v3/linalg.py`
- Create: `rulespace_v3/blocks.py`
- Create: `rulespace_v3/calibration_authority.py`
- Create: `tests/test_v3m0_response_block.py`
- Create: `tests/test_v3m0_calibration_authority.py`

**冻结接口：**

```python
@dataclass(frozen=True)
class V3M0ControlApplicationEvidence:
    evidence_schema_version: str
    permit: "CalibrationApplicationPermit"
    application_construction_sha: str
    qualification_sha: str
    actual_factory_sha: str
    ablated_factory_sha: str
    actual_transition: MeasuredTransition
    ablated_transition: MeasuredTransition
    actual_dynamics_certificate: DynamicsCertificate
    ablated_dynamics_certificate: DynamicsCertificate
    paired_response_outcome: "V3M0ApplicationPairedResponseOutcome"
    evidence_sha: str

class VerifiedV3M0ControlApplicationEvidence:
    """Opaque、V3-M0-only；live seal 绑定 parent/spec/paired response 全 body。"""

    @property
    def evidence(self) -> V3M0ControlApplicationEvidence: ...

@dataclass(frozen=True)
class CalibrationApplicationPermit:
    permit_schema_version: str
    scope: Literal["v3m0-synthetic-control-application-v1"]
    parent_freeze: ParentFreezeManifest
    application_spec: V3M0SyntheticControlApplicationSpec
    calibration_manifest: WindowThresholdCalibrationManifest
    selection: WindowThresholdSelection
    selected_fejer_order: int
    window_protocol: WindowCalibrationProtocol
    response_grid: ResponseKGridManifest
    source_readout_bridge_grid: BridgeKGridManifest
    source_readout_bridge_steps: tuple[int, ...]
    reference_reciprocal_index: tuple[int, ...]
    preregistered_phase_bands: tuple[tuple[float, float], ...]
    expected_shell_rank: int
    expected_shell_rank_source_id: Literal[
        "parent-freeze-control-application-spec-v1"
    ]
    source_basis: BasisManifest
    readout_basis: BasisManifest
    readout_calibration_spec: ControlReadoutCalibrationSpec
    permit_sha: str

class VerifiedCalibrationApplicationPermit:
    """Opaque、V3-M0-only、pre-response；只绑定 parent/calibration/application spec。"""

    @property
    def permit(self) -> CalibrationApplicationPermit: ...

@dataclass(frozen=True)
class V3M0ApplicationResponseRunSpec:
    run_spec_schema_version: str
    permit_sha: str
    application_spec_sha: str
    fejer_order: int
    source_basis: BasisManifest
    readout_basis: BasisManifest
    response_grid: ResponseKGridManifest
    source_readout_bridge_grid: BridgeKGridManifest
    source_readout_bridge_steps: tuple[int, ...]
    source_trial_vectors: FrozenComplexTensor
    bridge_tolerance: float
    run_spec_sha: str

class VerifiedV3M0ApplicationResponseRunSpec:
    @property
    def run_spec(self) -> V3M0ApplicationResponseRunSpec: ...

@dataclass(frozen=True)
class V3M0ApplicationEndpointReference:
    reference_schema_version: str
    status: BlockStatus
    permit_sha: str
    actual_transition_sha: str
    actual_dynamics_certificate_sha: str
    reference_reciprocal_index: tuple[int, ...]
    preregistered_phase_bands: tuple[tuple[float, float], ...]
    expected_shell_rank: int
    candidate_phases: tuple[float, ...]
    candidate_ranks: tuple[int, ...]
    runner_up_overlaps: tuple[Optional[float], ...]
    projector: Optional[FrozenComplexTensor]
    failure: Optional[EndpointReferenceFailure]
    reference_sha: str

class VerifiedV3M0ApplicationEndpointReference:
    @property
    def reference(self) -> V3M0ApplicationEndpointReference: ...

@dataclass(frozen=True)
class V3M0ApplicationEndpointShellOutcome:
    shell_schema_version: str
    status: BlockStatus
    permit: CalibrationApplicationPermit
    run_spec: V3M0ApplicationResponseRunSpec
    reference: V3M0ApplicationEndpointReference
    point_audits: tuple[ShellPointAudit, ...]
    shell_phases: tuple[float, ...]
    shell_projectors: Optional[FrozenComplexTensor]
    failure: Optional[EndpointShellFailure]
    shell_sha: str

class VerifiedV3M0ApplicationEndpointShell:
    @property
    def outcome(self) -> V3M0ApplicationEndpointShellOutcome: ...

@dataclass(frozen=True)
class V3M0ApplicationPairedResponseOutcome:
    outcome_schema_version: str
    status: BlockStatus
    permit: CalibrationApplicationPermit
    application_construction_sha: str
    qualification: CertificateBackedQualificationOutcome
    run_spec: V3M0ApplicationResponseRunSpec
    shell: V3M0ApplicationEndpointShellOutcome
    actual: Optional[SourceReadoutResponse]
    ablated: Optional[SourceReadoutResponse]
    failure: Optional[PairedResponseFailure]
    pair_sha: Optional[str]
    outcome_sha: str

class VerifiedV3M0ApplicationPairedResponseOutcome:
    @property
    def outcome(self) -> V3M0ApplicationPairedResponseOutcome: ...

@dataclass(frozen=True)
class V3M0ApplicationConstruction:
    construction_schema_version: str
    permit: CalibrationApplicationPermit
    construction_status: BlockStatus
    ablation_pair_snapshot: AblationPairSnapshot
    actual_factory_sha: str
    ablated_factory_sha: str
    ablation_manifest_sha: str
    ablation_construction_sha: str
    construction_sha: str

class VerifiedV3M0ApplicationConstruction:
    """Opaque；从 permit 的 closed DAG 唯一 materialize 并重验 matched pair。"""

    @property
    def construction(self) -> V3M0ApplicationConstruction: ...

@dataclass(frozen=True)
class ResponseBlockOperatorSpec:
    spec_schema_version: str
    source_metric_whitener: FrozenComplexTensor
    h_metric_whitener: FrozenComplexTensor
    curvature_incidence_operator: FrozenComplexTensor
    curvature_metric_whitener: FrozenComplexTensor
    curvature_normalizer_id: str
    derivation_id: Literal[
        "control-readout-calibration-spec-recursive-v1"
    ]
    source_spec_sha: str
    spec_sha: str

@dataclass(frozen=True)
class ResponseBlockAudit:
    audit_schema_version: str
    q_all_orth_residual: float
    q_active_orth_residual: float
    curvature_hermitian_residual: float
    q_curv_orth_residual: float
    s_curv_hermitian_residual: float
    source_metric_min_lower: float
    h_metric_min_lower: float
    curvature_metric_min_lower: float
    active_rank: int
    curvature_rank: int
    audit_sha: str

@dataclass(frozen=True)
class ResponseBlock:
    block_schema_version: str
    branch: Literal["actual", "matched_ablated"]
    source_readout_response: SourceReadoutResponse
    response_sha: str
    pair_sha: str
    operator_spec: ResponseBlockOperatorSpec
    q_all: FrozenComplexTensor
    q_active: FrozenComplexTensor
    v_curv: FrozenComplexTensor
    q_curv: FrozenComplexTensor
    s_curv: FrozenComplexTensor
    audit: ResponseBlockAudit
    paired_response_sha: str
    calibration_manifest_sha: str
    selected_fejer_order: int
    application_authority_sha: str
    block_sha: str

class VerifiedResponseBlock:
    """Opaque；下游 Task 13–15 的唯一 ResponseBlock 输入。"""

    @property
    def block(self) -> ResponseBlock: ...

def issue_v3m0_calibration_application_permit(
    parent: VerifiedParentFreeze,
    calibration: VerifiedWindowThresholdCalibration,
    application_instance_id: str,
) -> VerifiedCalibrationApplicationPermit: ...
def verify_calibration_application_permit(
    permit: CalibrationApplicationPermit,
    parent: VerifiedParentFreeze,
    calibration: VerifiedWindowThresholdCalibration,
) -> VerifiedCalibrationApplicationPermit: ...
def build_v3m0_application_response_run_spec(
    permit: VerifiedCalibrationApplicationPermit,
) -> VerifiedV3M0ApplicationResponseRunSpec: ...
def verify_v3m0_application_response_run_spec(
    raw: V3M0ApplicationResponseRunSpec,
    permit: VerifiedCalibrationApplicationPermit,
) -> VerifiedV3M0ApplicationResponseRunSpec: ...
def build_v3m0_application_endpoint_reference(
    permit: VerifiedCalibrationApplicationPermit,
    run_spec: VerifiedV3M0ApplicationResponseRunSpec,
    actual_transition: VerifiedTransition,
    actual_certificate: VerifiedDynamicsCertificate,
) -> VerifiedV3M0ApplicationEndpointReference: ...
def verify_v3m0_application_endpoint_reference(
    raw: V3M0ApplicationEndpointReference,
    permit: VerifiedCalibrationApplicationPermit,
    run_spec: VerifiedV3M0ApplicationResponseRunSpec,
    actual_transition: VerifiedTransition,
    actual_certificate: VerifiedDynamicsCertificate,
) -> VerifiedV3M0ApplicationEndpointReference: ...
def build_v3m0_application_endpoint_shell(
    permit: VerifiedCalibrationApplicationPermit,
    run_spec: VerifiedV3M0ApplicationResponseRunSpec,
    reference: VerifiedV3M0ApplicationEndpointReference,
    actual_transition: VerifiedTransition,
    actual_certificate: VerifiedDynamicsCertificate,
) -> VerifiedV3M0ApplicationEndpointShell: ...
def verify_v3m0_application_endpoint_shell(
    raw: V3M0ApplicationEndpointShellOutcome,
    permit: VerifiedCalibrationApplicationPermit,
    run_spec: VerifiedV3M0ApplicationResponseRunSpec,
    reference: VerifiedV3M0ApplicationEndpointReference,
    actual_transition: VerifiedTransition,
    actual_certificate: VerifiedDynamicsCertificate,
) -> VerifiedV3M0ApplicationEndpointShell: ...
def materialize_v3m0_application_construction(
    permit: VerifiedCalibrationApplicationPermit,
) -> VerifiedV3M0ApplicationConstruction: ...
def verify_v3m0_application_construction(
    construction: V3M0ApplicationConstruction,
    permit: VerifiedCalibrationApplicationPermit,
) -> VerifiedV3M0ApplicationConstruction: ...
def issue_v3m0_application_prestructure_authority(
    construction: VerifiedV3M0ApplicationConstruction,
    factory_role: Literal["actual", "matched_ablated"],
) -> VerifiedPrestructureAuthority: ...
def build_v3m0_application_paired_response(
    permit: VerifiedCalibrationApplicationPermit,
    construction: VerifiedV3M0ApplicationConstruction,
    qualified_ablation: VerifiedCertificateBackedQualification,
    run_spec: VerifiedV3M0ApplicationResponseRunSpec,
    actual_transition: VerifiedTransition,
    ablated_transition: VerifiedTransition,
    actual_certificate: VerifiedDynamicsCertificate,
    ablated_certificate: VerifiedDynamicsCertificate,
    shell: VerifiedV3M0ApplicationEndpointShell,
) -> VerifiedV3M0ApplicationPairedResponseOutcome: ...
def build_v3m0_control_application_evidence(
    permit: VerifiedCalibrationApplicationPermit,
    construction: VerifiedV3M0ApplicationConstruction,
    response: VerifiedV3M0ApplicationPairedResponseOutcome,
) -> VerifiedV3M0ControlApplicationEvidence: ...
def verify_v3m0_control_application_evidence(
    evidence: V3M0ControlApplicationEvidence,
    permit: VerifiedCalibrationApplicationPermit,
    construction: VerifiedV3M0ApplicationConstruction,
    response: VerifiedV3M0ApplicationPairedResponseOutcome,
) -> VerifiedV3M0ControlApplicationEvidence: ...
def build_selected_control_response_block(
    response: VerifiedPairedResponseOutcome,
    calibration: VerifiedWindowThresholdCalibration,
    branch: Literal["actual", "matched_ablated"],
) -> VerifiedResponseBlock: ...
def build_permitted_response_block(
    permit: VerifiedCalibrationApplicationPermit,
    evidence: VerifiedV3M0ControlApplicationEvidence,
    branch: Literal["actual", "matched_ablated"],
) -> VerifiedResponseBlock: ...
def verify_selected_control_response_block(
    block: ResponseBlock,
    response: VerifiedPairedResponseOutcome,
    calibration: VerifiedWindowThresholdCalibration,
    branch: Literal["actual", "matched_ablated"],
) -> VerifiedResponseBlock: ...
def verify_permitted_response_block(
    block: ResponseBlock,
    permit: VerifiedCalibrationApplicationPermit,
    evidence: VerifiedV3M0ControlApplicationEvidence,
    branch: Literal["actual", "matched_ablated"],
) -> VerifiedResponseBlock: ...
```

- [ ] 先测 Hermitian 正定 metric whitening、metric-null quotient、非正定 metric 拒绝。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_response_block -v
  ```

  预期为 linalg/blocks 模块缺失。
- [ ] permit/block authority RED：raw/self-hashed permit、错 case/instance、摘要替代
  application body、错 calibration/selection/T/protocol/grid/steps/basis/readout spec、
  physical scope、object-new/slot copy/expired weakref 均不能签发；permit raw/API 中若出现
  paired response、control evidence 或其他运行后字段也必须 schema-reject，证明它是真正
  pre-response capability。raw ResponseBlock、篡改 branch/full response body/operator
  body/audit/matrix 后重签、用 caller metric/operator、把 C04–C20 伪装成 selected
  C01–C03，或把 physical adapter 伪装成 V3-M0 synthetic 都拒绝。
- [ ] `ResponseBlock` 是 branch-specific，不是 paired block。对固定
  `branch∈{actual,matched_ablated}`，从同一个 verified paired outcome 机械选出该支完整
  `SourceReadoutResponse`，再由该支独立冻结
  `Q_all→Q_active→Vcurv→Qcurv→Scurv`。raw block 递归内嵌 branch response full body、
  `response_sha/pair_sha` 与完整 `ResponseBlockOperatorSpec/ResponseBlockAudit`；metric 与
  operator 全部从 registry/permit 的 `ControlReadoutCalibrationSpec` 机械派生并完整 hash，
  API 不接受 caller `source_metric/physical_h_metric/curvature_operator`。audit 保存全部
  orth/Hermitian/positive-margin/rank residual，不得静默 symmetrize 或 clip。
- [ ] C01–C03 的 `build_selected_control_response_block` 只接受 opaque paired outcome、
  `VerifiedWindowThresholdCalibration` 与固定 branch。pair SHA 必须出现在 registry 顺序
  冻结的 `selection.selected_evidence_refs`，response 的 `run_spec.fejer_order` 逐位等于
  selected T，并把 branch response full body/SHA、pair/calibration SHA 与 T 写入 block
  self-hash。raw manifest、失败 outcome、裸 T 或裸阈值不可输入。
- [ ] C04–C20 不冒充三支 selected calibration response。pre-response permit issuer 只接受
  `application_instance_id` 与 opaque parent/calibration；完整 lazy spec 必须由 issuer 在
  ParentFreeze 中按唯一 instance ID 解析，API 不接受 caller application spec、case ID、
  参数 body、raw metric 或运行后 evidence。issuer 从 parent+calibration 机械冻结 selected
  T、完整 protocol/grids/steps/bases/readout spec。随后唯一可达链为：
  permit → application run spec freeze → closed DAG materializer 得 exact matched construction/pair →
  `synthetic-application-v1` role-specific authorities → 两支 transition/certificate →
  certificate-backed qualification + application reference/shell →
  application paired response → full application
  evidence → 两个 branch blocks。`build_permitted_response_block(permit,evidence,branch)`
  是 C04–C20 唯一 block 入口；permit 不依赖 response/evidence，因而无循环。V3-M1
  physical/adapter scope 明确拒绝并延期到 M1 的 pre-run committed authority。
- [ ] raw `V3M0ApplicationConstruction` 只递归保存 `construction_status` 与完整
  `AblationPairSnapshot`，不得内嵌含 `VerifiedFactory` 的
  `AblationConstructionOutcome`。opaque `VerifiedV3M0ApplicationConstruction` 的私有
  seal 另行绑定 materializer 产生的 live construction/pair；跨进程 verifier 从 permit
  重放 closed DAG，并逐位对拍 snapshot、status 与全部 SHA 后才恢复 capability。
- [ ] permit 还从 application protocol 完整冻结 reference node、phase bands 与 expected
  shell rank。C04–C20 只能走 application-specific raw/opaque run spec→reference→shell
  builder/verifier；每层只从 permit 与 actual transition/certificate 派生，不接收 registry
  entry 或 `control_id`。application paired response 必须消费同 permit 的 verified run
  spec/shell；generic calibration-control shell 或错 permit 拒绝。
- [ ] `ResponseBlock` 的五个矩阵均为 `FrozenComplexTensor`；consumer 每次只取得 fresh
  complex128 view，不能通过 owning ndarray 改写证据。
- [ ] 覆盖 null/grey/signal、raw-noise、谱隙、共同 source/readout 等距换基不变量。
- [ ] `CausalSurvival`、`Geometry` 和 `Sigma` 后续只接受
  `VerifiedResponseBlock`，raw/self-hashed `ResponseBlock`、伪 permit、错 calibration/T/
  protocol/grid/basis/readout spec/paired response/control evidence 一律拒绝。
  response block 不提供 union-authority 通用 verifier：selected 与 permitted 两条路径分别
  由 `verify_selected_control_response_block(block,response,calibration,branch)` 和
  `verify_permitted_response_block(block,permit,evidence,branch)` hydrate。对同一 pair 的
  actual/ablated 两块，pair/calibration/application authority 必须逐位相同，仅 branch 与
  branch-derived response/selectors 不同。
- [ ] GREEN：重复定向测试，预期全部 `ok`。
- [ ] 提交：`feat(v3m0): build shared qualified response blocks`

## Task 13：`CausalSurvival`

**文件：**

- Create: `rulespace_v3/causal.py`
- Create: `tests/test_v3m0_causal.py`

**冻结输出：**

```python
@dataclass(frozen=True)
class SurvivalThresholdInstanceAudit:
    audit_schema_version: str
    application_spec: V3M0SyntheticControlApplicationSpec
    permit: CalibrationApplicationPermit
    actual_block: ResponseBlock
    ablated_block: ResponseBlock
    survival_spectrum: tuple[float, ...]
    measured_side_labels: tuple[Literal["below", "grey", "above"], ...]
    expected_side_labels: tuple[Literal["below", "grey", "above"], ...]
    signed_threshold_margins: tuple[float, ...]
    minimum_absolute_margin: float
    audit_sha: str

@dataclass(frozen=True)
class SurvivalThresholdControlEvidence:
    evidence_schema_version: str
    parent_freeze: ParentFreezeManifest
    instance_audits: tuple[SurvivalThresholdInstanceAudit, ...]
    evidence_sha: str

@dataclass(frozen=True)
class SurvivalThresholdCalibration:
    calibration_schema_version: str
    tau_surv: Literal[0.5]
    ambiguity_half_width: Literal[0.1]
    control_evidence: SurvivalThresholdControlEvidence
    calibration_sha: str

class VerifiedSurvivalThresholdCalibration:
    @property
    def calibration(self) -> SurvivalThresholdCalibration: ...

@dataclass(frozen=True)
class CausalResult:
    survival_spectrum: tuple[float, ...]
    epsilon_dof: float | None
    epsilon_cont: float | None
    chi_extra: float | None
    kappa_map: float | None
    d_proc_sq: float | None
    gain_ratio: float | None
    phase_shift: float | None
    ambiguous: bool

def calibrate_survival_threshold(
    blocks: tuple[
        tuple[VerifiedResponseBlock, VerifiedResponseBlock], ...
    ],
    permits: tuple[VerifiedCalibrationApplicationPermit, ...],
) -> VerifiedSurvivalThresholdCalibration: ...
def verify_survival_threshold_calibration(
    raw: SurvivalThresholdCalibration,
    blocks: tuple[
        tuple[VerifiedResponseBlock, VerifiedResponseBlock], ...
    ],
    permits: tuple[VerifiedCalibrationApplicationPermit, ...],
) -> VerifiedSurvivalThresholdCalibration: ...
def compute_causal_survival(
    actual: VerifiedResponseBlock,
    ablated: VerifiedResponseBlock,
    threshold: VerifiedSurvivalThresholdCalibration,
) -> CausalResult: ...
```

- [ ] 先测 full/zero/direct-sum anchor 得到 `1/0/1/2`。
- [ ] 先由 ParentFreeze 预注册的 C04/C11 等 control application permits 与
  actual/ablated branch blocks 逐实例重算完整 `s` spectrum、measured verdict 与 signed/
  absolute margin；expected labels 只从 ParentFreeze spec 派生。raw evidence 递归内嵌
  full permit、application spec 与两块 full body，不能只存 SHA。随后才签发
  `VerifiedSurvivalThresholdCalibration(tau=0.5,half_width=0.1)`；跨进程必须调用
  `verify_survival_threshold_calibration(raw,blocks,permits)` 全量重算。
- [ ] causal 固定使用 actual block 的 `Q_curv` 定义 actual sector；`Y₀` 必须把同一
  `Q_curv_actual` 送入 matched-ablated branch 的完整 response，不能读取 actual response
  冒充 ablated。两块必须同 pair/calibration/application authority 且 branch 异或。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_causal -v
  ```

  预期为 causal 模块缺失。
- [ ] 测相位翻转、纯增益和可逆非标量内部混合：主存活不变，Procrustes 辅报变化。
- [ ] 测干涉、rank-r 缺模补零、full-`Q_all` 新生模式的 `χ_extra`；`U₀,all` 每个
  active mode 必须独立通过 raw-noise/relative-gap 双门。
- [ ] `eigh` 前检查 Hermitian/orth residual；特征值超 `[0,1]` 容差即 undefined，
  禁止 clip 后判门。
- [ ] 任一 `|s_i-0.5|<0.1` 时 DOF verdict/`epsilon_dof` 记 ambiguous/null，
  但保留连续谱与 `epsilon_cont`；`Y₀=0` 时 Procrustes 的 `kappa_map,d_proc²,gain_ratio,
  phase_shift` 均为 null，不强填 float。
- [ ] 多 block 的 `epsilon_dof` 与 `epsilon_cont` 都按 `N_curv` DOF 加权，不按 block
  数平均。
- [ ] GREEN：重复定向测试，预期全部 `ok`。
- [ ] 提交：`feat(v3m0): measure causal subspace survival`

## Task 14：`GeometricDistance`

**文件：**

- Create: `rulespace_v3/geometry.py`
- Create: `tests/test_v3m0_geometry.py`

**冻结输出与公式：**

```python
@dataclass(frozen=True)
class GeometryCoverageThresholdInstanceAudit:
    audit_schema_version: str
    application_spec: V3M0SyntheticControlApplicationSpec
    permit: CalibrationApplicationPermit
    response_block: ResponseBlock
    g_spectrum: tuple[float, ...]
    c_spectrum: tuple[float, ...]
    measured_geometry_side_labels: tuple[
        Literal["below", "grey", "above"], ...
    ]
    measured_coverage_side_labels: tuple[
        Literal["below", "grey", "above"], ...
    ]
    expected_geometry_side_labels: tuple[
        Literal["below", "grey", "above"], ...
    ]
    expected_coverage_side_labels: tuple[
        Literal["below", "grey", "above"], ...
    ]
    geometry_signed_threshold_margins: tuple[float, ...]
    coverage_signed_threshold_margins: tuple[float, ...]
    geometry_minimum_absolute_margin: float
    coverage_minimum_absolute_margin: float
    audit_sha: str

@dataclass(frozen=True)
class GeometryCoverageThresholdControlEvidence:
    evidence_schema_version: str
    parent_freeze: ParentFreezeManifest
    instance_audits: tuple[GeometryCoverageThresholdInstanceAudit, ...]
    evidence_sha: str

@dataclass(frozen=True)
class GeometryCoverageThresholdCalibration:
    calibration_schema_version: str
    tau_geom: Literal[0.05]
    geom_ambiguity_half_width: Literal[0.01]
    tau_cover: Literal[0.5]
    cover_ambiguity_half_width: Literal[0.1]
    control_evidence: GeometryCoverageThresholdControlEvidence
    calibration_sha: str

class VerifiedGeometryCoverageThresholdCalibration:
    @property
    def calibration(self) -> GeometryCoverageThresholdCalibration: ...

def build_geometry_coverage_threshold_calibration(
    blocks: tuple[VerifiedResponseBlock, ...],
    permits: tuple[VerifiedCalibrationApplicationPermit, ...],
) -> VerifiedGeometryCoverageThresholdCalibration: ...
def verify_geometry_coverage_threshold_calibration(
    raw: GeometryCoverageThresholdCalibration,
    blocks: tuple[VerifiedResponseBlock, ...],
    permits: tuple[VerifiedCalibrationApplicationPermit, ...],
) -> VerifiedGeometryCoverageThresholdCalibration: ...
```

```text
g = sv((I-P_kerC) S_curv), length=N_curv with explicit zero extension
δ_geom_DOF = #{g_i≥0.05}/N_curv
δ_geom_E = sqrt(mean_i g_i²)
c = eig(U_Tphys† P_phys U_Tphys)
rank_deficit = dim(T_phys)-#{c_i≥0.5}
coverage_phys = #{c_i≥0.5}/dim(T_phys)
```

- [ ] 先测 TT、TT⊕row、full-h、低秩 TT 四类 `g/c`。
- [ ] 先用 C15–C17 的 verified permits/branch blocks 逐实例重算完整 `g/c` spectra、
  measured verdict 与 signed/absolute margins；expected geometry/coverage labels 只从
  ParentFreeze spec 派生。raw evidence 递归内嵌 permit/spec/block full body，不得只存 SHA。
  再签发 `VerifiedGeometryCoverageThresholdCalibration(0.05/0.01,0.5/0.1)`；跨进程只可用
  `verify_geometry_coverage_threshold_calibration(raw,blocks,permits)` hydrate。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_geometry -v
  ```

  预期为 geometry 模块缺失。
- [ ] `g` 长度固定为 `N_curv`，禁止较小维 SVD 漏方向。
- [ ] quotient 后重建 `P_phys`；`t+a g` 与 `t` 的 coverage 谱逐位相同。
- [ ] 输出完整 `g/c` 谱、`N_response,N_curv,max_g,rms_g,frobenius_leakage,
  delta_geom_dof,delta_geom_E,N_cover,coverage_phys,rank_deficit` 以及
  `K_cover` Hermitian residual。另保存固定 seeds
  `("v3m0-degrot-0","v3m0-degrot-1","v3m0-degrot-2")` 的简并簇共同酉旋转三次漂移
  与 `degenerate_rotation_drift_max`。
- [ ] 三次旋转分别比较完整 `g/c` 谱、主坐标和 projector invariants，最大漂移必须
  `≤1e-12`；超线传播 representation-invariance FAIL/undefined，不得只记诊断。
- [ ] 任一 `|g-0.05|<0.01` 时 geometry DOF verdict ambiguous；任一
  `|c-0.5|<0.1` 时 coverage/rank verdict ambiguous。不得默认求秩。
- [ ] actual/ablated unary geometry 各自使用其 branch block 独立导出的
  `q_active/q_curv/s_curv`，不得复用 actual selector 或 response。
- [ ] full-positive 输入必须触发 observer-collapse 诊断。
- [ ] GREEN：重复定向测试，预期全部 `ok`。
- [ ] 提交：`feat(v3m0): measure quotient geometric distance`

## Task 15：`SigmaFit` 与 D-M2-6

**文件：**

- Create: `rulespace_v3/sigma.py`
- Create: `tests/test_v3m0_sigma.py`

**冻结接口：**

```python
def fit_sigma(
    k_values: np.ndarray,
    g_max: np.ndarray,
    geometry_manifest_id: str,
    *,
    deterministic: bool,
) -> SigmaResult: ...
```

- [ ] 先测 manifest mismatch 拒绝；输入只允许 Geometry 同 manifest 的
  `y=max(g(k))`，且 geometry evidence 必须由 `VerifiedResponseBlock` 与
  `VerifiedGeometryCoverageThresholdCalibration` 产生；raw ResponseBlock 不可输入。
- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_sigma -v
  ```

  预期为 sigma 模块缺失。
- [ ] 复刻旧 R37 的 pure fit（不得 import 会在模块导入时写结果的 runner），用冻结数据
  对拍 constant/power/AIC/LOO。
- [ ] deterministic 分支在冻结 `α∈{0.05,0.06,…,4.00}` 网格实现高阶
  `k^(α+2)` 和 H3 删除最大 k 点双测；只有高阶模型相对简单幂律
  `ΔAIC≥2`、`|A′|≤|A|/5`，且 H3 `|A_half|≤|A|/2` 两测同判，才可排零；
  clean-zero/true-floor 必须分判。
- [ ] noisy 分支才使用 `|A|≤2σ_A`；常数模型胜出时只令 `alpha=null`，
  不令整个 sigma block undefined。
- [ ] JSON 必存 `A,alpha,sigma_A,sigma_alpha,delta_aic,loo,fit_window,
  expanded_window,direction,alpha_identifiable`；常数胜出时字段
  `alpha=null,alpha_identifiable=false`，但整个 sigma block 仍 defined。
- [ ] GREEN：重复定向测试并与冻结 R37/D-M2-6 fixture 逐位对拍。
- [ ] 提交：`feat(v3m0): fit limiting constraint scale`

## Task 16：V3-M1 typed-adapter 合同交接

**文件：**

- Create: `rulespace_v3/adapters/__init__.py`
- Create: `rulespace_v3/adapters/contracts.py`
- Create: `rulespace_v3/adapters/r23.py`
- Create: `rulespace_v3/adapters/r30.py`
- Create: `rulespace_v3/adapters/r25.py`
- Create: `tests/test_v3m0_adapters.py`

**冻结接口：**

```python
@dataclass(frozen=True)
class AdapterContract:
    adapter_id: str
    target_spec_id: str
    frozen_source_sha: str
    state_schema_id: str
    source_manifest_id: str
    readout_manifest_id: str
    target_conditioned_roots: tuple[str, ...]
    target_blind_roots: tuple[str, ...]
    one_step_certificate_schema: str
    multistep_certificate_schema: str
    commitment_receipt_schema: str
    certification_state: Literal["PENDING_V3M1"]
```

- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_adapters -v
  ```

  预期为 adapters 模块缺失。
- [ ] R23 合同将 curl/placed-Yee/Lorenz/target selector 的来源标为 target-conditioned；
  R30 合同将 `C,kerC,inc,TT` 与 placed coefficients 标为 target-conditioned；
  R25 合同将 de Donder/Wilson/target repair/selector 来源标为 target-conditioned。
- [ ] 三个合同都保存冻结源码 SHA、状态/source/readout ID 和待认证的一步核、多步响应
  等价证书 schema，以及 Task 10 冻结的 raw Git blob/commit/clean-tree/ancestry commitment
  receipt schema；状态只能是 `PENDING_V3M1`。本任务不签
  `VerifiedPrestructureAuthority/CalibrationApplicationPermit`，V3-M0 parent scope 也不能
  被 adapter 接受；V3-M1 必须另签 verified parent/receipt 或停在 external-anchor 边界。
- [ ] 静态测试禁止 family-specific `if R23/R30/R25: epsilon=...`，并验证 numeric
  literal 不能移除 taint。
- [ ] 测试通过 import guard 证明本任务没有调用三个历史 factory、没有执行一步或多步
  物理响应，也没有写物理锚 JSON。
- [ ] GREEN：重复定向测试，预期全部 `ok`。
- [ ] 提交：`feat(v3m0): define historical adapter contracts`

## Task 17：控制总装、状态机与原子证据

**文件：**

- Create: `rulespace_v3/state.py`
- Create: `experiments/v3m0_controls.py`
- Create: `experiments/v3m0_preflight.py`
- Create: `tests/test_v3m0_controls.py`
- Create: `tests/test_v3m0_state.py`
- Generate: `data/results/v3m0_controls.json`
- Generate: `data/runtime/v3m0_state.json`
- Create: `docsv3/v3-小报告-V3M0-首轮实施与控制判读-2026-07-30.md`

**状态写入条件：**

```python
ready = (
    formal_all_pass
    and exact_all_pass
    and verified_window_signal_calibration
    and verified_survival_threshold_calibration
    and verified_geometry_coverage_threshold_calibration
    and all_c04_c20_application_permits_verified
    and all_downstream_response_blocks_verified
    and all_required_controls_pass
    and no_required_block_undefined
    and no_physical_anchor_run
)
```

- [ ] 先测 20 项 synthetic controls、表示不变量、任一 block fail/undefined 的全局传播。
- [ ] Task 17 只组合 opaque capabilities：C01–C03 selected blocks、C04–C20 application
  permits/blocks、window+h/curv calibration、survival calibration、geometry/coverage
  calibration 与各 block result。它不重签 threshold、permit 或 ResponseBlock；任一 raw
  substitute/错 body 使对应 required block undefined。
- [ ] 20 项 controls 逐名冻结，runner 与 JSON 必须按相同 ID 报告：

  ```text
  C01_BLIND_HOLDOUT_FULL
  C02_CONDITIONED_ZERO
  C03_EQUAL_RANK_DIRECT_SUM
  C04_CANONICAL_ANGLE_025_075
  C05_PHASE_AND_SCALAR_GAIN
  C06_INTERNAL_NONSCALE_MIXING
  C07_CONSTRUCTIVE_DESTRUCTIVE_INTERFERENCE
  C08_RANK_R_MISSING_MODES
  C09_PURE_GAUGE_DRESSING
  C10_FULL_SOURCE_EXTRA_MODE
  C11_NULL_GREY_SIGNAL_AMPLITUDE
  C12_NU_INC_IR_NORMALIZATION
  C13_BOTH_ZERO_UNDEFINED
  C14_UNSTABLE_UNCLASSIFIED_ENDPOINT_SHELL
  C15_TT_ROW_FULLH_LOWRANK_GEOMETRY
  C16_COVERAGE_025_075
  C17_QUOTIENT_GAUGE_COVERAGE
  C18_ABLATED_INDEPENDENT_UNARY
  C19_FULL_POSITIVE_OBSERVER_COLLAPSE
  C20_DM26_CLEAN_ZERO_TRUE_FLOOR
  ```

  预言映射冻结为：

  | ID | 必须复现的主预言 |
  |---|---|
  | C01 | `s={1}^N, ε_causal=1, δ_geom=0` |
  | C02 | `s={0}^N, ε_causal=0, δ_geom=0` |
  | C03 | 等秩直和 `ε_causal=1/2` |
  | C04 | `s=0.25/0.75` 分落 survival 阈两侧 |
  | C05 | phase/gain 改变辅报而主存活为 1 |
  | C06 | 非标量内部混合主存活为 1、`κ_map<1` |
  | C07 | 存活、`χ_extra`、Procrustes 按干涉预言分离 |
  | C08 | rank-`r` 消融恰产生 `r` 个补零 |
  | C09 | pure-gauge dressing 不改 `s` 谱 |
  | C10 | actual-inactive 新生正交模式由 full-source `χ_extra` 捕获 |
  | C11 | null/grey/signal 分别给 `0/undefined/1` |
  | C12 | 全冻结 IR 窗保持 `N_curv` 且 raw-noise 双门不误放 |
  | C13 | actual=ablated=0 时三坐标 undefined |
  | C14 | unstable→`UNSTABLE`；unclassified→`TRACE_UNCLASSIFIED`；endpoint phase band 空、reference/runner-up 并列或 loop 不一致→`ENDPOINT_SHELL_AMBIGUOUS`；actual rank zero→`RESPONSE_NULL` |
  | C15 | TT/TT⊕row/full-h/低秩 TT 给冻结长度 `g` 谱 |
  | C16 | `c=0.25/0.75` 分落 coverage 阈两侧 |
  | C17 | quotient gauge dressing 不改 `c` 谱 |
  | C18 | ablated 新生源方向不被 unary geometry/σ 漏报 |
  | C19 | full-positive 输入触发 observer-collapse |
  | C20 | clean-zero 排零、true-floor 保留有限底 |

- [ ] 9 项 representation invariants 逐名冻结：

  ```text
  I01_LAYER_SPLIT_MERGE
  I02_INSERT_S_SINV
  I03_COMMUTING_LAYER_REORDER
  I04_Q_LABEL_PERMUTATION
  I05_SOURCE_ISOMETRY
  I06_READOUT_ISOMETRY
  I07_PHASE_NONZERO_SCALAR
  I08_COVARIANT_CANONICAL_CHANGE
  I09_NO_WRAP_VOLUME_CHANGE
  ```

  所有 invariant 先比较 `K_surv` eigenvalue multiset、trace 与阈值计数；矩阵本身只要求
  酉相似，不要求逐项相等。适用时再比较完整 `s/g/c` 谱、`ε_causal/δ_geom/coverage/σ`
  坐标，所有主谱/主坐标最大漂移 `≤1e-12`。逐项预言为：

  | ID | 变换与机械比较 |
  |---|---|
  | I01 | layer split/merge 后比较 `K` 谱/trace/count 与 `s/g/c` |
  | I02 | 插入 `S S⁻¹` 后比较同上 |
  | I03 | 重排可交换层后比较同上 |
  | I04 | 保持邻接图的 q label 置换后比较同上 |
  | I05 | `G_source` 同步协变的 source 等距换基后比较同上 |
  | I06 | `G_h,inc,G_curv,Π_phys,G_phys,T_phys` 同步协变的 readout 等距换基后比较同上 |
  | I07 | 相位/越过绝对门的非零标量重标后主 `s/g/c` 与坐标不变；Procrustes gain/phase 预期可变，不纳入主轴不变量 |
  | I08 | physical/source metric 与目标子空间同步协变的局域 canonical 换基后比较同上 |
  | I09 | 两个均满足 no-wrap 的体积间比较同一 kernel 的主谱与坐标 |

- [ ] RED：

  ```bash
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest tests.test_v3m0_controls tests.test_v3m0_state -v
  ```

  预期为 controls runner/state 模块缺失。
- [ ] 测 runner 只能读取 Task 16 的纯合同，禁止导入或执行 R30/R23/R25 历史物理
  factory 模块。
- [ ] 结果 JSON 保存 raw spectra、margin、undefined reason，并逐名强制：

  ```text
  evaluator_version
  grammar_id
  target_spec_id
  trace_sha
  response_manifest_id
  parent_v2_sha
  source_metric_id
  physical_h_metric_id
  curvature_metric_id
  physical_quotient_id
  physical_quotient_metric_id
  nu_inc_id
  window_manifest_sha
  actual_unary_manifest_id
  ablated_unary_manifest_id
  toolchain_manifest
  ```

  写临时文件、fsync 后原子替换。
- [ ] 只有全过才写 `READY-V3-M1-ANCHOR-CERTIFICATION`；否则写精确 HALT 状态。
  状态机对 `UndefinedReason.WINDOW_UNRESOLVED` 的唯一映射是
  `HALT-V3M0-WINDOW`；其他 reason 不得借用该字符串，底层 calibration API 也不得直接产
  HALT 字符串。
- [ ] 运行完整验证：

  ```bash
  cd formal/v3m0
  PATH=/Users/prismer/.elan/bin:$PATH lake build
  ! rg -n '\\b(sorry|admit)\\b' V3M0 V3M0.lean
  cd ../..
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    experiments/v3m0_formal_nogo.py --check
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m unittest discover -s tests -p 'test_v3m0_*.py' -v
  /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m compileall -q rulespace rulespace_gpu rulespace_v2 rulespace_v3 experiments tools
  RULESPACE_BACKEND=numpy \
    /Users/prismer/workspace/science/ca-universe-lab/.venv/bin/python \
    -m rulespace_gpu.verify
  git diff --check
  ```

- [ ] 对 `data/results/v2m3_pilot.json` 和所有冻结 v1/v2 输入做前后 SHA 对拍。
- [ ] 写中文小报告，只陈述实测状态；不得把 V3-M0 PASS 写成物理锚、family 或 L4。
- [ ] GREEN：上述定向与全套命令全部通过。
- [ ] 提交：`feat(v3m0): certify causal geometry instrument controls`

## 结束判据

V3-M0 只有在以下证据同时存在时完成：

1. 六个 Lean 切片和聚合入口全绿，无 `sorry/admit`；
2. exact/no-go JSON 可由独立 verifier 重算；
3. trace/ablation/response/窗口阈值/共同 block/causal/geometry/sigma 的定向测试全绿；
4. 20 项 controls 与不变量矩阵全绿，且 margin 非零；
5. 旧 v1/v2 证据 SHA 未变；
6. 状态机未运行任何物理锚或 family；
7. 最低线全绿。

若任何一条失败，保留对应 HALT 与原始证据；不得通过改阈值、改 T 表、候选特判或重标
mechanism 恢复。
