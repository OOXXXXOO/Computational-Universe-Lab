# 预注册草案:M4 全绿族参数先验(草案待评审拍板)

*车道B,2026-07-23。主线-CP1v4-装配阶梯与证伪炮组.md §三d 第 ① 件。
状态:**草案,最终冻结权在评审**;评审拍板后本文件改名去掉"草案"并冻结,
之后只许收紧不许放松(北极星条款)。*

*发令条件 = M3 全绿(§三d);本文件与配套件(② `cp1_v4_family_eval.py`、
③ 停跑规则、④ dashboard J5 剪刀面板)只做准备,不发令。目标:M3 过线当天,
M4 一条命令发令。*

---

## 一、总原则

1. **先验 = 连续采样的参数**,构成 M4 流形搜索的坐标;**炮 = 离散开关**,
   不进先验,只在全绿点上重放、必须 FAIL。判据(北极星 M4 表):流形非空
   非孤点、全绿一号在流形内部、选择律 ≥2 条 + 先验对照、J5 δ 扫描存活率曲线。
2. 混进先验的离散病态开关会制造**不连通伪流形**,污染 M4 判据 1 的连通性
   分析——这是"放置开关只当炮"的核心理由(下文 §二.5),各参数同此标准裁定。
3. 判据口径(SV 阈、门 k 集、格子、θ_m 参考值)全部冻结,不采样、不调参。

## 二、逐参数裁定

### 汇总表(供评审拍板)

| 参数 | 裁定 | 采样(建议) | 相互作用判据 | 预期存活边界 |
|---|---|---|---|---|
| θ_g−θ_m(dθ,共锥失谐) | **先验**(微小失谐);大失谐=炮5 | U(−0.02, +0.02) | J5(直接,剪刀);SV(斜向壳修正缓变) | \|dθ\| ≲ 5.8e−3(=J5 门 1e−2 / tanθ_m),对称双侧、连通 |
| damp_margin(阻尼平台裕量) | **先验** | 10^U(−2, log₁₀0.3) | 约束门、Z 扇区证书、J1 | 上端 b→1 阻尼失效;下端平台变窄(与 mu_frac 联动) |
| mu_frac(μ_z 平台位置) | **先验** | U(−0.2, 1.2)(故意越界) | 约束门、N_prop(Z 扇区)、J1 | ≈[0,1](R22 平台平坦性);越界单侧死 |
| G(源耦合) | **先验(L3 后)**;现占位冻结 0 | L3 后拍板;参考 tensor_batch 教训 G∈10^U(−2,−0.7) | 牛顿、泄漏、J1 | L3 后才有 |
| tr_sign | **炮**(第1件),不进先验 | {1,0} 开关 | 牛顿/回授(L3+);L1–L2 层无牙(声明) | — |
| 放置开关(staggered/integer) | **炮**(第6件),不进先验 | 开关 | \|K@TT\| 证书、N_prop、SV 全崩 | — |
| 轴序 | **炮/附检**(建议),次选=离散对照臂 | xyz 冻结;附检换 zyx 重评 | 斜向 sv3 / oracle 表 | — |
| θ_m 本身 | 冻结 π/3,不采样 | — | 全部(参考锥) | — |

### 1. θ_g − θ_m(dθ)——**微小失谐进先验,大失谐是炮**(本草案的核心建议)

**J5 硬约束含义的裁定建议**:共核有两层——

- **(i) 算子恒等**:几何与物质 import 同一 walk 函数对象(L1 §二)。这是
  **构造冻结项**,不进参数空间。J5=0.00e+00 的机器级根源在此(L1 §六.3),
  动它等于换构造。
- **(ii) 喂给几何副本的角 θ_g**:这是**可微扰坐标**,建议进先验。

理由:若族中恒 dθ=0,J5 作为选择律**空洞**——M4 判据 3 要求选择律有先验
对照(44M 符号教训:没有对照的"律"可能是采样伪影),判据 4 的 δ 扫描曲线
需要 dθ≠0 的样本才画得出剪刀。且剪刀在 dθ 上**解析已知**:

> |c_gw/c_matter − 1| ≈ tan θ_m · |dθ| = 1.732·|dθ|(θ_m=π/3)

这给 δ 扫描曲线一条可对拍的解析预期——测得曲线偏离 tanθ 线本身就是伪影
探测器(评估器实测:dθ=0.003 → j5_dev=5.27e−3,与 5.20e−3 预期一致;
dθ=0.012 → 2.1e−2,只 J5 崩,剪刀干净)。

- 采样:dθ ~ U(−0.02, +0.02),覆盖 J5 门(1e−2 ⟹ |dθ|≈5.8e−3)±3.5 倍。
- 大失谐(0.4,J5 偏差 ~0.75)保持为炮5(F1),不进先验:它不属于"同族
  微扰",属于"另一个锥"。
- GW170817 叙事:δ 扫描曲线(dashboard 面板 ④)是其定量版;真实界
  |c_gw/c−1|≲1e−15 远在搜索分辨率之外,叙事上引用、不假装测到。

### 2. γ/(μ_z, κ₁)——**进先验(平台坐标化采样)**

不直接采样 (μ_z,κ₁) 裸值(平台随谱移动、裸值无跨规则可比性),采样
**平台坐标** (damp_margin, mu_frac):

- damp_margin ~ 10^U(−2, log₁₀0.3):R22 拍板带 3–5% 在内,两端故意外扩
  以画失效边界(上端 b→1 阻尼失效;下端裕量不足)。
- mu_frac ~ U(−0.2, 1.2):μ_z = μ_lo + mu_frac·(μ_hi−μ_lo),故意越出
  平台 [0,1] 以画稳定/收缩边界。
- 每 rule 由 `spectral_calibrate` 按当条规则的锥速 c=cos θ_g 现算平台
  (**遵守"装配层重算、禁抄数字"条款**,R22 §七.2)。

相互作用:约束门(平台非法 ρ_pred≥1 ⟹ 执行禁用 ⟹ 约束/计数全崩)、
Z 扇区证书(单位模=dim ker K、收缩半径=√(1−κ₁);off-plateau μ 在此被杀)、
J1(Jury 上界外失稳)。预期边界:mu_frac∈[0,1] 内平坦存活(R22 平台平坦
性),越界单侧死(实测:mu_frac=1.15 死于约束+计数,0.05 存活)。

**口径声明**:screen 评估器的格点执行是精确投影极限(λ∈{0,1} 二值使能,
L1 获准选项);(μ_z,κ₁) 的率动力学之牙在符号层(z_sector_certificate +
平台判 + 静止载波全 BZ 谱半径)。真 Z4c 格点率门是 L2/M3 保真层,M4 发令
前提是 M3 全绿,届时流形代表点复判自动覆盖。

### 3. G(源耦合)——**先验(L3 后);现在占位冻结 0**

L3 未装配,G 无动力学可测。占位列保留在 FAMILY_PARAM_NAMES(评估器对
G≠0 直接拒,防先验偷跑未装配部件)。L3 过门后再拍板范围;参考教训:
tensor_batch (b) 修——G 下限必须抬到 tier-2 可测(G∈10^U(−2,−0.7)),
否则比值判据放过原理上测不出的井。预期相互作用:牛顿比、规范泄漏率、
大 G 端非线性失稳。

### 4. tr_sign——**炮(第1件),不进先验**

- 离散开关非连续形变,不给流形加维度;
- **现阶段(L1+L2,无源)无动力学牙**:迹反转只进闭环回授与牛顿扇区,
  L3 后才咬。评估器如实声明(`tr_sign_note`)而非假装能测——防假阳/假阴。
- 若进先验:P(tr_sign=0) 样本全灭(预期),只稀释吞吐;炮的方式(全绿点
  上放一次、必须 FAIL)信息量相同而便宜。
- M4 发令时(M3 已全绿,L3 在内)此炮有牙,随九炮全表重放。

### 5. 放置开关(staggered / integer)——**炮(第6件),不进先验**

不是同族连续参数,是**另一套(错误的)微积分**:R19 负控 |K@TT|≈0.23 量级,
判据崩得离散而彻底(L1 F2 实测:交错种子 0.656 / 整数种子 0.228,逐位复现)。
混进先验会制造不连通伪流形,污染连通性分析(§一.2)。评估器保留
placement="integer" 开关供炮重放(在壳 |K@TT| mini 证书直查,自测已验)。

### 6. 轴序——**建议:炮/附检,不进先验**(次选:离散对照臂;给出双方案)

**建议方案(主)**:xyz 冻结;M4 收官做**附检**——lawful 流形抽样点换
zyx(回文对) 轴序重评,oracle 表随之重算,存活结论不变才算稳健。

理由:轴序是实现规范选择,全部"合法";其唯一物理签名是斜 k Trotter 壳
修正的方向分布,已被 oracle 逐点定量(L1 §三,(2,2,2) 条款)。它不给流形
加连续维度;进先验只会把"同一物理的不同实现"计成"不同规则",人为抬高
存活数、稀释 dθ/阻尼轴的统计功效。且 L1 的门 k 集豁免条款((2,2,2) oracle
对拍而非硬门)是**按 xyz 序预注册的**,换序等于换 oracle 表,静默混采样
将违反"不许静默绕开"新牌(§三c)。

**次选方案(若评审偏好)**:离散均匀对照臂 {xyz, zyx} 各 50%,流形分析
**分层**进行(不混层统计),每层各自对自己的 oracle 表负责。评估器已支持
axis_order 开关,两案切换零代码成本。

### 7. 附:冻结项盘点(不采样,防调参漂移)

θ_m=π/3(参考锥;动它=换归一,失去 J5 可比性)、SV 计数阈、门 k 集、
格子/T/trials(screen 口径,SCREEN_CALIBER 冻结)、种子策略(rule 序号
决定,精确 resume,同 tensor_campaign_runner 惯例)。

## 三、停跑规则(§三d 第 ③ 件,写死;与 payload 常量一字对应)

`cp1_v4_family_eval.STOP_RULES`(runner 每 batch 后必须调 `should_stop`):

| 规则 | 值 | 依据 |
|---|---|---|
| lawful_tensor 计数 ≥ **10³** 即停 | `lawful_target: 1000` | 流形统计够用(近邻连通性 + 逐参数边界分辨;44M 报告同量级经验) |
| 最大评估数(安全阀) | `max_rules: 500_000` | M4 预注册"10⁵ 起步,视漏斗形状放大";5×10⁵ 为本轮上限,再大须评审加签 |
| 最大墙钟(安全阀) | `max_wall_hours: 48.0` | 防漏斗意外过窄时无人值守空转 |

三条任一触发即停、落账;**不许中途改值**(改=新一轮预注册)。

## 四、评估器口径差声明(② 的一部分,M4 报告必须引用)

screen(`cp1_v4_family_eval.evaluate_rule`,~2 s/rule numpy)vs M3 全判据:

| 项 | screen | M3 全判据 |
|---|---|---|
| 格子/时长/试次 | 16³ / T=160 / 4 | 16³–24³ / T=1024–2000 / 8 |
| J5 | 装配算子 2×2 本征相位(bin-free) | 全谱管线同 bin 比值 |
| N_prop 探针 k | {(2,0,0),(2,2,0)}(轴+面斜) | 门 k 集全表 + (2,2,2) oracle 对拍 |
| SV 阈 | sv3<0.08,ttm>0.90(防阈值摆动假阴,L1 §六.2) | 0.05 / 0.95 |
| 约束 | 在壳 \|K@TT\|<1e−10 + relC 尾段<1e−10 | 1e−12 门 + 尾段率口径 |
| 阻尼 | 精确投影使能 + 符号层 Z 证书 | L2 真 Z4c 格点动力学 + 率门 |
| 炮 | 4 件接线自测(F1/F2/F3/F8) | 九炮全表 |

**结论条款**:screen lawful ≢ M3 全绿。M4 判据的最终陈述基于:流形统计用
screen 计数;**流形代表点(全绿一号邻域、各边界点、随机内点 ≥20 条)送
M3 全判据复判**,复判不过的区域从流形中剔除并如实报告。

## 五、配套件状态(§三d 四件)

1. ① 本文件(草案待评审拍板)。
2. ② `cp1_v4_family_eval.py`:自测 PASS(冻结点 lawful;F1/F2/F3/F8 四炮
   接线全部按要求 FAIL;walk 核/K_placed 与冻结 L1 逐位一致 Δ=0.0;
   L1 sha256 对拍 PASS)。harness 接线 = 换一行 import
   (`PARAM_NAMES / sample_tensor_prior / screen_tensor_rules` 契约别名)。
3. ③ 停跑规则:见 §三,常量已写死在 payload。
4. ④ dashboard_ladder.html 新增"M4 · J5 剪刀 δ 扫描"面板:读
   `m4_family_scan.jsonl`(字段 schema 见 payload `SCAN_ROW_FIELDS`),
   未发令时显示"待 M4 发令"。
