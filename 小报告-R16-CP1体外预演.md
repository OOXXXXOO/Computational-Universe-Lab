# R16:CP1 体外预演——全绿一号配方在符号空间整套跑通

*车道A,2026-07-20。`r16_cp1_invitro.py` → `r16_results.json`。目的:CP1 是 GPU 轮,先把 R14核+R15约束+无滞后阻尼**装在一起跑动力学**(逐 k 符号空间,对线性装配是精确的),把 CP1 该看到的数字先钉死。*

## 结果(D1-D5 全 PASS,3 个 k 含斜向)

| 证书 | 实测 | CP1 应复现 |
|---|---|---|
| D1 TT 保持 | **1.000000000000(精确)**,速度=物质群速(共享色散) | TT retention=1、J5=1 同时 |
| D2 约束衰减 | ~1 → 5e-16(机器底),**衰减率逐奇异方向 = (1−γσ²)^t 预言表已存** | 实测率 vs 预言表逐项对——不对处=装配 bug 定位器 |
| D3 纯规范 | 阻尼不碰(ker K);**Riemann(gauge) = 1e-32 恒零**(符号层) | 规范反常机器零 |
| D4 混合初条 | 末态 Riemann 能量只在 TT | N_prop=2 |
| D5 负控(无错排 K)| TT 有损(0.012@T=400 vs 错排精确 0) | 证伪炮:去错排必伤物理 |

## ⚠ 更正栏(2026-07-20,R18 后补;本文原稿有三处不足)

1. **γ=0.3 是错的**。σ²_max=2.2 只在 3 个小 k 采样上算;全 BZ 的实空间约束雅可比 σ²∈[3.01, 27.20] ⟹ **γ_max=2/σ²_max=0.0735,最优 γ*=0.0662**。CP1-v2 照 0.3 调必炸(实测 γ≥0.15 爆炸,与边界吻合)。
2. **本文的 K 是在壳的**(时间符号 2sin(ω/2)/c),实空间实现拿不到 ω,其雅可比时间槽是 O(1/c) 常数 ⟹ **只有单 k 在壳初条才可与本文逐项对拍**,一般初条会对出假差异。
3. **规范口径**:实空间阻尼连规范模一起压(|J·gauge|=0.97),阻尼不动点是 TT 而非 TT⊕规范——判据不可把它读成自由度丢失。

详见 `小报告-R18-实空间阻尼诊断.md`。

## 交给 CP1 的两把实现钥匙

1. **t+½ 读出点不用插值**:约束的空间项必须在半时间步读,而 split-step 的**子步中点态天然就是 t+½ 场**——直接用,避免逐模 /cos(ω/2) 的坑;
2. **γ 稳定域**:阻尼本征值 1−γσ²,实测 σ²_max≈2.2(测试 k 范围),要求 γσ²_max<1;γ=0.3 安全。σ² 谱随 k 的表在 r16_results.json,CP1 若见约束不降反弹,先查 γσ²>1。

## 本轮自抓的约定 bug(诚实入册)

第一版 Riem(gauge)≈0.1-2.4 非零——**我自己的坑**:规范块是迹反转 h̄ 形式,Riemann 公式吃 h,喂前必须 h=h̄−½η tr_η(h̄)(TT 无迹不受影响,所以别的列全对、只有这列错)。CP1 的涌现判据若在 walk 装配下测到 O(0.1) 规范反常且 TT 一切正常,**先查这个转换**再怀疑物理。

## 状态

理论侧三件套(R14/R15/R16)+ 预期数值表 + 两把钥匙 + 一个已知坑,全部交割。CP1 在真格点(3D 回文、实空间模板、sponge)上装配即可;斜 k 的 Trotter 壳修正是唯一预留的未知(R15 §五)。
