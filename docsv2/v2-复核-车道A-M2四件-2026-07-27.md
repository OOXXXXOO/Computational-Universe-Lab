# v2 复核:车道A —— M2′ 四件复核(2026-07-27):**通过,准予 PI 收口**

*承 c635507 复核包(轮 1–3 + 三裁 + 两补测全弧)。复核方式:沙盒重跑四件轻脚本
(preflight 7.5s / candidate 5.0s / dm26 0.14s / dw 0.07s,宿主产物已复位、沙盒物证
以 `*_sandbox.json` 四件固化)+ 炮组判定内存重放 + JSON 结构审计。主跑演化长跑按
M1′ 先例不做跨环境重跑,判定逻辑与谱层构造做。*

## 一、四件复核逐项

| 件 | 结果 |
|---|---|
| ① 沙盒判据字段双跑 | **PASS**。candidate 构造跨环境 0 个超线字段(最坏 1.5e-14,构造逐位级可移植);preflight/dm26/dw 判据字段全部线内,verdict 四脚本逐字同(DONE / REJUDGE-PASS / a:SCALING-CONFIRMED);超线字段逐一分类:`legacy_diagnostics_basis_dependent` 自标注诊断(设计内基漂移)、小 sinθ 谱分量(≤1.2e-11,M0′ 报告级)、噪声底 AIC 派生量(dw 轴向控制 Δω=1.7e-16 上的拟合装饰)——全部落在既定 spec 的报告级豁免内,无一判据受累 |
| ② 炮组重放 | **PASS**。五炮判定逻辑从冻结读数独立重算全真;**C1 ratio_A=4.000000 精确命中 v1 总账 43c 在册值**(tr₁ 健康对照 2.0−1ULP)——跨纲领版本数值血统连续;C3 op_min_ΔW=0.33537 预言命中;C5 比预言 4.10×;`all_guns_fail_as_required=True`、`p0_all_green=True`;两条 as-recorded 诊断核实为诊断级无判据受累 |
| ③ "同一次运行"审计 | **PASS**。每 L 单条运行记录、五相位(P0_wave→P4_packets)带 state_sha256 链、seed 对唯一(几何/物质各一);反事实读数 = 评估器参考分叉(ref1/2/3,从运行态复制演化,严格线性减法精确)而非第二次跑;禁门间换跑落实。**轮 2 原始 verdict_table 保留 G1/G6 FAIL 未改写**,落定记录在补测件——不改史纪律正确 |
| ④ ε 分扇区记账审计 | **PASS**。(ε_geo, ε_mat)=(0, 1) 整数实测;j_inv_geo=2=N_curv(几何全手搭如实)、j_inv_mat=0(物质全涌现);全 runtime/results 检索**无任何混合 ε 字段**——"禁混合 ε"从条款变成了可 grep 的事实 |

## 二、三裁执行的复核确认

裁定一:G1 按核 PASS 的前提(Δω 体对角标度审计分支 a)沙盒复证——O(k²) 良性从
假说升格为判定,轴向控制机器零;治理补条已入纲领 §四。裁定二:D-M2-6 双控制 8/8 +
重判 T1 塌缩 1562×(线 5×)、T2 半窗 0.286(线 0.5),两测同判,σ-ii PASS 沙盒复证。
裁定三:炮组 `two_way_content` 零双向内容,核实。三裁全部按文执行,零走样。

## 三、裁定

**M2′ 车道A 复核全链通过(轮 1 预飞审定 + 轮 2 三裁两补测 + 轮 3 炮组 + 本四件)。
准予 PI 收口。** 收口件按任务书 §1.3 封顶:可主张句 = L1/L2 级
"自旋 2 耦合闭环完成:(ε_geo=0, ε_mat=1) 点落图";禁句永续("自旋 2 涌现"/"GR 涌现"/
"M3 达成");`lit: true` 随收口提交;两项结构发现(体对角 Trotter 各向异性、placed
覆矢 O(k²))+ 双向回声移交 M3′ 接口清单;G6 计数清结、G1 不计数(三裁定案)。

## 四、一句话

同一台仪器,现在在地图上钉下了第三个闭环点:R30 的 ε=0、Maxwell 的 ε=1、和今天
这个 (0,1)——手搭的台,涌现的戏,两个整数,五发炮弹,零句越线的话。M3′ 的边界
制图,从三个真金基准点起步。
