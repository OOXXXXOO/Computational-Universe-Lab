# v2 小报告:M1′ 轮 1 草稿——预飞 + D-M1-1 择定 + 候选构造冻结

*lane B,2026-07-26。M1′ 任务书(`v2-任务书-M1-Maxwell闭环.md`)交付物 1、2;
轮 1 = 预飞 + 两案材料 + 构造冻结。**本轮零主跑、零炮组**(轮 2/3 内容),
本报告无任何涌现主张。宿主单环境交付状态;沙盒双跑位留车道A(M0′ 双环境语义)。*

## 一、预飞(任务书 §5)——PASS(宿主)

`experiments/v2m1_preflight.py` → `data/results/v2m1_preflight.json`。
判据/口径脚本头写死(TOL_JUDGE=1e-12;判据字段/报告级分层按 M0′ 可移植性 spec
终态);环境入册(numpy 2.0.2 / Accelerate / macOS-26.3.1-arm64,fp64)。

| 行 | 内容 | 对拍结果 |
|---|---|---|
| (i) R23 符号层 | M0′ 控制矩阵行 2 复跑(evaluate_v2_candidate → row2_r23 比对器) | **PASS**,判据字段 max diff = **1.78e-15**(≤1e-12;冻结 JSON 沙盒系谱系已注,M0′ 先例同值) |
| (ii) 件10 Yee 实空间 | photon_control 冻结核只读 import(hash 先行写死逐位校验 + JSON 内嵌 script_sha256 交叉证):yee_oracle 七 k + judge(seed 7, T=1024, trials 8, 16³) + solenoidal(seed 8) + eval_positive_gates,对拍 `photon_control_results.json` 冻结值 | **PASS**,整数逐位(N_prop 七 k 全 2、n_eig± 全对)+ 判据字段 max diff = **0.0(位级)**;报告级(sv3/rms/iso)diff 同 0 入册 |

判据核零重写(红线 3):(ii) 全部函数取自冻结 `photon_control.py`
(sha256 `38ea5bbf…402e4263`,与冻结 JSON 内嵌 script_sha256 逐位一致)。
**预飞 PASS ⟹ 主跑解锁前提之一达成;本轮按纪律不开主跑。**

## 二、D-M1-1 择定——A-CONFIRMED

两案对照材料全文入 `v2m1_candidate.json` 的 `D_M1_1.material_two_cases`,要点:

| | A 案(R23 放置字典组装) | B 案(QCA Maxwell 独立构造) |
|---|---|---|
| 证书链完备度 | R15→R17(4/4)→R19(硬门)→R23 P1–P6(6/6)→M0′ 行 2 双环境→件10→本件 CERT0–3 | exp1 Dirac-QCA 谱系存在;其余全部从零建 |
| 涌现语义强度 | 中强:"自旋 2 机器的自旋 1 特化自行落在教科书答案(Yee 1966)上";(E,B) 一阶骨架为特化输入,如实声明 | 酉性语义强,但与冻结判据仪器谱系断裂 |
| 风险 | 辛非严格酉(见 §三声明);与件10 同谱系 ⟹ 判据的牙须由轮 3 炮组独立保证 | 周期不可控;启用 = 新裁定 |

**择定记录**:PI 已按授权认可默认 A 案;lane B 核验前提成立
(预飞 PASS + CERT0–3 全过,无符号层病灶)→ **A-CONFIRMED**。
(若证书任一不过,脚本落 HALTED-AT-DECISION-POINT,不自行切 B 案——本轮未触发。)

## 三、酉性地位声明(强制项,原文入册 `D_M1_1.unitarity_status_statement`)

> 自旋 1 候选在 (E,B) 场表象下走辛演化(Yee leapfrog 辛格式),不是对存储场
> L2 范数的严格酉演化;与 v2 纲领"局域酉 QCA"基底公理的关系,如实分三层:
> (1) **谱层事实**(CERT3 实测):一步模式矩阵 M(k) 在 CFL 内(dt=0.5,
> dt·√3<1)对全部判据 k 与 200 随机 k,6 个本征值全部落单位圆
> (max‖λ|−1| = 2.9e-15,机器精度),守恒二次型为 Yee 能量
> H=|E|²+B(−)·B(+) 而非 |E|²+|B|²——即 M(k) 逐 k 酉等价于酉矩阵
> (k 依赖相似变换;可对角化诊断 cond(V)≤5.83 报告级入册)。
> (2) **未证部分(诚实边界)**:该 k 依赖相似变换非局域;本轮未构造、也不
> 主张任何严格局域酉 QCA 膨胀实现该步进。"酉等价正规形式"仅在逐 k 符号层
> 成立,地位 = **符号层假设**(任务书 §2 允许的如实标注分支,非证书)。
> (3) **关系裁定**:候选以"辛格式 + 逐 k 单位圆谱"身份进入 M1′ 门列;
> M1′ 各门均为该表象下可观测判据,不以基底酉性公理为前提;若纲领后续要求
> 基底严格酉,升级路径 = B 案谱系的局域酉化(Dirac-QCA 型)或显式酉膨胀,
> 属新裁定,不在 M1′ 范围。附注:R23 符号层阻尼 (I−γK†K) 为收缩映射,是
> in-vitro 判据仪器而非候选本体,不进入本声明的候选演化。

## 四、候选构造冻结(交付物 2)——DONE

`experiments/v2m1_candidate.py` → `data/results/v2m1_candidate.json`。
A 案组装 = 三条冻结字典输入(不含任何 Maxwell 差分口味输入):
D1 放置派生(A_μ at x+e_μ/2 → E 边点/B 面点,程序化派生并证 == Yee 1966 放置)、
D2 R17 口味一行规则(半格→后向差,整数→前向差)、
D3 R19 时间配对(leapfrog 切片语义,dt=0.5 承件10 CFL 点)。

| 证书 | 内容 | 读数 |
|---|---|---|
| CERT0 | 组装结构:各旋度分量全部项落同一目标点;目标点==被更新场声明放置;口味表逐字==Yee 1966(E-旋度全前向/B-旋度全后向) | 全 PASS(结构逐字核对) |
| CERT1 | R23 P6 对拍(evaluate_v2_candidate 统一评估器出数,row2_r23 比对器) | diff 1.78e-15;P6 组装符号≡i·κ err **2.56e-16** |
| CERT2 | 逐字退化为 Yee:字典 step vs 冻结 step_yee,随机 fp64 态 ×8 步 + 七判据 k 模式矩阵 | **位级 0.0 / 0.0** |
| CERT3 | 谱层酉性事实(声明支撑):七 k 实空间 + 200 随机 k 符号层单位圆谱;符号谱 vs 实空间谱 | 6.7e-16 / 2.9e-15 / 9.0e-16;cond(V)max 5.83(报告级) |

CandidateV2 schema 填全:sector="spin1"、geometry.family="yee_maxwell"
(params 含构造 spec 全文)、hand_built=**None**(零 DOF 手搭,D1 口径)、
lattice.judge_k_set = 件10 七 k 集、backend="numpy"、frozen_refs 全 hash
(M1′ 六件先行写死 + M0′ 冻结七件 + r15 注册表值)。

**构造冻结 hash**:
- construction_sha256(构造 spec 规范序列化)= `b7daa970f0702c9e4e12603740d4f40de33dee92cec17036dd61b76bb7f29da8`
- v2m1_candidate.py source_sha256 = `8aa00b0a9847ec0a6c05b857f6d8375e6d13879fd639342623d6f95b7f96b1ac`
- v2m1_candidate.json results_sha256 = `adbdcb4c622e76a49375c06dace29a84b85290486d23a6aee5d8256b297b385a`
- v2m1_preflight.py source_sha256 = `3dfef16aac712d2726226185cf03ed2638ce15abcd76ac37358809598fe8d563`
- v2m1_preflight.json results_sha256 = `d80c4dde110b0a7c7e057653f415c81b0464c9dc7db9375dc93e25eb8c6c1f75`
  (repo 惯例:该字段 = 写入前一刻哈希;落盘终态文件哈希 =
  `6736cd758b3dffbebdbb0a6ae6f687e053feeba42688846eb603da9833e55284`,
  已由 v2m1_candidate.json 的 `preflight_precondition.sha256_at_read` 入册)

主跑(轮 2)唯一合法输入 = 冻结 construction_spec + 上列 source hash;
**冻结先于主跑的红线已遵守(本轮零主跑)。**

## 五、复现命令

```bash
source .venv/bin/activate
RULESPACE_BACKEND=numpy python experiments/v2m1_preflight.py   # ~2s
RULESPACE_BACKEND=numpy python experiments/v2m1_candidate.py   # ~0.1s(须先有预飞 JSON)
```

## 六、诚实边界

1. **单环境交付状态**:两 JSON 均为宿主参考值(sandbox.status=PENDING);
   PASS 判定按任务书 §6 须沙盒双跑判据字段一致,归车道A 复核。
2. **G8 的 ε_DOF=1.0 在本轮全部是 D1 定义锚**(hand_built=None),不是实测;
   ε_DOF=1 由主跑 j_inv 不变量机器实测(红线 5),两 JSON 中已逐处标注。
3. **候选与件10 同为 Yee 家族**:CERT2 位级 0.0 说明字典组装与教科书格点
   逐字同一——这正是 A 案的涌现主张形态("特化自行落在教科书答案上"),
   同时意味着主跑正控对拍的谱系相关性高,判据的牙必须由轮 3 四炮独立保证。
4. **(E,B) 一阶旋度-旋度骨架是自旋 2 复形的自旋 1 特化输入**(R23 对应表);
   字典决定并被证书覆盖的是:放置、差分口味、目标点着陆、组装符号≡i·κ。
   hand_built=None 指零 DOF 手搭进精确 ker C,不指"没写下任何结构"。
5. **酉性**:候选是辛格式;严格局域酉膨胀未构造、未主张(§三声明第 2 层)。
6. 预飞 (ii) 位级 0.0 是同宿主确定性复跑的预期值,不构成跨环境证据;
   跨环境判据 = 沙盒双跑 1e-12(M0′ spec 终态)。

## 七、下一步(任务书 §9)

轮 2:主跑 `v2m1_maxwell_loop.py`(同一 rule、L∈{16,24,32,48} 每 L 一条运行
记录喂全门,G1–G8 逐 L;ε_DOF 实测);轮 3:四炮 + P0 正控 + dashboard/state +
小报告成文。任一门两轮不过 → §6 追因分支(候选病/仪器病/纲领病三支)。
