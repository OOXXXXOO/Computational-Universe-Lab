# 复核包 P1-3:"% 转义重跑"专项

*lane B 自查自报,2026-07-26/27。对应指令 §一-3。与 P1-1 同一事件的
"影响面"侧写;diff 与预注册字段映射见 P1-1,此处不重复。*

## 1. 触发层:报告层(打印/文案格式化),非判据层

- 崩溃语句 = 分支 A 的 verdict **文案字符串**组装(pre-fix 版 L1360):
  `"分支 A:G6 按预言穿线(逐 L ±50% 全命中)…epsilon_DOF 实测 = %s。" % (...)`。
  Python `%` 格式化把 `±50% 全` 中 `%` 后的 `全`(U+5168)读作格式符 →
  `ValueError: unsupported format character '?' (0x5168) at index 23`;
- 复现验证:对同字符串施加同参数,得**逐字相同**报文与 index(P1-1 §4a);
- 该语句只产生给人读的 `payload["verdict"]` 文案;分支**条件判定**
  (`g6_pass and all_pass and all_hit and all_cal_valid`,L1364)在其前一行
  已完成且不含格式化——崩溃不触任何判定逻辑与判据数值。

## 2. 影响面:物理段是否已完成?

首跑崩溃日志(`附件-轮3首跑崩溃日志-15点42.log`)证明崩溃时已完成:

- [cert] 冻结 15 件 + M0' 注册表 + construction + 轮 2 预言源校验(全 True);
- [eval] 符号层行 + [ctrl] 逐位控制行(全 PASS);
- **L=16/24/32/48 全部相位段**(P0 波段→P6 波包段,含窗标定与 T_cross 实测);
- **G7 全 BZ 谱半径扫描**(4 个 L)。

未完成(崩溃点之后):门列组装的 verdict 表打印、branch/status/verdict 终态
写盘、results_sha256 回填。即:**全部物理计算已完成,死在最后的文案上**;
增量 JSON 停在 status=RUNNING(含全部 runs 与 preregistration_r3)。

## 3. 是否影响任何入册数字?——否(逐位对照)

入册数字全部来自 15:43 起的确定性重跑;首跑数字未入册,但留在崩溃日志中,
可逐位对照:

1. **两跑日志全字段对照**(4 个 L 全覆盖):首跑崩溃日志 vs 重跑日志
   (`附件-轮3重跑日志-15点54.log`)——全部打印读数逐字一致:
   H_drift/divE/divB(P0)、sol_ratio(P1)、corr/coef4pi/gauss/cone(P2–4)、
   cone/gauss_mv(P5)、T/win/tau_late/R0/Tcross/tau 表/resid 表/floor(P6)、
   G7 radius(4 L)、控制行读数。唯一差异 = 挂钟秒数(6s vs 7s、416s vs 417s
   之类)。
2. **L=16 段位级证据**:当前脚本 `run_one_L(16)` 两次进程内重跑,与冻结件
   `runs['16']` 全字段(剔除挂钟秒)JSON 规范序列化**逐位一致**,5 个相位边界态
   sha256 逐位相同(P1-1 §4c)。
3. 15:33 版增量 JSON(首跑中间产物)已被重跑覆盖,**如实声明未保留**;上两条
   为其等价证据。

## 4. 结论

触发层 = 报告文案格式化;修复 = 单处 `%%` 转义(+6 行披露注,见 P1-1 diff);
物理段在崩溃前已全部完成;首跑与重跑读数在全部可对照字段上逐字/逐位一致;
入册数字不受影响。随 P1-1 自报第一落点(合法披露,记"整改-合规"注),
证据强度声明同 P1-1 §0。
