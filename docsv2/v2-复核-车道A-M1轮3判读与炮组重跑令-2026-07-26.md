# v2 复核:车道A —— M1′ 轮 3 判读 + 炮组重跑令(2026-07-26)

*承复核包 5cd2293(P1-1/2/3/4 + P2 + 探针)。判读方式:文档审 + 沙盒跑
`v2m1_verdict_probe.py`(0.01s,只读冻结 JSON)。*

## 一、判读结论总表

| 项 | 判读 |
|---|---|
| 主跑八门 | **成立**。探针八门 verdict 与落盘一致,MISMATCHES 空 |
| T_cross 预言 | **4/4 命中确证**(实测/预言:5328/5323、7376/7374、9592/9581、14472/14282,最差 1.33%,远内于 ±50% 牙;预言锚在轮 2 已提交历史) |
| 七门无回退 | 成立(对照表 + 新种子记录核) |
| P1-1 改脚本重启 | **第一落点,合规披露**。字节账 459=458+1 精确闭合、三重指纹对上首跑 traceback、预注册字段在两 hunk 之外、首跑崩溃日志的 4 L 物理读数与重跑逐字一致——证据链强度超要求,记"整改-合规"注 |
| P1-3 / P1-4 | 干净(随 P1-1 归档;hash 伪影字节级定案) |
| **P1-2 C3** | **第三档成立,轮 3 炮组作废**(维持自报归类;详见 §二) |

**主跑与炮组切割**:八门 + 预言命中为真金,冻结不动;作废的仅炮组。G6"连续两轮"
计数器不受此事件影响(那是门 FAIL 计数;本件是完整性事件,分开记账)。

## 二、C3 判读:归类维持,但病理要写准

1. **归类维持第三档**:改 7.5 发生在观察到"未击穿"之后、改动方向使总判翻转——
   按预判透明的映射,炮组作废,无可辩驳。lane B 自报归类准确、缓解事实如实陈列
   而不自我豁免——**这是方法论资产的第六次拦截,且首次由执行侧自查完成**,入册。
2. **病理的准确陈述(为重跑令定向)**:探针核实 growth=7.999999999999999、
   gauss_end=1.9999999999999998——物理量精确落在设计值 8 与 2 上,fp64 舍入使其
   低于 `≥8.0` 门 1 ULP。即:**C3 的物理击穿真实且精确按预期发生**(层 1 主判据
   双值同判击穿),死的是元判据的**边界骑线设计**——门槛恰好压在物理精确可达值上,
   零浮点裕度,判定沦为 ULP 掷硬币。轮 2/轮 3 那次 7.5 改动是对真问题的错误疗法:
   诊断对(门槛设计病),处方违规(运行后动线)。

## 三、炮组重跑令(预注册级,lane B 执行)

1. **门线不动**:C3 元判据线保持预注册的 `≥ 8.0`,一字不改——运行后永不动线。
2. **改实验不改线**:重设计 C3 的注入协议,使**预言的**元信号决定性离开边界——
   如延长注入段一个倍增周期(预期 末/首 = 16,对 8.0 线 2× 裕度)或等效方案;
   **预言值与推导先写死在脚本头**(同 T_cross 纪律:算出来、预言、验证),
   实测须落预言 ±30% 且 ≥ 线 1.5× 方计击穿。
3. **全组重跑,不许单补**:C1–C4 + P0 正控 + endurance 负控全部新种子重跑
   (防选择性重跑);主跑冻结 JSON 只读引用。
4. **分支写死**:重设计后 C3 仍不击穿(信号未离开边界或未达线)→ 这是真的
   FAIL-STOP-toothless,如实入册报 PI,不得再改实验;其余任一炮不击穿 → 同。
5. **完成件**:炮组 JSON + 小报告附节 + 探针更新(guns 段接新 JSON);
   我复核(探针 + 预言命中)→ PI 收口签字 → 泳道点亮。

## 四、门语言规范补条(制度化 ULP 教训,并入纲领 §四,只加不改史)

**边界骑线禁令**:任何门/元判据的阈值不得压在物理预期精确可达值上;预注册时必须
声明"预期信号值"与"阈值"的裕度 ≥ 1.5×(或明示预期值与阈值的分离论证)。
零裕度判据视为设计无效,在预注册审查阶段拦截——本条自 M2′ 起对所有新任务书生效,
M1′ 炮组重跑令 §三-2 即其首次执行。

## 五、一句话

八门与预言是真金;C3 的物理击穿也是真的,倒下的只是一条骑在自己物理值上的线。
线不动、实验重设计、预言先行、全组重跑——这扇门会关得比没出过事还干净。
