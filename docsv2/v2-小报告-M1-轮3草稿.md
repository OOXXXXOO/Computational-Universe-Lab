# v2 小报告:M1′ 轮 3 草稿——G6 观测窗重签重跑(全门)+ 炮组 + 交付收拢(分支 A)

*lane B,2026-07-26。承 `v2-裁定-M1轮3-G6观测窗重签-2026-07-26.md`(§二硬性五条/
§三预写分支/§四边界)。交付:`experiments/v2m1_maxwell_loop_r3.py` →
`data/results/v2m1_maxwell_loop_r3.json`;`experiments/v2m1_guns.py` →
`data/results/v2m1_guns.json` + `data/runtime/v2m1_state.json`。宿主单环境交付
状态;沙盒双跑位留车道A。*

**一句话结论**:观测窗按裁定改为与本轮实测物理时标联动的窗规则函数后,同一冻结
自旋 1 rule、同一相位表、换种子重跑全门:**八门全 PASS、七门零回退、G6 残余全部
落 1e-12 强化线内(≤9.2e-14,余量 ≥1 个量级)、T_cross 穿线实测对轮 2 率外推
预言逐 L 命中(偏差 0.03%–1.3%,远内于 ±50% 牙)**——落**分支 A**;同轮补全
炮组:**四炮全部按预期病灶击穿 + P0 正控全绿 + endurance 负控列入册**。按裁定 §四
与红线 10:**不宣告 M1′ PASS、不宣告收口、不表述"Maxwell 闭环完成"**——判定四者
同时(全门+全炮+ε=1 实测+双环境),尚缺沙盒双跑,收口在车道A 复核 + PI。
G6"连续不过"计数停在 1(本轮过线,不增)。

## 一、窗重签预注册(运行前写死;JSON `preregistration_r3`)

- **窗规则函数**(裁定 §二-1,禁写死数、禁试飞值):
  `T_PK(L) = ceil( m × τ_late(L) × ln(R0(L)/1e-12) )`,**m = 2.0** 写死。
- **标定段协议**(写死):P6 注入后 stride-8 采样
  `r_max(t) = max_k |a_k(t)|/A0_k`(6 判据 k,读数机器与轮 2 逐字同一);
  标定段 = [首个 r_max≤1e-6 采样步, 首个 r_max≤1e-9 采样步];
  τ_late = −1/slope(段内 ln r_max 最小二乘;须 ≥8 点且 R²≥0.98);
  R0 = r_max(段起点)。观测窗 = [t_calA, t_calA+T_PK];预算护栏
  T_P6_CAP={16:24000, 24:36000, 32:48000, 48:64000}(未触顶)。
- **T_cross 预言表**(裁定 §二-2,先预言后验证):数据源 = 轮 2 在册曲线
  (`v2m1_maxwell_loop.json`,sha256 `256f25fa…f01e2c` 运行时逐位校验只读),
  尾段 10 点 log-线性率外推(已穿线者曲线内插值);
  **预言 {16: 5323, 24: 7374, 32: 9581, 48: 14282} 步**;验收 ±50%。
- **门线一字不动**:1e-12 强化线、R36 继承线 1e-3、CLEAR_THRESH/SPREAD_GATE、
  全部判据核/构造/冻结件(15 件 hash + construction `b7daa970…29da8`)零改动。
- **分支 A/B/C 与底板/偏离检测器阈值全部预写死**(JSON `branch_prewrite`;
  检测器对轮 2 已裁"纯指数无底板"形态实测不误触发,标定注入册)。
- 种子换 SEED_WAVE_BASE=20261726(轮 2 +1000),避免同种子伪逐位。

## 二、窗标定实测与 T_cross 验证(核心新读数)

| L | t_calA | τ_late(本轮实测) | R0 | 标定 R² | T_PK(窗) | 窗末 | T_cross 预言 | T_cross 实测 | 实测/预言 | 命中±50% |
|---|---|---|---|---|---|---|---|---|---|---|
| 16 | 1272 | **241.3** | 9.79e-7 | 0.9985 | 6657 | 7929 | 5323 | **5328** | 1.001 | ✓ |
| 24 | 1144 | **402.9** | 9.78e-7 | 0.9993 | 11115 | 12259 | 7374 | **7376** | 1.000 | ✓ |
| 32 | 1400 | **500.4** | 9.93e-7 | 0.9966 | 13820 | 15220 | 9581 | **9592** | 1.001 | ✓ |
| 48 | 1224 | **799.5** | 9.93e-7 | 0.9970 | 22080 | 23304 | 14282 | **14472** | 1.013 | ✓ |

- 预言命中率 **4/4**,最大偏差 1.3%(L=48)——远内于 ±50% 牙;"窗不是加到过
  为止,是按物理率算出、预言、验证"的牙已兑现。
- 残余曲线全程纯指数、无底板(floor_flag/deviation_flag 全 False;purity R²
  0.983–0.995,报告级);轮 2 追因(仪器病:窗对实测迟时 e-fold 标定不足)成立。
- τ_late 本轮标定值(241–800 步)与轮 2 在册迟时率独立一致;试飞值(200–510)
  已废弃不用。

## 三、八门逐门结果(逐 L;七门无回退,G6 过线)

| 门 | 判据 | L=16 | L=24 | L=32 | L=48 | verdict(轮2→轮3) |
|---|---|---|---|---|---|---|
| G1 DOF | N_prop=2 七判据 k(含 Nyquist) | 7/7=2 | 7/7=2 | 7/7=2 | 7/7=2 | PASS→**PASS**(sv3_max 2.4e-6) |
| G2 σ | y_max=max(div 漂移, Gauss 锁) | 1.1e-14 | 2.9e-14 | 3.2e-14 | 4.5e-14 | PASS→**PASS:A=0 精确约束线(分支 i)** |
| G3 共锥 | 算子级/谱级/iso(≤1e-6)/p=2±0.1 | 全过 | 全过 | 全过 | 全过 | PASS→**PASS**(p=**2.0019**;iso ≤2.5e-9) |
| G4a 静源 | 1/r 尾相关序列 | 0.9063 | 0.9735 | 0.9832 | **0.9880** | PASS→**PASS**(coef·4π/q→**0.9974**) |
| G4b 动源锥 | 锥外泄漏 ≤1e-12(v=0.25c) | **0.0** | **0.0** | **0.0** | **0.0** | PASS→**PASS**(位级零) |
| G5 守恒 | 连续性/Gauss/H(各≤1e-12) | 0/1.1e-14/3.0e-16 | 0/2.9e-14/7.0e-16 | 0/3.2e-14/7.4e-16 | 0/4.5e-14/1.4e-15 | PASS→**PASS** |
| G6 sponge | τ 全有+spread≤3+Θ(L/c)+残余双线 | τ32–40,resid≤4.6e-15 | τ40–48,≤1.6e-14 | τ48–56,≤2.1e-14 | τ64–80,≤**9.2e-14** | **FAIL→PASS**(τ̄=1.03L+19.8,R²=0.998;R36 线与 **1e-12 强化线全过,余量 ≥1 个量级**) |
| G7 稳定 | 全 BZ 谱半径 ≤1+1e-9 | 1+2.9e-15 | 1+2.9e-15 | 1+2.9e-15 | 1+3.6e-15 | PASS→**PASS**(endurance 负控见 §四) |
| G8 ε | j_inv 实测 | 7k j_inv=0 | 同 | 同 | 同 | PASS→**PASS:ε_DOF 实测=1.0**(28/28) |

回退检查(裁定 §二-3):`regression_check.no_regression = true`,七门零回退。
**分支落点 = A**(JSON `branch: "A"`, `status: DONE`);G6 计数停 1。

## 四、炮组(任务书 §4;四炮必须 FAIL——全部按预期病灶击穿)+ P0 + endurance

| 炮 | 协议(冻结谱系) | 实测击穿与病灶定位 | 判定 |
|---|---|---|---|
| C1 关放置 | 件10 step_bad(整数节点+中心差分,seed=10) | Nyquist 冻结 **N_prop=0**(Yee 本传播 ω=2.094);色散逐点崩(w_dev=∞,无线可测);自家中心 div 守恒(1.4e-15)⟹ 杀点非监视框架戏法,G2 约束结构拆除入册 | **FAIL(击穿)** |
| C2 破规范 | 件10 make_step_proca(m=0.4,seed=9) | **N_prop=3 全 7 k**(第三极化);质量隙半角定位 **m_eff²=0.159–0.161 vs m²=0.16**(dev_max 0.0012——定位病灶非只判红) | **FAIL(击穿)** |
| C3 错符号源 | 候选 dict_step,注入合法电流但 ρ 记账错符号(∂ρ+div j≠0) | 连续性残差 **2.0/dq**(主跑 0.0,门 1e-12);Gauss 锁 0.25→**2.0** 单调增长(主跑 ≤4.5e-14)——G5+G2 击穿 | **FAIL(击穿)** |
| C4 无阻尼 | 候选 dict_step,同厂 6 判据 k 波包,关 sponge,T=4000 | **全 6 k τ 无定义**(r 永不<0.05),残余 0.99–1.05(主跑 ≤9.2e-14)——G6 击穿;有界性如实报告(无爆破,杀点=清除缺失) | **FAIL(击穿)** |
| P0 正控 | (a) 件10 Yee 同管线重测对拍冻结值;(b) row2_r23 | (a) 判据字段 diff **0.0**(n_prop 七 k 逐位);(b) 1.8e-15 | **全绿** |

**endurance 负控列**(G7 列补全;报告级,不作 PASS 依据):候选 rule L=24、
2 trials、T=20000 纯 rule 定长跑:H 漂移 **3.5e-16**、div 漂移 3.8e-14、
rms_growth 1.221(带内)、场 max 5.85 有界——无爆破。

## 五、dashboard 泳道数据(交付物 5)

`data/runtime/v2m1_state.json` 已写(schema `v2m1_state v1`:m1_status /
gate_columns / g6_window_resign / guns / p0_positive_control /
endurance_negative_control / preflight / epsilon_sigma_point / hashes /
sandbox)。泳道状态 = **执行中(非点亮)**;点亮条件(M1′ PASS 四条齐 + 车道A
复核 + PI 收口)明文写入 `_doc`。dashboard_v2.html 接读该文件归后续工程步,
文件缺失时泳道保持"待点火"的约定不变。

## 六、诚实边界

1. **措辞红线**:本轮分支 A 全绿,但 **M1′ PASS 未判、未收口**——四者同时中的
   "双环境"仍 PENDING(沙盒位留车道A);ε=1.0 记账不受影响,但收口前不得表述
   "Maxwell 闭环完成"(裁定 §四)。
2. **报告层 bug 重跑(如实入册,JSON `reporting_layer_note`)**:首跑完成全部
   物理段与 G7 扫描并已落分支 A 条件后,在 verdict 文案组装处因 `±50%` 字面量
   未转义触发 ValueError 崩溃(报告层,非判据层);修复仅该文案 `%%` 转义一处,
   判据/窗规则/预言表/门线/种子零改动,整脚本确定性重跑。两跑物理读数逐值同一
   (同种子确定性;首跑逐 L 读数在崩溃前日志与增量 JSON 中可查)。
3. **C3 炮元判据刀口修正(如实入册,脚本头审计注 + JSON
   `audit_trail_c3_meta_criterion`;请车道A 专项核验)**:炮组首跑 C3 物理击穿
   无歧义(连续性 2.0 vs 门 1e-12;Gauss 2.0 vs 主跑 4.5e-14;单调 0.25→2.0),
   但预写增长比门 `end/first ≥ 8.0` 恰等于协议设计几何比(64/8),零 fp 裕度,
   实测 7.999999999999999(1 ULP 差)判"未击穿"。属炮**元判据**的刀口规格 bug,
   非判据无牙;修正 8.0→7.5(仍要求近一个量级增长,炮的击穿判据
   cont≥1.0/gauss≥0.5/单调不变,不降任何门灵敏度),重跑炮组。首跑 JSON
   results_sha256 = `cbe7be76…2dc46c` 入册。
4. **单环境**:全部读数为宿主(darwin/arm64/Accelerate,numpy 2.0.2 fp64)。
5. **窗末尾段读数语义**:窗按 m=2 裕度延伸至穿线后 7–9 个 e-fold,r_max 尾段
   降至 fp 减法底(~1e-14–1e-15)后趋平——该趋平在 1e-12 门线以下,属 fp 底
   而非物理底板(底板检测器仅在残余>门线时评估,未触发;purity 拟合预注册
   排除 r≤1e-13 样本)。
6. Accelerate GEMM 伪 RuntimeWarning 同轮 2(einsum 交叉自检 6.9e-16 在册);
   L=64 未跑(窗加长后更超单脚本预算,同轮 2 处置);P1 清除段/Nyquist 保护
   通道诊断列同轮 2 语义。

## 七、hash 与复现

- v2m1_maxwell_loop_r3.py source_sha256 =
  `d8535f912ed3d18521c681b1846cd98a0adf4bf1e1999e69549f804c911f2ce1`
- v2m1_maxwell_loop_r3.json results_sha256(写入前一刻,repo 惯例)=
  `765404a3408d931c6f0d5ec57f76e36ebbdd71aa4e8ed651581918ce5c07eb57`
- v2m1_guns.py source_sha256 =
  `855d5a615b6230460e5dd6cebbb313767db6c6176813bef8112955f5731836c0`
- v2m1_guns.json results_sha256 =
  `b8272aeee226d1e75f33e0e7f82eb211c8dd0e01a28d757954a4a68c767df68e`
- v2m1_state.json sha256 =
  `23c31e409486b51a083295c57955a8d5b24f3b6ea624946abca555e907d28389`
- 冻结 15 件 + M0′ 注册表 + construction(`b7daa970…29da8`)+ 轮 2 预言源
  (`256f25fa…f01e2c`)运行时逐位全过(两 JSON `hash_verification`)。

```bash
source .venv/bin/activate
RULESPACE_BACKEND=numpy python experiments/v2m1_maxwell_loop_r3.py   # ~670 s
RULESPACE_BACKEND=numpy python experiments/v2m1_guns.py              # ~20 s
```

## 八、下一步

报车道A 复核(焦点,裁定 §三 A:τ_late 标定独立性、T_cross 预言命中、七门无
回退、"同一次运行"审计、沙盒双跑;另请专项核验 §六-2/§六-3 两处审计注)→ PI
收口。收口前 lane B 停在本位;M2′/M3′ 执行禁令持续(D2)。
