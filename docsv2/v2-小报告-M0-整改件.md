# v2 小报告:M0′ 整改件 —— 仪器不变量化补丁(lane B,2026-07-26)

*权威:`v2-复核-车道A-M0仪器迁移-2026-07-26.md` §三(判决实验与不变量表)§四
(整改令六条)§五(连带裁定);`v2-任务书-M0-仪器迁移.md` 红线七条继续有效。
本件 = 整改令六条的逐条执行记录 + 判决数复现 + 新旧对照 + 双环境状态。
措辞红线:整改后宿主侧全过也**不宣告 M0′ 收口**——收口须双环境 8/8
(沙盒双跑由车道A 复核执行)+ PI 签字。*

## 0. 一句话

复核抓出的仪器级缺陷(逐模残差统计与贪心 j_hand 建立在 ±ω 精确简并子空间的
LAPACK 任意基上,非观测量)已按整改令六条修复:σ/ε 读数全部换成基不变观测量
(主角谱 {sin θᵢ}、max sin θ、Frobenius 泄漏、不变量 j_hand),判决数逐位复现
(16³ 三 k:1.0000/0.1948/0.1493 与 1.035/0.304/0.211),整改版控制矩阵**宿主
8/8 + 基稳健自检全过**(3 固定种子酉旋转,漂移 ≤9.2e-16 ≪ 1e-12);σ 校准件
整件重做后,旧"面斜 1.95σ 正小底"**不再现**(基伪影确认),不变量口径给出的是
一个 −0.30%±0.04% 的**负**模型级截距(真常数底模型无关上界 ≤0.38%),而轴向
暴露出 max sinθ≡1 的 **O(1) 精确遮挡**(R29 轴向病灶的不变量形态)。待沙盒双跑。

## 1. 判决实验复现(首个自检,硬门)

不变量实现(`rulespace_v2/invariants.py`,新代码路径;冻结脚本只读调用取 U10/Q)
须复现复核 §三判决表。实测(宿主,fp64 numpy/Accelerate):

| k(16³) | max sinθ(本实现) | 复核参照 | frob 泄漏(本实现) | 复核参照 | 3 种子旋转漂移 |
|---|---|---|---|---|---|
| (2,0,0) | 1.0000 | **1.0000** | 1.0350 | **1.035** | 6.1e-16 |
| (2,2,0) | 0.1948 | **0.1948** | 0.3043 | **0.304** | 9.2e-16 |
| (2,2,2) | 0.1493 | **0.1493** | 0.2114 | **0.211** | 3.3e-16 |

最大偏差 3.7e-4(= frob 第三位小数舍入),在写死容差(sin 1e-4 / frob 1e-3,
表列显示精度)内;旋转不动性到机器精度。**判决数复现 PASS。**

不变量定义(预注册,脚本头写死,运行后未回改):
- σ 主判 `y = max sin θ`(span(B₊) vs ker C 主角谱;替代 resid_max);辅报
  `rms sin = frob/√m`(resid_mean 的严格不变量对应物);另报全谱、frob、
  ≥0.05 主角计数;
- ε 主判 `j_hand_inv = #{sin θᵢ(span(S_curv), ker C) ≥ 0.05}`(阈=SV_THRESH,
  整数、基无关),`ε_DOF = 1 − j_hand_inv/N_curv`;
- 基稳健自检 = 3 次固定种子(0/1/2)簇内酉旋转(|Δphase|<1e-9 簇),漂移门 1e-12。

## 2. 整改令六条逐条执行记录

**① σ 读数不变量化 —— 完成。**
`rulespace_v2/sigma.py` 新增 `invariant_plus_modes` / `calibration_16cube_invariant`
/ `collect_direction_invariant` / `judge_direction_invariant` / `direction_fits_invariant`;
R37 拟合协议(窗=分量≤π/4、剔 n=1、按比值去重、双模型 AIC、方向分层)**原样保留**
——`fit_constant`/`fit_power` 为 R37 冻结代码对象直接调用,唯一变化 = y 值。
16³–96³ 全系列重跑见 §5(总耗时 1.0s)。G2 门(`gates.py`)主判改不变量拟合,
旧口径拟合降级为 `legacy_fits_basis_dependent` 注记。

**② ε_DOF 不变量化 —— 完成,锚点重校如下。**
`rulespace_v2/epsilon.py` 新增 `measure_frozen_walk_epsilon_invariant`;
`epsilon_for_candidate` 的 `measure_rc3ii_R2` 通道改走不变量,旧贪心保留在
`per_k.legacy_greedy`。锚点重校(整改令原文三点):

| 锚点 | 要求 | 实测 | 判 |
|---|---|---|---|
| R30 | 仍 ε=0 | ε=0(定义锚,控制行1 PASS) | ✓ |
| R23 | 仍 ε=1 | ε=1(定义锚,控制行2 PASS) | ✓ |
| 冻结走行族 | 唯一确定锚点区间 | j_hand_inv = {(2,0,0):2, (2,2,0):3, (2,2,2):3},**ε∈[0.25, 0.5]**,3 种子整数逐位稳定、谱漂移 ≤9.2e-16 | ✓唯一 |

新旧并列:旧贪心口径宿主 j={4,3,3}→[0,0.25]、沙盒 →[0,0.5](±1 随基翻转,
两者皆基伪影);不变量口径唯一 [0.25,0.5],与两个旧读数都不同——旧口径不仅
跨环境漂移,其宿主值本身也无观测量意义。物理内容不变:走行族仍是"部分涌现"
(0<ε<1),既非 R30 全手搭也非 R23 零手搭。

**③ 控制矩阵行5/行8 判据改口 —— 完成,宿主 PASS + 基稳健自检全过。**
`rulespace_v2/controls.py`(模块头判据写死):
- 行5 逐位档 = N_prop 整数列 [4,4,5,5](r32)+ walk 因子逐位(≤1e-13,vs r36
  证书值)+ hash;不变量档 = j_hand_inv/主角谱/B₊ frob 基稳健(漂移≤1e-12)+
  宿主参考值入册;leak_resid_mean/curv_sv/贪心 j_hand 比对降级为
  `legacy_diagnostics_basis_dependent`(非判据);
- 行8 判据 = 16³ 三判据 k (max sinθ, frob) 复现复核判决数(容差=显示精度)+
  基稳健;旧"resid 逐位 diff=0.0"降级为 legacy 诊断(宿主谱系,复核失守正是此列);
- 基稳健自检列:行5/行8 = 3 固定种子酉旋转实测(max 漂移 9.2e-16);行1–4/6/7
  = n/a(判据全为逐位/整数/奇异值谱/确定性时序量,无简并逐模统计入判据,理由逐行
  写入 JSON);
- 跨环境:每行入册 `environment` 字段(见 §4)+ `cross_environment_status` =
  "待沙盒确认"(不变量宿主/沙盒 diff ≤1e-12 由车道A 双跑判定)。

宿主整改版结果(`data/results/v2m0_selftest_v2.json`):**8/8 全绿**;
row8 判决数复现 max diff 3.7e-4(门内);全部 legacy 诊断在宿主同基下仍逐位
(leak/sv diff 0.0、贪心 j 与 rc3ii 一致)——如实入册但不再参与判定。

**④ 环境入册 —— 完成。**
selftest/校准 JSON 均含 `environment`:numpy 2.0.2,BLAS=**accelerate**,
LAPACK=**accelerate**(numpy.show_config;threadpoolctl 对 Accelerate 返回空表,
如实记录),Python 3.x,macOS-26.3.1-arm64。行2 加注 `frozen_json_lineage`:
冻结 r23_results.json 为**沙盒系**(Linux/OpenBLAS;复核 ② 确证与沙盒逐位 0.0,
与宿主差 1.8e-15,ULP 级环境属性)。

**⑤ σ 校准件整件重做 —— 完成;新旧结论差异如实并列(见 §5)。**

**⑥ 小报告与 dashboard —— 完成。**
8/8 表述改口:本件 §0 与旧小报告头部状态注均改为"宿主 8/8 + 跨环境 6/8 →
整改后须双环境 8/8";`v2m0_state.json` schema 升 v2(m0_status 双环境口径、
invariants 块、行5/行8 基稳健/环境/跨环境字段、ε 锚点不变量+旧口径并列、
σ 校准不变量块+旧口径注记);`dashboard_v2.html`:σ 面板纵轴与散点改不变量
(y=max sinθ 拟合截距,轴向 O(1) 点钳位显示并注明)、tooltip 并列旧口径注记、
ε 区间条画不变量 [0.25,0.5] + 旧口径虚线注记、控制矩阵表加"基稳健自检"列、
徽章/页脚改双环境措辞并显示环境入册。

## 3. 整改版控制矩阵(宿主)一览

| # | 行 | 判 | 关键读数 | 基稳健自检 |
|---|---|---|---|---|
| 1 | R30 复形 | ✓ | N_prop=[2,2,2,2],sv diff 0.0,ε=0 | n/a(判据基不变) |
| 2 | R23 Yee/Maxwell | ✓ | diff ≤1.8e-15(冻结件沙盒系谱系已注),ε=1 | n/a |
| 3 | null_damped | ✓ | N_prop=2×4,sv diff 0.0 | n/a |
| 4 | teeth 裸波 | ✓ | N_prop=6×4 精确 | n/a |
| 5 | 冻结走行族 | ✓ | 逐位:[4,4,5,5]+walk 0.0;不变量:j_inv={2,3,3}, ε∈[0.25,0.5] | **9.2e-16 ✓** |
| 6 | tr_sign=0 炮 | ✓ | h00/φ→4、偏折→1,双 FAIL 击穿 | n/a |
| 7 | R36 清除件 | ✓ | τ∈[39,62]、1.59×、AC≤2.33e-15 | n/a |
| 8 | R37 16³ 校准 | ✓ | 判决数复现 max diff 3.7e-4(门 1e-3);不变量拟合入册 | **9.2e-16 ✓** |

**宿主 8/8;双环境状态:整改前沙盒 6/8 → 整改后待沙盒双跑(车道A)。**

## 4. σ 校准件新旧对照(16³–96³ 三方向)与面斜新判定

证书全过:冻结 hash 逐位、RC1a faithfulness、判决数复现、不变量 16–48 与
selftest row8 自洽 diff=0.0、legacy 对照通道 vs r37_results 逐位(宿主谱系,
沙盒重跑预期漂移——这正是复核结论本身);拟合集全点(14×3)基稳健漂移
max 4.1e-14 ≤ 1e-12。

| 方向 | 旧口径 16–96(resid_max,基依赖,**经复核不能成立**) | 不变量 16–96(y=max sinθ,判据) |
|---|---|---|
| 轴向 | A=0.0030±0.0739, α=1.02, ΔAIC=+20.5,"排零成立" | **max sinθ ≡ 1.0000**(全 14 点,散布 <3e-12):**O(1) 精确遮挡**——+ω 支恒含一个完全在 ker C 外的主角方向,即 R29 轴向病灶的不变量形态。旧口径把它涂抹成 0.18–0.31 的基依赖读数、再"排零"——同一伪影的两面 |
| 面斜 | A=0.0200±0.0103, α=1.77, ΔAIC=+43.1,排零 1.95σ("小底了结") | A=**−0.00302±0.00038**, α=0.95, ΔAIC=+163.3;LOO A∈[−0.0032,−0.0021] 排零翻转 14/14;扩窗 A=−0.0018;真常数底模型无关上界 **≤0.38%** |
| 体对角 | A=0.0155±0.0079, α=1.31, ΔAIC=+46.4,排零成立 | A=−0.00831±0.00079, α=0.81, ΔAIC=+142.1;上界 ≤0.99% |
| 辅报 rms | (resid_mean 类,近似不变) | 轴向 0.40830(=√(1/6),纯 sinθ=1 模);面斜 −0.00019±0.00007, α=0.99;体对角 −0.00679±0.00068 |

**面斜小底新判定(预注册分支如实落点:FACE-NZ)**:
- 旧件"+2% 正小底、1.95σ 边缘排零"**不再现**——正小底确系基伪影(复核预期
  的方向得到证实);
- 但不变量口径下形式排零判据(|A|≤2σ_A)**不成立**:截距为 **−0.30%±0.04%**
  (负号与"底"相反,量级 0.3%,α≈0.95 弱标度档;不变量系列散布远小于旧口径,
  σ_A 收紧一个量级,微小模型截断误差即形式显著)。诚实读法:**不存在正常数底;
  遗留一个 ≤0.4% 量级的负模型级截距**。预注册分支按"否则→NZ"如实落 NZ,
  不往 Z 上靠;终版封存措辞由车道A/PI 择定(建议文本已由脚本按预写分支产出,
  见 JSON `wording_update_suggestion`)。

**轴向诚实注记(as-run,不改判据)**:轴向 y 恒等于 1(散布 <3e-12),两模型 RSS
同在机器噪声底(~1e-24),AIC 在噪声上形式性偏好幂律(ΔAIC=+3.6,α=0.05 落在
网格下边界,B~2e-11)——预写 AXIAL-O1 分支的触发条件(要求"常数决定性胜出")
因此未形式触发,d3_tier 形式落"weak-scaling"。这是简并拟合对恒等常数序列的
病态,不是物理标度;原始读数(fit_y 全 1.0)在 JSON 内自明。判据头未回改,
此注记为报告层事实陈述。G2 门对走行族的不变量 verdict 相应为 FAIL
(O(1)-constant)——仪器如实显示 R29 轴向遮挡,与 v1 封存结论同向,非新物理主张。

**波及面复核确认**:v1 封存三重支撑(N_prop 秩/断崖、76.2° 主角、稳定性谱半径)
均为基不变量,不受影响;封存主句(死于计数+稳定性死锁,非残差本身)不动。

## 5. 双环境状态

| 项 | 宿主(本件) | 沙盒(车道A) |
|---|---|---|
| 环境 | numpy 2.0.2 / Accelerate / macOS-26.3.1-arm64 | Linux / OpenBLAS(复核环境) |
| 控制矩阵(整改版) | **8/8** | 待双跑 |
| 判决数复现 | PASS(≤3.7e-4,显示精度内) | 判决数本源自沙盒(复核 §三) |
| 基稳健自检 | 全过(≤9.2e-16;拟合集 ≤4.1e-14) | 待双跑 |
| 跨环境不变量比对(tol 1e-12) | 宿主参考值已入册 | **待沙盒确认** |

## 6. 交付物与 hash

| 文件 | sha256 |
|---|---|
| rulespace_v2/invariants.py(新) | `5d51e9733b076a71e66ab29dcb67837ada813b43f15d369617c1dbf8e1b696b3` |
| rulespace_v2/sigma.py | `5e68622ecd46fd084993cadb0e904875e3b57d2f45f35ca2fa5832c18e29ee3b` |
| rulespace_v2/epsilon.py | `10fbfed89436bf0f1dd1996d7c1b03b10aa5d5e5c4756f1b674e199fc8cd881c` |
| rulespace_v2/controls.py | `8785a70e5defe89399c1c39e3deae0042ab08a0aeb1cddf8af599aa4d69c2d1f` |
| rulespace_v2/gates.py | `f29f4cb8cd1a2d3e0c3730d4428c4870d151ba50eaa31a47f227135f981fcf68` |
| experiments/v2m0_selftest_v2.py | `87825f0ac3e3ecefecde953df791dee9ecc83eb4c78025cfea73cf49c90790d0` |
| → data/results/v2m0_selftest_v2.json | `c9a52ff970aebda7ba9211f378de34110890b046400f2ca03f124d3de800371b`(内嵌 results_sha256=`a1ac80ef…`) |
| experiments/v2m0_sigma_calibration_v2.py | `67fc28b6c88fe1ac850bc2c04f461a19374e5bc28a27c46e64e5a289d5c0c845` |
| → data/results/v2m0_sigma_calibration_v2.json | `15874e52b098b0b23c060614b74e6b96e6ac0e657a029eac8eaea740354f753a`(内嵌 results_sha256=`13c02d28…`) |
| visualizations/dashboards/dashboard_v2.html | `3856bc4e08099e29ecfd116bc8cd4ee3c24d8ea48d00fbbf120dd6cee576d9b9` |
| data/runtime/v2m0_state.json(schema v2) | `686e4be7e452e84adaa3d4f7dd34d75b46db273a1fd0a0bf451ca7bd262e2015` |

不变的(整改令明文,已核):交付物 1–3 架构、外壳化方式、hash 体系全保留
(frozen.py/candidate.py 未动);v1 冻结脚本零触碰(15 有先行记录 hash 逐位
校验 PASS;不变量计算是新代码路径,冻结脚本只读调用取 U10/Q);R37 拟合协议
窗/剔点/AIC 未动;旧结果 JSON(v2m0_selftest.json / v2m0_sigma_calibration.json)
保留未覆盖。

复现:
```bash
RULESPACE_BACKEND=numpy .venv/bin/python experiments/v2m0_selftest_v2.py          # 15s
RULESPACE_BACKEND=numpy .venv/bin/python experiments/v2m0_sigma_calibration_v2.py # 1s
```

## 7. v1 侧加注清单(§五连带,只加不改史,均一行)

1. `docs/reports/小报告-R37-残差标度审计.md` —— 头部状态注(基依赖发现 +
   方向性结论仍立 + 指向整改件)。
2. `docs/reports/小报告-RC3ii-三放松候选证据表.md` —— 头部状态注(逐模统计
   基依赖 + 定性结论由不变量支持不翻案 + 指向整改件)。
3. `docs/status/HANDOFF_02_结果总账.md` —— 43as 后补一行 **43at**(M0′ 交叉
   环境复核发现逐模统计基依赖,不变量化整改;○整改中待沙盒)。
4. (v2 侧,整改令 6)`docsv2/v2-小报告-M0-仪器迁移.md` 与
   `docsv2/v2-小报告-M0-σ校准节草稿.md` 头部状态注(8/8 表述改口、旧口径
   σ 结论不能成立,以整改件为准)。

## 8. 诚实边界

- 本件全部数字为**宿主单环境**;M0′ **不收口**,收口须车道A 沙盒双跑确认
  跨环境不变量 diff ≤1e-12 + PI 签字。
- 判决数复现容差(sin 1e-4/frob 1e-3)是复核表**显示精度**,非机器精度比对;
  机器精度级跨环境比对以双跑的全谱 diff 为准。
- 轴向 d3_tier 形式落点为简并拟合病态(§4 注记);预写分支条件未回改,
  原始读数自明。
- legacy 对照通道(cert5 逐位)是宿主谱系证书:沙盒重跑本脚本时该列**预期
  漂移**,而不变量列必须复现——这正是整改要证明的可移植性分界。
- threadpoolctl 对 Accelerate 返回空表,BLAS 字符串取自 numpy.show_config;
  沙盒侧(OpenBLAS)threadpoolctl 应有非空记录,双跑时并册。
- 仪器校准,非物理门:σ 校准件两分支均只产出封存 §五 措辞更新建议(交车道A,
  只加不改史);无 v1 判定更改,无 v2 物理门触发,无新物理主张。
