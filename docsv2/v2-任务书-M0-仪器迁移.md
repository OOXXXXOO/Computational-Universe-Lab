# v2 任务书:M0′ 仪器迁移(lane B 执行指令,预注册级,2026-07-26)

> **状态(2026-07-26 追加,只加不改史):M0′ 已收口。** 双环境 8/8(宿主 + 沙盒
> selftest_v2)+ σ 校准件不变量版(库级跨环境比对落位)+ cert5 微补丁(非宿主谱系
> 路径宿主侧验证 PASS)。整改弧见 `v2-复核-车道A-M0仪器迁移-2026-07-26.md`(HALT)、
> `v2-小报告-M0-整改件.md`(六条整改)、`v2-复核-车道A-M0整改沙盒双跑-2026-07-26.md`
> (双跑复核);收口签字 = `v2-收口-M0-2026-07-26.md` 入库提交。

*车道A 签发(承 PI 授权与 `v2-裁定-D1D5-2026-07-26.md`)。本任务书 = M0′ 的预注册:
交付物、判据、口径、红线全部预先写死;lane B 只让数字落位,不动判据。
权威上游:`v2-纲领-北极星-涌现边界制图.md` §四(门语言)§五(M0′ 行)、
`v2-资产重审计-2026-07-26.md`(I 类继承清单与唯一硬约束)。*

## 0. 目标与范围

**目标**:建成 v2 统一评估器与仪表,使"任意 (复形族成员 × 耦合 × ε × σ) 候选 → 全部
门列读数"成为一次函数调用;用已知答案的正负控矩阵证明仪器有牙。
**不在范围**:任何新物理主张;M1′ 的 Maxwell 闭环运行(仅其正控接口在本期打通);
自旋 2 的任何新演化设计。

## 1. 交付物清单(路径与命名写死)

| # | 交付物 | 路径 |
|---|---|---|
| 1 | v2 评估器包(numpy/jax fp64 参考实现,不改 `rulespace/`、`rulespace_gpu/` 任何现有文件) | `rulespace_v2/`(顶层新包:`candidate.py`(schema)、`gates.py`、`epsilon.py`、`sigma.py`、`controls.py`) |
| 2 | 候选 schema(dataclass + JSON 序列化) | `rulespace_v2/candidate.py` |
| 3 | 自检脚本(控制矩阵一键运行) | `experiments/v2m0_selftest.py` → `data/results/v2m0_selftest.json` |
| 4 | σ 机器校准件(≥64³ 标度审计) | `experiments/v2m0_sigma_calibration.py` → `data/results/v2m0_sigma_calibration.json` |
| 5 | dashboard v2 首版 | `visualizations/dashboards/dashboard_v2.html` + `data/runtime/v2m0_state.json` |
| 6 | 小报告 | `docsv2/v2-小报告-M0-仪器迁移.md`(体裁同 v1 小报告:hash、复现命令、诚实边界) |

## 2. 候选 schema(必含字段)

```text
CandidateV2 = {
  sector: "spin0" | "spin1" | "spin2",
  geometry: {family: "R30" | "yee" | "frozen_walk" | ..., params: {...}},
  coupling: {variant, kappa, eta_back, ...} | None,
  hand_built: {dof_indices | None},          # ε_DOF 的分子来源
  lattice: {L, judge_k_set, ray_directions},
  backend: "numpy" | "jax",                  # 证书一律 fp64
  frozen_refs: {file: sha256, ...}           # 所有引用冻结件的 hash,运行时逐位校验
}
```

`evaluate_v2_candidate(cand) -> GateColumns`(dict,含每门:原始读数、极限外壳拟合
(A, α, ΔAIC)、verdict、诊断)。**任何 verdict 不得由单一固定尺度读数产生**(纲领 §四)。

## 3. 门列(v2 门语言外壳 + v1 判据核,核不重写)

| 门 | 判据核(I 类继承) | 极限外壳 |
|---|---|---|
| G1 DOF/N_prop | Riemann-SVD 计数核(SV_THRESH=0.05,原样入册) | N_prop(k; L) 逐 L∈{16,24,32,48} 报告 + 断崖 svn 全列 |
| G2 约束标度 σ | R37 拟合协议全文(窗=分量 ≤π/4、剔 n=1、resid_max 主判) | (A, α, ΔAIC) 按 D3 两档 |
| G3 J5 共锥 | \|ω_aux−ω_shell\| 全 θ 线 | 阈 1e-6,逐 L 稳定性 |
| G4 Newton | h₀₀/φ 比 + 1/r 尾相关(source-lift 技术自 R25 静态件复用) | 比值随 L 序列报告,目标 2.00±0.02 且随 L 不发散 |
| G5 Eddington | 偏折比 2.00±0.02(置零 h_ij 反事实) | 同 G4 外壳 |
| G6 守恒 | T 行残差(fp64 门 1e-12) | 逐 L;分层归纳版:单层压缩 + 层间引理占位列(M2′ 启用) |
| G7 稳定 | BZ 谱半径超增长(门 1e-9) | 逐 L;endurance 定长跑仅作负控列 |
| G8 ε | ε_DOF 主判 + ε_E 辅报(D1) | 锚点校准见 §4 |

## 4. 控制矩阵(验收核心;"已知答案"全部来自冻结 JSON,禁止以 v1 证书直接填列)

每行 = 新评估器**重测**该对象,与冻结值比对。tol 列为最大允许偏差。

| 控制对象 | 期望(冻结出处) | 关键比对 | tol |
|---|---|---|---|
| R30 复形 | N_prop=2 断崖;ε_DOF=0(`r30_results.json`) | G1 svn 列 vs 冻结 | 1e-12 |
| R23 Yee/Maxwell | 光子 2 极化 + Lorenz 闭合;ε_DOF=1(`r23` 冻结件) | G1/G3 | 1e-12 |
| null_damped 正控 | N_prop=2,sv 断崖 ~1e4(卡点⑦ 报告) | G1 | 1e-10 |
| teeth 裸波负控 | N_prop=6 | G1 | 精确 |
| 冻结走行族(自旋 2) | N_prop=[4,4,5,5];泄漏 34–49%;ε_DOF∈[0,0.25](rc3ii/r36) | G1/G8 | 1e-13(walk 因子逐位) |
| tr_sign=0 炮 | Newton/偏折按 v1 记录击穿 | G4/G5 | 判 FAIL 即可 |
| R36 (a) 清除件 | τ∈[39,62]、k-无关 1.59×、AC 残余 ≤2.3e-15(`r36_results.json`) | G6/G7 附属清除列 | 1e-12 |
| R37 16³ 校准 | 三判据 k resid 与 `rc3ii_results.json` diff=0.0 | G2 | 0.0(逐位) |

**M0′ PASS = 控制矩阵 8/8 全落位 + §5 校准件完成。** 任一控制不落位 → 停手,写追因
小报告(禁止调 tol、禁止改判据核),报车道A。

## 5. σ 校准件:≥64³ 标度审计(D3,单独脚本)

- 复用 R37 全套协议与冻结件(hash 逐位校验,只读);格子系列 16³/24³/32³/48³/**64³**
  (96³ 若逐 k 路径内存可及则加,不可及则如实注明);三方向射线,新增拟合合格点须覆盖
  面斜 |k|<0.37 区间(64³ 的 n=2,3 点即 |k|≈0.196/0.294 处)。
- 报告:三方向 (A, σ_A, α, ΔAIC) 新旧对照 + 面斜小底是否排零(|A|≤2σ_A)。
- 两侧分支均只产出:v1 封存文档标度证据节的措辞更新建议(交车道A,只加不改史)。
  **不改任何 v1 判定,不触发任何 v2 物理门。**

## 6. dashboard v2 首版

面板:(ε, σ) 平面散点(锚点 R30/R23/冻结走行族)+ 三自旋测线泳道 + 控制矩阵
绿/红牌 + G 列读数表。数据源仅 `data/runtime/v2m0_state.json`(schema 由 lane B 定义
并写入小报告);**禁止读取旧 tensor campaign payload**(v1 总账 4.1 教训)。

## 7. 红线(全程)

1. 不改 v1 任何冻结脚本/JSON;所有引用冻结件运行时 sha256 逐位校验并入册;
2. 证书一律 fp64(numpy/jax);MLX 不得用于本期任何数字;
3. 判据核从 I 类清单继承,禁止重写算法内核(外壳化不等于重实现;对拍到 1e-12 以上
   偏差即视为重写,停手追因);
4. 拟合口径、tol、控制矩阵在脚本头写死;运行后不得回改(git 可查);
5. **v1 证书不得直接填入 v2 门列**(资产重审计唯一硬约束)——一切经新评估器重测;
6. 增量写盘;不碰 git 历史;总运行预算:全部谱层/小格实空间,单脚本 ≤30 分钟,
   无长跑,无 GPU。

## 8. 分工与时序

1. **lane B**:交付物 1–6,顺序:schema → gates(逐门挂控制)→ selftest → σ 校准 →
   dashboard → 小报告。每完成一门先跑该门的控制行,不攒总账。
2. **车道A 复核**(交付后 1 个工作日):控制矩阵独立重跑;ε/σ 移植与 RC3-(ii)/R37
   原机器对拍;dashboard 数据源审计;64³ 拟合稳健性(LOO + 扩窗诊断,口径同
   `../docs/reports/复核-车道A-R36R37三半验证-2026-07-26.md`)。
3. **PI**:抽查 + M0′ 收口签字;随后车道A 签发 M1′ 任务书(Maxwell 完整闭环,
   在 M0′ 收口前只许起草不许执行,D2)。

## 9. 验收一句话

> 同一个 `evaluate_v2_candidate`,喂 R30 得 (ε=0, N_prop=2),喂 R23 得 (ε=1, 光子 2),
> 喂冻结走行族得 [4,4,5,5] 与 34–49% 泄漏,喂炮组得击穿——八发八中,tol 内逐位;
> 面斜小底在 64³ 下有了答案。做到这句,M0′ 关门,Maxwell 点火。
