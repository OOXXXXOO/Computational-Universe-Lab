# v2 小报告:M0′ 仪器迁移(lane B,2026-07-26)

*任务书:`v2-任务书-M0-仪器迁移.md`(预注册,判据/tol 写死)。本报告覆盖交付物 1–6:
§1–§6 为核心三件(评估器包 + 候选 schema + 控制矩阵自检)与 state.json 数据源;
§7 σ 校准件摘要(全文见附件草稿)、§8 dashboard_v2、§10 收口总表由第二波收拢填入。*

## 1. 结论一句话(对齐任务书 §9)

> 同一个 `evaluate_v2_candidate`:喂 R30 得 (ε=0, N_prop=2),喂 R23 得 (ε=1, 光子 2),
> 喂冻结走行族得 [4,4,5,5] 与 34–49% 泄漏,喂炮组得击穿——**八发八中,tol 内逐位**
> (8 行中 6 行 diff=0.0 逐位;行 2 diff≤1.8e-15 为 ULP 级,行 6 按预注册判 FAIL 即中)。
> 面斜小底的 64³/96³ 答案见 §7:三方向"干净标度"档全立、截距全排零(面斜边缘 1.95σ,
> 模型无关上界 1.3%)——仪器校准结论,非物理门。

控制矩阵 **8/8 全落位** → M0′ 的 lane B 核心三件完成;`data/results/v2m0_selftest.json`
状态 `PASS-8of8`。

## 2. 评估器结构(交付物 1:`rulespace_v2/`,顶层新包,不改任何现有文件)

```
rulespace_v2/
  __init__.py    导出 CandidateV2 / GateColumns / evaluate_v2_candidate
  frozen.py      冻结判据核注册:15 个有先行 hash 记录的源文件逐位校验 +
                 6 个 record-only 文件入册 + 9 个冻结结果 JSON 入册;只读 import
  candidate.py   CandidateV2 dataclass(任务书 §2 必含字段)+ JSON 序列化;
                 GateColumns(每门 raw / per_L / limit_fit(A,α,ΔAIC) / verdict / 诊断)
  gates.py       G1–G8 门列外壳 + evaluate_v2_candidate(cand)->GateColumns
  epsilon.py     G8:ε_DOF 主判 + ε_E 辅报(D1 口径;锚点写死)
  sigma.py       G2:R37 协议外壳(D3 两档:α≥1 干净 / 0<α<1 弱)
  controls.py    控制矩阵 8 行定义 + tol 写死 + 逐行比对器
```

**判据核继承(I 类,全部只读 import,零重写)**:

| 门 | 冻结核(调用点) |
|---|---|
| G1 | `r32_reachability_probe`(kerC_basis/build_sectors/inc_matrix/walk_unit_modes/dominant_branch/curvature_subspace,SV_THRESH=0.05 原样);R30 族用 `r30_tensor_complex_dynamical`(leapfrog + placed Riemann-SVD,同 SEED 逐位复现);自旋 1 用 `r23_maxwell_control`(光子计数);控制行用 `rulespace_gpu.emergence_judge.judge_dof` |
| G2 | `r37_residual_scaling_audit`(collect_direction/judge_direction/resid_plus_modes 协议全文) |
| G3 | `rc3ii_relaxation_framework.j5_cocone_across_theta`(阈 1e-6);R30=频率-壳证书;R23=Lorenz 闭合 |
| G4/G5 | `rulespace_gpu.tensor_qca.judge_fact2/judge_fact3`(tr_sign 炮口) |
| G6 | R30 de Donder darkness(门 1e-10);清除列 `r36_r3_verification.part_C` |
| G7 | BZ 谱半径超增长(门 1e-9;样本按 `r36.part_B` 预采样 rng(3) 复刻) |
| G8 | `rc3ii_relaxation_framework.candidate_R2`(手搭计数机)→ ε_DOF=1−j_hand/N_curv |

**极限语言(纲领 §四)落地方式**:谱层机器(G1/G2/G3)逐 L∈{16,24,32,48} 报告——
机器是连续 k 谱机器,格子只通过可分辨 k=2πn/L 进入,判据方向的 n 随 L 重标
(n_L=round(n₁₆·L/16),实采 k 入册)。实空间时序类控制行(null_damped/teeth/R30
决定性跑/G4/G5 载体)是冻结 v1 协议的固定尺度对答案行,verdict 一律记 `CONTROL`,
不作物理判定。冻结走行族 G1 实测逐 L 稳定:axial 4/4/4/4、(0,3,0) 4/4/4/4、
face 5/5/5/5、body 5/5/5/5——[4,4,5,5] 不是单尺度读数。

## 3. 控制矩阵结果表(8 行,重测值 vs 冻结值 vs tol)

| # | 控制对象 | 重测值(新评估器) | 冻结值(出处) | tol | 实测偏差 | 落位 |
|---|---|---|---|---|---|---|
| 1 | R30 复形 | N_prop=[2,2,2,2];sv 断崖(sv₃≤1.6e-15);ε_DOF=0 | 同(`r30_results.json` decisive_run) | 1e-12 | **0.0(逐位)** | ✅ |
| 2 | R23 Yee/Maxwell | dims=[2](光子 2);C·gauge=7.77e-16;衰减 1.24e-15;retention−1=2.5e-14;ε_DOF=1 | dims=[2];7.35e-16;5.41e-16;2.7e-14(`r23_results.json`) | 1e-12 | 1.8e-15(ULP 级,见 §6) | ✅ |
| 3 | null_damped 正控 | N_prop=[2,2,2,2];sv₂/sv₃ 断崖 9.2e4–3.3e5 | 同(`r28_results.json` PC3) | 1e-10 | **0.0(逐位)** | ✅ |
| 4 | teeth 裸波负控 | N_prop_all=[6,6,6,6] | [6,6,6,6](卡点⑦ 结果 JSON) | 精确 | 0 | ✅ |
| 5 | 冻结走行族 | N_prop=[4,4,5,5];泄漏 [0.4687,0.4846,0.3408,0.3894];j_hand=[4,3,3]→ε_DOF∈[0,0.25];walk 因子 diff=0.0 | 同(`r32`/`rc3ii`/`r36` results) | 1e-13(walk 因子逐位) | **0.0(逐位)** | ✅ |
| 6 | tr_sign=0 炮 | h₀₀/φ=4.0001(→4)、偏折=1.0(→1),G4/G5 双 FAIL | v1 记录(总账 43c:2→4、2→1 双 FAIL) | 判 FAIL(读数窗 ±0.1) | 1.2e-4(对记录方向) | ✅ |
| 7 | R36 (a) 清除件 | τ=[62,58,60,41,40,39]∈[39,62];spread=1.5897435897435896;AC 残余=2.32752180379805e-15;BZ 谱半径=1.0000000000000033 | 同(`r36_results.json` part_C/part_B) | 1e-12 | **0.0(逐位)** | ✅ |
| 8 | R37 16³ 校准 | 三判据 k resid min/max 与 rc3ii 逐位;方向拟合 (A,α,ΔAIC) 对拍 | `rc3ii_results.json` R3 / `r37_results.json` | 0.0 逐位 | **0.0(逐位)** | ✅ |

**8/8 全落位。** 逐行明细(含全部 sv 列与逐键 diff)在 `data/results/v2m0_selftest.json`。

(ε,σ) 锚点落位:R30 → (ε=0, 精确约束 A=0 线);R23 → (ε=1, Lorenz 机器零);
冻结走行族 → ε_DOF∈[0,0.25],σ 按 R37 冻结拟合:axial (A=−0.191±2.13, α=0.37, ΔAIC=2.7,
PASS/弱标度)、face (A=0.0489±0.0123, α=2.93, ΔAIC=17.5, ambiguous/干净标度)、
body (A=0.0308±0.0081, α=1.53, ΔAIC=25.5, ambiguous/干净标度)——与 r37 冻结件逐位一致;
面斜/体斜的 ambiguous 正是 §7 的 64³ 校准件要了结的 v1 遗留问题。

## 4. hash 册

**冻结件校验(15 个有先行记录,全部逐位 MATCH)**:

| 文件 | sha256 | 记录出处 |
|---|---|---|
| experiments/rc1a_tensor_index_scan.py | `ca57a32534c865542d5057eba0e578e7c3aee6e8ca36f779ab103fac6f2ab84f` | rc3ii 记录 |
| experiments/r32_reachability_probe.py | `f1ab9307ea2540a0e733ebf34f975a131790927553ded86a6e1134cd08227442` | rc3ii 记录 |
| experiments/r15_walk_dedonder.py | `ce10bc05c5caa487154a72a4169a1095930afe2afd2d84e4f49bba1041bb18cc` | rc3ii 记录 |
| experiments/r25_dynamic_symbol.py | `a2f458f61c61caf125eb59e700f20c5e603def3b98cdb1ca9169db9e1933bfdb` | r36 记录 |
| experiments/r25_realspace_step.py | `a66f6082e03c45b55687ad60859257e0993dc04f30185e212ff3e4bd62331445` | r36 记录 |
| experiments/cp1_v4_L2.py | `556e56665766b29c22eedf226645e3530c95c8ddc27e2b99527eb6158a70fb67` | r36 记录 |
| experiments/r25_auxiliary_wilson_complex.py | `48eb8653dde48315104e0843d03c88e9947dd13fc2e5642a6264504e9330c1bd` | r32 记录 |
| experiments/r16_cp1_invitro.py | `5696d921903043ba91f3c287cba3eba8d2f2f68e1c69a04c482999bcaec26478` | r30 记录 |
| rulespace_gpu/emergence_judge.py | `0c0188e122f7ee9322b52c5f3901aeae39c1430f77728d878de3480f87312902` | r30 记录 |
| experiments/r28_placed_judge_reverdict.py | `3c10cd669b3f9e49daf91eab362bc249df23d9b25f38d478f66628ac07afabc7` | r30 记录 |
| experiments/r30_tensor_complex_dynamical.py | `eae6034d8403a93977988d0a56e80c8899b9ffe80fcacc4084fc0244b2d519a4` | r30 source_sha256 |
| experiments/rc3ii_relaxation_framework.py | `23cfc187db9454f5a89bb1b7467db6887efd465b8cbe964ec74633ce54a994a6` | rc3ii source_sha256 |
| experiments/r36_r3_verification.py | `2169f7acbaef06c0954750d9e63441e880389fb48613e2d38092095a22ff1abe` | r36 source_sha256 |
| experiments/r37_residual_scaling_audit.py | `b847808c96ef8c64c72973d0e415db202497beb0236575d351a91c1fa9508df0` | r37 source_sha256 |
| experiments/r25_emergence_timeseries.py | `ccb3de344eaba5593526fc06e13a709c52967e61bbbd3d16127f0e70ae3b9594` | 卡点⑦ source_sha256 |

record-only(无先行 hash 记录,当前值入册于 selftest JSON):`r23_maxwell_control.py`、
`r17_placement_operators.py`、`rulespace_gpu/tensor_qca.py`、`pathB_spin2.py`、
`spin2_evolver.py`、`tensor_coin_feedback.py`。9 个冻结结果 JSON 当前 sha256 同册。

**新交付物**:

| 文件 | sha256 |
|---|---|
| rulespace_v2/\_\_init\_\_.py | `1a832f91ff0181af8c9d9e448821e53caf42dbfb685c4a7f0d3d437273f8464a` |
| rulespace_v2/frozen.py | `45a31ecd7bd47c769da837a83b1cf6146fa249c10ff20af11e6fc0ca0c024d2f` |
| rulespace_v2/candidate.py | `1d56818709d9ae3f0f265460c82f3b5f24699645cb35900265596b36d45f1b23` |
| rulespace_v2/gates.py | `2666c27ed66349d2a9a5fbebba09ada6f3c044e2549d754ace927e308360158a` |
| rulespace_v2/epsilon.py | `df881c64347026ee4a4ea1c85895da267ab35fe9b49dabbf1b6e6511ea273803` |
| rulespace_v2/sigma.py | `d1423369f502823f5374db822a5f010718f5d26a67fe525714303ec9116b255d` |
| rulespace_v2/controls.py | `df323f45052ec7ab06ee0134e32bf013f2292abee58b63f6da9025000974a4c8` |
| experiments/v2m0_selftest.py | `8260fc2902e3f8009cefab251da4a7456463347d558ec29125cf69be5a8bbff3` |
| data/results/v2m0_selftest.json | `6c25c8a01899fc6aebb61b071dfb898474d04e1c2a92ed14625ea96d50a51f73` |
| data/runtime/v2m0_state.json | `41c823a245363bbfa348727dc30ff2bec5f497f8b61f29ad62c916a6b1e36b50` |

(两个 JSON 的 sha256 为写盘终态;selftest JSON 内部 `results_sha256` 字段按仓库惯例
是写入该字段前一刻的哈希。)

## 5. 复现命令

```bash
source .venv/bin/activate
export RULESPACE_BACKEND=numpy      # fp64;MLX 全程未用
python experiments/v2m0_selftest.py # 全 8 行,总耗时 ~15 s
```

单候选评估:

```python
from rulespace_v2 import evaluate_v2_candidate
from rulespace_v2.controls import control_candidates
gc = evaluate_v2_candidate(control_candidates()["row5_frozen_walk"])
```

## 6. 诚实边界

1. **行 2 非逐位**:R23 重测与冻结值差 ≤1.8e-15(量本身 ~1e-16 的机器零,ULP 级)。
   代码路径与消耗顺序为逐行复刻;偏差与 v1 运行(2026-07-20)以来 numpy/BLAS 构建
   漂移一致,在预注册 tol 1e-12 内。其余谱层行(1/3/5/7/8)全部 diff=0.0 逐位。
2. **行 6 无冻结 JSON 数值**:tr_sign=0 的 v1 记录是总账 43c 的文字记录(2→4、2→1,
   双 FAIL),无数值冻结件;本行按预注册判据"判 FAIL 即可"+读数窗 ±0.1 落位
   (实测 h₀₀/φ=4.0001、偏折=1.0)。G4/G5 载体是 v1 裁判组的 tensor_qca 判据核——
   它只为"炮有牙"作证,不为任何候选背书(旧五参数 surrogate 判定维持 v1 结论)。
3. **固定尺度控制行**:null_damped/teeth/R30 决定性跑/G4/G5 载体是冻结 v1 实空间
   时序协议(N=16 / L=40),verdict 记 `CONTROL`,不构成 v2 物理判定;纲领 §四的
   "无单一固定尺度 verdict"约束由谱层门(G1/G2/G3)的逐 L 报告承担。
4. **G1 逐 L 的含义**:计数机器是连续 k 谱机器,逐 L=同方向逐格采样(n 随 L 重标),
   不是四个独立动力学格子;这是 R37 已用的同一口径,如实声明。
5. **ε_E 是新口径**:辅报 ε_E(冻结走行族=R2 机器的 TT 扇区涌现能量占比;锚点取
   定义值)仅诊断入册,不参与任何判定(D1)。
6. **M0′ 整体未收口**:任务书判定"M0′ PASS = 控制矩阵 8/8 + §5 校准件完成"——两项
   在 lane B 侧均已交付(§3、§7),但这只是 **lane B 交付状态**;M0′ 收口须待
   **车道A 复核(控制矩阵独立重跑、ε/σ 对拍、dashboard 数据源审计、64³ 拟合稳健性)
   + PI 签字**,本报告不宣告 M0′ PASS 收口。本工作不含任何新物理主张;R30 存在性、
   v1 封存主句、卡点④ 状态均不变。
7. 运行环境:numpy fp64,单机,无 GPU/MLX,无长跑(全 8 行 14.1 s);增量写盘;
   未触碰 git 与任何 v1 冻结文件。

## 7. σ 机器校准件(≥64³ 标度审计)——摘要(交付物 4;全文见附件草稿)

**性质先行**:本件是 v2 σ 机器的**仪器校准**,不是物理门。两侧分支均只产出 v1 封存文档
标度证据节的措辞更新建议(交车道A,只加不改史);不改任何 v1 判定(R37 自身 ambiguous
在其预注册系列内不动;R36 (b) FAIL 不动),不触发/不填任何 v2 物理门,G2 正式读数不由
本件代填。

**协议与证书链**:R37 协议**逐字复用**(import 冻结脚本代码对象直接调用;拟合窗/剔点/
去重/双模型 AIC/方向分层一字未改),唯一改动 = 格子清单(D3 命令的正是这一点)。五级
证书全 MATCH:rc3ii 三冻结件 + R37 脚本 sha256 逐位、RC1a faithfulness diff 0.0、16³ 三判据
k 校准 vs rc3ii diff 0.0、旧系列 {16,24,32,48} 再推导 vs `r37_results.json` **逐位 0.0**
(先证同机,再指向新格子)。**96³ 已含**(逐 k 谱路径,无 L³ 数组;峰值 RSS 91 MB,
总 0.5 s,fp64)。面斜 |k|<0.37 区间覆盖达成(拟合合格点 |k|=0.185/0.278);每方向拟合点
m:6→14。

**关键表:三方向 (A, σ_A, α, ΔAIC) 新旧对照**(主判据 resid_max;旧 = R37 冻结 16³–48³,
新 = 16³–96³):

| 方向 | 旧 A±σ_A | 旧 α | 新 A±σ_A | 新 α | 新 ΔAIC | 排零 \|A\|≤2σ_A | D3 档 |
|---|---|---|---|---|---|---|---|
| 轴向 | −0.191±2.132(退化) | 0.37 | **+0.0030±0.0739** | **1.02** | +20.5 | 成立(0.04σ) | 干净标度 |
| 面斜 | +0.0489±0.0123(4.0σ 非零) | 2.93 | **+0.0200±0.0103** | **1.77** | +43.1 | 成立(1.95σ,边缘) | 干净标度 |
| 体对角 | +0.0308±0.0081(3.8σ 非零) | 1.53 | **+0.0155±0.0079** | **1.31** | +46.4 | 成立(1.97σ,边缘) | 干净标度 |

**结论(校准分支 Z,脚本头预写)**:三方向幂律决定性胜常数、截距全排零、D3 全"干净
标度"档。v1 遗留的"面斜是唯一不能在 ≤48³ 内排除 ≤5% 小底的方向"两层了结:(i) 拟合层
——面斜截距 0.049→0.020,降约一半且与 0 一致;(ii) **模型无关层——96³ 最小可分辨 |k|
处面斜原始读数 0.0128 ⟹ 任何真常数底 ≤1.3%**,"≤5% 小底"陈述可无条件收紧。

**诚实边界**:面斜/体对角主判据排零是**边缘成立**(1.95σ/1.97σ),LOO 下有翻转
(4/14、5/14);但扩窗诊断(1.70σ/0.81σ)与次级 resid_mean 口径(0.80σ/0.59σ)均更强
与 0 一致,截距随系列扩展持续塌缩("拟合窗伪影"诊断第三次独立加强)。轴向 96³ n=1 的
7 模简并(R29 奇点邻域)如实入册,该点不在拟合内。

**措辞更新建议(双档,定稿权在车道A/PI)**:主档 ="§五-3 所留面斜小底遗留就此了结";
退档 ="面斜小底上界收紧至 ≤1.3%(模型无关)且主判据排零成立(边缘);≤5% 量级的表述
废止"。两档全文与逐字建议见附件
**`v2-小报告-M0-σ校准节草稿.md`**(sha256 `756c0f28f3c8143961e6aeaf641baca495cb3b1673682fad154fc224cbd410a2`,
本节为其摘要,草稿保留为附件不并入正文)。

复现:`RULESPACE_BACKEND=numpy python experiments/v2m0_sigma_calibration.py`
(脚本 sha256 `55297b2c…`;结果 `data/results/v2m0_sigma_calibration.json`,
sha256 `7563ba4b…`)。摘要已回填 `data/runtime/v2m0_state.json` 的
`sigma_calibration_64` 节(原占位),供 dashboard 读取;`_doc` 同步更新,除该节与
`_doc.sigma_calibration_64` 外 state.json 其余键值逐位不变。

## 8. dashboard v2 首版(交付物 5:`visualizations/dashboards/dashboard_v2.html`)

单文件自包含 HTML(无外网请求、无第三方库),UI 全中文,明暗双主题(跟随系统
`prefers-color-scheme` + 手动切换,记忆于 localStorage)。面板四块(任务书 §6):

1. **(ε, σ) 平面散点**:横轴 ε_DOF∈[0,1](D1 主判),纵轴 σ 维 = 约束残差截距 A,各点
   直标 α。锚点:R30(ε=0)与 R23(ε=1)落 A=0 精确约束线;冻结走行族画 ε∈[0,0.25]
   区间条,三方向标记取 **§7 校准后的 (A, α)**(新 16³–96³ 拟合,含 ±σ_A 误差棒;悬停给
   ΔAIC/排零/D3 档/LOO 翻转)。spin0 泳道封册留位(不入平面,图例注明)。面板底部为
   σ 校准摘要条(分支 Z、截距塌缩、模型无关上界 1.3%、"仪器校准非物理门"声明)。
   若 `sigma_calibration_64` 未落位,自动回退 R37 冻结拟合并注明"σ 校准件待运行"。
2. **三自旋测线泳道**:spin0 = 封册留位(v1 总账 44h 事实一行,M0′ 不重测);spin1 =
   M1′ 待点火,"M0′ 收口后启动"(D2:收口前只许起草不许执行)+ R23 正控接口已打通;
   spin2 = v1 封存,显示封存主句一行("结构性不可达——死于计数+稳定性死锁,非残差
   本身")并指向 `docs/status/阶段封存-2026-07-26-可达性战役收束-自旋2M3结构性不可达.md`。
3. **控制矩阵绿/红牌**:8 行,直读 `control_matrix[]`(状态灯 + 门 + 重测偏差 max|Δ| +
   tol + 耗时 + 冻结出处)。
4. **G 列读数表**:7 候选 × G1–G8,直读 `gate_readings{}`;verdict 徽章区分
   PASS/FAIL/**CONTROL**(对答案行,非物理判定,表头注明);悬停单元格显示关键原始
   读数;未定义门显示"—"。

**数据源约定(红线,页内注释 + 页脚双声明)**:唯一数据源 =
`data/runtime/v2m0_state.json`(schema 见 §9),整页只发出这一个 fetch;
**禁止读取旧 tensor campaign payload**(旧五参数 surrogate 不得给 v2 背书,v1 总账 4.1
教训)。file:// 直接打开时 fetch 被浏览器拦截 → 显示"待运行/数据源不可读"横幅与
`python -m http.server 8000` 指引,不静默显示旧数据。措辞边界:页头与页脚均写死
"控制矩阵 8/8 为 lane B 自检交付状态;M0′ 收口待车道A 复核 + PI 签字",面板不宣告
M0′ 收口。

工程说明:散点三方向系列色经色觉安全六检双主题验证(暗面 `#1ea294/#c9812f/#4a8fe0`
on `#111b22`、亮面 `#0f9a8d/#c1741f/#3d6db0` on `#ffffff`,CVD ΔE≥12.4/13.3);状态色
(绿/红/蓝)仅用于状态,不复用作系列色;明暗双主题下全四面板经无头浏览器渲染检查,
无 console 错误。文件 sha256 见 §10。

## 9. state schema 说明(`data/runtime/v2m0_state.json`,v2m0_state v1)

顶层键(文件内 `_doc` 有同文说明,dashboard agent 以此为准):

- `m0_status`:`{control_matrix_pass: "8/8", overall: "PASS"|"HALT"}` → 总牌。
- `control_matrix[]`:8 行,每行 `{id, name, gates[], status: green|red, tol,
  max_abs_diff, remeasured{...}, frozen{...含 source}, seconds}` → 绿/红牌面板直读。
- `epsilon_sigma_anchors[]`:(ε,σ) 平面散点。每项 `{id, sector, swimlane,
  epsilon_dof | epsilon_dof_range, epsilon_E(_range), sigma{by_direction{A,σ_A,α,
  ΔAIC,verdict,band_D3} | note}, source}`。ε 轴 [0,1];σ 建议画 (α, A) 双读数。
  spin0 泳道为 v1 封册事实留位(`epsilon_dof: null`),M0′ 不重测。
- `gate_readings{}`:candidate_id → GateColumns(每门 `{raw, per_L, limit_fit,
  verdict, diagnostics}`;verdict=`CONTROL` 为对答案行,面板应与 PASS/FAIL 区分显示)。
- `sigma_calibration_64`:**已由第二波回填**(原占位)——交付物 4 摘要:
  `{status, calibration_branch, lattices, direction_fits{old_16_48/new_16_96
  (A,σ_A,α,ΔAIC) + 排零 + σ 水平 + D3 档 + LOO 翻转}, min_k96_raw_resid_max,
  face_small_floor(模型无关上界), wording_update_suggestion(双档), hash}`。
  dashboard 的 (ε,σ) 平面 σ 维以 `direction_fits.new_16_96` 为准;全量数据在
  `data/results/v2m0_sigma_calibration.json`。
- `hashes`:冻结件校验册(`checked` 为有先行记录并逐位比对;`record_only` 如实标注)。

回填以 round-trip 校验方式完成(dump 口径先证与原文件逐字节一致再重写),除
`sigma_calibration_64` 与 `_doc.sigma_calibration_64` 两键外,其余键值逐位不变。
回填后 `v2m0_state.json` 终态 sha256 见 §10(§4 表中该文件的值为回填前终态,如实保留)。

## 10. M0′ 交付物收口总表(lane B 侧;收口签字权不在本报告)

| # | 交付物 | 路径 | lane B 状态 |
|---|---|---|---|
| 1 | v2 评估器包(numpy/jax fp64) | `rulespace_v2/`(7 文件) | **完成**(§2;不改任何现有文件) |
| 2 | 候选 schema | `rulespace_v2/candidate.py` | **完成**(§2) |
| 3 | 自检脚本(控制矩阵一键) | `experiments/v2m0_selftest.py` → `data/results/v2m0_selftest.json` | **完成**(§3:8/8 全落位,PASS-8of8) |
| 4 | σ 机器校准件(≥64³) | `experiments/v2m0_sigma_calibration.py` → `data/results/v2m0_sigma_calibration.json` | **完成**(§7:96³ 已含,分支 Z;附件草稿全文) |
| 5 | dashboard v2 首版 | `visualizations/dashboards/dashboard_v2.html` + `data/runtime/v2m0_state.json` | **完成**(§8/§9:四面板,数据源唯一,σ 摘要已回填 state) |
| 6 | 小报告 | `docsv2/v2-小报告-M0-仪器迁移.md`(附件:`v2-小报告-M0-σ校准节草稿.md`) | **完成**(本件) |

第二波交付物终态 sha256:

| 文件 | sha256 |
|---|---|
| visualizations/dashboards/dashboard_v2.html | `50849f78d39923c213d60786f2fcf455a61ef47bc707b04d6abf6c6bafd9dd7b` |
| data/runtime/v2m0_state.json(σ 摘要回填后) | `9697d5cd938a9e9c783294b893abde576a8869e532b379716cdb94c918854133` |
| docsv2/v2-小报告-M0-σ校准节草稿.md | `756c0f28f3c8143961e6aeaf641baca495cb3b1673682fad154fc224cbd410a2` |

**收口状态与措辞边界**:交付物 1–6 在 lane B 侧全部完成;控制矩阵 8/8 与 σ 校准件完成
构成任务书 §4 的 M0′ PASS **前件**,但均为 lane B 自检交付状态。M0′ 收口 = **车道A 复核
(控制矩阵独立重跑、ε/σ 移植对拍、dashboard 数据源审计、64³ 拟合稳健性 LOO+扩窗)
+ PI 抽查与签字**(任务书 §8),本报告不宣告收口。M1′(Maxwell 完整闭环)任务书在
收口前只许起草不许执行(D2)。全程无新物理主张;v1 冻结件零触碰(hash 逐位校验只读),
未碰 git。
