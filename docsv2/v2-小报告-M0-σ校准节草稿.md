# 草稿节:M0′ 交付物 4 —— σ 机器校准件(≥64³ 标度审计)

*lane B 执行,2026-07-26。供核心 agent 并入 `v2-小报告-M0-仪器迁移.md` 占位节;
本文件不是 M0′ 主报告。权威:`v2-任务书-M0-仪器迁移.md` §5 + `v2-裁定-D1D5-2026-07-26.md` D3。*

**措辞红线(先于一切结论)**:本件是 v2 σ 机器的**仪器校准**,不是物理门。两侧分支均只产出
v1 封存文档标度证据节的**措辞更新建议**(交车道A,只加不改史);不改任何 v1 判定
(R37 自身 `branch="ambiguous"` 在其预注册系列内不动;R36 (b) FAIL 不动),不触发/不填任何
v2 物理门。

---

## 一、结论先行

**校准分支 = Z(排零成立,脚本头预写规则):扩展到 16³–96³ 后,三方向幂律模型均决定性胜
常数模型(ΔAIC +20.5/+43.1/+46.4),截距在主判据下均与 0 一致(|A|≤2σ_A),D3 档位三方向
全部落"干净标度"档(α≥1:1.02/1.77/1.31)。v1 遗留的"面斜是唯一不能在 ≤48³ 内排除 ≤5%
小底的方向"在两层意义上了结:(i) 拟合层——面斜截距从 0.049±0.012(显著非零)降到
0.020±0.010(与 0 一致,边缘 1.95σ);(ii) 模型无关层——96³ 最小可分辨 |k| 处面斜原始读数
0.0128,任何真常数底不允许读数落到它下面 ⟹ 面斜真常数底的直接上界 ≈1.3%,"≤5% 小底"
陈述可无条件收紧。诚实边界:面斜/体对角的主判据排零是边缘成立(1.95σ/1.97σ),LOO 下有
翻转(4/14、5/14 个剔除使 |A|>2σ_A);但扩窗诊断(1.70σ/0.81σ)与次级 resid_mean 口径
(0.80σ/0.59σ)均更强与 0 一致,且截距随系列扩展持续塌缩(R37/车道A 的"拟合窗伪影"诊断
被第三次独立加强)。**

## 二、协议与证书链(全部 MATCH,运行前逐位校验,任一失败即 abort)

R37 协议**逐字复用**:本脚本 import 冻结的 `r37_residual_scaling_audit.py` 并直接调用其
`fit_constant/fit_power/resid_plus_modes` 代码对象(拟合窗=分量 ≤π/4、剔 k=0 邻域一格
(仅 n≥2 来源入拟合)、按 n/L 精确去重、resid_max 主判/resid_mean 辅报、双模型 AIC、
|ΔAIC|≥2 决定性、方向分层不跨向平均)。唯一改动 = 格子清单(D3 命令的正是这一点)。
判据(D3 两档、排零 |A|≤2σ_A)与两侧措辞分支全部预写在脚本头,运行后未回改。

| 证书 | 结果 |
|---|---|
| 1. rc3ii 三冻结件 sha256(`rc1a…`=`ca57a325…`、`r32…`=`f1ab9307…`、`r15…`=`ce10bc05…`) | 逐位 MATCH |
| 2. R37 协议脚本 sha256 = `b847808c96ef8c64c72973d0e415db202497beb0236575d351a91c1fa9508df0` | 逐位 MATCH |
| 3. RC1a faithfulness(walk/K/map diff) | 0.0/0.0/0.0 PASS |
| 4. 16³ 三判据 k 校准 vs `rc3ii_results.json` | diff 0.0,6/6 |
| 5. 旧系列再推导:参数化收集器在 {16,24,32,48} 上重算三方向拟合集与 (A,σ_A,α,ΔAIC) vs 冻结 `r37_results.json` | **逐位 0.0**(先证同机,再指向新格子) |

`r37_results.json` 就地 sha256 = `3d86f2e3bebb3178369a56822cc47dd1726ee4a1ced2774918654c224885e9bb`
(其内嵌 `results_sha256=f3efc274…` 按车道A 口径(去字段重序列化)独立重算 MATCH)。

## 三、96³ 可及性(任务书要求先估内存)

**96³ 已包含**。机器是谱层的:格子只通过 k 采样进入,逐 k 路径 = 28×28 本征分解 + 固定
(256,10) 关联矩阵,无任何 L³ 数组;实测全程峰值 RSS 91 MB、总耗时 0.5 s(≪30 分钟预算),
fp64(RULESPACE_BACKEND=numpy)。

新拟合合格点(n≥2 来源的新比值):1/48(96³:n=2 把 48³:n=1 升格)、1/32(64³:n=2 与
96³:n=3 把 32³:n=1 升格)、3/64、5/96、7/96、5/64、11/96、7/64 —— 每方向 m:6→14。
**面斜 |k|<0.37 区间覆盖达成**:|k|=0.185(比值 1/48)与 0.278(1/32)两个拟合合格点
(任务书点名的 64³ n=2,3 即分量 0.196/0.294,对应面斜 |k|=0.278/0.417,均已入拟合)。

## 四、各格子最小可分辨 |k|=2π/L 原始读数(R37 必报项延伸;resid_max)

| 方向 | 16³ | 24³ | 32³ | 48³ | **64³** | **96³** |
|---|---|---|---|---|---|---|
| 轴向 | 0.0992 | 0.0853 | 0.0563 | 0.0442 | **0.0221** | **0.0200** |
| 面斜 | 0.0672 | 0.0518 | 0.0274 | 0.0219 | **0.0134** | **0.0128** |
| 体对角 | 0.0582 | 0.0454 | 0.0312 | 0.0213 | **0.0139** | **0.0100** |

三方向继续**单调下降**至 1.0–2.0%。这些 n=1 点仍按协议剔出拟合、原始入册。
注:轴向 96³ n=1(|k|=0.065)处 +ω 列数 = 7(非 6)——k→0 邻域简并(R29 奇点邻域),
如实入册;该点不在任何拟合内。

## 五、三方向 (A, σ_A, α, ΔAIC) 新旧对照(主判据 resid_max;旧=R37 冻结 16³–48³,新=16³–96³)

| 方向 | 旧 m | 旧 A±σ_A | 旧 α | 旧 ΔAIC | 新 m | **新 A±σ_A** | **新 α±σ_α** | **新 ΔAIC** | 排零 \|A\|≤2σ_A | D3 档 |
|---|---|---|---|---|---|---|---|---|---|---|
| 轴向 | 6 | −0.191±2.132(退化) | 0.37 | +2.7 | 14 | **+0.0030±0.0739** | **1.02±0.63** | **+20.5** | **成立**(0.04σ) | **干净标度** |
| 面斜 | 6 | +0.0489±0.0123(4.0σ 非零) | 2.93 | +17.5 | 14 | **+0.0200±0.0103** | **1.77±0.32** | **+43.1** | **成立**(1.95σ,边缘) | **干净标度** |
| 体对角 | 6 | +0.0308±0.0081(3.8σ 非零) | 1.53 | +25.5 | 14 | **+0.0155±0.0079** | **1.31±0.25** | **+46.4** | **成立**(1.97σ,边缘) | **干净标度** |

- 三方向幂律均决定性胜常数(ΔAIC 远超门 2),α≥1 全立 ⟹ **D3 归档:三方向"干净标度"档**。
- 轴向在旧系列的协方差退化(σ_A=2.13,α 与常数近共线)被新系列解除:α=1.02±0.63,拟合可用。
- 面斜/体对角截距随系列扩展塌缩(0.049→0.020、0.031→0.0155,各降约一半)——与"拟合窗
  伪影"预期一致(真常数底不随窗变);且两方向被剔除的更小 |k| 原始读数(§四)仍低于各自
  新截距,反压证据延续。

## 六、面斜小底排零判定(v1 遗留的了结对象)

- **主判据(预写):排零成立**——幂律决定性胜(ΔAIC=+43.1)、α=1.77>0、
  |A|=0.0200 ≤ 2σ_A=0.0206。
- **模型无关上界**:96³ 最小 |k| 原始读数 0.0128 ⟹ 任何真常数底 ≤1.3%(读数不可能落在
  真底之下)。v1 陈述中的"≤5% 小底"无条件收紧至 ≤1.3%。
- **诚实边界(如实入册,不改判)**:1.95σ 是边缘成立;LOO 下 4/14 个剔除翻到 2.05–3.12σ
  (翻转集中在剔掉最小两点或中段两点时);扩窗诊断(纳入 n=1 点,仅诊断)A=0.0119±0.0070
  (1.70σ,与 0 一致);次级 resid_mean 口径 A=0.0096±0.0120(0.80σ,与 0 一致)。
  即:主判据、扩窗、次级三个口径一致排零,LOO 显示该排零对个别点有敏感性。

## 七、稳健性(车道A 复核口径,预先承诺,均不改判)

| 方向 | LOO A 区间 | LOO 排零翻转 | 扩窗诊断 A±σ_A(排零) | 次级 resid_mean A±σ_A |
|---|---|---|---|---|
| 轴向 | [−0.0161, +0.0507] | 1/14(剔 \|k\|=0.785 → 2.89σ) | −0.0041±0.0399(成立,0.10σ) | +0.0175±0.0216(0.81σ) |
| 面斜 | [+0.0146, +0.0268] | 4/14(2.05–3.12σ) | +0.0119±0.0070(成立,1.70σ) | +0.0096±0.0120(0.80σ) |
| 体对角 | [+0.0115, +0.0231] | 5/14(2.07–2.58σ) | +0.0048±0.0059(成立,0.81σ) | +0.0071±0.0120(0.59σ) |

扩窗与次级口径三方向全部与 0 一致且幂律决定性胜(扩窗 ΔAIC 29.5/53.3/57.9;次级
34.0/33.1/33.7);LOO 的边缘翻转与截距随系列扩展的持续塌缩共同指向:剩余小截距是拟合
函数形式/窗伪影的残迹,不是物理底——但此为诊断陈述,主判据结论(排零成立)不依赖它。

## 八、给车道A 的措辞更新建议(两侧分支预写于脚本头,数字落 Z 侧;只加不改史,定稿权在车道A/PI)

建议在 v1 封存文档 `阶段封存-2026-07-26-…` §五(标度证据节)**追加**一条(不动原文 1–4):

> 5.(v2 M0′ σ 机器校准件补记,2026-07-26)R37 协议逐字复用、格子系列扩展至 64³/96³
> 后,面斜 |k|<0.37 区间获得拟合合格点(|k|=0.185/0.278):三方向幂律均决定性胜常数
> (ΔAIC +20.5/+43.1/+46.4),截距在主判据下均与 0 一致(|A|≤2σ_A;面斜
> 0.0200±0.0103,边缘 1.95σ,扩窗/次级口径下更强一致;LOO 存在边缘翻转,如实注明),
> α 三方向 ≥1(D3"干净标度"档)。96³ 最小可分辨 |k| 原始读数(1.0–2.0%)给出任何
> 真常数底的模型无关上界:面斜 ≤1.3%。**§五-3 所留"面斜是唯一不能在 ≤48³ 内排除
> ≤5% 小底的方向"就此了结**;R37 自身 ambiguous 落点按其预注册系列不改。此件为 v2
> 仪器校准,非物理门;封存主句(不可达死于计数+稳定性死锁,非残差本身)与本补记无关,
> 三重支撑不变。

(若车道A 认为 1.95σ 边缘 + LOO 翻转不足以写"了结",退一档的最小安全措辞为:"面斜小底
上界收紧至 ≤1.3%(模型无关)且主判据排零成立(边缘);≤5% 量级的表述废止"。两档均在
Z 侧事实内,选择权交车道A/PI。)

## 九、诚实边界与红线遵守

- 本件全程未触碰任何 v1 冻结文件(hash 逐位校验只读);未改 R37 拟合口径一字(直接调用
  冻结脚本代码对象,证书 5 先证同机);判据/分支/tol 预写脚本头,运行后未回改。
- "排零成立"是**校准件语言**:它了结的是 v1 封存文档标度证据节的一句遗留措辞,不构成
  任何关于残差物理的新主张,更不翻转 R37 的 ambiguous(其预注册系列 16³–48³ 内该落点
  正确)或 R36 (b) 的 FAIL。
- 面斜/体对角排零的边缘性(1.95σ/1.97σ)与 LOO 翻转两侧留白如实入册;轴向 96³ n=1 的
  7 模简并如实入册。
- 无 v2 物理门被触发或填列;G2 门的正式读数须待 M0′ 评估器控制矩阵流程,不得由本件代填。

## 十、复现与 hash

```bash
RULESPACE_BACKEND=numpy .venv/bin/python experiments/v2m0_sigma_calibration.py
```

- 脚本:`experiments/v2m0_sigma_calibration.py`
  (source sha256 = `55297b2c04439a792fa7e617a4309c518bf4da873e7cea8f70eaea95a12b67a2`)
- 结果:`data/results/v2m0_sigma_calibration.json`
  (results sha256 = `7563ba4b83488c238a22a090ee138d4ddecb96c976b3dd74f93c9b4f07fd99cf`,
  加字段前序列化口径,同 R37;顶层 `calibration_branch="Z"`)
- 冻结引用:R37 脚本 `b847808c…`、rc3ii 三件 `ca57a325…`/`f1ab9307…`/`ce10bc05…`、
  `r37_results.json` 就地 `3d86f2e3…`(内嵌 hash 口径重算 MATCH)。
- fp64,总 0.5 s,峰值 RSS 91 MB;96³ 已含;增量写盘;未碰 git。

**一句话**:扩到 96³ 后三方向幂律"干净标度"档全立、截距全排零(面斜边缘 1.95σ、
模型无关上界 1.3%),v1 遗留的面斜 ≤5% 小底措辞可了结——这是仪器校准结论,
不改任何 v1 判定,不触发任何 v2 物理门。
