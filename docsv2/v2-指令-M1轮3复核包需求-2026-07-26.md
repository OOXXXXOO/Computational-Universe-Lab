# v2 指令:M1′ 轮 3 复核包需求(车道A → lane B,2026-07-26)

*承 c2927c2(轮 3 落分支 A:预言 4/4 命中、八门全绿、七门零回退、四炮全击穿)。
按裁定,M1′ 收口链 = 车道A 复核判读 → PI 收口 → 泳道点亮。本指令开列复核包清单与
并行许可;复核以文档审 + 轻量探针为主,演化长跑不重跑(分工纪律)。*

## 一、复核包清单(按优先级,全部入 `docsv2/v2-复核包-M1轮3/` 或小报告附节)

### P1(红线敏感,最高优先,逐条披露)

1. **中途改脚本重启的完整披露**(73011→73470 字节那次):
   - 两版脚本逐行 diff(原样附上,不摘要);
   - 修改动机与 bug 描述(修了什么、为什么必须重启);
   - **预注册字段未动映射表**:窗规则公式、T_cross 预言表、门线 1e-12、判据核调用、
     三分支文本——逐项指出其在两版中的行号与逐字一致性;
   - 重启的确定性证明:同种子下重启后运行与重启前已完成段的重叠部分逐位比对
     (若留有重启前部分落盘)或等价证据。
2. **"炮组 C3 元判据 8.0→7.5"专项**:
   - 这是什么判据、8.0/7.5 各是什么含义、改动发生在**运行前还是运行后**(git 时间戳
     + commit 证据);
   - 改动依据:预注册笔误修正?口径对齐?——引用可证文本;
   - 影响面分析:C3 炮的击穿判定在 8.0 与 7.5 两个值下是否同判(若同判,附双值判定表;
     若不同判,此炮作废重跑);
   - 红线声明:若属运行后为使击穿成立而调值 → 自报 HALT,不要等我裁。
3. **"% 转义重跑"专项**:触发原因(格式化/打印层?)、是否影响任何入册数字、
   重跑前后数字逐位对照。

### P2(裁定三项判读的素材)

4. **T_cross 独立重算素材**:轮 2 在册指数率的 JSON 路径与字段名、预言表生成公式的
   脚本头位置——使我可从轮 2 冻结数据独立重算四个 T_cross 并对照命中表(±50% 牙 +
   实测最差 1.3% 的复算)。
5. **τ_late 标定独立性证明**:每个 L 的标定段与判门段时间窗划分表;声明并可查证
   "标定段数据不进入判门统计"(字段级指认)。
6. **七门无回退对照表**:轮 2 vs 轮 3 逐门逐 L 读数并列 + 新旧种子记录(换种子
   避同种子伪逐位,已传达,需入表佐证)。

### P3(轻量复核探针,给我在沙盒分钟级用)

7. **判定重算探针** `experiments/v2m1_verdict_probe.py`:只读轮 3 落盘 JSON,
   独立重算八门 verdict + 预言命中判定 + 炮组击穿判定(不重跑任何演化);fp64、
   <1 分钟、无 GPU。我在沙盒跑它验证"判定逻辑与落盘数字自洽",这是 M1′ 复核的
   跨环境件(演化长跑不做跨环境要求,判定逻辑做)。

## 二、并行许可(不越 D2 串行硬门)

- **M2′ 预注册草拟**可以开始(设计与草拟不禁,执行禁):R30 复形 + 涌现物质耦合的
  候选构造草案、门列草案(G6 分层归纳版启用)、炮组草案——交车道A 审后备用;
- dashboard 泳道接线代码可写好待命,**不点亮**(收口件);
- M1′ 小报告定稿(含两处审计注专项的结论);
- v2m1 结果文件与炮组数据自本指令起**冻结**,复核期间不得重写。

## 三、复核判读的判定预告(透明,防歧义)

- P1-2(C3 元判据)三种落点:运行前笔误修正且双值同判 → 记披露注,不阻收口;
  运行前修正但双值不同判 → C3 单炮重跑(其余不动);运行后调值 → 轮 3 炮组作废,
  HALT 报 PI。
- P1-1(改脚本重启)两种落点:预注册字段逐字未动 + 动机为实现 bug → 合法披露,
  记"整改-合规"注;任何预注册字段变动 → 轮 3 主跑作废重跑(窗/预言/门是裁定的牙,
  牙动了跑数无效)。
- 复核通过后我出判读报告 → PI 收口 → 泳道点亮;届时方可使用"Maxwell 闭环完成 /
  M1′ PASS"表述,ε=1 锚点升级为"完整闭环级"入 (ε,σ) 地图。

## 四、一句话

轮 3 的数字很好,好数字更要经得起最挑剔的审——把两处审计注亮在灯下,把预言表锚回
轮 2 的历史,把判定逻辑装进我一分钟能跑的探针里。这三件做完,Maxwell 这扇门就是
干干净净关上的。
