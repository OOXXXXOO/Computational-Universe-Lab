# visualizations

- `dashboards/`：浏览器实验室、campaign console、装配阶梯和 3D 可视化。
- `figs/`：实验生成的 PNG/GIF/MP4。
- `assets/`：仓库封面和其他非实验位图。

用 `python tools/campaign_server.py` 从仓库根目录启动服务。旧 tensor dashboard 只认识旧五参数
surrogate；R25 dashboard 尚未实现。
